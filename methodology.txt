================================================================================
MYSLEWER — MOBILE CRANE FLEET OPERATIONS APP
BUILD METHODOLOGY & REASONING LOG (SOURCED VERSION)
================================================================================
Purpose: for a different AI model, or a fresh session of this one, picking
this project back up — explains not just WHAT the app does but WHY, and
where each explanation actually came from.

SOURCING KEY — every section below is marked with how it's known:
  [CONVERSATION]  — confirmed directly by the person across chat sessions
  [CODE]          — reverse-engineered from inspecting the actual files
                    (comments, data patterns, structural evidence)
  [INFERRED]      — reasonable inference, not confirmed either way — treat
                    with more caution than the two tags above

COVERAGE NOTE (UPDATED — see section 8 for full correction): an earlier
version of this note speculated that Tab 4 (Reeving Plan) was built in a
separate Claude Code session on the person's tablet, based on chat-history
search turning up nothing else relevant. That guess was WRONG. It was
later confirmed — via file birth timestamps predating this conversation's
own tool-call history, and via the person clarifying directly — that Tab 4
was built in THIS SAME conversation thread, in a portion that occurred
before a context-compaction event stripped it from active context. The
person had uploaded a batch of OEM "EINSCHERPLAN" (reeving plan) PDF
documents earlier in this same thread; Claude extracted diagrams and data
from those into the SVG set and manifest now living in reeving/. The
specific turn-by-turn build reasoning from that portion is still not
recoverable (compaction is lossy), but the provenance itself is now
confirmed rather than speculative. Tabs 1-3 and the rebrand remain
[CONVERSATION]-sourced with direct access to that reasoning; Tab 4 is
still effectively [CODE]-sourced for the DETAILS of how it works, even
though its origin (this same thread) is now known.
about Tab 4 genuinely isn't knowable from the code alone, that's stated
explicitly rather than guessed.

--------------------------------------------------------------------------------
1. OVERALL STRUCTURE  [CODE + CONVERSATION]
--------------------------------------------------------------------------------
Single-file HTML/CSS/JS Progressive Web App, no build step, no framework, no
backend. Static files, deployed via GitHub Pages.
Repo: 886ppak/liebherr (repo name is a legacy artifact from before the
rebrand to "Myslewer" — see section 7).

  index.html              - entire app: HTML + <style> + <script>, 4 tabs
  manifest.json            - PWA install manifest
  sw.js                    - service worker (offline caching)
  icons/                    - 4 PNG icon sizes
  reeving/manifest.json    - reeving-diagram dataset (NOT the same file as
                              root manifest.json - different purpose, don't
                              confuse or merge them)
  reeving/svg/              - 182 SVG diagram files

Four tabs: Rope Retentioning - Minimum Hook Block Weights - Outrigger
Support Positioning ("CAD tab" internally) - Reeving Plan.

--------------------------------------------------------------------------------
2. TAB 1 - ROPE RETENTIONING  [CONVERSATION]
--------------------------------------------------------------------------------
Purpose: how many parts of line (reeving) are needed to strip enough rope
off the drum for retentioning work, plus the resulting tensioning test
weight.

DO NOT "FIX" THIS FORMULA:
  reeving = Math.ceil((rope - totalLength) / totalLength)
This was explicitly confirmed by the person, more than once, as intentional
- it is NOT a lifting-capacity reeving calculation, even though it looks
like one at a glance and an AI reviewing it cold is likely to assume it's a
bug. It calculates minimum parts of line to spool sufficient rope off the
drum. Leave it exactly as-is unless the person explicitly asks to change
what the calculation is FOR, not just how it looks.

Data: FLEET_BLOCK_SPEC, one entry per crane/boom-config (1650-t3 and
1650-t5 are separate top-level keys - different drum lengths). Each has
w1/p1, w2/p2 (winch drum length/pull), boom length, and blocks[] - each
block has real OEM ballast plate COMBINATIONS (aux[]), not generic weight
increments. This was a deliberate correction after the person's actual
ballast data showed generic "types" didn't match real available plate
combos. r-hook / r-checkweight fields are READONLY, driven only by
dropdown selection - specifically to stop someone entering a ballast number
that doesn't correspond to a real physical plate combination.

--------------------------------------------------------------------------------
3. TAB 2 - MINIMUM HOOK BLOCK WEIGHTS  [CONVERSATION]
--------------------------------------------------------------------------------
Formula: G_kg = L x M x n x F
  L = boom length, M = rope weight/m (by diameter), n = reeving/falls,
  F = empirical reeving-factor table indexed by n.

UNRESOLVED, FLAGGED NOT FIXED: n appears both as a direct multiplier and
inside F (which itself scales with n) - looks like possible double-counting
of the same effect. Raised with the person once; not confirmed wrong, so
left unchanged. Don't silently "correct" this in a future pass - ask again
first, or at minimum re-flag before changing behavior.

Out-of-range rope diameter or reeving count leads to an explicit warning
banner, not a silent fallback. An earlier version silently fell back to a
default value, which was identified as actively dangerous for a tool used
to pick ballast in the field.

minPlateIncrement per crane was corrected from generic assumed values to
each crane's actual confirmed OEM increment (1650: 375kg, 1300: 500kg,
1110: 200kg, etc.), cross-checked against Tab 1's aux[] data for
consistency.

--------------------------------------------------------------------------------
4. TAB 3 - OUTRIGGER SUPPORT POSITIONING ("CAD tab")  [CONVERSATION]
--------------------------------------------------------------------------------
The most conceptually involved tab - nearly every sign convention here was
arrived at through iteration and explicit correction, not derived cleanly
from first principles. Read this fully before touching the math.

4.1 Coordinate system
  Origin = crane's slew center (turret rotation axis), NOT chassis center.
  Front = boom end = top of OEM diagram = internally NEGATIVE Y.
  Rear = cab/counterweight end = bottom of OEM diagram = internally POSITIVE Y.
  baseX = r*sin(angle), baseY = r*cos(angle).
  User "+FWD" input must DECREASE internal Y: shiftY = -shiftYinput.
  This inversion was a specific confirmed correction - removing it silently
  reverses forward/back on the canvas.

4.2 Outrigger labeling (do not renumber)
  P1 = Rear Right, P2 = Front Right, P3 = Front Left, P4 = Rear Left.
  Slew direction symbol: positive angle points "<", negative angle points
  ">" - this was initially backwards and had to be corrected once already.

4.3 Which pads MUST move
  Only the LATERAL component of a shift determines which pads are in the
  crane's actual path of travel:
    shiftX > 0 (right) -> P1, P2 must move
    shiftX < 0 (left)  -> P3, P4 must move
    shiftX === 0 (pure fwd/bwd) -> nothing is required to move
  Confirmed rationale: pads sit laterally offset from the crane's own
  footprint, so a pure fwd/bwd walk never runs the crane over a pad - even
  on a diagonal shift, the lateral component alone governs.

4.4 Three reference frames per leg, shown in the results table
  a) "If Moved" - from CURRENT position: newX = baseX+shiftX, newY = baseY+shiftY
  b) "If Left in Place" - from NEW crane center: relX = baseX-shiftX, relY = baseY-shiftY
  c) "Baseline" - standard OEM target offset, unaffected by any shift.
  Plus a MUST MOVE / optional tag (per 4.3).

4.5 Chassis footprint collision check
  Flags when a pad's "If Moved" target is currently under the crane's own
  chassis - physically impossible to pre-place a mat there before the crane
  actually moves.
  FOOTPRINTS{} = {width, front, rear} per crane, measured from slew center;
  boom overhang deliberately excluded (boom is airborne mid-walk, doesn't
  interfere with ground mats - confirmed, not assumed).
  Collision test: |x| <= width/2 AND -front <= y <= rear.
  Width: pulled from each OEM sheet's "B" dimension (525/80 R25 tire row,
  the fleet's confirmed standard tire). LRT 1100's sheet uses a different
  layout than the LTM sheets - its width (3300mm) had to be confirmed
  directly by the person against the raw drawing, not assumed from the
  dimension that looked most likely at a glance.
  Front/rear (slew-center-relative, not chassis-center) had no single
  printed dimension anywhere - every crane's split was derived via two
  independently cross-validated methods:
    METHOD A ("top-line"): total boom-tip-to-rear-tip dimension at the top
      of the drawing, minus the printed tail segment, minus the front
      overhang segment (boom-tip to front-of-cab) - remainder is
      slew-center-to-front-of-cab.
    METHOD B ("bottom-chain"): sum the individual axle-spacing segments
      along the bottom of the drawing, front-to-back, stopping at the
      point nearest slew center - which sometimes lands PARTWAY through a
      printed segment, not on a clean boundary (e.g. 1300 required
      subtracting exactly 320mm from partway through one segment; 1650
      needed only 1550mm out of a printed 2450mm segment). This partial-
      segment behavior is real, confirmed by the person, not a rounding
      artifact - don't assume a full segment boundary marks the slew
      center without checking.
  Every crane's final front/rear numbers were confirmed by the person
  running at least one method themselves and comparing against the
  independent P1/P4 cross-check (4.6) before being accepted.

4.6 Cross-validation technique (useful for adding cranes later)
  Each leg's stored r/angle already encodes its own slew-center distance via
  r*cos(angle) - compare that against a hand-measured front/rear split as a
  sanity check. A >1-2% disagreement means one of them is wrong. This
  caught a ~220mm error in an early auto-derived guess for the 1160.

4.7 Canvas rendering
  Footprint rectangle centered on the CURRENT (unshifted) origin.
  Every optional (non-MUST-MOVE) leg gets a second "ghost" dot at its "If
  Moved" position, connected by a thin line, red-ringed if it collides with
  the footprint. Added specifically because on a pure fwd/bwd shift EVERY
  leg is optional (4.3) - without the ghost dot nothing visibly changes on
  the canvas even though the numbers underneath do, which confused the
  person before this was added.

--------------------------------------------------------------------------------
5. TAB 4 - REEVING PLAN  [CODE - see coverage note at top]
--------------------------------------------------------------------------------
Everything in this section is reconstructed from reading the actual
implementation. No conversation record of its design decisions was found.

5.1 What it does
  Lets someone pick a crane + boom/head configuration, then a reeving
  way-count (or a named special/auxiliary config), and see: the OEM sheave-
  routing notation as text, a hook-block-type badge, and a diagram.

5.2 Data shape - reeving/manifest.json
  Maps config keys (e.g. "1650-t3", "1250-sks") to one of two shapes:
    a) {normal: [...], american: [...]} - toggle style, for configs with a
       simple numbered progression (most telescopic booms). Each entry:
       {way, hook, lines}. SVG filename for these is NOT stored per-entry -
       it's constructed at render time as way_${way}.svg (seen directly in
       the activeReeveData() function).
    b) {configs: [...]} - flat style, for configs mixing special/auxiliary-
       device/two-hook variants that don't fit a numbered toggle. Each
       entry: {id, label, hook, lines, svg} - svg IS stored explicitly here,
       sometimes null where no artwork is linked.
  isToggleStyle(cfg) picks which shape a given config uses by checking
  whether cfg.normal or cfg.american exists.

5.3 Diagram artwork provenance [CODE - verified by direct inspection]
  All 182 SVGs share the identical transform matrix
  "matrix(0.999726, 0, 0, -0.999726, 0, 2389.082419)" (LRT 1100 files use a
  proportionally scaled variant), each cropped to a different viewBox
  region of the same underlying coordinate space. This is strong physical
  evidence for a single batch PDF-to-SVG vector extraction from one source
  document, matching the in-app disclaimer's own claim: "Diagrams are the
  actual OEM reeving plan artwork (doc. 98036716), extracted directly - not
  redrawn." Not hand-traced; not independently generated per crane.

5.4 UI fix made in this thread (not part of original build)
  Dropdown option text was edited to include the crane model name in every
  <option> itself, not just the parent <optgroup> label - a collapsed
  <select> only ever displays the selected OPTION's own text, so the
  original version showed just "T3 Telescopic Boom" with no crane name
  visible unless the dropdown was reopened. Read as ambiguous.

5.5 Offline bulk-download feature (added in this thread)
  A button that walks the ENTIRE manifest - every crane/config/way, not
  just the currently-selected one - dedupes every referenced SVG filename
  via allReeveSvgFilenames(), and fetches them with limited concurrency (6
  at a time) so every diagram becomes available offline in one action
  instead of only caching on-demand as each is viewed. Relies entirely on
  the existing service-worker fetch-caching behavior (6) - the button
  itself has no direct Cache API access, it just triggers the GETs.

5.6 KNOWN DATA GAP [CODE - verified]
  21 SVG files exist on disk (mostly lrt1100_tele_* and ltm1110_sks_*)
  that are not referenced by ANY manifest entry - confirmed by diffing the
  full set of manifest-referenced filenames against the actual svg/
  directory listing. Whether these are meant to replace currently-linked
  files, fill in existing null svg fields, or are simply unused leftover
  exports is NOT determinable from the code alone. Don't silently link them
  in without asking the person first.

--------------------------------------------------------------------------------
6. SERVICE WORKER / OFFLINE CACHING  [CONVERSATION + CODE]
--------------------------------------------------------------------------------
Two deliberately separate caches:
  APP_SHELL_CACHE (versioned, e.g. 'app-shell-myslewer-v4') - precaches
    index.html, manifest.json, icons. Bump CACHE_VERSION on any change to
    those files. Old app-shell-* caches deleted on activate.
  CONTENT_CACHE (fixed name 'app-content-v1', never versioned or auto-
    cleared) - holds on-demand fetched content, primarily reeving diagrams.
    Deliberately separate so a crew member who's already viewed certain
    diagrams offline doesn't lose access just because an unrelated app
    update bumped CACHE_VERSION. If this cache ever needs a genuine reset,
    change its name once - don't fold it into the versioned cleanup logic.
Fetch handler: cache-first, network fallback, cached-index.html fallback
for failed navigation (so reloads/deep-links work offline).

--------------------------------------------------------------------------------
7. REBRANDING (Liebherr -> Myslewer)  [CONVERSATION]
--------------------------------------------------------------------------------
Originally built as "Liebherr Fleet Operations" (the manufacturer's name),
rebranded to the person's own brand "Myslewer." Changed: <title>, visible
<h1>, manifest.json name/short_name, all 4 icons (regenerated as a plain
bold text wordmark - several graphic concepts were explored first: a
slew-ring motif, an outrigger-cross motif matching the CAD tab's own site
plan, a stylized double-hook based on a reference photo the person
provided - but plain text was chosen as simplest and most legible at small
icon sizes), and the service worker's cache name/version bumped
accordingly.
Deliberate exception: Tab 1's disclaimer text ("cross-check reeving, pull
rating, and ballast against the current Liebherr load chart...") still
says "Liebherr" - correctly, since it refers to the actual crane
manufacturer's OEM load charts, not the app's own branding. Don't strip
this in a future rebrand pass.

--------------------------------------------------------------------------------
8. KNOWN GAPS - DO NOT ASSUME COMPLETE
--------------------------------------------------------------------------------
CORRECTION (this section was wrong in an earlier version of this doc): it
previously claimed "LTR 1220 and LTM 1150 have partial/missing data... no
CAD/mat-position chart for 1220/1150." The LTM 1150 part was FABRICATED —
not from the real app. It was pulled from a pasted chat-log snippet showing
an alternate, NEVER-SHIPPED draft (a "unified crane registry" rewrite with
a CRANES array that happened to include a placeholder 'ltm1150' entry).
That draft never went live. LTM 1150 does not exist anywhere in the actual
app - not in Tab 1, Tab 2, Tab 3, or the Reeving Plan tab. Verified by
direct grep against the live index.html: zero matches for "1150".
Lesson for future sessions: don't treat pasted chat-log content as
automatically describing the shipped app — alternate drafts and abandoned
attempts can appear in logs too, and must be cross-checked against the
actual current file before being written into documentation as fact.

Verified, current state (checked directly against index.html):
- LTR 1220 (as "1220"/"1220-tele") HAS real data in Tab 1 (Rope
  Retentioning), Tab 2 (Hook Block Weights), and the Reeving Plan tab.
  It has NO entry at all in Tab 3 (Outrigger/CAD) - not even a disabled
  option, it's simply absent from the crane dropdown and from
  cadFleetData. If CAD data for the 1220 is ever sourced, it needs adding
  to both the dropdown options and cadFleetData/FOOTPRINTS.
- 21 orphaned reeving SVGs not linked into any manifest entry (see
  section 5.6) — lrt1100_tele_1 through 14 plus special1-3, and
  ltm1110_sks_1-4. Two candidate source PDFs for filling this gap were
  identified: EINSCHERPLAN____98025975_000_en.pdf (simple 3-config sheet:
  load hook, 1-pulley, 3-pulley) and EINSCHERPLAN_TELE__98025310_001.pdf
  (full 1-14 way "Normaleinscherung" master sheet). Neither has been
  matched to a specific crane/config or processed into the manifest yet -
  confirm which crane these belong to before linking them in.
- Tab 2's possible n double-counting in G = L x M x n x F - flagged, never
  resolved either way.
- Tab 4's actual design/build reasoning is NOT recoverable from chat
  history — only from code inspection (see coverage note at top). If the
  person can locate the Claude Code / tablet session that built it, that
  would be a strictly better source than this document's section 5.
  UPDATE: confirmed via file birth timestamps that Tab 4 (reeving/svg/*,
  reeving/manifest.json) was built within THIS SAME overall conversation
  thread, in a portion that occurred before a context-compaction event —
  not a separate/external session as originally assumed. The person
  uploaded a batch of OEM "EINSCHERPLAN" (reeving plan) PDFs earlier in
  this same thread; those are the actual source documents, extracted via
  PDF rasterization/vector extraction into the SVG set. The specific
  turn-by-turn reasoning from that portion of the conversation is still
  not recoverable (compacted out of context), but the provenance is now
  confirmed rather than speculative.
- This document should be updated whenever a future session makes a
  material change to any convention above - especially section 4 (CAD
  tab), since those sign conventions are easy to silently reverse and hard
  to notice visually until a real shift scenario is tested.
- GENERAL CAUTION for whoever reads this doc: verify claims against the
  live index.html directly (grep for the specific crane ID or feature)
  before trusting any specific factual claim in this document, including
  claims elsewhere in this same document. Errors can and do slip in.

--------------------------------------------------------------------------------
9. HOW TO RE-EXTRACT REEVING DIAGRAMS FROM AN OEM SOURCE PDF  [CONVERSATION]
--------------------------------------------------------------------------------
Use this whenever a reeving diagram is missing, incomplete, or genuinely
wrong for some crane/config, and the person can supply the source
"EINSCHERPLAN" PDF for it. First done for LTR 1220 (all 15 diagrams: ways
1-14 + 16), which turned out to have TWO separate bugs worth telling apart
before jumping to this procedure:

  a) DISPLAY bug (fixed once, fixed everywhere, no re-extraction needed):
     inline <svg> elements with only a viewBox (no width/height attrs)
     don't reliably compute proportional height via CSS height:auto, and
     once .snippet-wrap got overflow:hidden (for pinch-zoom), that
     ambiguity started silently cropping the BOTTOM of every diagram.
     Fixed globally in fixInjectedSvgSizing() (section 4.7-adjacent JS) by
     explicitly setting aspect-ratio from the SVG's own viewBox at
     injection time. If a diagram looks cut off at the bottom, check this
     is actually still working before assuming the source data is bad.
  b) DATA gap (needs this section's procedure): the SVG file itself is
     genuinely missing content — verified by rendering the raw file's
     path data independently (see 9.1) rather than trusting the app's own
     display. LTR 1220's diagrams were missing the hook symbol + caption
     text entirely; the original extraction (whoever/whatever did it,
     see section 5) simply hadn't captured that part of each diagram.

9.1 Diagnose before re-extracting — confirm it's actually a data gap
  No SVG rendering tool is available in this sandbox by default
  (rsvg-convert, inkscape, cairosvg all absent; ImageMagick's convert
  delegates to rsvg-convert, which is also absent). matplotlib IS
  available and can render the path data manually:
    - Parse the SVG's viewBox and every <path d="..." transform="matrix(...)">.
    - For each path, tokenize the d-string (this fleet's diagrams only
      ever use M/L commands — straight segments, no curves — so a simple
      "M x y L x y L x y..." tokenizer is sufficient; check first with
      `grep -oE '[A-Za-z]' file.svg | sort -u` before assuming this holds
      for a new document).
    - Apply the path's own transform matrix to every point, plot with
      matplotlib, set axis limits to the viewBox, invert the y-axis
      (SVG y grows downward), aspect='equal', axis off.
  If the rendered result already contains the full diagram, the bug is
  (a), not (b) — don't re-extract, go fix the display code instead.

9.2 Get a clean full-page vector extraction
    pdftoppm -png -r 150 source.pdf /tmp/page       # raster reference
    pdftocairo -svg source.pdf /tmp/page_full.svg    # full-page vector
  Both worked despite these PDFs being RC4-encrypted with copy:no — the
  encryption only blocks text/content extraction via some tools, not
  rendering/rasterization via poppler's own tools. If pdftocairo ever
  fails outright on a future document, that's the point to flag rather
  than force it.
  pdftocairo's output has NO per-diagram cropping — it's the whole page,
  one flat list of hundreds of <path> elements, each usually sharing one
  transform matrix. This is a DIFFERENT, independent extraction pipeline
  from whatever produced the original ~161 diagrams (see section 5.3) —
  it will NOT reproduce their exact styling by default (see 9.5).

9.3 Locate each diagram's crop box precisely — do NOT guess proportions
  The naive approach (assume N evenly-spaced columns across page width,
  e.g. column width = page_width/N) DRIFTS across a wide grid and will
  clip content by the 4th-7th column even though the first 1-2 columns
  look fine. This happened on the first attempt here — don't repeat it.
  Reliable method: detect each "Reeving N-way" LABEL's precise pixel
  position via ink-density column/row scanning on the RASTER image,
  restricted to a tight y-band (or x-band, for rows) that contains ONLY
  the label glyphs — not the underline beneath them, not pulley circles,
  not caption text below. Getting this band wrong (e.g. accidentally
  scanning the underline instead of the letters) gives confidently wrong
  clustered positions that still look plausible — cross-check against a
  manual crop-and-view of at least one column before trusting it.
  Concretely (numpy on the raster array):
    col_ink = (255 - arr[y0:y1, :]).sum(axis=0)   # darkness per x-column
    threshold = col_ink.max() * 0.1
    # find contiguous runs above threshold, merge runs <150px apart
    # (one run per glyph-cluster; merge gap bridges letter-spacing)
  Do this once for columns (using a y-band that isolates label text) and
  once for rows (using an x-band inside one known-good column, scanning
  the full page height). Verify the detected pitch is CONSISTENT across
  the full range (e.g. 927px every column, not drifting) before trusting
  it — if one detected gap is a clear outlier vs the rest, trust the
  consistent pitch and treat that one detection as noise.
  Crop box per diagram = (label_x - margin, label_y - margin,
  label_x - margin + crop_width, next_row_y - margin), where crop_width
  should comfortably clear the widest diagram content (checked visually)
  without bleeding significantly into the next column.

9.4 Filter paths into per-diagram files (don't duplicate the whole page)
  Convert each pixel crop box to the SVG's own coordinate space using the
  scale factor (PDF page point-size / raster pixel-size — pdfinfo gives
  the former, the PNG's own dimensions give the latter). Then, rather
  than writing the ENTIRE full-page path list into every output file
  (which would bloat each one to ~1.5MB), filter to only the paths whose
  OWN transformed bounding box intersects that diagram's crop rectangle
  (small padding, ~5 units, to avoid edge-case exclusion). This keeps
  output file sizes in the same 50-95KB range as the rest of the fleet's
  diagrams. Write each as a standalone
    <svg xmlns="..." viewBox="x y w h">...filtered paths...</svg>

9.5 Fix the stroke color — pdftocairo output does not match fleet convention
  The ~161 pre-existing diagrams all use stroke="#e8eaed" (light,
  legible against this app's dark background). A fresh pdftocairo
  extraction outputs plain stroke="rgb(0%, 0%, 0%)" (black) instead —
  invisible against the dark background, and easy to miss in review
  since the SVG itself still "looks right" in isolation on a white
  background. Fix both the specific new files (sed replace the stroke
  value) AND rely on the permanent CSS safety net already in index.html
  (`.zoom-inner svg path { stroke: #e8eaed !important; }`) which forces
  the correct color regardless of what any diagram file carries — added
  specifically so this can't silently recur on a future re-extraction.

9.6 Verify before deploying
  Render every new file with the same manual matplotlib technique from
  9.1, tile them into one contact-sheet image, and visually check the
  whole set at once (label, full diagram, caption all present, nothing
  bleeding badly from a neighboring column) before replacing the live
  files. This caught the stroke-color issue and confirmed the crop grid
  was correct across all 15 files in one pass, rather than discovering
  problems one crane at a time after deploy.
  Then: bump CACHE_VERSION in sw.js, copy the new SVGs into
  reeving/svg/, redeploy. No manifest.json changes needed if the
  filenames match what's already referenced there.

--------------------------------------------------------------------------------
10. TAB 5 - COUNTERWEIGHT COMBINATIONS  [CONVERSATION]
--------------------------------------------------------------------------------
Purpose: a new-operator-facing tool for the crane's main superstructure
counterweight (rear ballast stack) - distinct from Tab 1/2's hook-block
auxiliary ballast (aux[] in FLEET_BLOCK_SPEC), which is a different weight
system entirely. Two entry points into the same state: tap plates directly
in a visual stack, or jump straight to a known combination from a dropdown.

10.1 Data source and extraction method
  Person supplied Liebherr doc LTM 1250-5-1-001 (0213656-02), chapter 4.07
  "Counterweight" (c) Liebherr-Werk Ehingen GmbH 2021, as an "unlocked" PDF
  (permissions removed, no longer encrypted - unlike the reeving PDFs in
  section 9, poppler wasn't even needed here for that reason). Still hit an
  obstacle: pdftotext/pdfinfo/poppler-utils were not installed in this
  session's sandbox at all (unlike whatever session wrote section 9 - tool
  availability isn't constant run to run, don't assume section 9's toolchain
  is present). Installed PyMuPDF via pip instead and read the page text
  directly.
  The extracted text was garbled - e.g. "Counterweight" read back as
  "&RXQWHUZHLJKW". This turned out to be a uniform per-character code-point
  shift of -29 applied by the embedded Arial subset font (i.e. real_char =
  encoded_char + 29), NOT a font-rendering bug - decoded correctly for
  every letter, digit, and punctuation mark once identified. Two exceptions
  confirmed by testing: literal ASCII space (0x20) is untouched, not
  shifted; and a handful of already-correct high-value/non-ASCII glyphs
  (e.g. the "+-" copyright-line character at 0xB1, and a bullet-point
  character in a separate LucidaSansUnicode font) are also untouched -
  shift was only applied for character codes <=100 excluding literal space.
  If asked to extract another counterweight-combination PDF later, check
  whether this same shift pattern applies before assuming it does - test
  by decoding one word and confirming it reads correctly, the way this was
  verified here.

10.2 Data shape - it's a subset-sum catalog, not a literal single stack
  Every one of the 21 combinations printed in the source chart was checked
  against the subset-sum of a fixed set of 8 physical components' weights -
  all 21 reconcile exactly, with no remainder. That means the real data
  model is "which of 8 fixed components are present," not a monotonically-
  growing single physical stack - e.g. going from 41t to 43t swaps plate 3
  out for plate 5, it doesn't just add another plate on top. Only these 21
  of the 8 components' 256 possible subsets are OEM-published/approved.
  This directly parallels Tab 1's aux[] philosophy (real OEM combinations
  only, not generic increments) - same reasoning, independently arrived at
  for a different weight system.
  One combination is an exception worth knowing about: the 5t combo is
  plate 6 ALONE - it's the only non-zero combo that does NOT include the
  base receptacle plate (plate 1). Confirmed directly from the source
  chart, not a data-entry error.

10.3 Interaction design - free click + validation, not free click alone
  Considered restricting the click-to-build mode to only ever produce one
  of the 21 known-valid subsets (matching Tab 1's readonly-ballast-dropdown
  philosophy exactly). Person explicitly chose a looser design instead:
  free-click any plate combination, but flag with an explicit warning
  banner - not a silent block, not a silent accept - whenever the resulting
  subset isn't one of the 21 published combos. This mirrors Tab 2's
  precedent (explicit warning banner over silent fallback) rather than
  Tab 1's precedent (hard-block invalid input via readonly field). Don't
  "tighten" this into a hard block in a future pass without asking first -
  it was a deliberate choice, not an oversight.
  The dropdown ("Jump to Known Combination") and the clickable stack share
  the same underlying selection state - selecting from the dropdown updates
  the stack's highlighted plates and vice versa is effectively true too
  (clicking to reconstruct a known combo shows the same "approved" status
  the dropdown entry would have given it).

10.4 Visual design - superseded once, now click-on-the-real-diagram
  Went through two visual designs in the same session before landing.
  v1 (abstract bars): plate weights only, no thickness/footprint/stacking
  sequence in the combination chart (section 10.1's source), so the first
  build used an abstract vertical bar stack - bar height proportional to
  weight, stacking order INFERRED from how combos build up across the
  chart (e.g. plate 1 appears in every non-zero combo except the 5t one,
  suggesting it's the base plate), hook ballast 8 drawn detached since it
  seemed unlikely to be a literal stacked plate.
  The person then supplied a separate OEM exploded-view assembly diagram
  (parts numbered 1-8, matching this tab's component numbering exactly),
  which CONFIRMED the inferred order was right on both counts - plates 1-7
  mount bottom-to-top in exactly that numeric sequence (plate 1, a small
  twin-pin mounting piece, directly on the turntable; plate 7, the
  tallest/boxiest piece, at the top), and component 8 mounts to the LEFT
  and RIGHT sides of the stack rather than on top of it.
  v2 (current - click directly on that diagram): once the order was
  confirmed, the person asked for the click/select interaction to happen
  ON the real diagram itself rather than in an abstract bar list ("easier
  ... to visualize from what's in front of them, not a text box"). This is
  the shipped design. Mechanics:
    - The exploded-view image is embedded directly in the PDF as a RASTER
      (PNG XObject, 1831x1620), not live vector paths - confirmed by
      checking every vector drawing on that PDF page against the image's
      bounding box; zero overlap. So unlike the reeving diagrams (real
      extracted vector SVGs, section 5.3/9), this is a genuine raster
      image, extracted via PyMuPDF's doc.extract_image() and stored as
      counterweight/img/ltm1250-cwt-exploded.png (parallel directory to
      reeving/svg/, same "actual OEM artwork, extracted directly, not
      redrawn" framing, same in-app disclaimer convention).
    - Each component has a hotspots[] array (COUNTERWEIGHT_DATA), rendered
      as an SVG <polygon> overlay (viewBox="0 0 1831 1620", matching the
      diagram's own natural pixel size exactly) positioned over the <img>.
      First pass used generous hand-fit BOUNDING-BOX rectangles (eyeballed
      off a pixel grid overlaid on the raw PNG). Person then asked for the
      highlight to follow each plate's actual silhouette instead ("easier
      ... not a text box" feedback extended to "not a box at all, either").
      Re-derived algorithmically with OpenCV rather than hand-traced:
        1. Crop a generous region around each part (from the same rough
           boxes as the v1 rectangles).
        2. Threshold to a binary ink/paper mask, morphological CLOSE to
           bridge small 1-2px gaps in the linework.
        3. Flood-fill the mask's true exterior from a crop corner; the
           silhouette is everything the flood fill did NOT reach (ink
           itself plus any fully-enclosed interior detail - holes, ridges).
        4. Morphological OPEN (9x9 kernel) to strip thin spikes - this
           specifically removes leader-line-to-callout-number artifacts
           that the first attempt (without this step) picked up as part of
           the plate's own silhouette (e.g. a thin tail running from plate
           6 or plate 5's body up to their number label).
        5. cv2.findContours (RETR_EXTERNAL) on what's left, take the
           largest contour, cv2.approxPolyDP (epsilon = 0.6% of perimeter)
           to reduce point count into a clean polygon.
      Verified by re-drawing every resulting polygon on top of the source
      image and visually confirming a clean fit (no leftover spikes, no
      leakage into neighboring parts) before pasting the point lists into
      COUNTERWEIGHT_DATA. This is closer to a real traced outline than the
      v1 rectangles, but it's still an algorithmic approximation (ink
      thickness inflates the boundary slightly outward; opening can round
      off a genuinely sharp small corner) - not hand-verified point-by-
      point against the CAD source, because there is no CAD source, only
      this raster. If the diagram image is ever replaced (higher-res
      rescan, different crane), this whole derivation needs re-running
      against the new raster - the polygons are fit to this specific PNG's
      pixels, not to anything in the PDF that would carry over automatically.
      Two bugs caught after the first pass shipped, both worth knowing
      about if re-deriving polygons for a future crane's diagram:
        - Plate 1's OPEN kernel (9x9, same as every other part) was large
          enough to sever the thin neck connecting its left mounting post
          to the base plate, silently dropping that post from the
          silhouette entirely. Fixed by using a smaller kernel (3x3) for
          this part specifically - there's no single kernel size that's
          safe for every part, thin real geometry (like this post) can
          look identical to a leader-line artifact to a fixed-size opening.
          Always visually check parts with thin connected sub-features, not
          just the ones near a text label.
        - Plate 1's crop box was also just too small on the right/bottom -
          cut off before the plate's actual right edge and the small
          connector-tab detail near the "11t" label, so that whole corner
          was outside the crop and could never have been traced regardless
          of kernel size. Caught by the person comparing a screenshot
          against the live selection, not by the earlier "re-draw on the
          source image" verification pass - that check confirms the traced
          polygon fits cleanly within its crop, it does NOT confirm the
          crop itself was generous enough to contain the whole part. Pad
          crop boxes more generously than seems necessary, especially near
          image edges or small printed labels/tabs, and specifically check
          each part's true extent against a pixel grid (like section 9.3's
          reeving-diagram approach) rather than eyeballing a bounding box
          once and moving on.
    - Component 8 ("2x Hook Ballast 8") gets TWO hotspots (left box, right
      box in the diagram) that both toggle the SAME single component id -
      it's one logical 20t item in the data model (matching the source
      chart's single "2x Hook Ballast 8: 20.0t" line), just drawn/clickable
      in two places since the physical pair really does mount on both
      sides. Clicking either one selects/deselects both simultaneously.
    - Selected hotspots get a gold outline + translucent fill directly on
      top of the diagram; unselected ones are fully invisible (transparent
      fill/stroke) so the diagram reads clean until something's picked.
      Hover/focus gets a faint stroke for discoverability. Polygons carry
      tabindex/role=button/aria-label and an Enter/Space keydown handler,
      not just a click handler, for basic keyboard reachability - the app
      doesn't do this everywhere (e.g. the CAD tab's canvas has no keyboard
      equivalent), but it was cheap to add here and there's no reason not to.
  The abstract bar-stack (v1's .cwt-stack/.cwt-plate CSS, PX_PER_T sizing)
  and the bounding-box rectangle hotspots (v2) were both removed entirely,
  not kept as fallbacks - don't resurrect either without asking, the
  polygon-on-diagram overlay is the intended interaction now.

10.5 Plate 6 centering notice
  Several combos in the source chart printed a NOTICE: "Counterweight
  plate 6 not centered correctly!" specifically when plate 6 is included
  without BOTH plates 3 and 4 also present. Implemented as a second,
  independent warning condition (separate from the approved/not-approved
  check) - a selection can be simultaneously OEM-approved AND trigger this
  centering notice (e.g. the 38t combo: plates 1+2+5+6 is an approved
  total, but still lacks 3+4, so still shows the centering warning).

10.6 Scope - one crane only, deliberately (UPDATED - see 10.7, no longer true)
  Only LTM 1250-5.1 ('1250') had data at first. Person explicitly chose to
  scope the first version to just this crane rather than block on sourcing
  combination charts for the rest of the fleet. COUNTERWEIGHT_DATA is keyed
  by crane id specifically so adding another crane later is "add another
  top-level key with components[]/combos[]," not a rebuild - LTM 1110-5.1
  was added this same session (10.7) following exactly that pattern, though
  it needed real extensions to the data model, not just new numbers.

10.7 LTM 1110-5.1 added - same session, new patterns needed
  Source: Liebherr doc LTM 1110-5-1-000 (0214357-01), chapter 4.07
  "Counterweight" (pages 12-20/49), (c) Liebherr-Werk Ehingen GmbH 2021 -
  a 14-page excerpt the person supplied directly (not encrypted this time,
  no unlock step needed), confirmed complete (28.2t is genuinely the top
  of the range, not a cut-off). Extracted via the exact same PyMuPDF
  character-shift decode as 10.1 - same -29 offset worked unchanged, worth
  re-verifying on a case-by-case basis anyway (see 10.1's own caution about
  this), but it held here. Also note: poppler/pdftotext was AGAIN not
  installed in this sandbox session (see 10.1 - tool availability isn't
  constant), PyMuPDF was already available from before so no reinstall
  needed this time.

  Unlike 1250, this crane's data didn't fit the existing model unchanged -
  three genuinely new patterns were needed. Don't assume 1250's model is
  "the" model; check for these same three questions before adding a future
  crane:

  a) Non-counting items exist. Two components - "Winch 2*" and "Replacement
     Ballast" - are both 0.8t and occupy the same physical mounting slot
     (confirmed via the diagram - slot 8, above plate 7). Checking every
     combo that lists either one against its own printed total revealed:
     Replacement Ballast's 0.8t always adds to the total; Winch 2*'s 0.8t
     NEVER does (the printed total only reconciles with it excluded, in
     all 8 combos where it appears). This is a real, confirmed OEM
     accounting rule (the winch is a working machine component, not
     ballast, even though it occupies the ballast-adjacent slot and gets
     listed in the same "Composition" breakdown) - NOT a data error, don't
     "fix" it to count like everything else. Modeled as `countsToward:
     false` on the component - cwtComboWeight() skips it when summing, but
     the Composition table still displays it (labeled "not counted in
     total") since the OEM doc does list it as part of the breakdown.
  b) Mutually-exclusive slots exist. Winch 2* and Replacement Ballast can't
     both be present (same physical slot) - at most one of the two is ever
     selected. Modeled via `slotGroup: 'slot8'` on both; clicking the one
     hotspot (only the Winch 2* entry carries actual hotspot points, the
     Replacement Ballast entry's hotspots is deliberately empty) cycles
     none -> Winch 2* -> Replacement Ballast -> none via
     cycleCwtSlotGroup(), rather than a plain per-plate toggle. The visual
     highlight and label reflect whichever member is currently active, not
     always the hotspot-owning component - see the `activeInGroup` lookup
     in renderCwtTab().
  c) Always-paired plates with unresolvable overlapping silhouettes exist.
     Plates 3 and 4 (both 6.0t) appear together in every single one of the
     38 published combos - never one without the other - AND their
     silhouettes overlap with no clean dividing line in the source
     diagram (unlike every other plate pair on both cranes so far, which
     sit with a visible gap). Attempted the same flood-fill contour method
     as everywhere else; it fuses them into one blob because there's
     genuinely no boundary to detect. Rather than guess a line, modeled as
     `linkedId` - plate 4 owns the one clickable region (a generous
     rectangle spanning both, since a precise outline isn't extractable
     here either), and toggling it syncs plate 3 to match via a new check
     in toggleCwtPlate(). The Composition table still lists them as two
     separate 6.0t line items (matching the OEM doc), only the click
     target and highlight are merged.
     Also: 25.4t appears TWICE in the chart with genuinely different
     compositions ({2,3,4,5,+Winch2*} and {2,3,4,6,7,+Replacement
     Ballast}, both on top of 1+3+4) - a real chart quirk, not a
     transcription duplicate, confirmed by both compositions individually
     reconciling to 25.4 by subset-sum. cwtFindMatchingCombo() already
     matches on the full item SET not just total weight, so this doesn't
     cause any ambiguity in validation - only the dropdown needed checking
     (its label lists components, not just weight, so the two entries are
     visually distinct there too).

  Also un-hardcoded three things that were 1250-specific in the first
  build, now crane-generic:
    - The diagram <img>/svg viewBox are no longer fixed to 1250's file and
      1831x1620 - each crane's data carries diagramImage/diagramWidth/
      diagramHeight, applied in renderCwtTab() on every render (cheap,
      idempotent, simpler than trying to only-update-on-crane-change).
    - The Fig./doc-number disclaimer text below the diagram was hardcoded
      HTML; now a `figRef` field per crane, injected via a span.
    - The plate-6-centering notice was a hardcoded check for id 'p6' and
      the literal 1250 warning text. Generalized to loop over any
      component with `centerTrim: true` (currently only 1250's p6) - the
      warning TEXT still hardcodes "plates 3 and 4" since that's what the
      1250 doc's requirement actually says, not a generic placeholder.
  Scope stayed the same as 1250 per the person's explicit choice: no
  fastening/bitt-location detail (section 4 of the 1110 doc, and the
  per-combo pin/bolt/screw "Note" blocks embedded in section 5 itself)
  was surfaced in the tool - out of scope for now, same as 1250.

  Diagram: Fig. 155645 (page 5 of the excerpt), extracted the same way as
  1250's (raster PNG XObject embedded in the PDF, not vector - see 10.4's
  method). This one includes a bonus assembled/composite view alongside
  the exploded stack (kept in the same image, only the left/exploded half
  has hotspots - the assembled half can't be clicked per-part, it's a
  single fused illustration). Hotspot derivation mostly followed 10.4's
  algorithm unchanged, with two new lessons:
    - Kernel size for the morphological OPEN step isn't a fixed constant
      even within one diagram - plate 1's posts needed 3x3 (9x9 severed
      them, same failure mode as 1250's plate 1), everything else here
      used 7x7. Check parts with thin connected sub-features individually,
      don't assume one kernel size works for a whole diagram.
    - Crop boxes were sized generously up front this time (informed by the
      1250 plate-1 lesson), and no post-ship crop-boundary bug turned up
      for this crane as a result - reinforces that the earlier miss was a
      process gap (verify-against-source-image doesn't catch an undersized
      crop, only a bad trace within a correctly-sized crop), not a one-off.

10.8 LTM 1130-5.1 added - different diagram style, per-unit weight trap
  Source: Liebherr doc LTM 1130-5-1-008 (0212849-05), chapter 4.07
  "Counterweight" (pages 2-15/39), (c) Liebherr-Werk Ehingen GmbH 2020.
  28 combos, all verified by subset-sum - but only after catching a real
  data trap on the first pass (below). Same PyMuPDF -29 character-shift
  decode as every prior crane, unchanged.

  a) Per-unit vs combined-pair weight - a real trap, not a formatting
     quirk. The source chart lists "2x auxiliary ballast 8 / Individual
     weight 6.35" for one line. First-pass subset-sum check using 6.35 as
     the item's TOTAL contribution failed by exactly 6.35t on every combo
     that included it (e.g. the 39.1t combo undershot to 32.75 using
     6.35, but landed exactly on 39.1 using 12.7). "Individual weight"
     here means weight PER SIDE, doubled for the pair's actual
     contribution - the opposite convention from LTM 1250's "2x Hook
     Ballast 8 / 20.0t" line, where 20.0 WAS already the combined total,
     not per-unit (confirmed the same way, via its own subset-sum check
     passing directly with no doubling needed - see 10.2). Two "2x ..."
     items on two different cranes' charts, two different conventions -
     the label format alone doesn't tell you which one you're looking at.
     ALWAYS verify every "2x"/"pair"-style line item's convention via
     subset-sum before trusting it, per-crane, no matter how similar the
     wording looks to a crane already done. Modeled here with the doubled
     value (12.7) baked directly into the component's weight field, same
     pattern as 1250's p8 (a single combined-weight component with two
     hotspots, not two separate weighted components).
  b) "Replacement weight 7 or winch 2*" is ONE line item here (single
     name, single weight, 2.9t) that always counts fully toward the
     total in every combo it appears in - confirmed by subset-sum, no
     exclusion needed. Do NOT assume this crane needs 1110's
     countsToward/slotGroup treatment just because the name pattern
     ("winch...*") looks similar - 1110's Winch 2*/Replacement Ballast
     were two distinctly named, distinctly weighed, mutually-exclusive
     items with an asymmetric counting rule; this crane's chart presents
     the winch-or-replacement-weight choice as a single pre-merged line,
     no asymmetry to encode. Check each crane's chart on its own terms.
  c) Several totals carry a trailing "*" in the source chart (e.g. "0.0*",
     "2.2*", "5.9*") with no footnote text captured in this excerpt
     explaining what it means - possibly a road-travel-configuration flag
     (the doc's intro references chapter 3.04 for road-travel counterweight
     distribution) but this is NOT confirmed, don't assume it. The
     asterisk is not encoded in the app's data at all - stripped, numeric
     value used as normal. If a future full manual or the person confirms
     what it means, come back and encode it (e.g. as a badge/note) rather
     than guessing now.
  d) Diagram (Fig.103261) is a different STYLE from 1250/1110 - a side/
     front elevation view (plates as flat horizontal bands, each spanning
     the full stack width, weight printed directly on each band) plus a
     separate rear view below it showing the two auxiliary-ballast wings
     (item 8) mounted left/right. This is actually SIMPLER to make
     hotspots for: plates 1-7's boundaries are straight horizontal lines,
     so plain rectangles (read directly off a pixel grid, no OpenCV
     contour tracing needed) fit them exactly - unlike the isometric
     1250/1110 diagrams where a rectangle was only ever an approximation.
     The two auxiliary-ballast wings (item 8) DID get the full OpenCV
     contour treatment (mirror-image shapes, worth tracing properly) -
     and hit the SAME crop-too-narrow failure as 1250's plate 1 on the
     first attempt (right edge of the crop cut through the wing's real
     shape, so the traced contour silently stopped at the crop boundary
     instead of the part's actual edge) - re-confirms 10.4's lesson that
     "verify by redrawing on the source image" only catches a bad trace
     within a correctly-sized crop, not an undersized crop; still have to
     deliberately check each part's true extent, every time.
     Item "10" printed on the diagram (a small centering cone) is NOT a
     counterweight component - it's a mounting fixture mentioned in the
     text ("taking down counterweight plate 1 on the centering cone 10"),
     zero weight, never appears in any combination's composition list.
     Correctly excluded from components/hotspots - don't add it if a
     future pass looks at this diagram and wonders why "10" has no data.
  Scope: same as every prior crane - no fastening/tightening-torque/
  bitt detail (sections 1.2-2.3 of this doc) surfaced in the tool.

10.9 LTM 1160-5.2 added - repeated identical plates, imprecise hotspots
  Source: Liebherr doc LTM 1160-5-2-003 (0212601-04), chapter 4.07
  "Counterweight" (pages 8-10/28), (c) Liebherr-Werk Ehingen GmbH 2021 -
  a short 3-page excerpt (just the combination chart, no assembly/
  fastening pages included this time, unlike 1110/1130). Same PyMuPDF
  -29 decode as every prior crane.

  a) Repeated identical plates, not present/absent. The chart's
     "Composition" breakdown lists "Counterweight plate 3 / 10" up to
     THREE times in a single combo (e.g. the 50t combo lists it 3x) -
     confirmed against the diagram, which shows three separate physical
     10t plates each individually numbered "3" by their own callout line
     (plus a 4th, differently-numbered "2", visually identical/same
     weight but a distinct part). This is a genuinely different pattern
     from every prior crane (present-or-absent, or 1110's mutually-
     exclusive slot) - here the same named plate can be included 0, 1, 2,
     or 3 times. Modeled as three separate components (p3a/p3b/p3c) with
     the SAME name ("Counterweight Plate 3") and SAME weight (10t) rather
     than inventing a quantity/counter UI control - this matches how the
     source doc itself presents it (repeated line items, not "3x"), reuses
     the existing per-component toggle/hotspot machinery with zero new
     mechanism, and the Composition table naturally ends up showing
     "Counterweight Plate 3" multiple times too, same as the OEM chart.
     If a future crane needs an even larger repeat count, this same
     pattern (one lettered component per physical copy) still works, it
     just means more entries - reconsider only if the count gets large
     enough that hand-listing every copy stops being reasonable.
  b) Diagram (Fig.124343) is a JPEG (not PNG like every prior crane) and
     is WHITE-ON-BLACK (inverted from every prior crane's white-
     background/black-line convention) - handled fine as-is, the
     .cwt-image-wrap's white background CSS just never shows through
     since the JPEG has no transparency and fully covers the wrap. Also
     the smallest fleet plate set encountered so far - just 4 distinct
     named components (1, 2, 3, 4) versus 1250/1110's 7-8.
  c) Hotspot precision is LOWER than every other crane so far, and this
     is a deliberate, documented trade-off, not an oversight. The diagram
     shows the SAME 5 physical plates from two simultaneously-visible,
     overlapping isometric faces (front-left face and front-right face of
     one stack, at different screen positions due to the projection), all
     tightly interlocked with no clean gaps between them - the flood-fill
     contour method that worked cleanly for 1250/1110's more separated
     isometric plates was not attempted here because there's no clean
     "outside" region between adjacent plates for it to find. Hotspots
     are hand-picked rectangles read off a pixel grid (same category as
     1250's original v1 attempt, before it was upgraded to traced
     outlines per the person's feedback) - functional and roughly
     positioned, but not outline-accurate. If this needs the same
     polish pass 1250 got, that's a known follow-up, not a bug.
  Scope: same as every prior crane, no fastening/bitt detail (none was
  even included in this particular excerpt to begin with).

10.10 LTM 1650-8.1 added - PARTIAL DATA, quantity stepper, load-chart-vs-
      total-weight distinction (a THIRD independent confirmation of the
      Winch-doesn't-count pattern)
  Source: Liebherr doc LTM 1650-8-1-000 (0213131-03), chapter 4.07
  "Counterweight" - person supplied pages 3-19, but the doc's own footer
  reads "4.07 ± 19/93" - this is 17 pages out of a 93-PAGE chapter. This
  is the fleet's flagship 700t-class crane; the confirmed data only
  covers "load chart" configurations 0 through 175t, and the document
  almost certainly continues well past that (an 8-axle crane this size
  routinely ballasts heavier than 175t). DO NOT present this crane's data
  as complete anywhere - the in-app sourceNote says so explicitly
  ("PARTIAL DATA... confirmed combinations from 0-175t only"), keep that
  wording (or equivalent) if this section is ever edited. If the person
  supplies the remaining ~76 pages later, this is a data-extension job,
  not a rebuild - same components, more combos entries, possibly more
  qty10 slotGroup members if quantities go past 14.

  a) "According to the load chart" vs "Total weight" - a third,
     independent confirmation of the same pattern first found on LTM 1110
     (Winch 2*) and reasoned-about differently on LTM 1130 (single merged
     line, no exclusion needed there). This document is explicit about it
     in plain prose, no reverse-engineering required this time: "When
     calculating the counterweight combinations, winch 2 and winch 3 are
     not taken into consideration, but are still listed in the total
     weight column." Two winches, both countsToward:false, confirmed
     against every one of the 64 non-zero-level combos (each load-chart
     level has 4 winch variants: neither, winch 2 only, winch 3 only,
     both - all 4 correctly compute to the SAME load-chart total once
     their weights are excluded, exactly as the doc states). Winch 2 =
     4.7t, Winch 3 = 7.9t (includes luffing pulley block per the doc).
     Three cranes, three different presentations of "some component's
     weight doesn't count toward the number that matters" (1110: two
     separate mutually-exclusive named items; 1130: merged into one
     line, no exclusion; 1650: two independent items, explicit prose
     rule) - reinforces 10.7's lesson that this needs checking per-crane,
     every time, never assumed from a prior crane's pattern.
  b) Quantity, again, but too many repeats for 1160's approach. LTM 1160's
     three physically-identical plates got three separate lettered
     components (p3a/p3b/p3c) because three was a manageable number to
     hand-list. This crane's "Counterweight plate 10t" appears in
     quantities of 0/2/4/6/8/10/12/14 (in just the partial data available
     - could go higher) - hand-listing 7 lettered copies (p10a...p10g) was
     judged unwieldy and, more importantly, doesn't communicate "this is a
     quantity, always in steps of 2" the way a stepper does. Components
     p10_2/p10_4/.../p10_14 share slotGroup:'ten'.
     FIRST ATTEMPT (shipped, then corrected same session): reused
     cycleCwtSlotGroup() unmodified from 1110's Winch 2*/Replacement
     Ballast pattern - only p10_2 carried a hotspot, repeat clicks on that
     one spot cycled through the quantities. This was a real usability bug,
     not just a style choice: the diagram itself draws all 7 physical
     plate positions per side (14 icons total, all identical-looking,
     all equally "clickable-looking" to someone reading the picture), but
     only ONE of those 14 icons actually did anything - the person
     reported "I can't select the slabs to build up to the different
     combos" almost immediately after this shipped. Caught by the
     person's own use of the feature, not by this session's own testing
     (Playwright testing had confirmed the single hidden hotspot DID work
     when clicked - it just never checked whether the OTHER 13 visible,
     equally-plausible click targets also worked, which is exactly the
     kind of gap automated testing of a predetermined interaction misses
     and a real user immediately finds).
     FIX: added `quantityGroup: true` to distinguish this ladder pattern
     from 1110's alternatives pattern, gave every one of the 7 levels its
     own hotspot on BOTH the left and right diagram icons (14 hotspots for
     this one component group), and added a new function,
     selectCwtSlotLevel(), with different semantics from
     cycleCwtSlotGroup(): clicking a level jumps straight to that
     quantity (fills the stack from the bottom up to and including the
     clicked level, highlighting every icon up to there, not just the one
     clicked) rather than always advancing by one step; clicking the
     CURRENT top level removes it, one step back down. This matches how
     someone would actually think about a physical stack - "I want it
     built up to here" / "take the top one off" - and every visible icon
     in the diagram now does something, which is the real fix (the
     original bug was fundamentally "there are visible things that look
     clickable but aren't," not "the cycle direction is wrong").
     cycleCwtSlotGroup() itself is unchanged and still correct for 1110's
     genuinely-alternative (not quantity) slotGroup case - the two
     functions coexist, selected by the quantityGroup flag at render time.
     LESSON for any future quantity-ladder crane: if the source diagram
     draws multiple physical copies of a repeated part, give EVERY drawn
     copy its own hotspot from the start - don't default to "one hidden
     hotspot, cycle on repeat click" just because it's less code. A
     hotspot that doesn't line up with something the diagram visibly shows
     is a trap, not a shortcut.
     "Counterweight plate 5t" only ever appears at a fixed quantity of 2
     in the confirmed data (never 0-but-not-2) - modeled as a single plain
     toggle (weight=10, the pair's combined contribution), NOT a stepper,
     since there's only one nonzero state to represent. If the missing
     76 pages reveal other 5t quantities, promote it to a slotGroup the
     same way p10 was done.
  c) Symmetry requirement stated as a hard rule in the source ("only
     permissible with a symmetrically assembled counterweight... left and
     right stacks must be symmetric") - not encoded as a runtime check,
     because every published combo in the chart is already internally
     symmetric by construction (plates always added in the pairs the
     chart itself specifies) - there's no way to reach an asymmetric state
     through this tool's own UI using only the documented items, so a
     redundant runtime check wasn't added. If a future free-click state
     could somehow desync left/right (it currently can't, since p10_x/
     p5x2 represent both-sides-combined quantities, not per-side toggles),
     revisit this.
  d) "Center section" (item 4 in the diagram - frame lower/upper section,
     ballasting device, assembly winch) is NOT a counterweight component
     and never appears in any combo's own composition list - same
     starting treatment as 1130's centering cone (item 10) and 1160's
     implicit turntable structure (fixed machine structure, zero weight).
     UPDATE (same session, after ship): initially excluded from
     components/hotspots entirely, matching those other two fixtures.
     Person reported that selecting the base 25t receptacles left item 4
     dark/unhighlighted on the diagram, when physically it's bolted
     between the two receptacle halves and is never NOT there once
     receptacles are fitted - leaving it unhighlighted looked like an
     omission, not a deliberate "this isn't counterweight" statement.
     Difference from 1130/1160's fixtures: those are genuinely separate,
     unrelated hardware (a mounting cone, the turntable itself) that a
     crew member wouldn't expect to see highlighted alongside ballast
     plates. This one is functionally inseparable from item 1 specifically
     - not a general "highlight all fixed structure" request.
     Fix: added item 4 as a real component (id 'p4center', weight 0) with
     its own hotspot, linked bidirectionally to p1 via `linkedId` (p1 ->
     'p4center' and p4center -> 'p1') so selecting either highlights both,
     the same linkedId mechanism 1110 uses for its always-paired plates
     3/4 - reused as-is, no new mechanism needed. Two follow-on fixes this
     required, both worth remembering for any future zero-weight/purely-
     visual linked component:
       - onCwtComboSelect() only set cwtSelected from the combo's own
         items[] list, which (correctly) never mentions a zero-weight
         component - so jumping via the dropdown left item 4 unhighlighted
         even though clicking p1 directly worked. Fixed by having
         onCwtComboSelect() also pull in any linkedId partner after
         setting the base selection, mirroring what toggleCwtPlate()
         already did for direct clicks.
       - cwtFindMatchingCombo() compares the exact SET of selected ids
         against each combo's items[] - once p4center joined cwtSelected
         alongside p1, the set no longer matched any stored combo
         (p4center is never listed in the OEM's own combo definitions),
         so the 25t combo started reporting as "not approved" despite
         being exactly right. Fixed by filtering out weight-0 components
         before comparing, since a zero-weight item can never be part of
         what defines a weight-based combination in the first place. This
         is the general fix - it wasn't specific to p4center, and now
         applies to any future zero-weight linked/echo component too.
  e) Diagram (Fig.156378) is the cleanest yet for hotspot purposes -
     genuinely non-overlapping, symmetric left/right layout with every
     part drawn as its own separate, unambiguous shape (unlike 1160's
     interlocked isometric view). Hotspots here are still hand-picked
     rectangles (not OpenCV-traced) purely as a time/effort call given
     this crane's data is already partial and will need revisiting once
     the rest of the source document is available - not a statement that
     this diagram couldn't support traced outlines like 1250/1110's.
  Scope: same as every prior crane, no fastening/bitt detail surfaced.

10.11 LTM 1300-6.2 EU-model PDF - EXTRACTED, DELIBERATELY NOT SHIPPED
  Person supplied a counterweight-combination PDF explicitly filenamed
  with "GG 918110808 EU model" and flagged directly, unprompted, before
  processing: this is the EU-market variant (different road-axle load
  limits than the global/export version already referenced elsewhere in
  this app as plain "LTM 1300-6.2" / crane id '1300' in Tab 1/2/3), and
  the person will source the global variant separately later.
  Extracted and fully verified anyway (10 combos: 13/16/26/36/46/56/66/
  76/86/96t, all reconciled by subset-sum against 7 components -
  Receptacle Plate 1 (13t), Plate 7 (3t), Plate 2 (10t), Plate 3 (10t,
  appears as 1 or 2 copies), Plate 4 (10t), Auxiliary Ballast 5 (10t x2),
  Auxiliary Ballast 6 (10t x2)). Diagram (Fig.121718) also extracted.
  Notable data point if/when this does get used: at the 66t/86t "special
  ballast" tiers, three spacer pins physically replace what would
  otherwise be a second Plate 3 - those tiers use only ONE Plate 3 copy
  (same as the 36-46t tiers) plus the aux ballast items, NOT a second
  Plate 3 - a real branch in the data, not a simplification.
  Deliberately NOT wired into COUNTERWEIGHT_DATA under '1300' or any
  other key - the diagram and combo data are saved outside the repo (this
  session's scratchpad, not committed) specifically so they don't get
  mistaken for shippable data. DO NOT add a '1300' entry to
  COUNTERWEIGHT_DATA using this PDF's data without first confirming with
  the person whether it actually matches the global-variant crane already
  in Tab 1/2/3's '1300' entries, or whether it needs its own separate key
  (e.g. '1300-eu') to avoid silently overwriting/conflating two distinct
  physical counterweight systems under one crane id.

10.12 LRT 1100-2.1 PDF - HELD, subsection mismatch found
  Person supplied a 1-page counterweight-combination excerpt filenamed
  for LRT 1100-2.1, and the document's own footer confirms "LRT 1100-2-
  1-002" - but the actual visible content on that single page is headed
  "2.1 LRT 1090-2.1", a DIFFERENT model number, with its own small
  combination table (0t / 8t* / 12t, keyed to "crane type" rather than a
  plate-combination breakdown - this appears to be a much simpler
  fixed-block counterweight system than every other crane processed so
  far, consistent with the LRT 1100/1090 being a smaller rough-terrain
  class machine, not modular multi-plate). Whether "LRT 1090-2.1" here is
  a genuinely different variant sharing this chapter with "LRT 1100-2.1"
  (most likely, given the doc explicitly numbers this as subsection "2.1"
  implying a "2.2" or similar likely exists elsewhere in the source
  document for the 1100 itself), or some other explanation, is NOT
  determinable from the one page supplied.
  NOT extracted into the app. Diagrams (Fig.151204 "Counterweight" and
  Fig.145613 "Counterweight 1", showing a single fixed counterweight
  block bolted to the superstructure plus a small "centering cone" fixture
  - same non-component-fixture pattern as 1130's item 10 and 1650's item
  4) were pulled for reference but not saved as shippable assets pending
  clarification. If the person confirms "1090" data is fine to use for
  the "1100" crane entry, or supplies the correct page, this is a small,
  fast job (far simpler data model than any crane so far - just 3 fixed
  states, no plate combinations) - don't over-build for it.

10.13 Stray red outline on tapped hotspots - CSS focus-state gap, not a
      data or logic bug
  Person reported a red-tinted highlight on whichever plate was tapped
  most recently on LTM 1650's diagram (screenshot showed one "10t" plate
  glowing red instead of blending into the gold selected state like the
  rest). Root cause: .cwt-hotspot's :hover/:focus-visible rule set
  `outline: none`, but a touch tap on an SVG element with tabindex="0"
  generally produces plain :focus, not :focus-visible, on mobile
  browsers (:focus-visible is mostly reserved for keyboard-driven focus).
  The base .cwt-hotspot rule never reset outline at all, so the browser's
  own default focus ring - independent of this app's palette entirely -
  showed through on tap, layered on top of the correct gold fill/stroke.
  Global CSS already sets -webkit-tap-highlight-color: transparent on
  every element, which handles the WebKit tap-flash overlay, but that's a
  different mechanism from the focus outline - fixing one doesn't fix
  the other.
  Fix: moved `outline: none` onto the base .cwt-hotspot rule so it always
  applies regardless of focus state, keeping the intentional gold stroke
  on :hover/:focus-visible only. This is a general CSS gap, not specific
  to LTM 1650 - it would affect any crane's hotspots on a touch device,
  just hadn't been reported yet. Confirmed fixed via a touch-emulated
  Playwright context (has_touch/is_mobile), not just a regular click -
  a plain .click() doesn't reliably reproduce the same focus state a real
  tap does, worth remembering when testing future touch-only interaction
  bugs on this tab.

10.14 CORRECTION to 10.13 - the red was the person's own annotation, not
      a rendering bug. Real issue: a hotspot undershot its own plate.
  10.13's diagnosis was wrong. The person clarified the red in their
  screenshot was THEIR OWN markup, drawn on top of the screenshot to
  point at a real problem: the gold "selected" highlight box for the
  bottom-most (7th, ×14) level of the 10t-plate quantity ladder didn't
  fully cover that plate's actual printed outline - a visible sliver of
  the real plate stuck out past the bottom of the highlight box, dark/
  unhighlighted, even though that hotspot correctly had the "selected"
  CSS class applied (confirmed by direct DOM inspection - this was never
  a logic/class bug, only a coordinate one).
  Root cause: p10_14's hotspot y-range (782-900 area) was derived from a
  pixel-grid crop of the source diagram that was itself cropped too
  short (per-side grid images used in 10.10's original derivation only
  extended to y=940-970), so the last plate's true bottom edge - actually
  around y=900 - got read as y=875, undershooting by ~25px on both the
  left and right hotspot. Fixed by re-cropping generously past the
  believed edge and re-measuring directly against the raw image (same
  "pad crop boxes more generously than seems necessary" lesson from
  10.4/10.8, now applying to a hotspot's own boundary, not just the crop
  box used to derive it).
  10.13's actual fix (outline:none on the base .cwt-hotspot rule for
  touch taps) was real and worth keeping - it's just not what this
  specific report was about. Two independent, unrelated things happened
  to look similar in a compressed screenshot; don't assume a single
  explanation covers everything a screenshot shows without confirming
  which parts are the app's own rendering vs. the person's own markup.

10.15 LTM 1130-5.1 diagram replaced entirely - DXF-sourced exploded CAD
      line drawing, pixel-exact hotspots straight from vector geometry
  Superseded 10.8's photo-style side-elevation PNG for THIS crane only.
  The person offered to draw precise rectangles over the existing diagram
  to fix imprecise hand-measured hotspots (the general fix-up effort that
  also produced 10.14). For LTM 1130 specifically they instead extracted
  and exploded the actual 3D CAD model, added a dedicated "boxes" DXF
  layer with one rectangle per plate, and sent the DXF directly - a
  different, cleaner diagram, not an annotation of the old PNG. Confirmed
  this before touching anything: the two drawings don't share a scale or
  aspect ratio (old PNG is a 2055x2944 illustrated portrait diagram with
  printed weight labels; the DXF traces a wide exploded elevation with no
  labels at all), so proportionally mapping DXF coordinates onto the old
  PNG was never viable - the DXF had to become the new diagram image.
  Process:
  - Parsed the DXF with ezdxf. Modelspace had three relevant layers:
    PDF_Visible (the traced line geometry, 290ish entities), PDF_TANGENT_
    LINES (13 tangent/silhouette edges for curved features), and boxes (a
    layer the person added themselves, purpose-built as one closed
    LWPOLYLINE rectangle per plate).
  - First pass only found 8 boxes on the "boxes" layer. Needed 9: 7 for
    the main stack (plates 1-7) + 2 for the auxiliary ballast pair (item
    8's left/right wings, which sit apart from the main column). Rendered
    the boxes layer overlaid on the PDF_Visible geometry (matplotlib) and
    found a real, fully-drawn plate outline between two of the boxes with
    no rectangle around it - the person confirmed this was the one they'd
    missed ("I MISSED a box") and sent a corrected DXF with a 9th box
    filling exactly that gap. Lesson carried over from 10.4/10.8/10.14:
    when a count looks short by exactly one, render the underlying
    geometry and look for the missing outline directly rather than
    guessing coordinates for it.
  - Orientation was NOT recoverable from geometry alone. DXF Y-up vs.
    image Y-down is a coin flip without an external reference, and this
    diagram has no printed labels to anchor against (unlike the old PNG,
    which had "1".."8" and weights baked into the artwork). Rendered both
    orientations, sent both to the person with an explicit guess overlaid
    ("box0 = item 7" etc.), and they corrected it: box0 (smallest DXF Y)
    is the BOTTOM of the stack (item 1), box5 (largest DXF Y) is the TOP
    (item 7) - i.e. raw CAD Y-up, unflipped, was correct. Don't assume a
    flip is needed just because "image space is usually Y-down" - check.
  - Final box->component mapping (bottom to top): box0=p1, box1=p2,
    box2=p3, box3=p4, box8=p5 (the recovered 9th box), box4=p6, box5=p7.
    box6/box7 (the two wings, symmetric about the main column, gaps to
    the column matching within ~0.1 units) = p8's two hotspots.
  - Rendered the final diagram by plotting PDF_Visible + PDF_TANGENT_
    LINES directly (LWPOLYLINE/ARC/SPLINE, flattening splines via ezdxf's
    own .flattening()) with matplotlib, axes filling the full figure
    (add_axes([0,0,1,1]), no autoscale padding) so the saved PNG's pixel
    grid has an exact, known affine relationship to DXF coordinates:
    pixel_x = (dxf_x - xmin) / (xmax-xmin) * png_width
    pixel_y = (ymax - dxf_y) / (ymax-ymin) * png_height   (note the flip:
      image rows increase downward, confirmed-correct DXF Y increases
      upward, so this axis - and only this axis - needs inverting)
    Cropped to the boxes' bounding extent plus padding, not the DXF's
    full extent - the full PDF_Visible bbox reached far outside the
    boxes (unrelated page content, e.g. other views/title block), which
    would have wasted resolution rendering things nobody needed.
  - Applied that same formula to every box's own corner coordinates to
    get its hotspot polygon - no hand measurement, no visual tracing,
    exact vector-to-pixel math. Verified by drawing the computed polygons
    back onto the rendered PNG before touching index.html at all (a
    disposable Python/PIL check, not a Playwright test) - confirmed each
    polygon sat exactly on its own plate before shipping.
  - New file: counterweight/img/ltm1130-cwt-exploded-v2.png (1999x1030,
    landscape - the exploded wings push the aspect ratio wide, a real
    difference from every other crane's portrait/near-square diagram, not
    a bug). Old ltm1130-cwt-exploded.png deleted, nothing else referenced
    it.
  - Found and fixed a wording bug while touching this: the shared
    disclaimer under the diagram had "actual OEM exploded-view artwork,
    extracted directly, not redrawn" hardcoded as a suffix after every
    crane's figRef. True for the other 4 cranes, no longer true for this
    one (it's now a redrawn CAD tracing). Moved that claim out of the
    shared HTML and into each crane's own figRef string instead, so it
    can vary - 1130 now says "diagram re-drawn from an exploded 3D CAD
    model, not the original OEM artwork" where the others keep the
    original "extracted directly, not redrawn" wording.
  Combos/weights/component names were untouched - identity (which
  numbered item is which plate) was never actually in question, only
  hotspot precision. This is purely a diagram + hotspot-geometry swap.

10.16 On-diagram plate number labels - LTM 1130 ONLY, deliberately not
      applied fleet-wide
  Added a small text label (e.g. "7 / Winch #2") centered on each hotspot,
  always visible (not just on hover/select) - new `shortLabel` field per
  component, new cwtPolygonCenter() helper (average of a hotspot's own
  vertices - good enough for these mostly-convex plate outlines, not a
  true area centroid), rendered as an SVG <text> with a white stroke halo
  (paint-order:stroke) so it stays legible over both black line art and
  white background. pointer-events:none so the label never blocks the
  polygon's own click target underneath it.
  Tried this on all 5 cranes first, then reverted everything except LTM
  1130. Reason: 1250, 1110, 1160, and 1650's diagrams are the actual OEM
  artwork extracted directly (see 10.4/10.7/10.9/10.10) and already have
  the manufacturer's own printed reference numbers baked into the image
  with leader lines - adding another number layer on top just duplicated
  them, and for 1650's quantity-ladder plates (7 stacked hotspots per
  side, all labeled "10t") the labels overlapped into an unreadable mess
  since those hotspots are packed tightly together. LTM 1130 is the one
  exception with nothing baked in (10.15's diagram is a plain CAD line
  trace, no printed labels at all), so it's also the one crane where this
  actually adds information instead of duplicating it.
  Mechanism: `shortLabel: ''` on a component means "render no label"
  (falsy, skipped) - used for every non-1130 component and for 1650's
  zero-weight linked Center Section. Omitting `shortLabel` entirely would
  fall back to the full `name` string as the label, which is worse than
  either option here, so every component across all 5 cranes now sets it
  explicitly one way or the other - don't add a new component without
  also setting this, or it'll silently fall back to printing its full
  name on the diagram.
  If the person re-does another crane's diagram from CAD (per 10.15's
  process) the way they did for 1130, give it real shortLabels too at
  that point - the reason to skip it is "this diagram already has printed
  numbers," not "this feature is 1130-specific by design."

10.17 "Jump to Known Combination" dropdown now syncs to manual selection
  Previously the dropdown only ever changed the selection (one-way: pick
  a combo -> plates highlight). Tapping plates by hand never touched the
  dropdown, so a person building a combo manually who happened to land on
  a real OEM-published total had no way to see that confirmed there
  except the "OEM-approved combination" checkmark on the total-weight
  card - the dropdown itself stayed on whatever it last showed (usually
  "0.0 t - No counterweight"), which read as if the app hadn't noticed.
  Fixed by making it two-way: cwtFindMatchingCombo now returns the
  matched combo's own index in COUNTERWEIGHT_DATA[model].combos (was
  previously just the combo object via Array.find - switched to
  findIndex and spread the index onto the returned object as `.idx`, one
  call site, no other callers to break), and renderCwtTab sets
  #cwt-combo-select's value to that index whenever a match exists. When
  the manual selection does NOT match any published combo, the dropdown
  is explicitly set to selectedIndex = -1 (no visible selection) rather
  than left showing a stale/misleading previous value - the existing
  "not an OEM-approved combination" warning box already covers that case
  so there's no need for the dropdown to also try to communicate it.
  Verified both directions still work correctly together: picking from
  the dropdown drives the selection (unchanged, onCwtComboSelect), and
  building the same combo by hand now drives the dropdown back (new) -
  tested by tapping LTM 1130's plates 1-7 one at a time (lands on the
  real 29.3t combo, dropdown updates to show it), then adding plate 8
  (42.0t, also real, dropdown updates again), then removing plate 2 only
  (39.6t, not a published combo, dropdown drops to no selection).

10.18 LTM 1110-5.1 diagram replaced too - same DXF-exploded-CAD process as
      10.15, plus a real box-to-plate mix-up in the person's own CAD
      export, caught via the manufacturer's weight stamp baked into the
      geometry instead of guessed
  Same overall pipeline as 10.15 (DXF "boxes" layer -> render PDF_Visible/
  PDF_TANGENT_LINES geometry -> exact affine vector-to-pixel math -> new
  diagram + hotspots), so this entry only covers what's different.
  First upload had 7 boxes; needed 8 (one per component that owns a
  hotspot: p1, p2, p4 (see below), p5, p6, p7, p8w, plus - unlike 1130 -
  a genuinely separate 8th here because this crane's p3/p4 pair turned
  out to have its own clean split, see below). Same "count is short by
  exactly one, don't guess the shape, ask for the missing box" call as
  10.15 - the person confirmed and re-sent with the 8th box added.
  Orientation: raw CAD Y-up (unflipped) was correct again, same as 10.15
  - box0 (smallest Y) is the bottom of the stack, item 1.
  The person warned up front that they'd mixed up two plates while
  exploding the 3D model, so position/order alone couldn't be fully
  trusted this time (unlike 10.15, where the naive top-to-bottom guess
  turned out right). Rather than ask them to re-check by eye, checked the
  DXF geometry itself first: two of the boxes contain actual embossed
  weight text as part of the traced CAD geometry (the physical plates
  have their tonnage cast/stamped into the steel, and since this is a
  literal 3D-model trace rather than a photo, that text came through as
  real vector line-work, legible after rendering) - one box clearly reads
  "2,5t" (matches plate 7's 2.5t) and another reads "3,6t" (matches plate
  5's 3.6t). That pinned two positions with certainty rather than
  inference. The box sitting where the naive bottom-up count expected
  "plate 6" was the one stamped "2,5t" - i.e. actually plate 7 - and the
  thin sliver box one level up (proportioned right for the lightest
  plate, 0.3t) was actually plate 6. Confirmed this read back to the
  person with the interpreted image before touching index.html, same as
  every other ambiguous case this session - they confirmed it verbatim,
  plus clarified which physical side of the 3/4 pair is which (box2=4,
  box3=3 - doesn't change app behavior either way since they're linked,
  but worth getting right for anyone reading the data later).
  Lesson: when a person flags their own source data might have an error,
  don't just re-ask them to double check by eye - look for independent
  evidence in the data itself first (here, literal printed numbers that
  had nothing to do with position/order) and lead with that. It's a
  stronger confirmation than "does this look right to you" on a second
  pass of the same reasoning that produced the mistake in the first
  place.
  p3/p4 upgrade: 10.7 originally merged these into one shared hotspot
  because the OLD extracted-photo diagram had no clean dividing line
  between their silhouettes. This new CAD tracing has a real, clean split
  (they sit side by side, not overlapping), so they now each get their
  own precise hotspot - kept in sync both directions via linkedId on
  BOTH components now (previously only p4 had linkedId: 'p3'; toggling
  p3's own hotspot did nothing since it had none). Verified both
  directions: tapping plate 3 alone selects plate 4 too and vice versa,
  and toggling either back off deselects both. This is a real, confirmed
  fleet rule per the person directly ("those two slabs always get
  selected together cause you can't use one and not the other ever"),
  not just a diagram-tracing artifact - keep the linkedId even if a
  future diagram somehow made them look separable.
  Since this new diagram also has no baked-in printed reference numbers
  (only the incidental weight stamps noted above, not "1".."8" leader-
  line labels), it gets the same on-diagram shortLabel treatment as 1130
  (10.16) - '1' through '7', plus '8 / Winch #2' / '8' for the p8w/p8b
  slot pair, matching 1130's "N / Winch #M" convention for a plate/winch
  dual-purpose slot.
  New file: counterweight/img/ltm1110-cwt-exploded-v2.png (1122x1400).
  Old ltm1110-cwt-exploded.png deleted, nothing else referenced it.

10.19 LTM 1110-5.1 combo list gap - a genuinely missing 29.0t combo, found
      by trusting the person's arithmetic over my own extraction
  The person asked why the "full stack" combo showed 28.2t instead of
  29.0t. I explained the countsToward:false rule (correct, unchanged) but
  that didn't fully answer them - they went and checked their own PDF and
  came back with more pages ("it was cut off from the pdf i supplied").
  Re-extracted the new pages with the same -29 decode as always and ran
  every entry through the same subset-sum check used throughout this
  whole feature. Result: 39 real combos on the supplementary pages, 38 of
  which were ALREADY in the app's data (this new PDF overlapped almost
  entirely with the original extraction) and exactly one new one: 29.0t =
  full 7-plate stack + Replacement Ballast (as opposed to the existing
  28.2t = full stack + Winch 2*). Two different, both genuinely
  published, full-stack combos - not the same combo miscounted. Added it
  to the '1110' combos array (see comment there) and to the crane's combo
  count in the header comment.
  Also found a real anomaly while doing this: one entry on the new pages
  prints "28.7 t" for a composition (plates 1,2,3,4,5,6 + Replacement
  Ballast) that actually sums to 26.5t - a 2.2t gap that does NOT match
  the countsToward:false/Winch-2* exclusion pattern (no Winch 2* is even
  present in that entry's own component list) and doesn't correspond to
  any known plate weight either. Checked this thoroughly before writing
  it off: pulled the raw per-character codes for that specific text run
  via get_text("rawdict") and decoded them by hand against the same -29
  shift used everywhere else - the digits "28.7" really are what's
  encoded in the PDF, not a decode error on my end, and there is no
  hidden/overlapping text block near it that could be a dropped 7th
  component. This is a single, isolated instance in the whole fleet's
  extracted data (every other entry across all 5 cranes has reconciled
  exactly) - treated as a genuine printing error in Liebherr's own
  document rather than invented a 7th component to make it add up.
  NOT added to the app. If a future page of this same document clarifies
  it, revisit; until then this specific 28.7t line should be treated as
  unreliable if anyone encounters it in the physical manual too.
  Lesson for next time this kind of question comes up: when someone
  pushes back on a total that "should" be a round/expected number, don't
  just re-explain the existing rule and stop there - the discrepancy can
  be a real gap in what was extracted, not a misunderstanding on their
  end. Re-checking the source here found a real missing combo AND a real
  document error, neither of which a second explanation of the same rule
  would have surfaced.

10.20 Winch 2* vs Replacement Ballast - added an explicit non-
      interchangeability warning, LTM 1110 only so far
  Prompted by the same conversation as 10.19: once the person understood
  the weight/total mechanics, they raised the more important point - the
  app was presenting these two as basically the same choice (same slot,
  same 0.8t, one UI toggle) with nothing calling out that they are NOT
  interchangeable in what the crane can actually do. Winch 2* fitted
  means the crane has a working second auxiliary winch. Replacement
  Ballast in the same slot means it does not - that slot is inert dead
  weight only. Someone rigging for a job that needs winch 2 could easily
  read "same weight, same slot" as "doesn't matter which," which is
  wrong and could leave them without a winch they were counting on.
  New per-component field `slotWarning` (set on both p8w and p8b, worded
  from each one's own perspective) plus a small addition to renderCwtTab's
  existing notices-array pattern (same mechanism already used for
  "combination not OEM-approved" and LTM 1250's centerTrim warning) - any
  currently-selected component with a slotWarning string gets it appended
  to the warning box, regardless of which one is active. Verified both
  directions render correctly (selecting Winch 2* shows the "this IS a
  winch" wording, cycling to Replacement Ballast swaps to the "this is
  NOT a winch" wording).
  Scope note: only LTM 1110 has this slotGroup pattern with a real
  functional (not just cosmetic) difference between alternatives right
  now. If another crane's data ever has a similar "same slot, same
  weight, different real-world capability" pair, give it a slotWarning
  too - don't assume every slotGroup/quantityGroup pairing needs one
  (LTM 1650's 10t plate quantity ladder, for example, has no such
  distinction - every rung really is just more of the identical plate).

10.21 LTM 1250-5.1 diagram replaced too - same DXF process, plus the
      first case where weight stamps couldn't fully disambiguate
  Same pipeline as 10.15/10.18. 9 boxes on the DXF's "boxes" layer for 8
  components (7 main-stack plates + 2 wing halves for p8) - correct count
  first try, no missing box this time.
  Orientation: raw CAD Y-up again correct, same as every crane so far -
  box0 (smallest Y) = bottom = plate 1.
  Identity: plates 1 (11t), 5 (12t), 6 (5t), and both p8 wings (10t each)
  were all confirmed via the manufacturer's own weight stamps baked into
  the CAD geometry, same as 10.18. But plates 2, 3, and 4 are all
  coincidentally 10.0t EACH - three boxes, all stamped "10t", with
  nothing in the geometry itself to tell them apart. This is different
  from 10.18's swap: there weight stamps resolved the ambiguity directly;
  here they can't, because the stamps themselves are identical across
  the genuinely different plates. Identity of 2 vs 3 vs 4 matters for
  real - the combos array picks specific subsets (e.g. {p1,p2,p4,p5}
  without p3), so getting the order wrong would highlight the wrong
  physical plate for a real published combo even though the total
  weight would still happen to come out right in some cases.
  Sent the rendered guess (bottom-to-top positional order matching the
  OLD diagram's already-established 1-2-3-4-5-6-7 order, i.e. assuming
  this new export preserves the same physical stacking sequence) and the
  person confirmed it directly rather than independently re-deriving it -
  worth being explicit that this one component of the mapping rests on
  their confirmation plus positional continuity with prior data, not on
  independent evidence the way 10.18's swap catch did. If a future
  diagram for this crane ever contradicts this order, 10.18's swap is the
  reminder that position-matches-old-diagram is a reasonable prior, not a
  guarantee.
  New file: counterweight/img/ltm1250-cwt-exploded-v2.png (2625x1400).
  Old ltm1250-cwt-exploded.png deleted. Also caught and fixed a stale
  reference this time before shipping: the tab's static HTML had a
  hardcoded placeholder `<img src>` and default SVG viewBox pointing at
  LTM 1250's OLD file/dimensions (used only as the pre-JS initial markup,
  overwritten by renderCwtTab() on load, but still worth keeping correct
  so there's no flash of a broken image / wrong-aspect-ratio hotspot
  layer before the first render call completes) - updated both to the
  new file and 2625x1400. Checked 1130 and 1110 for the same issue when
  this was noticed; neither had a stale reference left over.

================================================================================

10.22 LTM 1650-8.1 hotspots re-traced from a user-supplied alignment DXF -
      the 10.13/10.14 undershoot was systemic, not one bad box
  Unlike 1130/1110/1250, this crane's diagramImage stayed the original OEM
  JPG (Fig.156378) - no exploded-CAD replacement was available for it. What
  the person supplied instead: the SAME JPG re-opened in AutoCAD with an
  IMAGE entity anchored at insert (0,0) and u_pixel/v_pixel scaled so DXF
  world coordinates map directly onto the JPG's pixel grid (confirmed by
  image_size 1831x1266 in the IMAGE entity matching this crane's existing
  diagramWidth/diagramHeight exactly, and by rendering the DXF geometry
  over the actual JPG and checking it lands on the real edges rather than
  guessing blind). On top of that image the person hand-traced boundary
  LINEs (layer '0', the 8 real plate/receptacle edges per stack) and
  LWPOLYLINEs (winches w2/w3, the combined p1 receptacle footprint with a
  notch cut out for p4center) directly on the true printed edges - i.e.
  the DXF's only job here was precise pixel measurement of an existing
  image, not a new rendering.
  Conversion: pixel = (dxf_x / 1.313675, image_height - dxf_y / 1.313675).
  The scale factor is u_pixel's magnitude, NOT 1 - the DXF world extents
  (2405.3 x 1663.1) are the image's pixel size already multiplied by that
  factor, and skipping the division silently produces coordinates that
  are still roughly plausible-looking (all boxes land in the right
  general area, right order, right relative spacing) but are wrong by a
  constant factor - the kind of error that only surfaces by overlaying
  the result on the source image and looking, not by inspecting the
  numbers alone. First attempt here made exactly that mistake.
  Result vs. old hand-picked rectangles (10.10e/10.13/10.14): every
  10t-plate quantity-ladder level (p10_2..p10_14) shifts up to ~27px off
  the true printed edge, in both directions independently (a hotspot's
  top and bottom don't drift by the same amount, since the old ones were
  evenly-spaced guesses rather than measurements of the real, unevenly-
  gapped plate positions). p5x2 shrinks from a 60px-tall guess to its
  true ~38px (it really is a thinner plate than the 10t ones - confirmed
  by the DXF, not assumed). w2/w3 and p1/p4center also tightened to the
  DXF-measured boundaries. All 21 hotspots re-verified by rendering them
  as red outlines over the actual diagramImage and confirming each one
  hugs its real printed edge before shipping.
  p4center's hotspot now spans the full center-column height (932-1249),
  merging the DXF's small upper "notch" polyline (932-1001, physically
  just the bit of the center section poking up above the two receptacles)
  with the unmarked gap below it (1001-1249, same y-range as the p1
  boxes) - the DXF only explicitly traced the notch, but the wider outer
  polyline's path shows that lower gap belongs visually to the center
  section, not to either p1 receptacle, consistent with the original
  hand-picked p4center already covering that same full height.
  No combo data, weights, or component list changed - hotspot geometry
  only. diagramImage/diagramWidth/diagramHeight all unchanged (still the
  same 1831x1266 JPG).

================================================================================

10.23 LTM 1300-6.2 (global/export variant) added under crane key '1300' -
      PROVISIONAL combo list, first crane shipped without an OEM chart
  Different from every prior crane addition: no OEM counterweight-
  combination document exists yet for THIS variant. What exists is the
  10.11 EU-model chart (held, never wired in) plus a person-supplied DXF
  of this variant's actual physical counterweight stack (exploded 3D CAD,
  10 boxes visible: box0 through box9) and the person's own direct
  knowledge of each box's weight/position, worked out interactively box
  by box rather than read off a document.
  Component mapping (box -> app id):
    box0 (bottom, 6t)            -> p1 "Receptacle Plate 1"
    box1 (10t)                   -> p2 "Counterweight Plate 2"
    box2, box3 (10t each)        -> p3a, p3b "Counterweight Plate 3"
                                     (identical/interchangeable, not a
                                     mounted pair - same modeling as
                                     LTM 1160's repeated Plate 3, 10.9)
    box4 (10t)                   -> p4 "Counterweight Plate 4"
    box5 (10t, top of stack)     -> p5 "Counterweight Plate 5" - unique
                                     to this variant, no EU equivalent
    box6+box9, box7+box8 (side-  -> auxA "Pair A", auxB "Pair B" -
    mounted, 10t each, always       both-or-neither toggle-together,
    both-or-neither per pair)       same "separate" modeling as 1130's
                                     p8 (10.8). Real names/numbers for
                                     these two pairs are UNCONFIRMED.
  Why box0 is 6t but the EU chart's equivalent base is 16t (13t
  Receptacle + 3t Plate 7): this variant's receptacle is a single 6t
  piece, not two separate parts - box5 (Plate 5, no EU equivalent) makes
  up the remaining 10t. Confirmed directly by the person, not derived.
  Combo list is PROVISIONAL: no OEM chart for this variant exists yet, so
  the 10.11 EU chart's verified tier structure (16/26/36/46/56/66/76/86/
  96t, each tier = previous + exactly one more item, confirmed by
  subset-sum against the real EU PDF) was mirrored directly, substituting
  this variant's p1+p5 for the EU chart's p1+p7 base pair at every tier.
  The EU chart's 13t tier (Receptacle alone, no Plate 7) has no
  equivalent here since box0 isn't a separate piece from... itself - the
  lightest non-zero tier is p1+p5 together (16t, matching the EU chart's
  own 16t tier's total). An empty (0t) combo was added despite the EU
  chart never listing one, for consistency with every other crane in this
  app - low-risk addition, easy to remove if the real chart confirms this
  crane has no true zero-ballast state.
  NOT modeled: the EU chart's 66t/86t spacer-pin mechanic (three spacer
  pins physically replace the second Plate 3 copy at those two tiers,
  10.11) - carries no weight and isn't confirmed to apply identically to
  this variant's hardware, so left out rather than guessed.
  sourceNote on this entry says PROVISIONAL and DO NOT use for real lift
  planning in plain language, not just in this file - visible directly in
  the app's Composition panel for this crane. Replace the combos array
  (and re-verify components) the moment the person supplies this
  variant's own OEM counterweight-combination document; do not silently
  treat this data as equivalent to a verified chart in the meantime.
  New file: counterweight/img/ltm1300-cwt-exploded.png (4643x1300),
  rendered from the person's DXF - white background, matching 1130's own
  DXF-exploded PNG convention (confirmed by inspecting pixel (0,0) on
  both files before treating the white background as a bug).

================================================================================

10.24 LTM 1650's quantity ladder replaced with free independent toggles;
      LTM 1300's auxiliary-ballast pairing corrected and confirmed-named
  Two related fixes from the same conversation, both about matching what
  a crew member actually expects when tapping the diagram.
  a) LTM 1650 10t-plate stack: 10.10 modeled this as a quantity LADDER
     (selectCwtSlotLevel) - clicking any position jumped straight to that
     level and force-filled every position below it, matching how the
     stack is physically BUILT (bottom-up, no gaps possible). Correct
     mechanically, but the person pushed back hard: they wanted to tap
     specific boxes - in whatever order - and have the total build up,
     using the visible boxes purely as "here's what you're selecting"
     labels, not as an enforced build sequence. Confirmed the ladder
     logic itself had no bug (verified 95t was reachable, p1+p5x2+the
     correct single level-6 hotspot, in one click each) - this was a
     product decision, not a defect.
     Replaced the 7 quantityGroup/slotGroup components (p10_2..p10_14,
     cumulative weights 20-140) with 7 fully independent components
     (p10a..p10g, each a flat 20t = one L+R pair) using a NEW
     `interchangeableGroup` flag instead of slotGroup/quantityGroup. Each
     keeps its own hotspot pair (both sides of one position share one id,
     same as before - a position is filled/removed as a unit, you can't
     leave one side of a pair unbalanced) and now falls through
     renderCwtTab's plain toggleCwtPlate path - no special-case rendering
     needed once quantityGroup was removed, since "independent toggle" was
     already the default behavior for anything without slotGroup.
     The catch: 7 interchangeable positions means C(7,k) different ID
     sets all represent "k pairs mounted," but the combos array can only
     literally list ONE set per weight tier. Naively comparing exact ID
     sets (the pre-existing cwtFindMatchingCombo behavior) would flag
     every selection except the one exact canonical subset as "not
     OEM-approved," even though the source document never distinguishes
     WHICH position is filled, only how many. Fixed by adding
     cwtMatchSignature(): before comparing, any component carrying
     interchangeableGroup collapses to a `groupName:count` token instead
     of its own id, on BOTH the live selection and every stored combo's
     item list - so a match only requires the same COUNT from the group,
     not the same specific members. cwtFindMatchingCombo now diffs
     signatures instead of raw sorted-id-joins. cwtComboWeight needed no
     change (already summed by iterating whatever ids are actually
     selected). This wasn't needed for LTM 1160's own repeated-plate
     components (p3a/p3b/p3c, 10.9) since that crane's combos only ever
     reach 3 total copies and were already always listed as the same
     front-loaded canonical subset - the mismatch risk exists there too
     in principle, just never surfaced given only 3 members and shallow
     combo depth. Not retrofitted onto 1160 in this pass - flagging here
     in case it's worth doing if 1160 ever gets the same free-click
     complaint.
     selectCwtSlotLevel() and the quantityGroup branch in renderCwtTab
     were deleted outright rather than left dead - nothing else in
     COUNTERWEIGHT_DATA used quantityGroup once 1650 stopped.
     Verified in-browser: clicking 3 non-contiguous positions in
     arbitrary order (7th, 1st, 4th) correctly sums to 85t and is flagged
     OEM-approved, same as clicking the "canonical" first 3.
  b) LTM 1300 (global/export variant, 10.23) auxiliary ballast pairing
     was wrong: the first pass paired box6+box9 together and box7+box8
     together because those are the two boxes drawn adjacent to each
     other on the SAME side of the exploded-view layout - but the person
     confirmed the real physical pairing is LEFT+RIGHT symmetric (one
     ballast box mounted on each side of the crane, like every other
     component on this diagram), i.e. the INNER pair is box8 (left, near
     the main stack) + box6 (right, near the main stack), and the OUTER
     pair is box7 (left, far from the stack) + box9 (right, far from the
     stack) - the opposite grouping from what the layout's spatial
     adjacency suggested. ids renamed aux5/aux6 (from the placeholder
     auxA/auxB, then Inner/Outer mid-fix) once the person separately
     confirmed the numbering itself: "Auxiliary Ballast 5" and "Auxiliary
     Ballast 6" carry over directly from the held EU chart (10.11)
     regardless of variant, unlike the main plate numbering (which does
     differ - this variant's 6t single-piece Receptacle Plate 1 vs the EU
     chart's separate 13t Receptacle + 3t Plate 7). sourceNote updated to
     call this specific piece of the data confirmed rather than
     provisional, while the combo list as a whole stays provisional
     pending this variant's own OEM chart.

================================================================================

10.25 LTM 1160 diagram replaced with a person-supplied side-angle
      reference image
  10.9's isometric-corner-view diagram (imprecise hand-approximated
  hotspots, two overlapping faces at once, too intertwined for contour
  tracing) is replaced with a cleaner side-angle exploded view the person
  supplied directly - every plate drawn as its own separate,
  non-overlapping shape, with weights (4t/10t) and two dimensions
  (2750mm, 4270mm) baked into the image itself. Source is NOT confirmed
  to be the same chapter 4.07 document used for the combination data -
  the image crops off partial multi-language caption text on its left
  edge (French "...nt simplifié", Cyrillic "для"), suggesting a separate
  spec-sheet/brochure source rather than the German assembly manual.
  sourceNote and figRef both now say so explicitly rather than implying a
  single unified source, matching how the DXF-sourced diagrams (10.15/
  10.18/10.21) already caption themselves as person-supplied rather than
  claiming OEM artwork.
  Component ids/weights/combos are UNCHANGED from 10.9 - only
  diagramImage/diagramWidth/diagramHeight and each component's hotspots
  moved. Low-res source (523x464), upscaled 3x with Lanczos resampling
  before saving (matches the resolution other diagrams render at) -
  saved as JPEG not PNG, since this is a photographic/rendered raster
  with gradients, not clean CAD line art; PNG came out to ~970KB for the
  upscaled version vs ~210KB as JPEG at quality 90, for no visible
  difference on this source.
  Still hand-picked rectangles, not traced outlines - irregular die-cut
  plate edges on a low-res raster, same call as 10.9 and every other
  raster-photo-sourced diagram in this app.
  Caught one real bug before shipping: the first-pass rectangle for
  Plate 4 (the top 4t plate) extended down to y=178 (in the original
  523x464 coordinate space) to cover its full visible edge profile, which
  overlapped Plate 3's top box (starting at y=97) across roughly two
  thirds of the width - any click landing in that overlap zone hit
  Plate 4's hotspot instead of Plate 3's, since it's the topmost element
  in SVG paint order for overlapping regions. Found by clicking through
  all 6 components in a Playwright test and hitting a real timeout ("Plate
  4 intercepts pointer events") rather than by eyeballing the overlay
  image, where the overlap wasn't obviously visible. Fixed by shrinking
  Plate 4's box to y=27-90, sacrificing coverage of its lower edge
  profile (visible only on the left portion of the plate) in exchange for
  zero overlap with the stack below. Worth remembering for any future
  hand-picked-rectangle diagram: a visual overlay check alone doesn't
  reliably catch overlapping click targets - a scripted click-every-
  component pass does.

10.26 Diagram sizing normalized across all cranes; pinch-to-zoom added
  Two related UI requests. Before this, .cwt-image-wrap used width:100%
  + height:auto on the <img>, so each diagram rendered at ITS OWN native
  aspect ratio - fine while every diagram happened to be roughly
  landscape, but LTM 1110's diagram is portrait (0.80, width < height)
  while every other crane's is landscape (1.13 to 3.57), so 1110 rendered
  more than twice as tall as the others at the same container width,
  dominating the page and requiring much more scrolling to get past it.
  Fixed by giving .cwt-image-wrap a fixed height (min(460px, 60vw), the
  vw term only matters on very narrow viewports) and switching the <img>
  to width:100%/height:100%/object-fit:contain, which letterboxes any
  aspect ratio to fit the same box instead of dictating the box's own
  height. The hotspot SVG needed NO matching logic added - it already
  used preserveAspectRatio="xMidYMid meet" (same letterbox-to-fit
  behavior as object-fit:contain) with position:absolute/inset:0 filling
  the same box as the img, so once both share one fixed-size parent, they
  stay pixel-aligned automatically at any aspect ratio, zoomed or not.
  Verified all 6 cranes now report identical #cwt-image-wrap bounding
  boxes at a given viewport, and that hotspot clicks still land correctly
  on both extreme aspect ratios (1110 portrait, 1300 at 3.57).
  Pinch-to-zoom: added as a self-contained feature (cwtZoomState /
  cwtInitZoom() etc.) applying a CSS transform to a new #cwt-zoom-inner
  element wrapping the img+svg pair, itself a child of the now
  fixed-size, overflow:hidden #cwt-image-wrap - so zooming/panning can
  never grow the page layout, only pan/scale within the fixed box.
  Listeners are bound once (wrap.dataset.zoomBound guard) directly to
  #cwt-image-wrap, which renderCwtTab() never replaces - only the img src
  and the SVG's own children get swapped on re-render, so the zoom
  listeners and current zoom state both survive a crane switch or combo
  selection (zoom state is explicitly reset on crane switch in
  onCwtCraneChange() though, so switching crane doesn't carry over a
  zoom/pan position sized for a differently-shaped diagram).
  Deliberately did NOT set touch-action:none on the wrapper - that would
  block the browser's native single-finger vertical scroll for ANY touch
  starting on the diagram, even when not zoomed in, trapping a user's
  attempt to scroll past the diagram. Used touch-action:pan-y instead
  (blocks native pinch-zoom/horizontal-pan, allows native vertical
  scroll), and only claim a single-finger touch as a pan gesture in JS
  when cwtZoomState.scale > 1 at touchstart - at scale 1, single-finger
  touchmove is never preventDefault'd, so normal page scroll passes
  through untouched. Verified both cases directly: a vertical swipe over
  the diagram at scale 1 scrolls the page (confirmed via window.scrollY
  changing), and the same swipe after zooming in pans the image instead.
  Pinch math: track the two touch points' distance and midpoint at
  gesture start, scale relative to that start distance, and solve
  translate each frame so the content point that was under the midpoint
  at gesture start stays there as scale changes (standard "anchor the
  pinch center" approach) - without this the image would zoom from its
  top-left corner instead of from wherever the fingers actually are.
  Clamped so panned/zoomed content can never show empty space past its
  own edge (cwtClampZoomState). Double-tap and double-click both reset to
  1x - the only way back without a dedicated UI button - gated on the
  gesture never having moved (hadGesture flag) so releasing a real pinch
  or pan doesn't also fire a false reset.
  Verified end-to-end via Chromium's CDP Input.dispatchTouchEvent (not
  just Playwright's synthetic .tap(), which can't simulate true
  multi-touch): two-finger pinch reaches scale 4 and clamps there,
  single-finger drag after zooming in pans and updates translate to match
  the finger's exact delta, and double-tap correctly resets state to
  {scale:1, tx:0, ty:0}.

================================================================================

10.27 LTM 1650 diagram background inverted to match the other 5 cranes
  Only LTM 1650 still used its raw OEM JPG extraction (Fig. 156378) as
  diagramImage, and that extraction had a black background baked into
  the pixels - every other crane's diagram (DXF-rendered PNGs, plus
  1160's new reference-image JPG) has a white background, so 1650 stood
  out. Confirmed the file was pure grayscale first (R=G=B for every
  pixel, zero channel difference measured across the whole image) before
  touching it, since a plain color invert on a genuinely-colored image
  would produce nonsense - safe here because it isn't one. Inverted with
  PIL's ImageOps.invert() and re-saved in place (same filename, same
  dimensions) - white background, dark linework, matching the other
  diagrams' convention exactly. No data/hotspot changes; this is pixels
  only.

================================================================================

10.28 Real bug: counterweight diagrams could never actually update on a
      returning user's device, regardless of how many times
      CACHE_VERSION was bumped
  Person reported still seeing the old black-background LTM 1650 diagram
  after refreshing, despite 10.27's fix being live on main with a fresh
  CACHE_VERSION. Root cause: counterweight diagram images were never in
  sw.js's APP_SHELL list (only index.html/manifest/icons were) - the
  service worker's fetch handler cache-first-then-network's everything
  NOT in APP_SHELL into CONTENT_CACHE, a permanently-unversioned cache
  that the activate handler explicitly never clears (intentional, so a
  crew doesn't lose offline access to reeving diagrams they've already
  viewed across app updates). The first time anyone's browser fetched a
  counterweight diagram, it landed in CONTENT_CACHE and stayed there
  forever - every subsequent CACHE_VERSION bump only ever touched
  APP_SHELL_CACHE, which these images were never part of. This means
  EVERY counterweight diagram fix shipped this session (10.24's LTM 1160
  replacement, 10.23's LTM 1300 addition, 10.27's LTM 1650 invert) was
  invisible to anyone who'd viewed that crane's tab before the fix
  shipped, no matter how many version bumps followed.
  Fix, two parts:
  a) Added all 6 counterweight diagram paths to APP_SHELL, so they're
     precached and refreshed on every CACHE_VERSION bump exactly like
     index.html.
  b) That alone isn't sufficient - a stale copy could already be sitting
     in CONTENT_CACHE from before this fix, and caches.match() (used by
     the fetch handler for anything not explicitly checked against a
     named cache first) doesn't guarantee it prefers the newer entry in
     APP_SHELL_CACHE over an older one in CONTENT_CACHE; iteration order
     across caches isn't something to rely on. Fixed by (1) making the
     fetch handler check APP_SHELL_CACHE explicitly FIRST and return
     immediately on a hit, before ever consulting CONTENT_CACHE, and (2)
     having the activate handler purge any CONTENT_CACHE entry whose URL
     matches something now in APP_SHELL, cleaning up the orphaned
     duplicate rather than leaving it to linger unused.
  Verified with a real service-worker lifecycle test, not just a code
  read-through - Playwright against a local server, swapping the served
  sw.js and image mid-test to simulate "old file cached on a device" then
  "fix deployed to origin, browser checks for SW update":
    1. Serve the OLD sw.js (pre-fix, matching what was actually live) and
       the OLD black image. Load the app, browse to LTM 1650, confirm the
       image is black and lands in CONTENT_CACHE (reproducing the bug).
    2. Swap the served files to the NEW sw.js and the NEW white image
       (simulating a fresh deploy to origin) without touching the
       already-loaded page.
    3. Call registration.update() and wait for the controllerchange
       event specifically (not just polling reg.active, which can
       resolve fractionally before THIS client's controller actually
       flips over and produced a false negative on an earlier, less
       careful pass at this same test).
    4. Reload and re-check the same <img> element used by the real
       Counterweight tab: black -> white, confirmed pixel-for-pixel.
  Also confirmed CONTENT_CACHE's stale entry for the same URL was gone
  after the new SW activated, not just shadowed.
  This class of bug - "shipped a fix, bumped the version, it's still not
  visible" - won't recur for future counterweight diagram changes now
  that they're in APP_SHELL, but is worth remembering as a category: any
  asset that CAN change over the app's development lifetime needs to be
  in the versioned precache, not left to fall into the permanent content
  cache by default just because it's an image fetched on demand.

================================================================================

10.29 LTM 1300 diagram line weight thickened - was 1-2px on a 4643px
      canvas, effectively invisible at the app's actual display width
  Measured before touching anything: sampled dark-pixel run lengths
  across the image and confirmed line strokes were 1-2px wide on a
  4643x1300 canvas - at the app's real display size (~900px wide in a
  normal viewport), that's sub-pixel, explaining why it read as washed-
  out/faint compared to the other diagrams.
  Deliberately did NOT re-render from the source DXF to fix this - the
  DXF's own $EXTMIN/$EXTMAX header didn't cleanly match the existing
  PNG's 4643x1300 aspect ratio (7162x1775 modelspace units vs a 3.57
  aspect ratio image), meaning the exact figsize/dpi/margins used for the
  original render aren't recoverable from the DXF header alone, and
  guessing at them risks silently shifting where each plate lands in
  pixel space - which would have broken every hotspot coordinate already
  measured and shipped for this crane (10.23/10.24). Thickening the
  EXISTING raster in place sidesteps that risk entirely: it's a pure
  image-space operation (PIL ImageFilter.MinFilter, grayscale erosion,
  applied once) that darkens/expands existing dark pixels without moving
  anything, so every previously-derived hotspot rectangle is still
  exactly as valid as before.
  Tested single vs double MinFilter(3) pass side by side, downscaled to
  the app's actual ~900px display width before judging (full-resolution
  comparison would have been misleading - the goal is legibility at
  display size, not at 4643px). Single pass was the right call - clearly
  more defined, "10t" labels still crisp; double pass started looking
  heavy-handed on the small text. Same 4643x1300 dimensions preserved,
  re-verified hotspot click-through and alignment in-browser before
  shipping - unaffected, as expected from a pixel-darkening-only change.

================================================================================

10.30 LTM 1300 Receptacle Plate 1 - genuine gap in the source DXF export,
      one support post's inner line stopped short of the base line
  Person spotted this visually from a cropped screenshot (no coordinates
  given), not from a report against a specific known plate - took several
  wrong guesses across LTM 1130/1250/1110/1650 before landing on the
  right crane (confirmed by the person after being shown candidates) and
  then the right shape within it (the "5"-like notch cutout at the top of
  one of Receptacle Plate 1's two support posts, an actual physical
  detail on the part, not a text label). Once located: the post's LEFT
  edge (x=2682-2685) ran continuously from the notch at y=867 down to
  y=1081, then just stopped - a ~95px gap before the black base line at
  y=1176-1180. The MATCHING right edge of the same post, and the mirror-
  image post on the other side of the same receptacle, both ran
  continuously to the base with no gap - confirming this was a one-off
  export artifact, not a systematic issue.
  Fixed by measuring the existing segment's exact color (129,129,129,
  matching its anti-aliased 132,132,132 edge) and width (4px) directly
  from the image, then drawing a matching line segment closing the exact
  gap (y=1081 to y=1177) with PIL - not a re-render, not a redrawn
  outline, just closing the one missing stroke to match its neighbors
  pixel-for-pixel. Same image dimensions, no hotspot coordinates
  affected. Re-verified click-through in-browser before shipping.
  Worth remembering: when a person reports "missing line" or similar
  without pixel coordinates, checking every candidate crane's full
  diagram at thumbnail scale isn't enough - this defect was invisible at
  thumbnail/overview zoom and only became findable by cropping into the
  specific shape at high magnification with a coordinate grid overlaid,
  the same technique used for hotspot alignment work throughout this
  session.

================================================================================

10.31 Second, genuinely separate gap on LTM 1300 Receptacle Plate 1,
      found via a person-supplied alignment DXF pointing at it directly
  Person drew a single LINE entity over a screenshot of the diagram (a
  downscaled ~1996x559 copy of the shipped 4643x1300 PNG - image_size in
  the DXF's own IMAGE entity confirmed the scale factor, same recovery
  method as the LTM 1650 alignment DXF, 10.22) and sent the DXF rather
  than describing coordinates. Converted the line's endpoints through the
  same wcs-to-pixel math, scaled up by the ratio between the reference
  image and the shipped PNG (4643/1996 = 2.326), landing on x=2535-2683,
  y=1079 in the full image.
  This is NOT the same defect as 10.30 (same general area, easy to
  confuse) - 10.30 was a missing VERTICAL segment on the support post;
  this is a missing HORIZONTAL segment on the wedge-shaped part directly
  below it, where the wedge's flat top should run continuously into the
  post's edge but instead had a 143px gap (x=2538-2681 at y=1079) between
  the end of the ramp's diagonal-then-flat top and where the vertical
  post line crosses that row. Confirmed both the gap's existence and its
  exact extent via a raw pixel row/column scan (not just eyeballing a
  crop) before patching - the visual crop alone was ambiguous about
  whether a line was continuous through that junction or not, since the
  post's vertical line crosses directly through the same area.
  Fixed the same way as 10.30: sampled the existing segment's exact color
  (6,6,6, near-black, distinct from the gray (129,129,129) used for
  10.30's line - different parts/layers in the source use different
  shades) and width (~3px), drew the missing stroke with PIL. Same
  dimensions, hotspots unaffected, re-verified click-through in-browser.
  Method worth keeping: when a person can supply a DXF with even a single
  guide line over a screenshot, that's far more precise and far less back
  -and-forth than describing "there's a gap around here" - convert
  through the image's own embedded scale info exactly like the alignment
  DXFs, don't ask them to estimate pixel coordinates by hand.

================================================================================

10.32 LTM 1160 hotspots upgraded from hand-picked rectangles to traced
      outlines - person drew a box around each plate's real silhouette
  10.25 shipped the new side-angle diagram with rectangle hotspots
      ("still hand-picked rectangles, not traced outlines... irregular
      die-cut plate edges"). Person sent back a DXF (same alignment-DXF
      technique as 1650/1300) with an actual outline traced around each
      of the 6 plates in AutoCAD, following each plate's real chamfered/
      notched silhouette rather than a bounding box.
  Complication: the three 10t plates (p3a/p3b/p3c) are drawn touching
  each other in the diagram, and were traced as ONE connected outline
  (86 LINE entities total, forming 4 separate connected components: p4
  alone, the merged p3a+b+c stack, p2 alone, p1 alone) rather than 3
  independent closed shapes - the person asked directly whether they
  needed to redraw complete separate boxes instead. They didn't: since
  the perspective in this diagram is a true isometric angle (each
  plate's own top/bottom edges are NOT flat horizontal lines - they
  slope), a naive "split by Y-coordinate range" approach would have cut
  through the wrong points. Used proper planar-graph face-tracing
  instead (treat the shared-edge graph as a planar subdivision, sort
  each vertex's neighbors by angle, walk consistently-oriented half-
  edges to recover each bounded face) - this is the general solution for
  "person traced touching shapes as one connected outline, recover the
  individual polygons," not a 1160-specific hack, and doesn't care about
  the perspective distortion since it works purely from graph topology,
  not coordinates. Confirmed the 3 extracted faces against the known
  divider vertices (p3a contains the stack's topmost-left corner, p3c
  contains the bottommost-left corner, p3b is the remainder) before
  trusting the split.
  The other 3 shapes (p4: 9 vertices, p2: 22, p1: 21) were already simple
  closed loops (degree-2 everywhere) - ordered by direct cycle walk, no
  face-tracing needed.
  Scale/insert recovery used the exact same method as the 1650/1300
  alignment DXFs (IMAGE entity's insert point + u_pixel/v_pixel + own
  image_size, cross-checked against the shipped diagram's known
  1569x1392 dimensions before trusting it - this reference image was the
  full-resolution black-background inversion sent for annotation, so no
  extra reference-to-shipped scale-up factor was needed this time, unlike
  1300's downscaled screenshot in 10.31).
  Verified all 6 polygons render without self-intersection, checked
  visually against the diagram, then re-verified in-browser: all 6 tap
  targets register correctly, sum to the right weight, gold highlight now
  hugs each plate's actual outline instead of a rectangular
  approximation. No component ids, weights, or combos changed - hotspot
  geometry only.

================================================================================

10.33 LTM 1160 and LTM 1300's interchangeable Plate 3 copies weren't
      actually interchangeable for combo-matching purposes - swept every
      crane for the same latent bug flagged back in 10.24
  Person reported directly: selecting p1+p2+p3a on LTM 1160 correctly
  flagged 30t as OEM-approved, but p1+p2+p3b (or p3c) - same weight,
  physically identical plate - did not. Root cause was exactly what
  10.24 predicted when fixing LTM 1650's interchangeable 10t positions:
  "This wasn't needed for LTM 1160's own repeated-plate components
  (p3a/b/c, 10.9) since that crane's combos only ever reach 3 total
  copies and were already always listed as the same front-loaded
  canonical subset - the mismatch risk exists there too in principle,
  just never surfaced... flagging here in case it's worth doing if 1160
  ever gets the same free-click complaint." It did.
  Fix was one line per component - just adding
  interchangeableGroup: 'plate3_1160' to p3a/p3b/p3c. No logic changes
  needed; cwtMatchSignature() (built for 1650) already handles any
  interchangeableGroup-tagged component generically.
  Person then asked for a full sweep, not just the reported crane.
  Checked every crane's component list for duplicate `name` values
  (the actual signal for "physically identical/interchangeable part")
  lacking the tag. Found three affected cranes total:
    - LTM 1160: p3a/p3b/p3c ("Counterweight Plate 3" x3) - fixed here.
    - LTM 1300: p3a/p3b ("Counterweight Plate 3" x2) - same bug, same
      fix, same session (was flagged as a known risk in 10.31/10.32's
      area but not yet triggered/fixed until now).
    - LTM 1650: p10a-p10g - already fixed in 10.24, confirmed still
      correctly tagged, no action needed.
  LTM 1250, 1110, and 1130 have NO duplicate-named components at all -
  every part in those three is uniquely positioned, so this bug class
  structurally cannot occur there. Nothing to fix, not just nothing
  found.
  Verified both fixes directly: LTM 1160 p1+p2+p3b now correctly reads
  30.0t/OEM-approved (previously flagged as not approved despite correct
  weight). LTM 1300 p1+p5+p2+p3b (note p5 is mandatory in every listed
  1300 combo, unlike 1160 - a first test pass without it correctly
  showed "not approved" for the RIGHT reason, worth remembering before
  assuming a not-approved result means the interchangeableGroup fix
  didn't work) now correctly reads 36.0t/OEM-approved.

================================================================================

10.34 Added a "Full OEM manual" reference link to the Counterweight tab,
      starting with LTM 1110
  Person wants a way to point at the actual complete OEM manual for each
  crane, not just the extracted/re-drawn diagram and the short sourceNote
  citation already shown. Explicit design constraint from the person:
  "id rather not chop up the offical document as this is my way of
  showing a source of truth maybe we just put the hyperlink in there and
  tell them what page number to go to them selfs." Two attempts at
  auto-jumping to the right page were tried and both failed on live
  testing before that decision was made: Google Drive's viewer does not
  honour a #page=N URL fragment on either /view or /preview - person
  confirmed both open to page 1 regardless of the fragment. A
  direct-download link would let the browser's native PDF viewer handle
  #page=N (which does support it), but large files can trip Drive's
  "can't scan for viruses" interstitial, which would break the link for
  no real gain over just asking the person to flip to a page number
  themselves - not worth it against the stated preference to keep the
  source document whole and unmodified anyway.
  Implementation is deliberately tiny: two new optional fields per crane
  in COUNTERWEIGHT_DATA, manualUrl and manualPage. When present, a plain
  link ("open document") plus a "go to page N for the counterweight
  combinations" line renders under the Composition card's existing sourceNote
  disclaimer; when absent (every crane except 1110 so far) the element
  stays hidden, so this doesn't force placeholder text onto cranes
  without a manual link yet. Person has the manuals for the other cranes
  and confirmed they'll supply a link + page number for each in turn -
  this field just gets filled in per crane as those arrive, no further
  code changes needed per crane after this.
  First link: LTM 1110's Google Drive copy, page 861 for the
  counterweight combinations (originally mislabelled "LTM1100" by the person in
  the same message that shared the link - initially read as the
  separate, still-pending LRT 1100-2.1 tab per 10.12's page-mismatch
  issue; person corrected this directly: "what i shared was for the ltm
  1110t" - so it maps to the already-shipped LTM 1110, not LRT 1100).
  Verified in-browser: selecting LTM 1110 shows the link with the
  correct href and "page 861" text; every other crane's tab shows no
  link element at all (checked LTM 1250 directly via Playwright, and by
  code inspection the same applies to 1130/1160/1650/1300 since none
  have manualUrl set yet).

================================================================================

10.35 Reference Documentation card redesign - matched the Reeving tab's
      "Offline Availability" block, added best-effort auto-page-jump,
      and set aside a placeholder for a future "Driving with the
      Equipment in Place" tab
  Person wanted the manual link to look more substantial, pointing
  directly at the Reeving tab's "Offline Availability" card (title +
  muted description line + full-width gold .btn) as the visual target,
  rather than a plain disclaimer line buried under the Composition
  table. Rebuilt cwt-manual-link as its own card, cwt-manual-card: card
  title, a .hook-badge showing "Page N" (reused from the Hook Block tab
  - green pill, already used elsewhere for this exact "here's the
  number that matters" purpose), a short muted explanation, and a
  full-width .btn styled/sized identically to reeve-cache-all-btn that
  opens the document in a new tab. Hidden entirely (display:none) for
  any crane without manualUrl set, same as before.
  Person also asked to convert the stored links into whatever form
  gives the auto-page-jump function its best shot, accepting it may
  only work on some devices: "if no too bad they can manually do it
  them selfs." Google Drive's own /view and /preview viewers don't
  honour #page=N (confirmed dead in 10.34), but a raw PDF response
  opened directly in a browser's native PDF viewer does support #page=N
  per the PDF Open Parameters spec - Drive's direct-download endpoint
  (drive.google.com/uc?export=download&id=<id>) serves exactly that.
  Implemented as a generic helper, cwtManualHref(data), rather than
  hand-converting each stored link: it regex-matches the standard
  /file/d/<id>/ pattern out of whatever share-link URL is stored in
  manualUrl, rebuilds it as the uc?export=download form, and appends
  #page=N from manualPage. This means every future crane's link just
  gets pasted in as the normal Drive "view" share link (whatever format
  the person copies from Drive's share dialog) with no manual
  reformatting - the conversion happens once, generically, at render
  time. Falls back to using manualUrl as-is (plus the #page= fragment)
  if a link doesn't match the expected Drive pattern, so a non-Drive
  link still works as a plain link. Explicitly best-effort per the
  person's own framing - large files can still hit Drive's "can't scan
  for viruses, download anyway" interstitial, and there's no way to
  detect either outcome in advance, so the Page N badge is always shown
  regardless of whether the jump itself works.
  Separately, person flagged a new tab coming later - "Driving with the
  Equipment in Place" (Driving_with_the_equipment_in_place) - and said
  documents/links for it should just be held per crane until that tab
  is actually built, not forced into the counterweight tab's Reference
  Documentation card. Noted here for the record; no data field or UI
  added yet since no tab exists to render it in and no links have been
  shared for it yet - next thing to do when the first one arrives is
  hold it in a plain per-crane object (not yet designed) until the tab
  itself is scoped.
  Verified in-browser (Playwright): LTM 1110 shows the card with badge
  text "Page 861" and button href
  "https://drive.google.com/uc?export=download&id=1MFDP-Uu-HRINNT5bitsJU3TrK79YTVuf#page=861";
  switching to LTM 1250 hides the card entirely (display:none).

================================================================================

10.36 Wired up the rest of the Counterweight manual links, added the same
      Reference Documentation card to the Reeving Plan tab, second-pass
      dev-jargon sweep on 1250/1110/1130's sourceNote, and held the links
      that don't have a home yet
  Person sent a large batch of Drive links + page numbers in one message,
  organized by crane, each labelled by document type: "CWT combos",
  "reeving tele", "Driving with Eq", plus one one-off ("boom to front
  pg101 | boo to rear pg102" under the LTM1100 heading). Treated each
  type separately rather than assuming they all go in one place:
    - CWT combos: added manualUrl/manualPage to the 5 remaining cranes
      that already have a Counterweight tab - LTM 1130 (807), LTM 1160
      (841), LTM 1250 (881), LTM 1300 (931), LTM 1650 (987). LTM 1110's
      link/page (861) was already wired in 10.34/10.35 from an earlier
      message and matches this batch's "LTM1100" entry exactly - no
      change needed there, just confirms the crane-label mapping from
      10.34 was correct.
    - reeving tele: person said directly "reeving tele goes in reeving
      plan tab." Added a second Reference Documentation card, structurally
      identical to the Counterweight one, to the Reeving Plan tab
      (reeve-manual-card / renderReeveManualLink()), driven by a new
      REEVE_MANUAL_LINKS map keyed by the same crane ids already used in
      REEVE_CRANE_GROUPS. Reused cwtManualHref() as-is (already generic)
      and factored the description text out into cwtManualDescText() so
      both cards share the exact same page-aware/page-less wording
      instead of duplicating it. None of these links came with a page
      number (they point at the whole reeving/telescoping chapter, not
      one page) so the "Page N" badge just stays hidden via the same
      manualPage-optional logic already in place - confirmed all 8 cranes
      (1100, 1110, 1130, 1160, 1220, 1250, 1300, 1650) render the link
      with the badge hidden, card visible, correct converted href, no
      console errors.
    - Driving with Eq: person already said (previous message) to hold
      these until that tab is designed. Stored as data only - see below -
      not rendered anywhere.
    - CWT combos for LRT 1100-2.1 (587) and LTR 1220 (819): neither crane
      has a Counterweight tab yet (LRT 1100's build is still the pending
      task from 10.12; LTR 1220 was never started for this tab, though it
      already exists on the Load Chart/Hook Block/Reeving tabs). A
      reference link with no data behind it to open the tab and see isn't
      useful, so held rather than wired.
    - "boom to front/rear" (LTM 1110 only, pg 101/102): doesn't match any
      existing tab or feature - not Counterweight, not Reeving, not
      Driving-with-Equipment. Flagged back to the person rather than
      guessing at a placement.
  All four held categories are stored in a new PENDING_REFERENCE_LINKS
  const (not read by any rendering code - a holding pen, not a feature)
  so the links and page numbers survive past this conversation instead of
  only existing in chat history. Each entry becomes a real
  manualUrl/manualPage field once its tab exists or its placement is
  confirmed.
  Separately, person flagged that LTM 1110's sourceNote still had the
  same dev-process sentence called out and removed elsewhere before:
  "Diagram re-extracted from an exploded 3D CAD model (user-supplied DXF)
  for precise, pixel-exact plate outlines." Checked all sourceNotes and
  found it verbatim in THREE cranes, not just the one reported - LTM
  1250, LTM 1110, and LTM 1130 all had it (LTM 1250 and 1110 word-for-
  word identical down to the GmbH year; LTM 1130 same sentence with its
  own year). Removed the sentence from all three, leaving the source
  citation and the "Reference tool only — cross-check..." safety line
  intact. LTM 1160's sourceNote has a different, actually-relevant
  caveat ("Diagram is a separate reference image, not confirmed to be
  from the same source document") - left alone, that's provenance
  information a user needs, not production process trivia. This is a
  second sweep pass distinct from 10.somewhere earlier in the session;
  these three must have been written or reverted after that first pass
  and never re-checked.

================================================================================

10.37 Corrected the one "unplaced" link from 10.36 - it's Driving with
      Equipment, mislabelled
  The single link flagged as unplaced in 10.36 ("boom to front pg101 |
  boo to rear pg102" under the LTM1100 heading, no tab named) - person
  confirmed directly: "ah its driving with eq i labled it wrong." Moved
  it from PENDING_REFERENCE_LINKS.unplaced into .drivingWithEquipment
  under '1110', alongside the other five cranes already held there.
  Kept as two separate page refs rather than a range, unlike every other
  crane's single page/range in that bucket - "101 (boom to front) / 102
  (boom to rear)" - since front and rear boom position are two distinct
  pages, not a span. The now-empty unplaced bucket was removed. Nothing
  rendered anywhere yet, same as the rest of drivingWithEquipment - still
  waiting on that tab being designed.

================================================================================

10.38 Reverted the direct-download link conversion from 10.35 - it forced
      an actual file download instead of opening in a tab
  Person noticed the Reference Documentation buttons were triggering a
  download rather than opening the manual to look at. Root cause: Google
  Drive's uc?export=download endpoint (used in 10.35 to give the
  #page=N auto-jump its best shot) sends a Content-Disposition:
  attachment response header, which forces the browser to save the file
  instead of displaying it - regardless of whether the browser has a
  native PDF viewer that would otherwise honour #page=N. That's a strictly
  worse outcome than what the button is for (open and read the manual),
  and the auto-jump it was chasing was already only a maybe (10.34 found
  Drive's own /view and /preview never honour #page=N at all). Reverted
  cwtManualHref() to a plain pass-through of the stored manualUrl -
  Drive's normal /view link, which opens in a new tab like any other
  link and doesn't force a download. Also softened the description text
  in cwtManualDescText() to stop promising a "some devices" auto-jump
  that isn't attempted anymore - now just tells the person to use the
  page number themselves. This lands back on the person's original
  design from 10.34: plain hyperlink + page number, nothing clever
  attempted. Verified in-browser: LTM 1300's button href is now the
  plain drive.google.com/file/d/.../view?usp=drive_link link, not a
  uc?export=download one.

================================================================================

10.39 Built the LTR 1220 Counterweight tab - new crane, box-traced DXF +
      OEM combo PDF, both supplied by the person
  Two-part delivery: first a DXF with the diagram's actual OEM artwork
  imported via AutoCAD PDFIMPORT plus 8 hand-drawn boxes around each
  distinct plate silhouette (same box-tracing workflow as 1160's 10.32),
  sent for review before any data was attached; then, once boxes were
  confirmed good, a combo PDF with the actual weights/composition table.
  Box review: no IMAGE entity this time - the entire diagram was
  PDFIMPORT'd as vector LWPOLYLINE geometry (216 entities on
  PDF_Visible) with 8 more on a Boxes layer. Rendered the DXF to a PNG,
  zoomed into every shared box boundary, both lifting lugs, the notched
  base plate, and the stepped hitch/pin detail at the top of the
  pedestal - every box hugged the actual linework exactly, no clipping,
  no gaps at shared edges. Sent back a numbered reference render (not
  live) for the person to identify each box against.
  PDF decode: same -29 character-shift substitution cipher as every
  other Liebherr combo PDF this session (10.1/10.4/10.34's caveat that
  it needed re-confirming per document) - confirmed it still applies,
  but this time the plain page.get_text() concatenation lost the
  numeric weight values entirely because digits are encoded as control
  characters (0x13-0x1A) sitting below the printable-ASCII floor my
  original decode function handled. Switched to per-span dict-mode
  extraction and a decode that shifts every character except literal
  space by +29 with no wraparound (rather than the mod-94 rotation used
  before) - recovered the full combo table: 0.0t (empty), 10.0t
  (receptacle plate 1 alone), 30/50/70t (receptacle + 1/2/3 pairs of
  "counterweight plate 2" + "counterweight plate 3").
  Person's plain-language box assignment, verbatim: "0 and 1 are the
  same thing" (the base plate and pedestal outlines are two disjoint
  shapes but one physical/OEM-listed part), "3/2 are a pair, 5/4 are a
  pair, 7/6 are a pair" (three stacked tiers, each a left+right pair),
  "the pairs get selected together as you cant just put on one"
  (corrected mid-sentence from "can just put one on" - both-or-neither,
  not independently selectable), and "like the other cwt combos with
  multi of the same slab type i want them to be able to built and
  selected any any order" (the 3 tiers are interchangeable with each
  other, not fixed positions).
  Modelled as: p1 (Receptacle plate 1, 10.0t) with separate:true and
  TWO hotspots (boxes 0+1) for one id - same pattern as 1300's
  aux5/aux6, just reused for "one part, two disjoint outline pieces"
  instead of "one part, two mirrored mounting positions". Each tier is
  two ids - p2x (right, "Counterweight plate 2") and p3x (left,
  "Counterweight plate 3") - linked to each other via linkedId (1110's
  p3/p4 pattern, gives the both-or-neither click behaviour) AND each
  tagged with an interchangeableGroup shared across all 3 tiers'
  same-named id (plate2_1220 for all three p2x, plate3_1220 for all
  three p3x - two groups, not one, since the OEM chart names them as
  two distinct parts even though they're always equal in count). This
  is the first crane to combine linkedId and interchangeableGroup on
  the same components - confirmed in the engine they're orthogonal
  (linkedId governs toggle-time selection, interchangeableGroup only
  affects combo-signature matching) with no special-casing needed.
  Diagram rendering picked up a real refinement over the dpi-cranking
  workaround from 10.somewhere (ezdxf's Frontend.draw_layout(finalize=
  True) silently overriding whatever figsize/xlim/ylim was set
  beforehand, discovered again here): rather than fighting it, let
  finalize=True auto-fit as it always does, then READ BACK ax.get_xlim()
  /get_ylim() afterward and use those (now-authoritative, content-driven)
  values plus the actual saved-canvas pixel size to build an exact
  linear world-to-pixel transform - spot-checked against multiple known
  box corners/edges before trusting it for the real hotspot coordinates,
  all within 1-4px (line-width tolerance). Also used
  Configuration(background_policy=BackgroundPolicy.WHITE, color_policy=
  ColorPolicy.BLACK) to get a clean white-bg/black-line render directly
  instead of the invert-after-the-fact approach used for 1650 (10.28) -
  cleaner since this DXF's dark-bg render didn't have a uniform
  near-zero-channel-difference background the way 1650's did, so a
  blind invert produced an off-white/beige cast rather than true white.
  Wired up as a normal new crane entry - manualUrl/manualPage moved out
  of PENDING_REFERENCE_LINKS.cwtCombosPendingTab now that the tab it was
  waiting on exists (LRT 1100-2.1 remains held, still no tab). Verified
  in-browser: p1 alone reads 10.0t/OEM-approved; clicking only the
  right-side hotspot of any tier auto-selects its left-side partner
  (and vice versa) via linkedId; deselecting one side drops both;
  selecting tier-2 + tier-3 (skipping tier-1) still correctly reads
  50.0t/OEM-approved, proving the interchangeableGroup matching doesn't
  care which physical tiers were picked, only how many.

================================================================================

10.40 Third dev-jargon sweep pass - this time figRef, not sourceNote
  Person flagged LTM 1300's figRef directly: "Re-drawn from a
  user-supplied exploded 3D CAD model (DXF) of this variant's actual
  counterweight stack - not OEM artwork." Same class of issue as
  10.36/10.37's sourceNote sweeps, just a different field this time -
  10.36 only checked sourceNote and left figRef alone on the (wrong)
  assumption that "not OEM artwork" needed the production-process detail
  attached to be credible. Checked every crane's figRef and found the
  same pattern in five of seven: 1250/1110/1130 all said "diagram
  re-drawn from an exploded 3D CAD model, not the original OEM artwork"
  verbatim; 1160 said "Re-drawn from a user-supplied reference image,
  not the chapter 4.07 OEM artwork..."; 1300 had the DXF mention flagged
  above; 1220 (built this session, 10.39) said "...AutoCAD PDFIMPORT..."
  - same mistake made fresh, not even old debt.
  Stripped the process description from all six, keeping only the
  user-relevant fact (is this the actual OEM artwork or not, and for
  1160/1300, whether it's confirmed to be the same source as the combo
  data): 1250/1110/1130 -> "diagram re-drawn as clean line art, not the
  original OEM artwork"; 1160 -> "Not the chapter 4.07 OEM artwork used
  for the combination data above."; 1300 -> "Diagram re-drawn as clean
  line art from this variant's actual counterweight stack — not OEM
  artwork."; 1220 -> "Diagram traced directly from the OEM combination
  chart's own artwork, not a freehand redraw." 1650 was already clean
  ("actual OEM exploded-view artwork, extracted directly, not
  redrawn.") - no DXF/CAD/PDFIMPORT/user-supplied mention, left as-is.
  Grepped both sourceNote and figRef together afterward for
  DXF/3D CAD/user-supplied/PDFIMPORT/AutoCAD/pixel-exact across all
  seven cranes - zero matches. Verified all seven render their new
  figRef text correctly in-browser.

================================================================================

10.41 New tab: Driving with Equipment in Place - test build, LTM 1110 only
  Built as an explicit test per the person's request ("lets do a test to
  see how you go and send a preview before anything goes live") before
  committing to the full design. Preceded by a training exchange (not
  logged here in detail, see chat) that established the vocabulary:
  VEHICLE (carrier/chassis) vs SUPER STRUCTURE (rotating turret/cab/
  boom), FRONT/REAR as fixed ends of the vehicle (not the boom's current
  direction), and that "boom direction to front/rear" means the
  superstructure has been slewed 180° so the boom points over one end
  of the vehicle or the other - confirmed directly by the person: "you
  are right it was rotated 180 degrees if the booms facing the rear
  thats the rear charts and if the facing the front front chart."
  Source PDF (LTM 1110-5-1-000, doc 0213844-00, chapter 15.05) has NO
  character-shift cipher, unlike every Counterweight Combinations PDF
  processed so far this session - page.get_text() came back plain and
  immediately readable. Worth remembering per-document-category, not
  assumed universal: the cipher is a Counterweight-chart-specific export
  quirk, not a property of every Liebherr PDF in this app.
  Data model, per the person's own field breakdown from the training
  exchange:
    - Inputs: crane model, boom direction (front/rear, forced choice -
      no default), counterweight location (superstructure/vehicle,
      forced choice, only rendered for cranes with
      hasVehicleOption:true - not exercised yet, no crane in the test
      data has this set), counterweight weight (populates every output
      field below it).
    - Outputs per matched row: hook block max weight, boom angle range,
      support width, max axle load for axle group A, max axle load for
      axle group B. Axle group labels (e.g. "1 ... 2" / "3 ... 5") are
      stored per-crane-per-direction rather than hardcoded, since axle
      count/grouping varies by chassis.
    - Forced-choice reasoning matches the Counterweight tab's existing
      philosophy (10.something on Counterweight location) - a silent
      default risks someone reading pre-populated axle-load numbers as
      correct for their actual setup without ever confirming which
      configuration they're really in. Both toggles start with neither
      button active; the weight dropdown stays disabled with a
      placeholder until a valid direction (+location where applicable)
      is chosen.
    - Duplicate-weight rows (same counterweight, two valid chart entries
      differing in boom angle/support width/axle loads) are never
      merged - both render stacked under one "Option A" / "Option B"
      label with a divider between them, per explicit instruction:
      "id like them both displace one below each other with clean
      deliniation please."
      UPDATE (person found the actual OEM explanation later, in LTM
      1160-5.2's own Driving w/ Equipment manual - supersedes whatever
      informal reasoning either of us had for why two rows exist per
      counterweight): the manual's own Note reads "For 'travel operation
      with equipment in place', for the same set up configuration there
      are different values for the 'Telescopic boom angle to the
      horizontal' and the resulting 'maximum axle load per axle'."
      followed by two labelled examples - Example 1 = the permissible
      boom angle WITHOUT optimized axle load values, Example 2 = the
      OPTIMAL boom angle WITH optimized (i.e. more favorable, lower)
      axle load values for that same physical setup. So "Option A" /
      "Option B" in the app aren't two arbitrary alternate readings of
      the chart - they're specifically the un-optimized vs
      axle-load-optimized boom angle for driving with the same
      equipment fitted, straight from the OEM's own worked-example
      structure. Current UI labels ("Option A"/"Option B") don't convey
      this - worth relabeling if the person wants it (e.g. "Permissible
      angle" / "Optimized angle (reduced axle load)"), not yet done as
      of this note.
  Data extraction: per-span dict-mode text extraction (ArialMT for data,
  Arial-BoldMT for headers), rows auto-grouped by pattern-matching 6
  consecutive fields against expected shapes (weight: \d+\.\d, hook
  block: "N ... N", boom angle: contains °, support width: \d.\d\d,
  two integer axle loads) rather than fixed positional slicing, since
  row count varies per crane/direction and this needed to self-
  terminate at the trailer/footer text. Verified against the source: 23
  rows on each of front/rear, correctly split into 14 distinct
  counterweight values front (9 duplicated) and 12 distinct rear (11
  duplicated) - reconciled by hand against the row count before trusting
  the parse.
  One field intentionally NOT built yet: support width as a percentage
  of full outrigger spread (100/75/50/0%) - person asked for this, but
  the only outrigger dimension data in the app (cadFleetData, used by
  the Outrigger Support Positioning tab) is pad radius+angle from the
  slew center at full deployment only, a different measurement to the
  "min. B" beam-spread figure in this chart, and the two front pads'
  angles aren't symmetric (135°/143° for the 1110) so deriving a width
  from that data risked reading it wrong. Declined to guess at a
  safety-relevant dimension - person will locate the actual
  percentage-to-metres reference document instead.
  Sent three screenshots as the requested preview (empty forced-choice
  state, Front 0.0t, Rear 29.0t - both showing the stacked Option A/B
  layout) before anything was committed. Person approved pushing to
  live afterward: "lets just leave as default for now till i have find
  the info push to live and lemme see it in actions."

================================================================================

10.42 Driving with Equipment in Place - added LTM 1300, which forced a
      real schema change (boom length is now a variable, not fixed)
  10.41's LTM 1110 test only had one boom notation (T-11.5) across both
  its front/rear pages, so DRIVING_EQ_DATA had no concept of boom
  length at all - direction was the only axis. LTM 1300's chapter
  (029931-00, 15 pages) broke that assumption immediately: TWO boom
  configurations - T-14.7 (0/0/0/0/0/0), fully retracted, with front,
  rear, AND vehicle-mount tables; and T-19.6 (0/0/46/0/0/0), section 3
  extended to 46%, front only, no rear, no vehicle option at all.
  Person's instruction before building: "only list the options for
  cranes that has chart that differ from full retracted." Restructured
  DRIVING_EQ_DATA so every crane has a boomConfigs array - LTM 1110
  keeps its single T-11.5 entry, LTM 1300 gets two. The new Boom
  Configuration picker (mode-toggle, same forced-choice pattern as
  direction/location) only renders when boomConfigs.length > 1, so
  every single-config crane (1110, and every crane added after this
  without a partially-extended chart) never shows it at all - confirmed
  1110 unaffected, boom-config-group stays display:none exactly as
  before.
  Cross-availability had to be wired properly rather than just hidden:
  Rear and Vehicle-location are only valid for boom configs that
  actually have that data. Added .mode-btn:disabled CSS (opacity 0.35,
  cursor not-allowed) and disable Front/Rear per the selected boom
  config's own directions object (T-19.6 disables Rear entirely, since
  cfg.directions.rear doesn't exist for it) - Location group itself
  hides completely when the boom config has no vehicleWeights, same
  pattern as before just re-evaluated on boom config change now, not
  only crane change. Additionally, choosing Vehicle location forces
  Direction to Front and disables Rear regardless of boom config, since
  every vehicle-mount page in the source PDF is boom-to-front only -
  there's no rear vehicle-mount table to ever show.
  Vehicle-mount data itself is stored differently to the
  front/rear tables - keyed by the discrete package weight (96, 86, 76,
  66, 56, 46, 36, 26, 16, 13, 6t) rather than living inside a
  direction's row list, since these are 11 separate OEM pages, not one
  table with a varying weight column. Confirmed on every single page:
  "Counterweight on superstructure" reads 0.0t, matching the design
  decided before any of this data existed - added a
  "Counterweight on superstructure: 0.0t" row to the output specifically
  for the vehicle-location case, since it's genuinely present in the
  source table and worth surfacing, not implied.
  Nailed down an ambiguity from the original design conversation: the
  person's own worked example ("96.0 500...1600 55°...82° 2.68 33 34 /
  96.0 ... 69°...79° ... 32 32") is NOT from a vehicle-mount page at
  all - it's the T-14.7 REAR superstructure table's own 96.0t row,
  confirmed by exact match against the extracted data. The "96t" in the
  vehicle-mount page titles ("96t counterweight, placed on the vehicle")
  is an unrelated, separately-numbered package - worth flagging since
  skimming the source PDF could easily conflate the two.
  Extraction used the same per-span dict-mode + pattern-matched 6-field
  grouping as 10.41, run per-page across all 15 pages; boom notation,
  direction, and (where present) the vehicle package weight were parsed
  from each page's own header spans rather than assumed from page order.
  Verified in-browser end to end: T-14.7 Rear 96.0t reproduces the
  person's example exactly; Vehicle location correctly lists all 11
  packages, forces Front, disables Rear, and shows 0.0t superstructure
  per row; switching to T-19.6 disables Rear and hides Location
  entirely, front-only 16.0t/13.0t data renders correctly; no console
  errors on any path.
  Per the person's own note: they may drop T-19.6 (and any other
  not-fully-retracted chart) from this tab later on. Kept for now since
  it's real OEM data and they explicitly said to try it and push to
  live regardless.

================================================================================

10.43 LTM 1110's Rear table was badly truncated - fixed with a fuller PDF,
      and hit a genuinely new extraction bug along the way
  Person: "i missed a few pages from the list so look over it again more
  on the to the rear side of things," with a re-supplied version of the
  same document (same doc code 0213844-00/ver_2197110102) - this one 5
  pages instead of the original 2. Page 0 is Front, unchanged from what
  was already shipped. Pages 1-4 are ALL "Boom direction to the rear"
  (same vtab_..._02001_00_000 code repeated across all four) - one long
  table continuing across page breaks, not four separate charts. The
  original 2-page PDF only had page 1 of that continuation, so 10.41's
  Rear data stopped at 24.4t - the real table goes all the way to 0.0t.
  This version of the document also turned out to have the -29
  character-shift cipher active (10.41's original 2-page PDF did not -
  confirms per-document/per-export encoding is not something to assume
  either way, per the earlier note to build a training reference for
  this).
  Hit a new, nastier variant of the digit-decoding bug first found on
  LTR 1220 (10.39): calling .strip() on a span's RAW, still-encoded text
  - either to trim it or just to test truthiness before deciding whether
  to keep it - silently corrupts or drops content, because several of
  the cipher's control-character byte values for digits (found here:
  raw \x1c, representing digit '9') fall inside Python's Unicode
  whitespace definition. Two distinct symptoms from the same root cause:
  calling .strip() and keeping the result truncated "10.9" down to
  "10." (lost the trailing digit); calling raw.strip() only as an
  emptiness check before appending the (unstripped) text dropped entire
  rows outright, whenever a span's encoded bytes happened to ALL fall in
  that whitespace-like range. Fix: never call .strip() on cipher text
  before decoding - decode first with the +29 shift, THEN strip/test the
  decoded (now normal-ASCII) result if needed. Also had to special-case
  the degree sign here: its raw byte (0x83) doesn't survive the
  no-wraparound +29 shift into the correct Unicode code point for '°'
  (U+00B0/176) the way ASCII letters/digits do, so it's mapped
  explicitly rather than trusting the arithmetic.
  Re-extracted all 5 pages with the fixed decoder: page 0 (Front) came
  back byte-identical to what's already shipped, confirming no other
  damage; pages 1-4 (Rear) combined to 78 rows across 39 distinct
  counterweight values (previously only 12 were shown), every one with
  exactly 2 boom-angle-range options, continuity checked at every page
  break (e.g. 24.4t's second option starts page 2 right after its first
  option ends page 1, no gap or duplicate). Replaced the Rear rows array
  wholesale rather than appending, since the old 12-weight set was a
  strict prefix of the new 39. Verified in-browser: Rear now lists 39
  weights, 0.0t shows both options correctly, Front (14 weights) and
  LTM 1300 unaffected.

================================================================================

10.44 Driving with Equipment in Place - added LTM 1130, LTM 1160, LTM
      1250; recommended fleet hook block cross-reference
  Person sent four more PDFs in one batch (1650, 1130, 1160, 1250),
  explicitly asking to hold the 1650 one till last "as you might have
  questions." Processed the other three, none needed a question, all
  pushed live per the standing instruction ("if no problems push to
  live").
  None of these three PDFs use the character-shift cipher - plain text
  throughout, same as 1110's original 2-page upload. Confirms again this
  is a Counterweight-chart-specific quirk, not universal.
  Three genuinely new data-shape variations, each requiring a small
  schema/extraction change rather than just new numbers:
    - LTM 1130 (doc 028799-00, 2016): only "boom direction towards the
      rear" is present for the plain telescopic boom - no Front table in
      this upload, and the document's own next-section header
      ("2 Telescopic boom T-12.7 (0/0/0/0/0) with folding jib K-2.9")
      confirms there's at least one more config with no table for it
      either. Built with only directions.rear present - the existing
      per-boom-config Front/Rear disable logic (built for 1300's T-19.6)
      handled this with no changes needed. This document's table also
      has NO "Hook block" column at all (older/simpler chart format) -
      added conditional rendering so that row only appears when
      r.hookBlock is actually present on the row object, instead of
      assuming every crane has one.
    - LTM 1160 and LTM 1250: introduce a different config axis to 1300's
      boom length - "Outer radius of counterweight" (5.3m/4.8m for 1160,
      5.6m/4.8m for 1250), each with its own Front AND Rear table (no
      front/rear gaps here, unlike 1130/1300). Reused the boomConfigs
      array mechanism as-is since it's the same shape (a labelled config
      axis with its own directions) - the label text just describes a
      radius instead of a boom length, no code changes needed beyond
      that. Front's lightest rows are numerically IDENTICAL between the
      two radii on both cranes - confirmed directly against the source
      text before trusting it, not a copy-paste artifact; a genuine OEM
      coincidence at the light end where the counterweight's mounting
      radius apparently doesn't move the numbers.
    - Extraction itself needed a real fix for 1160/1250: a single PDF
      page can contain the TAIL of one table and the START of the next
      back to back (1160 page 3, 1250 page 4 both do this) - page-per-
      table can't be assumed. Rebuilt extraction to concatenate every
      page's spans into one continuous stream and split on the
      document's own vtab_ codes (a unique marker per table,
      independent of where page breaks fall) instead of splitting per
      page. Also, LTM 1250's counterweight column prints whole numbers
      without a decimal point ("5" / "0" instead of "5.0" / "0.0"),
      unlike every other crane so far - widened the weight-shape regex
      from \d+\.\d to \d+(\.\d)? to catch both, stored as a decimal
      internally either way for consistency.
  Separately, person's own idea: "since we know that each crane models
  hook block weight are from the minium block weight tab can we put a
  the recommend one." Added driveEqRecommendedHookBlock(craneKey,
  hookBlockRange) - cross-references the chart's hook block ALLOWANCE
  (e.g. "0 ... 900" kg) against FLEET_BLOCK_SPEC (the same data already
  driving the Minimum Hook Block Weights tab) and shows the heaviest
  fleet block whose own net weight fits inside that range, as a new
  "Recommended fleet block" row directly under Hook block. Picks
  heaviest-that-fits on the basis that a crew defaults to the biggest/
  most capable block still legal for the drive, not the lightest
  available; only compares each block's base net weight, not any
  auxiliary-weight variant, since aux weights are a lifting-capacity
  choice, unrelated to what's legal for road travel. Silently shows
  nothing when a crane has no FLEET_BLOCK_SPEC entry yet, the range
  doesn't parse, or nothing in the fleet fits - never a broken or
  misleading row. Verified: LTM 1110 front recommends the 100t 5-Sheave
  DM Block (900kg, sits right at the 0...900 boundary); LTM 1300 rear
  recommends the 125t 5-Sheave DM Block (1600kg, right at the
  500...1600 boundary); LTM 1130 (no Hook block column at all) shows no
  recommendation row, as expected.
  Regression-checked LTM 1110 and LTM 1300 after all changes - both
  still render identically to before.

================================================================================

10.45 Relabelled "Recommended fleet block" to "Maximum fleet block" -
      same computation, clearer meaning
  Person: "can you find which one would be the maximum based on weight
  so operators know if they have the smaller one on they weill be in
  the green." The underlying pick was already exactly this - heaviest
  fleet block that still fits inside the chart's hook block allowance -
  but "Recommended" reads as "go get this one," when the actually useful
  fact for a crew is a ceiling: whatever block is actually fitted, if
  it's this weight or lighter, they're compliant. Renamed the function
  (driveEqRecommendedHookBlock -> driveEqMaxCompliantHookBlock) and the
  displayed row label ("Maximum fleet block (lighter is fine)"), and
  added a line to the tab's disclaimer spelling out the ceiling framing
  explicitly, since a mislabelled safety-relevant figure is worse than
  no figure at all. No change to the selection logic itself.

================================================================================

10.46 Prepped for LTM 1650's 3-band hook block data - no new selector
      needed after all
  1650's chart has three separate hook block bands (0 / 700-3500 /
  3500-4600 kg), each with its own weight-vs-angle sub-table - flagged
  in 10.-something as needing design input before building. Walked
  through options with the person; landed on something much simpler
  than a new forced-choice selector: "i just want to know what the
  maxium size block ... can be used with that counterweight -boom
  direction etc ... if it says zero allowed then just say No hookblock."
  Once band tables are extracted, each band's rows just become more
  entries in the SAME flat rows array for that weight - no different
  from how boom-angle variants already stack as Option A/B/C. The only
  code change needed: when a row's hookBlock is the bare string "0" (no
  range), the "Hook block" line now reads "No hook block" instead of
  "0 kg", and driveEqMaxCompliantHookBlock is skipped for it explicitly
  (its own regex wouldn't have matched "0" anyway, but this makes the
  intent explicit rather than relying on that as an accident). Confirmed
  with the person directly this also satisfies "only offer the hook
  block recommendation if it is permissible" - since the fleet-block
  lookup already only ever runs against each row's own range, a
  narrower/heavier band's row can never inherit a recommendation from a
  lighter band's row it's stacked next to.
  Regression-checked all five live cranes (1110/1130/1160/1250/1300) -
  no visible change, since none of them currently have a bare "0"
  hookBlock value; this is purely prep for 1650's actual data, which is
  still on hold while the person checks whether more pages exist beyond
  the 8-page/477-page excerpt already reviewed.

================================================================================

10.47 Driving with Equipment in Place - added LTM 1650, closing out this
      tab's initial crane-by-crane rollout
  Person re-checked and supplied a fuller 14-page PDF (same doc version,
  different upload) covering all 8 sections this crane's charts are split
  into: sections 1.1-1.4 (T3, without winch 2 and winch 3) and 2.1-2.4
  (T3, with winch 2, without winch 3), each split further into T-16.7 vs
  T-29.0 boom length and front vs rear boom direction - all at the single
  8.4m outer radius this document provides. This document turned out to
  use the same character-shift cipher as LTM 1110/1300 (raw+29, literal
  space passed through, degree symbol special-cased) rather than the
  plain text of the original 8-page excerpt - confirms the earlier
  finding that this varies per export, not per crane or per document
  family, so every new PDF still needs its own check rather than
  assuming.
  Modelled the two independent axes (boom length, winch rigging) as a
  single flat list of 4 boomConfigs entries rather than inventing a
  second axis of UI - exactly the flexibility this mechanism was already
  proven to have (1160/1250 used it for outer radius, 1300 for boom
  length alone). All 4 share the same 3-band hook block structure (0 /
  700-3500 / 3500-4600 kg) confirmed uniform across every section of this
  document, so the bare-"0" "No hook block" display and the max-
  compliant-fleet-block cross-reference built in 10.45/10.46 apply
  unchanged with zero new code - only the data changed.
  One wiring wrinkle: FLEET_BLOCK_SPEC keys this crane's hook block fleet
  data by boom variant ('1650-t3'/'1650-t5', from the counterweight tab's
  earlier build) rather than by plain '1650', but Driving w/ Equipment's
  boomConfigs axis here is boom-length-and-winch, not t3/t5 - the two
  don't line up. Rather than splitting DRIVING_EQ_DATA to match, aliased
  the lookup ('1650' -> '1650-t3') inside driveEqMaxCompliantHookBlock,
  since both fleet-spec variants share identical base block weights for
  every block they have in common and t3's list is the superset.
  Multi-page extraction reused the vtab_-code-splitting approach from
  10.44 (sections 1.2/2.2 span 3 pages each, 1.3/2.3 span 2 pages each) -
  no new extraction technique needed, just re-verified page-to-vtab
  mapping against the section headers before trusting row order.
  Verified in-browser (Playwright): all 4 boom configs selectable, front/
  rear cross-availability, Option A-F stacking where multiple rows share
  a weight across different hook block bands, "No hook block" display,
  and the fleet-block ceiling recommendation surfacing correctly for both
  the 700-3500 and 3500-4600 kg bands. Regression-checked all five prior
  cranes (1110/1130/1160/1250/1300) with no console errors and no visible
  change.
  Known gaps, called out to the person rather than silently omitted: this
  document only covers the single 8.4m outer radius and the two winch
  states shown above - no other outer radius and no "with winch 2 and
  winch 3" section are in this upload, so those remain unavailable until
  supplied. Person confirmed 8.4m is near enough the fleet standard for
  this crane either way.

================================================================================

10.48 Reordered and renamed the tab bar
  Person's first phrasing ("from right to left: Reeving Plans, CWT
  Combinations, ... Rope Retentioning") read as ambiguous - built it as
  the reverse (Rope Retentioning first, Reeving Plans last), person
  flagged that as wrong, clarified directly: "from left to right, reeving
  plans > CWT combos > driving with eq > min block weight > outrigger
  support > rope re-tensioning." Corrected to that literal left-to-right
  order: Reeving Plans, CWT Combinations, Driving with Equipment in
  Place, Minimum Hook Block Weights, Outrigger Support Positioning, Rope
  Retentioning. Reordered the <nav> button markup only - switchTab() keys
  off the clicked button's tabId, not DOM position, so the tab-content
  divs didn't need reordering to match; Rope Retentioning keeps the
  active-on-load default (its tab-content div's "active" class), just now
  as the last button rather than the first. Two renames alongside the
  reorder: "Reeving Plan" -> "Reeving Plans", "Counterweight Combinations"
  -> "CWT Combinations". Verified in-browser: all six tabs click through
  to the correct panel in the final order, no console errors.

================================================================================

10.49 Made Reeving Plans the default tab on load
  Person asked for Reeving Plans (not Rope Retentioning) to be the tab
  open on first load. Moved the "active" class from the Rope Retentioning
  button/tab-content pair to the Reeving Plans pair. Caught one wiring gap
  in testing: unlike calcCAD()/initCwtTab()/initDriveEqTab(), which run
  unconditionally in the page's bottom-of-script init block regardless of
  which tab is active, initReeveTab() (populates the crane/config selects
  and loads the reeving diagram manifest) was ONLY ever called from
  inside switchTab() when navigating TO reeve-tab by click - it had never
  needed to run on page load before, because Reeving Plans was never the
  default tab before. Added an explicit initReeveTab() call to the
  init block so the tab isn't blank on first load now. Verified in-browser:
  Reeving Plans opens by default with crane/config selects and the first
  reeving diagram already populated, no console errors.

================================================================================

10.50 Relabelled "Maximum fleet block" to "Maximum known size hook block"
  Person asked for wording that makes it obvious the figure belongs to
  the exact crane selected, then settled on "maximum known size hook
  block" as the label. Updated the table row label and the tab
  disclaimer paragraph in Driving with Equipment in Place to match; no
  change to driveEqMaxCompliantHookBlock() or the underlying selection
  logic, wording only. Checked: this tab (and its FLEET_BLOCK_SPEC data
  with per-block aux ballast options) only exists on the beta-trial
  branch - main/live has no Driving with Equipment tab at all yet, so
  there is nothing to change there today. Live's own FLEET_BLOCK_SPEC
  constant is an unrelated, older schema used by a different tab
  (Minimum Hook Block Weights) and wasn't touched.

================================================================================

10.51 Added an "Advanced" disclosure to the Boom Configuration picker
  Person's ask: any boom config that isn't the plain/baseline chart for a
  crane (extended section, extra winch fitted, whatever the crane's own
  sections are) should be tucked away from the main toggle instead of
  sitting alongside it as an equal choice. Person flagged they'll be
  adding more of these to most other cranes going forward, so the split
  needed to be data-driven rather than inferred from label text - LTM
  1300's T-19.6 signals "not baseline" via a 0/0/46/0/0/0 percentage
  pattern, but LTM 1650's "with winch 2" configs have no percentage
  notation at all, so a regex on the label wouldn't generalize.
  Introduced a plain `advanced: true` flag on the boomConfig object
  itself (schema comment updated above DRIVING_EQ_DATA) - renamed from
  the unused `fullyRetracted: false` that was already sitting on LTM
  1300's T-19.6 entry (added in 10.42, never wired to anything). Flagged
  LTM 1300's T-19.6 and both of LTM 1650's "with winch 2" configs.
  onDriveEqCraneChange() now splits a crane's boomConfigs into the
  existing #de-boom-config-toggle (unflagged) and a new collapsed
  <details> disclosure, #de-boom-config-advanced, holding a second
  mode-toggle row for flagged configs - only shown at all when a crane
  actually has an advanced entry. Both toggles tag each button with its
  original array index (data-idx) so setDriveEqBoomConfig() can drive the
  active state across both containers without assuming position. Verified
  in-browser (Playwright): LTM 1300 shows only T-14.7 in the main toggle
  with T-19.6 under Advanced; LTM 1650 shows both no-winch configs main
  and both with-winch configs under Advanced; selecting an Advanced entry
  flows through direction/weight/output exactly like a main entry; single-
  config cranes (e.g. LTM 1110) still hide the whole Boom Configuration
  group as before; no console errors.

================================================================================

10.52 Fixed LTM 1650's advanced flags - was splitting on winch, not boom
      extension
  10.51 flagged LTM 1650's two "with winch 2" configs as advanced and
  left both "without winch" configs in the main toggle. Person caught
  it: that's the wrong axis. T-16.7's boomNotation is (0/0/0) - fully
  retracted - regardless of winch state, while T-29.0's is (100/0/0) -
  section 1 extended to 100%. The actual rule (10.51, and the person's
  original ask) is about boom retraction, not equipment fitted. Moved
  `advanced: true` off "T-16.7, with winch 2, without winch 3" and onto
  "T-29.0, without winch 2 and winch 3" instead, so the split is now:
  both T-16.7 variants (retracted) in the main toggle, both T-29.0
  variants (extended) under Advanced - winch state no longer affects
  which group a config lands in. LTM 1300's T-19.6 was already correct
  (its only boomConfig pair differs by extension, not equipment, so both
  readings agreed). Verified in-browser: main toggle shows the two
  T-16.7 entries, Advanced shows the two T-29.0 entries.

================================================================================

10.53 Corrected LTM 1110's Winch 2*/Replacement Ballast slotWarning wording
  The existing slotWarning text (10.19-era) called the two "NOT
  interchangeable" and framed the difference as winch capability (actual
  winch fitted vs dead weight). Person corrected this: they ARE
  interchangeable - only one is ever fitted in the shared slot, that part
  was always true - the real, OEM-confirmed distinction (per the
  countsToward comment above the components array, 10.7) is that they
  affect the combo's TOTAL COUNTERWEIGHT differently despite matching
  0.8t weight: Replacement Ballast's 0.8t counts toward the total,
  Winch 2*'s does not. Rewrote both slotWarning strings (p8w, p8b) to
  lead with "are interchangeable" and state the counts-toward-total
  difference explicitly instead of the capability framing. Also fixed
  the summary comment above the notice-building code (previously said
  "NOT functionally equivalent... one gives actual winch capability") to
  match. Verified in-browser: selecting a combo with Winch 2* shows the
  corrected notice, selecting one with Replacement Ballast shows its
  counterpart, no console errors.

================================================================================

10.54 Smoothed the 10.53 slotWarning phrasing
  Person flagged "its 0.8t is NOT counted toward this combo's total
  counterweight, unlike Replacement Ballast's" as awkward - the trailing
  possessive with no noun after it ("Replacement Ballast's") read oddly.
  Reworded both p8w/p8b slotWarning strings to name which item this
  combo actually has fitted and state the effect plainly against the
  on-screen "Total Counterweight" label (the #cwt-status metric card)
  instead of the vaguer "combo's total counterweight": e.g. "This combo
  has Winch 2* fitted, so its 0.8t does not count toward the Total
  Counterweight figure — Replacement Ballast's 0.8t would." Wording only,
  no change to the underlying claim (still verified true per 10.53/the
  28.2t vs 29.0t OEM combo pair). Verified in-browser: both notices
  render as expected, no console errors.

================================================================================

10.55 Outrigger Support Positioning: MUST MOVE now a real collision check,
      not a left/right heuristic
  Previous logic (requiredIds) hardcoded P1/P2 as MUST MOVE for any
  rightward shift and P3/P4 for any leftward shift, regardless of shift
  magnitude, direction Y-component, or actual pad geometry - the person
  said at the time this was just an assumption, not something checked.
  Person asked directly: would each pad actually collide with the crane's
  new chassis position if left exactly where it is?
  The math already existed for the other half of this - clashesWithFootprint()
  and, for every pad, its "if left in place" position relative to the
  crane's NEW center (relX, relY, used for the "If Left in Place" table
  column). Replaced the heuristic with `clashesWithFootprint(relX, relY, fp)`
  per pad: a pad is MUST MOVE only if its own current spot genuinely falls
  under the chassis footprint once this shift is complete - checked
  individually per pad/crane/shift, not by which side of the crane it's on.
  Updated the "MUST MOVE" disclaimer to describe the real rule instead of
  "direct path of travel."
  Consequences worth knowing: a pure fore/aft shift can still require no
  pad to move (X offset unaffected, and every pad's baseline X sits well
  outside the footprint half-width for every crane in FOOTPRINTS) but a
  diagonal shift can now pull in a front or rear pad that the old rule
  never flagged. Also confirmed a genuine "sweep window": a pad only
  clashes within a specific shift-magnitude range in the matching
  direction - e.g. LTM 1650's P2 is MUST MOVE at shiftX=+4m but clears
  again by shiftX=+8m, since by then the crane has already moved past
  that pad's original spot entirely. This is expected box-overlap
  behavior, not a bug.
  Verified in-browser (Playwright) across LTM 1100 and LTM 1650: pure
  lateral shifts reproduce sensible required/optional splits, pure
  fore/aft shifts require nothing at small magnitudes, a diagonal shift
  correctly flags a non-lateral pad, and the shift-magnitude sweep window
  behaves as expected in both directions on LTM 1650. No console errors.

================================================================================

10.56 Outrigger Support Positioning: added an opt-in old/new position
      compare view
  Person's idea: show the new (shifted) crane center in blue to match its
  existing target-dot color, and the old center in yellow to match the
  existing front-direction arrow's color, with a toggle to turn the
  comparison on/off rather than always showing both. Off by default -
  confirmed with the person first, since the canvas already reads fine
  with just the single grey footprint + blue new-center dot, and always-on
  would add clutter nobody asked for.
  Added a checkbox ("Compare old/new position") in the Visual Site Plan
  card-title, mirroring the existing Rope Retentioning tab's inline
  checkbox pattern (label wrapping input, card-title's flex layout
  right-aligns it for free). New `compareMode` module-level var, flipped
  by toggleCompareMode(), which just re-runs calcCAD() to redraw - no
  separate redraw path needed.
  Refactored the single hardcoded footprint-rectangle draw into
  drawFootprintBox(offsetX, offsetY, strokeColor, fillColor) so the same
  code draws both the old footprint (offset 0,0, unchanged grey, always
  drawn) and, only when compareMode is on AND the shift is nonzero, a
  second footprint outlined in blue at the shifted offset, plus a small
  yellow dot at the old center. Gated on "shift nonzero" specifically
  because at zero shift the old and new positions are identical - drawing
  both would just be two overlapping outlines for no information.
  Nothing about the underlying leg math, MUST MOVE determination (10.55),
  or default (untoggled) rendering changed - purely an additive, opt-in
  overlay. Verified in-browser (Playwright, LTM 1650, 3m lateral shift):
  toggle off matches the pre-existing look exactly; toggle on shows the
  second blue footprint outline and a visible yellow dot at the old
  center once zoomed in; no console errors.

================================================================================

10.57 CWT Combinations: added a 3D model view for LTM 1130, alongside the
      existing 2D diagram
  Person supplied a person-made GLB/GLTF export (Onshape CAD, same source
  as the 2D exploded diagrams) of LTM 1130's counterweight stack and asked
  whether a real 3D viewer was feasible. Proved it first with a standalone
  proof-of-concept before touching the app - loaded the GLB with Three.js,
  confirmed each physical plate is its own named mesh node ("Part_N"), and
  confirmed click-to-select via raycasting works against the real geometry,
  not just a list. Person then asked to wire it to actual combo data and
  ship it to both beta and live for hands-on testing.
  New dependency: this is the app's first external JS library. Vendored
  (not CDN - the app has to keep working offline) under vendor/three/:
  three.module.min.js + three.core.min.js (its internal split-out chunk),
  GLTFLoader.js + OrbitControls.js (three's examples/jsm addons, patched to
  import three.module.min.js and each other via relative/importmap paths
  instead of a bundler), and utils/BufferGeometryUtils.js +
  utils/SkeletonUtils.js (GLTFLoader's own internal deps - the addon
  doesn't work without them, easy to miss since they're not imported from
  the entry point directly). ~970KB vendored total, cached once regardless
  of how many cranes eventually get a model - and not fetched AT ALL until
  needed: rather than a static <script type="module"> tag (which would
  fetch+parse it on every single page load, including for people who never
  touch CWT Combinations), setCwtView('3d') dynamically import()s
  vendor/cwt3d.js on first switch to the 3D view. The page-level importmap
  (mapping the bare "three" specifier to vendor/three/three.module.min.js)
  still has to be a static tag - browsers only honor importmaps present
  before the first module load - but that's inline JSON, no fetch of its
  own. The browser caches the imported module by URL, so toggling 3D off
  and back on, or switching crane, resolves instantly after the first
  fetch rather than re-importing. New vendor/cwt3d.js is the viewer
  itself; being an ES module, it cannot see index.html's classic-script
  top-level const/let bindings, so it talks to the rest of the app purely
  through window: reads window.COUNTERWEIGHT_DATA (now explicitly exported
  there) and calls window.toggleCwtPlate(id) on a pick, exactly the same
  function the 2D hotspots call, so 2D and 3D drive one shared selection
  state with no parallel logic to keep in sync. renderCwtTab() calls
  window.__cwt3dOnRender(...) on every render (cheap no-op unless the 3D
  view is actually visible and already loaded); setCwtView('3d') calls
  window.__cwt3dActivate(...) to load-or-reuse the model.
  Part-to-component mapping: the GLB carries only Onshape's arbitrary
  "Part N" numbers, no weight/name metadata, so which mesh is which of the
  app's p1-p8 isn't in the file - inferred from geometry instead (see the
  long comment on '1130'.model3d in index.html for the full derivation):
  Part_10/Part_3 are an exact mirror pair -> p8's two physical halves;
  Part_1 has a distinctly different shape/position from the rest -> p7
  (a winch mechanism, not a flat plate); the remaining six share one exact
  footprint and stack contiguously, matched to p1-p6 by thickness-vs-known-
  weight ratio (consistently ~13-19 across all six, same footprint+
  material assumption). Flagged to the person as inferred, not confirmed -
  a wrong pairing only mislabels which plate highlights on click, it does
  NOT touch cwtComboWeight/cwtFindMatchingCombo or any total/approved-combo
  logic, which stay driven entirely by the existing components list.
  Also added: an explode offset (each named part nudged outward from the
  model's center on load, proportional to overall model size) since the
  GLB stores the assembled position, not the pulled-apart view the 2D
  diagrams show; a lazy load (the ~4.2MB GLB is fetched only on first
  switch to 3D view, never on page load, matching the reeving-diagram
  fetch-on-demand pattern rather than the always-cached app shell); and a
  "2D Diagram / 3D Model" toggle on the Counterweight Stack card, hidden
  entirely for every crane without a model3d entry (currently every crane
  but 1130).
  Verified in-browser (Playwright): toggle only appears for LTM 1130;
  switching to 3D loads and renders the model; clicking a plate directly
  on the model selects it, and that selection is immediately reflected in
  the Composition panel, Total Counterweight, AND the 2D hotspot view when
  switching back - full round-trip, not view-local state; switching away
  to a crane with no model hides the toggle and falls back to 2D with no
  crash; switching crane always resets to 2D view. No console errors.
  Regression-checked all seven cranes' 2D hotspot rendering afterward -
  unchanged.

================================================================================

10.58 Fixed 3D explode direction - was pushing parts sideways, not along
      their real stacking axis
  Person tested and reported the plates didn't line up, despite matching
  the 2D diagram cleanly in Onshape (the same file the 2D diagram was
  itself extracted from) - so the source geometry wasn't in question, the
  bug had to be in explodeParts() (10.57). Confirmed directly against the
  raw GLTF JSON rather than guessing: every "occurrence of Part N" parent
  node carries a baked ROTATION as part of its transform matrix (the same
  rotation on every one, just different translation) - not a pure
  translation like the explode code assumed. explodeParts() computed the
  "push outward" direction correctly in world space, but added it straight
  to `group.position`, which Three.js interprets in the PARENT's local
  frame - a frame rotated ~90 degrees from world space here. That mismatch
  is exactly what pushed parts sideways instead of along their real
  stacking axis.
  Fix: convert the target position to world space first (current world
  position + offset), then convert THAT into the parent's local space via
  `group.parent.worldToLocal(...)` before assigning it to group.position -
  correct regardless of whatever rotation/scale sits between the node and
  world space, rather than assuming there isn't any. Also added an
  explicit `root.updateMatrixWorld(true)` before any of this, since the
  model isn't in the scene graph yet at explode time (that happens after,
  in __cwt3dActivate) and matrixWorld is otherwise only refreshed on
  render.
  Verified in-browser (Playwright, LTM 1130): the six stacked plates now
  separate cleanly along one consistent vertical axis instead of drifting
  sideways/diagonally: screenshotted from two angles to confirm. Re-ran
  the full click round-trip (3D pick -> Composition panel -> Total
  Counterweight -> 2D hotspot) after the fix - still correct. No console
  errors.

================================================================================

10.59 Real evenly-spaced explode gap (120mm) instead of a uniform outward
      push
  Person asked two things: whether to rename Onshape parts to match the
  app's own component ids for future models (yes - would replace the
  10.57 geometry-inference mapping with a direct, certain one), and
  whether to explode/space the plates in Onshape itself, with what gap.
  Answered: don't bake spacing into the CAD export - keep it in real
  assembled position, since spacing is trivial to retune here without a
  re-export, and each new crane is one less step for the person that way.
  That answer meant actually fixing the explode to be a real gap rather
  than what 10.57/10.58 shipped: pushDistance = maxDim * 0.18 applied
  uniformly to every part's centroid regardless of position - for LTM
  1130 that's ~1058mm, a fixed displacement per part, not a controlled
  gap between adjacent faces (confirmed by direct measurement before
  changing anything, not assumed).
  Reworked explodeParts(): parts are now clustered by XZ proximity
  (50mm tolerance - this model's actual stacked-plate matches are exact
  to several decimal places, so real stacked plates cluster trivially;
  non-stacked parts like the winch piece or the auxiliary ballast pair
  don't share XZ with anything and land in their own single-member
  cluster). A multi-member cluster (a real stack) is spaced by the new
  explodeStack(): find whichever axis (x/y/z) has the most spread between
  the cluster's centers - Y here, but not assumed, so a differently-
  oriented future export still stacks correctly - sort along it, then walk
  bottom to top accumulating exactly STACK_GAP (0.12 = 120mm) between each
  part's own real thickness, so the gap is the same physical distance
  everywhere regardless of that plate's own thickness. Single-member
  clusters keep the old proportional outward push (pushOutward()) since
  there's nothing to space them evenly against. Both paths still go
  through the 10.58 world-to-local fix (factored out to applyWorldOffset())
  rather than duplicating that logic.
  STACK_GAP is the one number to change if 120mm ever looks wrong - a
  one-line edit, no new model export needed.
  Verified in-browser (Playwright, LTM 1130, both default and rotated
  views): all six plates now show a clean, visibly even gap between them
  rather than the previous uneven/bunched push; winch piece and both
  ballast halves still separated clearly; click-to-select and the full
  Composition/Total/2D-hotspot sync re-verified working after the rework.
  No console errors.

  UPDATE (superseded as the default, not wrong at the time): this was
  the right call for the cranes shipped up to this point, but starting
  at 10.71 the person's own workflow shifted to spacing the stack by
  hand in Onshape and shipping preExploded: true instead - not a
  reversal of this reasoning, a different tradeoff that won out once
  composite-part swaps (10.75) and multi-crane batches made "exact
  control over the file, no heuristics to second-guess" worth more
  than "spacing is a one-line edit here." STACK_GAP/explodeParts()
  stay in cwt3d.js, real working infrastructure for the case, not
  dead code - just not the current default for a NEW crane. See
  section 11 for the procedure as it actually runs today.

================================================================================

10.60 Consolidated "Cache All Offline Content" - one shared button instead
      of reeving-only
  Person's reasoning: with 3D models joining reeving diagrams as content
  that's only cached once actually viewed, the existing reeve-only
  "Download All Diagrams" button (10.something earlier) would leave 3D
  models stranded the same way an unviewed reeving diagram used to be -
  unavailable if a crew member needs a crane they've never opened before,
  offline, on-site. Agreed a single consolidated button was better than a
  second parallel one; person specified exact placement (below Reference
  Documentation, on every tab that has one) and confirmed the title
  ("Cache All Offline Content").
  Removed the old reeve-tab-only "Offline Availability" card (sat above
  Reference Documentation there) and added one shared version, styled
  identically, below Reference Documentation on all three tabs that have
  one: Reeving Plans, CWT Combinations, Driving with Equipment in Place.
  Clicking it from ANY of the three runs the same complete download,
  everywhere - not a tab-scoped subset - since the point is comprehensive
  offline readiness regardless of which tab happened to be open. The three
  DOM copies share one cache-all-btn/-progress-wrap/-progress-bar/-
  progress-text class set (no ids, since there are three of each now) and
  cacheAllOfflineContent() updates all three via querySelectorAll in
  lockstep.
  Renamed cacheAllReeveDiagrams() -> cacheAllOfflineContent() and widened
  its file list (new allOfflineContentUrls()) to cover every reeving SVG
  (unchanged), the 3D viewer support files (vendor/cwt3d.js + all of
  vendor/three/*, only included at all if at least one crane has a
  model3d), and every crane's model3d.url. Counterweight 2D diagram images
  are deliberately NOT in this list - those are already precached in the
  app shell (sw.js APP_SHELL, guaranteed available from install), so
  there's nothing this button needs to chase down for them.
  "Only downloads what it doesn't already have, unless it's a new
  revision" - person's exact requirement - falls out of the existing
  architecture rather than needing new logic: a plain fetch() per URL is
  enough, since the service worker's own fetch handler is cache-first, so
  an already-cached URL resolves instantly with zero network request.
  "New revision" is handled per-file by that file's own URL, not by this
  function: vendor/cwt3d.js and now every model3d.url (previously
  unversioned - added ?v=1 to LTM 1130's GLB here, matching the ?v=
  convention already on cwt3d.js, since the person may well replace this
  exact model once its parts are renamed per their own question this
  session) carry an explicit version query - bump it and the URL changes,
  which is a cache miss, which forces a real fetch, with no "is this
  stale" check needed anywhere. Added CWT3D_VERSION as the one shared
  source of truth for cwt3d.js's version number, referenced by both
  setCwtView()'s dynamic import and cacheAllOfflineContent(), so the two
  can't drift and end up caching different versions of the same file.
  Verified in-browser (Playwright): the button/progress elements appear on
  all three tabs (three .cache-all-btn found); triggering it from Reeving
  Plans downloads all 169 files (reeving diagrams + 3D viewer + the one
  GLB) successfully with zero failures; a second run immediately after
  completes 5.5x faster (0.28s vs 1.53s) purely from the service worker's
  cache-first short-circuit, confirming already-cached files aren't
  re-fetched. No console errors.

================================================================================

10.61 Added a tick/cross to state whether offline content is actually
      fully downloaded
  Person asked for a status indicator alongside 10.60's button rather
  than just a fire-and-forget download action. New checkOfflineContentStatus()
  reads the REAL current state from the Cache Storage API - the same
  allOfflineContentUrls() list from 10.60 diffed against whatever's
  actually sitting in the content cache - rather than just remembering
  whether cacheAllOfflineContent() last ran successfully, since content
  also gets cached one file at a time simply by viewing it (a reeving
  diagram, a 3D model), exactly as it always has; the status needs to
  reflect that too, not only a full-download run.
  Doesn't hardcode sw.js's CONTENT_CACHE name as a second literal that
  could drift from the one in sw.js - finds it at runtime as whichever
  cache bucket isn't one of the versioned app-shell-* ones, since sw.js
  only ever opens those two. Added a small span inside each "Offline
  Availability" card-title (card-title's existing flex/space-between
  layout right-aligns it for free, same trick as the CWT compare-view
  checkbox and the Auxiliary Ballast enable checkbox before it) - ✓
  Downloaded in var(--success) green, or ✗ Not fully downloaded (N
  missing) in var(--danger) red.
  Checked on three occasions: once on page load (covers whichever tab
  loads by default, main vs beta differ on that - see the comment left in
  place of assuming one), once on switchTab() for the three tabs, and once
  at the end of cacheAllOfflineContent() itself. That last one caught a
  real race first: the service worker's fetch handler writes each response
  into CONTENT_CACHE via a fire-and-forget cache.put() not awaited by the
  page's fetch() promise, so checking status the instant the download loop
  finishes could still show 1-2 "missing" for writes that hadn't landed
  yet. Fixed with one extra checkOfflineContentStatus() call 600ms later -
  not a poll/retry loop, this was never a real ongoing race, just a
  one-time confirmation that the last couple of writes had caught up.
  Verified in-browser (Playwright): shows the correct cross with an
  accurate missing-count before any download; download then immediately
  shows the correct tick (no lingering false negative after the 600ms
  fix); tick persists correctly across a full page reload (real Cache
  Storage state, not in-memory-only). No console errors.

================================================================================

10.62 Fixed checkOfflineContentStatus() showing the full original miss
      count even after a genuinely successful download
  Worked fine in every sandbox test (10.61), but person hit it on a real
  device: button correctly read "All Offline Content Cached", status span
  next to it still showed "Not fully downloaded (169 missing)" - the exact
  original count, as if the download had done nothing at all, despite it
  having actually worked (offline content was genuinely usable).
  Root cause was the status check's own approach, not the caching itself:
  it opened sw.js's CONTENT_CACHE bucket by GUESSING its name ("whichever
  cache isn't app-shell-*") rather than asking the browser directly.
  Getting the exact original count back means that guess resolved to no
  bucket at all on this device/browser (cacheNames.find() returning
  undefined), not a partial miss - so the check fell back to comparing
  against an empty list, reporting everything missing regardless of what
  was actually cached.
  Replaced the whole "find the bucket, open it, list its keys, compare
  URLs" approach with caches.match(url) per required URL - this searches
  every cache bucket for the origin without the caller needing to know or
  guess any bucket's name, which is also effectively what the service
  worker's own fetch handler relies on to decide something's already
  cached. Strictly more robust than the bucket-name guess it replaces, not
  just a fix for this one device.
  Verified in-browser (Playwright): correct cross with accurate count
  before download, correct tick immediately after, tick persists across a
  full reload - same coverage as 10.61's original tests, now against the
  more robust lookup. No console errors.

================================================================================

10.63 CWT Combinations: added LTM 1300's 3D model - first crane with parts
      labelled by the person to match the app directly, not inferred
  Person's own labelling, matching this crane's real component ids
  (p1-p5, aux5, aux6) rather than Onshape's arbitrary numbering like LTM
  1130's model had - so this partMap is a direct, confirmed lookup, not
  10.57's geometry-based guess. Flagged two things about the labelling
  before supplying the file: wing weights 5/6 labelled "5L"/"5R" and
  "6L"/"6R" (left/right), and the receptacle (Plate 1) labelled "1" on
  every one of its several small CAD bodies.
  Neither turned out to be as simple as "just add more partMap entries" -
  both exposed real gaps in 10.57/10.58's matching logic, which had only
  ever seen LTM 1130's uniquely-named parts:
  - GLTFLoader auto-renames sibling nodes that share a name ("1" becomes
    "1", "1_1", "1_2", "1_3", "1_4" for the receptacle's 5 bodies; same
    for the crane's two identical, interchangeable Plate 3 copies, both
    labelled "3" -> "3"/"3_1"). The old matching code only ever checked
    the exact node name against partMap, so it would have silently found
    ONLY the first instance of each and missed the rest. New
    partMapKeyFor() tries the exact name first, and only falls back to
    stripping one trailing "_<digits>" and matching THAT base if the exact
    name isn't a key - so this never fires for a model with no name
    collisions (LTM 1130 still matches everything on the first try) and
    can't misfire on a genuine partMap key that happens to end in
    "_<digits>" (none do today, but nothing stops it).
  - The two Plate 3 copies need to be independently selectable as p3a/p3b
    (their real app ids) despite being unable to tell apart by name at
    all. partMap values can now be an array (`'3': ['p3a', 'p3b']`) -
    cwt3d.js sorts that name's matched instances along whichever axis has
    the most spread between them and assigns ids by position, rather than
    needing a real distinguishing name.
  - The receptacle's 5 bodies all map to the SAME single id (p1) - if
    exploded individually like 10.57/10.58 treated every part, they'd
    scatter as 5 separate fragments instead of moving as the one physical
    item they actually are. explodeParts() (and loadModel()) now work on
    "participants" rather than raw GLB nodes: a name that maps several GLB
    nodes to one single app id becomes ONE rigid block (combined bounding
    box, every member node moved by the identical world-space offset via
    10.58's applyWorldOffset - preserving their relative arrangement to
    each other); an array-mapped name, or a name with only one matching
    node, still explodes node-by-node exactly as before. Confirmed this
    was the right call before generalizing it, not assumed: an array
    mapping means "these need to look independently selectable" and a
    string mapping across multiple nodes means "this is one item made of
    several bodies" - the two cases actually do need different explode
    behavior, not just different appId assignment.
  New file counterweight/models/ltm1300-cwt.glb (?v=1, same cache-busting
  convention as LTM 1130's, methodology.txt 10.60).
  Verified in-browser (Playwright): all 8 of LTM 1300's component ids
  (p1, p2, p3a, p3b, p4, p5, aux5, aux6) individually select, highlight,
  and show the correct weight in the Composition panel; screenshotted p1
  selected from two angles - the receptacle's 5 bodies light up as one
  contiguous shape, not scattered fragments; screenshotted p3a+p3b
  selected together - the two Plate 3 copies show clearly separated in
  the stack and both highlight, "Selected: 2 part(s)"; re-ran all 8 of LTM
  1130's own component ids afterward to confirm the refactor didn't
  regress it - identical results to before; full crane sweep confirms the
  3D toggle appears only for LTM 1130 and LTM 1300, 2D hotspot rendering
  unchanged on all seven cranes. No console errors or partMap
  count-mismatch warnings anywhere in testing.

================================================================================

10.64 Redid LTM 1130's 3D model with direct labels; fixed a real explode
      bug it exposed (auxiliary ballast pair glued together)
  Person redid the original LTM 1130 GLB (10.57's, mapped by inferring
  geometry from Onshape's arbitrary "Part N" numbers) with parts labelled
  to match the app's own component names directly, same approach as LTM
  1300 (10.63) - explicitly kept as the proof of concept either way, not
  requested because the original was wrong so much as confirmed-vs-
  inferred being worth having. Confirmed the post-sanitizing node names
  by actually loading the file rather than guessing: GLTFLoader replaces
  spaces with underscores and drops "/" outright (not replaced), so
  "Replacement Weight 7 / Winch 2*" becomes "Replacement_Weight_7__Winch_2*"
  (double underscore - both spaces around the dropped slash survived).
  Person flagged the replacement-weight/winch part is labelled identically
  on both its physical halves ("labelled it twice as it was two parts") -
  same shared-name pattern as LTM 1300's receptacle, so partMapKeyFor()
  and the rigid-block explode logic (10.63) already handled it with zero
  new code.
  What WASN'T already handled: the auxiliary ballast pair (p8) is ALSO
  labelled identically on both halves, but unlike the receptacle or the
  winch part, its two halves are mounted on genuinely opposite sides of
  the crane. 10.63's rigid-block logic doesn't distinguish "one item
  split across several CAD bodies" from "one both-or-neither id mounted
  in separate locations" - it glued the ballast pair into one block too,
  combining two boxes from opposite sides of the crane into a bounding
  box centered right back near the model's own middle, so pushOutward
  barely moved it and it visually overlapped the main stack instead of
  separating - caught by looking at the actual render, not assumed to be
  fine because the underlying selection/weight logic was already correct
  (it was - only the explode positioning was wrong).
  A pure geometry-based distinguishing rule doesn't exist here: the
  receptacle's 5 bodies span ~2.4m from each other (must glue) while the
  ballast pair's 2 bodies span ~3.7m (must NOT glue) - too close in
  magnitude to threshold on distance alone. Used a signal that already
  existed in the app's own component data instead of inventing a new CAD-
  labelling convention or a fragile heuristic: `separate: true`, set on
  exactly this kind of both-or-neither multi-location component already
  (originally added for the 2D hotspot system, well before this 3D viewer
  existed - LTM 1130's p8 and LTM 1300's aux5/aux6 all already carry it).
  explodeParts()/loadModel() now take a separateIds set (built from
  `data.components.filter(c => c.separate)`) - a shared-name key only
  becomes a rigid block if its target id is NOT in that set; a separate-
  flagged id keeps exploding each of its instances independently instead,
  same as before 10.63 ever existed for it specifically.
  Verified in-browser (Playwright): before the separate-flag fix, p8
  visibly overlapped the main stack in a screenshot; after, both halves
  clearly separate from the stack and from each other. Re-ran all 8 of
  LTM 1130's own ids (correct weights, correct highlighting) and all 8 of
  LTM 1300's (unaffected by the fix - p1's receptacle still glues, since
  p1 was never separate-flagged; aux5/aux6 were already independent
  before this change since 1300's L/R halves have distinct names, not a
  shared one, so groupsByKey never had >1 node under one key for them in
  the first place). Full crane sweep clean, no console errors.

================================================================================

10.65 Fixed exploded-stack ordering for rigid blocks whose combined bbox is
      tall enough to overlap its neighbors' Y-range; added extra L/R gap
      for genuinely paired (separate: true) items
  Reported on LTM 1300: Receptacle Plate 1 (p1) rendered in the MIDDLE of
  the exploded stack instead of at the very bottom, even though every
  other plate sits on top of it. Same bug then reported on LTM 1130's
  Plate 1. Confirmed the cause with real geometry before touching any
  code (methodology-standard practice all session) by loading each GLB
  headless and dumping explodeParts()'s own groupsByKey combined
  center/size per key: LTM 1300's receptacle (5-body rigid block, 10.63)
  has combined size.y = 0.815 - tall enough that its bounding box spans
  almost the same Y-range as plates 2, 3 and 4 above it. explodeStack()
  sorted by bbox CENTER, and the receptacle's center (y=1.475) landed
  between plate 2's (y=1.205) and plate 3's (y=1.5225) - so plate 2 got
  placed underneath it instead of the reverse. Both plate 1 and plate 2
  actually start at the exact same bottom edge (y=1.0675) - the
  receptacle isn't a flat slab sitting below plate 2, its 5 bodies
  include structural bits that reach up alongside the whole lower stack,
  which is exactly why sorting by center (the "average" height) rather
  than base position was wrong.
  Fix: explodeStack() now sorts by BOTTOM edge (center - size/2 on the
  stack's dominant axis) instead of center. Bottom-edge ties (both start
  at the stack's true floor) are broken by size, larger first - the block
  whose base reaches this low while also being the tallest is the one
  everything above is actually resting on. For a normal single-node plate
  stack (no overlapping bboxes) bottom-edge order and center order are
  identical, so this doesn't touch already-correct cranes.
  Verified against real geometry for both cranes before shipping (not
  just eyeballed after): re-ran the same headless dump with the new sort
  key - LTM 1300 order came out 1,2,3,3,4,5 (was 2,1,3,4,5); LTM 1130
  came out 1,2,3,4,5,6 (was already ambiguous/wrong in the same way).
  Confirmed visually in-browser after: LTM 1300's receptacle and LTM
  1130's Plate 1 both now sit clearly at the bottom of their stacks.
  Second, unrelated request same session: LTM 1300's auxiliary ballast
  pairs (aux5 = 5L/5R, aux6 = 6L/6R) needed more visual separation
  between the left half and the right half specifically, not just more
  separation from the main stack. These are lone (non-stacked)
  participants pushed outward from the model's center by a single shared
  pushDistance (10.63/10.64) - tuned for "clear the main stack," not for
  "read as obviously split from its own other half." Added a second,
  larger pushDistance (1.6x) that only applies to participants whose
  mapped app id is itself separate: true - i.e. exactly the both-or-
  neither multi-location components (aux5/aux6 here; LTM 1130's p8 gets
  the same treatment for free, consistent with 10.64's reasoning, since
  nothing here is crane-specific). A lone part that ISN'T flagged
  separate (e.g. LTM 1130's winch/replacement-weight) keeps the original
  distance - this only widens the gap for genuinely paired items.
  Verified in-browser: LTM 1300's aux5 and aux6 pairs now sit clearly
  apart from each other as well as from the main stack. Full crane sweep
  re-run after both changes, clean, no console errors.

================================================================================

10.66 LTM 1130's winch/replacement-weight (p7) hidden behind the exploded
      stack - moved it to sit on top instead, generically
  Reported: p7 (mounted on top of the main plate stack in real life) was
  hard to see in the 3D view, hidden behind the stack. Cause: p7 is a
  lone (non-stacked) participant, so it got the same radial outward push
  as any other lone part (10.63) - fine for something genuinely mounted
  to the SIDE, wrong for something mounted on TOP, since its own XZ
  position sits slightly off-center from the stack's footprint and the
  push exaggerated that sideways offset instead of correcting for it.
  Fix, generic rather than 1130-specific: components can now carry
  `explodeTop: true` (same reused-signal pattern as `separate`, 10.64/
  10.65 - a flag on the app's own component data, not a geometry guess or
  a hardcoded id check). explodeParts() pulls any such participant out of
  the normal cluster/push pipeline entirely and, once the real plate
  stack has been exploded, places it via a new placeAtStackTop(): directly
  above the tallest exploded cluster (found generically as whichever
  cluster ended up with the most members, not assumed to be "the" stack
  by id), centered over that cluster's XZ centroid rather than the part's
  own original XZ, with the same STACK_GAP clearance a real stacked plate
  gets. Generic over whichever axis the stack is actually stacked on,
  matching explodeStack()'s own axis detection.
  Set explodeTop: true on LTM 1130's p7 only (the one component this
  applies to today). Verified in-browser: p7 now renders clearly on top
  of the stack, centered, from multiple rotation angles - previously
  tucked behind/beside it. Re-ran all 8 of LTM 1130's own ids (correct
  weights, correct highlighting, no console errors) to confirm restructuring
  explodeParts() around the new stackTopParticipants split didn't disturb
  the existing rigid-block/pair/plain-stack participants. Full crane
  sweep clean.

================================================================================

10.67 Added an opt-out for the 3D viewer's automatic explode entirely
      (model3d.preExploded)
  After three rounds of explode-positioning bugs across the two cranes
  with 3D models so far (10.63-10.66 - a rigid block's combined bbox
  landing it mid-stack, paired items needing extra mutual clearance, a
  lone part needing to anchor above the stack instead of push outward),
  the person offered to just position parts already exploded in Onshape
  before exporting, rather than relying on cwt3d.js's own heuristics and
  catching each new edge case after the fact. Better trade for future
  cranes: the automatic explode is a genuinely good default (it's
  handled every case seen so far, generically, once each was found), but
  it's still an INFERENCE from raw assembled-position geometry, and every
  crane's export is a new chance for a geometry shape the existing rules
  don't anticipate - whereas the person doing the positioning themselves
  in the same tool that produces the 2D diagrams they already hand-place
  is the actual source of truth, not a guess.
  Added `model3d.preExploded: true` as a per-crane opt-in - when set,
  loadModel() skips the explodeParts() call entirely and uses the GLB's
  own positions as-is. Not the default (nothing existing sets it - LTM
  1130 and LTM 1300 both keep using the automatic explode, unaffected),
  so this is purely additive. Full crane sweep re-run after adding the
  flag, clean, no console errors - confirms the untouched code path
  (explodeParts still runs for both existing cranes) wasn't disturbed by
  adding the early-exit check.

================================================================================

10.68 CWT Combinations: added LTM 1110's 3D model - first crane to use
      preExploded (10.67), first real test of it
  Person's own Onshape export, already positioned in their intended
  exploded layout rather than assembled position - set preExploded: true
  and mapped partMap straight to the CAD's own node names, no explode
  heuristics involved at all for this crane.
  Receptacle Plate 1 is 3 separate CAD bodies (Receptacle_Plate_1left/
  base/right), same multi-body-glued-into-one-id pattern as LTM 1300's
  receptacle (10.63) - all 3 map to p1.
  One naming ambiguity caught before mapping, not guessed past: the file
  has TWO nodes both literally named "Counterweight_Plate_7" - one large
  (900 mesh children, ~2.3m, matches p7's real dimensions/2.5t weight)
  and one tiny/thin (135 mesh children, ~0.8m x 0.08m x 0.03m, ~400x
  smaller by volume). Given this crane's own CAD has a documented history
  of a real plate 6/7 mix-up (10.7), asked rather than assumed which was
  which - confirmed by the person: the large one (GLTFLoader auto-renames
  it to "..._7_1" on load, since it's the second node sharing that exact
  name) is the real plate 7; the tiny one is just the Liebherr brand
  label decal baked onto the back of the slab as its own body, not a
  component. partMap deliberately maps only "Counterweight_Plate_7_1" (the
  real plate) to p7 and leaves the tiny decal node out of partMap
  entirely, same as any other unmapped part - shown but not clickable.
  Also confirmed absent from the file entirely: the Winch 2*/Replacement
  Ballast slot (p8w/p8b, 10.7's mutually-exclusive slotGroup pair) - no
  geometry anywhere near that size/location, so those two ids simply have
  no 3D representation. Not a bug - the 2D diagram still covers them
  fully, and namedParts naturally has no entries for an id with no
  matching GLB node (nothing to highlight, nothing to break).
  Verified in-browser: 3D toggle appears for 1110, all 7 modeled ids
  (p1-p7) resolve to their correct real weights and highlight the
  correct mesh (p1 = bottom receptacle, p7 = top plate, matching the
  person's own exploded layout) with no explode logic run at all. Full
  crane sweep across all 7 cranes clean, no console errors - confirms
  adding a third model3d crane didn't disturb LTM 1130/1300's existing
  (non-preExploded) behavior, and that allOfflineContentUrls() picked up
  the new crane automatically (it derives model3d cranes generically from
  COUNTERWEIGHT_DATA, no manual list to update).

================================================================================

10.69 Added model3d.extraParts - supplementary GLBs for a part that can't
      come through the main assembly export at all
  Follow-up to 10.68: the person asked why LTM 1110's Winch 2* (p8w)
  wasn't visible in 3D. Confirmed rather than assumed - their own Onshape
  instance list showed 11 real instances (a distinct "Replacement
  Ballast/Winch 2*" item, visibly a substantial box in their own exploded
  view, plus the two "Counterweight Plate 7" bodies from 10.68), but the
  GLB actually traversed only 10 groups. The missing one wasn't a mapping
  bug - the geometry itself never made it into the file.
  Root cause, tracked down with the person live in their Onshape session
  rather than guessed at from here: that instance lives in a Composite
  Part (a merged/boolean result, its own object type in Onshape) which
  the assembly-level glTF exporter silently drops even though it's a
  normal referenced instance in the tree. Tried making it a plain
  independent solid first ("Copy here..." on the composite) - didn't
  work, produced a second composite instead of converting the type, so
  abandoned that path rather than keep guessing at Onshape's own object
  model from outside it.
  Landed on exporting the Composite Part on its own as a separate small
  GLB instead (its direct Export... command handles it fine standalone -
  confirmed after a first attempt came through Draco-compressed, which
  the app's vendored GLTFLoader has no decoder for; the person turned
  compression off and re-exported successfully).
  Added model3d.extraParts: [{ url, appId, rotationY }] - loadModel() is
  now async and, after the main model is positioned (explode or
  preExploded, either way), loads each extra GLB, treats its whole
  contents as one rigid unit mapped to one app id (no partMap needed - a
  dedicated file needs no name matching), and places it via a new
  placeAboveWholeModel(): directly above the ENTIRE main model's
  bounding box, centered over its XZ footprint, same STACK_GAP clearance
  as everything else. Deliberately does NOT reuse explodeStack()'s
  generic "most-spread axis" detection - a whole model's bbox is
  reliably wider/deeper than tall, so that heuristic would pick the
  wrong axis here; Y is hardcoded as up instead (true for every export
  seen so far). rotationY corrects for a standalone single-part export
  carrying its own Part Studio's default orientation, unrelated to the
  main assembly's - the person confirmed the first attempt was rotated
  wrong, corrected to 90 deg and re-verified visually before shipping,
  same iterate-on-a-screenshot approach as every other tuned constant
  this session (STACK_GAP, pairPushDistance, etc).
  All main-model roots now sit inside a wrapping THREE.Group alongside
  any extra parts, so frameCamera()/scene-add/clearScene() (which only
  ever handled a single opaque "entry.root") needed no changes at all -
  the container is just another Object3D as far as they're concerned.
  allOfflineContentUrls() updated to walk extraParts[].url too, so the
  Cache All Offline Content button and status ticks stay accurate without
  a separate manual list.
  Verified in-browser: p8w's real weight and countsToward:false behavior
  unaffected (namedParts entries work identically whether they came from
  the main file or an extra one), highlights the correct box sitting on
  top of the stack, no console errors. Full crane sweep re-run clean
  across all 7 cranes plus a full LTM 1110 id-by-id pass (p1-p7, p8w,
  p8b) - p8b, which still has genuinely no geometry anywhere, confirmed
  to fail gracefully (correct weight shown, nothing to highlight, no
  error) same as before this change.
  Follow-up same day: placeAboveWholeModel() centered on the WHOLE main
  model's XZ footprint, which is usually wider/deeper than the specific
  top plate the part actually sits on (lower plates in a stack are often
  bigger) - looked visibly off-center, sitting toward a corner rather
  than centered on its own plate. Fixed generically: find every mesh
  whose own top edge sits within STACK_GAP of the model's highest point,
  union those into a "top slab" box, and center on THAT instead of the
  whole model - no hardcoded app id for "which part is the top plate."
  Also added rotationX/rotationZ alongside the existing rotationY, since
  a standalone single-part export's orientation is a coin flip relative
  to the main assembly and might need correcting on any axis, not just
  yaw. Exact rotation for LTM 1110's Winch 2* still unresolved as of this
  entry - the person is dialing in the correct "lying flat" orientation
  directly in Onshape's own rotate tool rather than more blind
  trial-and-error rotationX values from here, and may re-export with it
  baked in (or swap the part directly into the main assembly export
  instead of using extraParts at all, in which case this whole mechanism
  won't even be needed for this specific part - it'd just become a normal
  partMap entry like any other plate). Left at rotationY: 90 only (the
  one axis not contradicted so far) pending whichever file arrives next.
  Resolved same day: person swapped Winch 2* directly into the main
  Onshape assembly (as "Part_1") and re-exported, exactly the "won't even
  be needed" outcome above - added 'Part_1': 'p8w' to the regular
  partMap, removed the extraParts entry and the standalone composite GLB
  entirely. Bumped the main model's ?v= (content changed). extraParts/
  placeAboveWholeModel() stay in cwt3d.js unused by any crane right now -
  real, working infrastructure for the same situation recurring on a
  future crane, not speculative code. Verified in-browser: p8w renders
  lying flat, correctly positioned on top of the stack per the person's
  own layout, no console errors. Full crane sweep clean.

================================================================================

10.70 Added Draco decoder support; wired 3D clicks into the existing
      slotGroup cycling behavior
  Draco: the person's next batch of GLBs (LTM 1300/1130 redo, LTR 1220,
  LTM 1250 - new, worked on separately) all failed to load with "No
  DRACOLoader instance provided." Rather than ask for four re-exports
  with compression toggled off (and every future one after that, since
  it's apparently Onshape's default GLB export setting), vendored a
  matching DRACOLoader.js + decoder (JS/WASM) from the same three.js
  release already in use (r185, confirmed against the vendored
  three.core.min.js's own REVISION string) and wired one shared decoder
  instance into loadGLTFAsync()'s GLTFLoader. Decoder files vendored
  locally under vendor/three/draco/, not fetched from Google's CDN (same
  reasoning as every other vendored dependency this app uses - see
  vendor/three's own original header). Added to allOfflineContentUrls()
  alongside the rest of the 3D viewer's own files. Verified: all four
  pending files decode and traverse cleanly now: 15/8/11/10 group nodes
  respectively - full crane sweep re-run clean, no regression on the
  already-shipped (non-Draco) LTM 1130/1300/1110 files either.
  Slot cycling: separately, the person noticed LTM 1110's Winch 2*/
  Replacement Ballast slot (methodology.txt 10.7) didn't cycle in 3D the
  way its 2D hotspot already does, now that Winch 2* has real geometry
  (10.69/10.70's Part_1 swap) to click on. onCanvasClick() previously
  only ever called window.toggleCwtPlate() - added a slotGroup lookup
  per namedPart (built once per load from the app's own component data,
  same reused-signal pattern as `separate`/`explodeTop`) so a click on a
  slot member's mesh now calls the SAME window.cycleCwtSlotGroup()
  already used by the 2D view, instead of a plain toggle. Verified both
  via direct cycleCwtSlotGroup() calls and real canvas clicks on the
  winch mesh - cycles none -> Winch 2* -> Replacement Ballast -> none
  correctly (Replacement Ballast itself still has no 3D geometry, same
  as before - clicking past it just deselects, no error).

================================================================================

10.71 CWT Combinations: LTM 1300/1130 redone with preExploded; added LTR
      1220 and LTM 1250 - first two cranes with 3D models built entirely
      around a preExploded file, no auto-explode debugging at all
  Person redid LTM 1300 and LTM 1130's models in their own already-
  exploded layout (same as LTM 1110, 10.67/10.69) and sent LTR 1220 and
  LTM 1250 as new cranes, all four in the same batch, all Draco-
  compressed (10.70 shipped same day, otherwise none of these four would
  have loaded at all).
  LTM 1300/1130: partMap unchanged for both - same node-naming convention
  as the originals, still a direct lookup. Just preExploded: true added
  and the old explain-the-auto-explode-heuristics comments trimmed down
  (10.63-10.66's rigid-block/bottom-edge-sort/pair-clearance/explodeTop
  fixes no longer apply to either crane now, though the code itself stays
  in cwt3d.js - still real, working infrastructure for whichever future
  crane's export isn't pre-exploded).
  LTR 1220: partMap is NOT a direct lookup here - plate 2/3 are 3 side-by-
  side pairs (not a vertical stack; each pair sits at its own tier,
  tiers stacked on each other), and all 6 CAD bodies are labelled just
  "Counterweight_plate_2"/"_3" with no a/b/c suffix telling which tier is
  which. Inferred the app's own a/b/c ids from Y position instead,
  cross-checked two ways before shipping: (1) matched against this
  crane's own combo progression, where tier a is added first, b second, c
  third, each combo a strict superset of the last - so bottom-to-top by Y
  should read a/b/c in that order if the physical build-up matches the
  combo order, which it should since combos are cumulative; (2) checked
  against the 2D hotspot's own SVG Y-coordinates, where p2c/p3c's hotspot
  sits at the smallest image-Y (= highest up on the page = physically
  topmost), consistent with (1). Verified a third way, empirically, after
  shipping the inferred map: selected tier a alone in the live 3D view -
  highlighted directly above the receptacle, at the bottom. Selected tier
  c alone - highlighted the very top of the stack. Both matched the
  inference exactly.
  LTM 1250: fully direct, unambiguous partMap - every CAD body's name
  matches an app component name exactly, including "Counterweight_7"
  having no "Plate" in it same as the app's own "Counterweight 7" name
  for p7 (the one component on this crane that isn't itself called a
  "plate"). Hook_Ballast_8 is 2 CAD bodies sharing one name (GLTFLoader
  auto-renames the second to "_1"), matching p8's separate:true side-
  mounted pair.
  Verified in-browser for all four: every component id resolves to its
  correct real weight and highlights the correct mesh, receptacle sits at
  the bottom on both redone cranes and both new ones, LTM 1130's winch
  still on top post-redo, LTM 1250's Hook Ballast pair highlights both
  sides together. Full 7-crane sweep clean, no console errors - 5 of 7
  cranes now have 3D models (1110, 1130, 1220, 1250, 1300).

================================================================================

10.72 Fixed LTM 1110's slot-cycling color: picking the slot member with no
      3D geometry looked identical to nothing being selected
  Reported: cycling LTM 1110's Winch 2*/Replacement Ballast slot (10.70)
  from Winch 2* to Replacement Ballast turned the mesh back to its
  baseColor - visually indistinguishable from the slot being empty, even
  though it's genuinely occupied (just by the member with no exported
  geometry, per 10.69/10.70's confirmed-absent Replacement Ballast body).
  syncHighlight() only ever checked `selectedSet.has(p.appId)` - had no
  way to know a DIFFERENT id from the same slotGroup was the one actually
  selected. Added ALT_SELECTED_COLOR (pink, 0xec4899) and a slotGroup
  cross-check: for a namedPart whose own appId isn't selected but shares
  a slotGroup with whichever id IS selected, show pink instead of falling
  through to baseColor. Needed `data.components` inside syncHighlight()
  to look up slotGroup siblings, so its signature gained a data
  parameter, threaded through from window.__cwt3dActivate/__cwt3dOnRender
  (both already receive it) at all three call sites.
  Generic, not 1110-specific - applies to any future crane with the same
  "slot has two possible members, only one physically exists in the
  export" situation. Verified in-browser through the full cycle: none
  (grey) -> Winch 2* (green, has real geometry) -> Replacement Ballast
  (pink, no geometry of its own - color carried by Winch 2*'s mesh) ->
  back to none (grey). Full crane sweep clean, no console errors.

================================================================================

10.73 Outrigger Support Positioning: added ground bearing pads (bog mats),
      with real rectangle-vs-chassis collision detection, not just the
      leg's own point
  Person places 3.5m x 2.4m mats under P1-P4 sometimes to improve ground
  bearing pressure, and wants the MUST MOVE / clash logic to account for
  the mat's own footprint - "the real thing that needs collision
  detection" - not just the leg's single contact point, which is all
  clashesWithFootprint() ever checked. Confirmed before writing any math:
  the mat is centered on the leg's own contact point (not offset to one
  edge), and its 3.5m length always runs inline with the carrier's own
  length axis regardless of which corner it's under (never rotated per-
  leg - it's a physical mat sitting on the ground, not a rotated marker).
  Person also asked for a PER-LEG toggle, not one global on/off - mats go
  down one at a time as they're actually placed on site, not all four at
  once.
  Added padClashesWithFootprint(): a straightforward axis-aligned
  rectangle-vs-rectangle overlap test between the pad (centered at the
  leg's plotted x/y, half-width/half-length from the two new Pad Length/
  Pad Width inputs) and the chassis footprint rectangle - both already
  expressed in the same un-rotated local frame FOOTPRINTS/calcCAD() use
  for everything else, so no new coordinate math needed, just a proper
  AABB test instead of a point-in-box test. checkClash() is the single
  entry point calcCAD() calls for both the "if moved" and "if left in
  place" checks - transparently swaps to the pad-aware test only for
  legs with their own checkbox on (cad-pad-1..4), point-only test
  unchanged for every other leg, so default behavior (all four off) is
  byte-for-byte the same as before this change.
  Drawn on the site plan as a translucent violet rectangle at the leg's
  own final position (drawPadBox(), same rotated-polygon approach as the
  existing chassis footprint box), plus a lighter dashed ghost pad at an
  optional leg's "if moved" target spot so the footprint that WOULD be
  needed there is visible before deciding to relocate it.
  Verified with real numbers before screenshotting anything (LTM 1130,
  1.8m lateral shift): P1's own point sits at relX=1967mm, outside the
  chassis's 1425mm half-width - point-only check correctly says
  "optional." Its 2.4m-wide pad's near edge sits at 1967-1200=767mm,
  well inside that same 1425mm boundary - pad-aware check correctly
  flips it to "MUST MOVE" once P1's pad checkbox is on. Confirmed live in
  the browser: exact same flip, and the screenshot shows the pad
  rectangle visibly overlapping the dashed chassis footprint outline,
  matching the math. Regression-swept the outrigger tab across all 7
  cranes with every pad checkbox off (a real shift applied on each,
  not just the zero-shift case) - identical "optional" results to
  pre-change behavior, confirming the default path is untouched. No
  console errors.

================================================================================

10.74 Outrigger ground pads: recolored to match this tab's own existing
      current/future convention (yellow now, blue if moved)
  10.73's pad rectangles/ghost pad were both violet, a color not used
  anywhere else on this tab. Person asked for the pad's CURRENT position
  to read solid + yellow, and its GHOSTED (if-moved) position to read
  dashed + blue - which is exactly the convention this tab already uses
  everywhere else (compare mode's old-center dot is the same yellow
  #e5a900; the existing ghost target dot/line for an optional leg is
  already the same blue #38bdf8), so this brings pads in line with
  something already established rather than inventing a third color
  language.
  Current pad (drawPadBox at o.x/o.y): solid yellow outline/fill,
  unchanged from 10.73 otherwise. Its own marker DOT also switches to
  yellow when a pad is present (overriding the leg's must-move green /
  optional amber dot specifically) - the must-move/optional status isn't
  lost, since it's still fully carried by the connecting line's own
  color+dash style and the P-label, both left untouched; only the dot
  fill changes. Ghost pad (drawPadBox at o.movedX/o.movedY): recolored
  from dashed violet to dashed blue, matching the ghost dot that was
  already blue. Legend split from one "ground pad" swatch into two:
  "pad (current)" (solid yellow) and "pad (if moved)" (dashed blue).
  Verified in-browser: solid yellow pad+dot at the current position,
  dashed blue pad at the ghost target, no change to must-move/optional
  line styling or the table. Incidentally re-confirmed 10.73's "if
  moved" pad-aware clash check still fires correctly through this color
  change too - LTM 1130 with P3's pad on flagged "NOT POSSIBLE under
  current footprint" for its ghost target, visibly overlapping the
  actual chassis footprint outline in the screenshot. Pads-off default
  path re-verified byte-for-byte unchanged (identical table output to
  pre-10.73 baseline). No console errors.

================================================================================

10.75 LTM 1110's winch (p8w) swapped for its own real geometry via STEP
      reimport; discovered and fixed a real Group-vs-Object3D gap in
      cwt3d.js's node matching along the way
  10.69's "Part_1" was a working substitute (person pulled a part from
  LTM 1130's own document, since Onshape allows cross-document part
  reuse) - functional, but not this crane's own actual winch geometry.
  Person went back and did the proper fix on the source Composite Part
  itself: export it to STEP, reimport as a plain solid, swap that into
  the assembly in place of the old composite-linked instance, re-export.
  Confirmed by loading the new file directly: the composite's own
  geometry is now really there, landing as "Replacement_Ballast_<1>",
  visibly more detailed than the substitute (real housing/bolt-hole
  geometry, not a stand-in slab).
  What DIDN'T just work: this node came in as an Object3D, not the Group
  type every other matched node in every crane so far has been - the
  groupsByKey traversal only ever checked `obj.type === 'Group'`, so it
  silently skipped this node entirely (found by broadening a diagnostic
  dump to include Object3D types too, after the Group-only dump came up
  empty for anything winch-sized). Root cause is a structural
  difference in how a STEP-reimported node comes into the scene graph
  vs. a native Onshape occurrence, not a naming quirk - so the fix is
  generic, not a one-off: groupsByKey's traversal now accepts Object3D
  nodes alongside Group, still filtered by name via partMapKeyFor()
  exactly as before. Confirmed this can't misfire for any existing
  crane: the auto-generated "occurrence_of_X" wrapper nodes (Object3D
  type everywhere already, present in every crane's export) never
  coincidentally share a name with a real partMap key - partMap only
  ever lists a part's own name, never its wrapper's - so loosening the
  filter only ever adds a match, never creates a false one.
  partMap updated to 'Replacement_Ballast_<1>': 'p8w' (was 'Part_1').
  Verified in-browser: all 8 of LTM 1110's own ids resolve to correct
  weights, p8w highlights the new, more detailed winch mesh correctly
  positioned on top of the stack. Full 7-crane sweep plus a dedicated
  re-check of every other crane's own component ids (1130, 1220, 1250)
  confirms the loosened type filter didn't disturb anything - identical
  results to before this change. No console errors.
  Also downloaded, this same session: the LTM 1650 carrier (46MB) and
  LTM 1110 carrier (32MB) GLBs, via direct-download Nextcloud links
  (curl straight to disk, bypassing the chat upload size limit) -
  received and validated (real glTF binary headers, not error pages),
  not yet inspected/wired up - that's the next mission's work, not part
  of this fix.

10.76 Outrigger Support Positioning: added a 3D carrier (chassis) preview,
      the "next mission" flagged in 10.75
  New vendor/carrier3d.js module, deliberately much simpler than
  cwt3d.js: no partMap, no click selection/highlighting, no explode
  logic. The carrier is a single rigid whole - its own real assembled
  position from the CAD export IS the intended view (nothing to pull
  apart into an exploded layout), and its materials already carry the
  crane's own Liebherr colours baked in from Onshape (confirmed by
  inspecting both GLBs' material list directly - yellow/red/grey/black
  already present, not flat grey placeholder), so nothing needed
  recolouring either. Reuses the same vendored three.js/GLTFLoader/
  DRACOLoader/OrbitControls modules and shared Draco decoder as
  cwt3d.js - both these carrier exports and the counterweight exports
  come out of Onshape Draco-compressed by default.
  Explicitly scoped as "look and see" only, per the person's own
  framing - a toggle button on the Outrigger tab, NOT wired into that
  tab's own must-move/clash-detection math. That integration (matching
  the pad-collision work from 10.73/10.74) is possible future work, not
  part of this.
  Files moved from the session's scratch download location into
  outrigger/models/ (ltm1110-carrier.glb, ltm1650-carrier.glb) -
  matches the existing counterweight/models/ convention. New
  CARRIER_MODELS map (index.html) keyed the same as
  cadFleetData/COUNTERWEIGHT_DATA ('1110', '1650') - only 2 of the
  outrigger tab's 7 cranes have an exported carrier model so far, so
  the preview card only shows for those two, hidden entirely for the
  other 5 (checked: 1130/1300 correctly hide it, 1110/1650 correctly
  show it).
  Switching crane always resets the toggle back to hidden/off - same
  reasoning as CWT's setCwtView reset, don't try to carry "3D on"
  across a crane that might not have a model. Tracked with its own
  carrier3DModelKey/carrier3DActive state rather than piggybacking on
  calcCAD()'s own modelKey read, since calcCAD() also runs on every
  shift/pad input change - resetting an open preview every time someone
  nudges a shift value would be a real regression, not just noise.
  allOfflineContentUrls() extended to include both carrier GLBs and
  carrier3d.js's own vendor file list, deduped against cwt3d.js's
  existing vendor/three/* entries (both viewers share the same core
  three.js/GLTFLoader/DRACOLoader/OrbitControls files) via a Set - so
  "Cache All Offline Content" on the other tabs picks these up too,
  without listing the same vendored files twice.
  Verified in-browser: card hidden for 1130/1300, shown for 1110/1650;
  toggle shows/hides the canvas and correctly fetches+renders each
  crane's own model (confirmed visually - Liebherr yellow/black LTM
  1110, primarily grey/white LTM 1650 chassis - matches what each
  crane's own material list contains); switching crane while the
  preview was open resets the toggle back to "Show 3D Carrier Model"
  and hides the canvas; no console errors.

10.77 3D Carrier Preview: tied outrigger pad positions to the site plan
  Person's own framing after seeing 10.76 work: "can you try tie them
  to a site plan". Adds leg markers and pad boxes (current position:
  solid; "if moved" target: ghosted) into the 3D scene, positioned
  using the SAME leg/pad data the 2D canvas itself draws from
  (calcCAD()'s own mappedOutriggers/lastFp - x/y/movedX/movedY/pad
  passed through unreinterpreted, including the exact same "only ghost
  when it differs from current" condition the 2D canvas already uses)
  - not a separate parallel calculation that could quietly drift from
  the 2D plan's own numbers.
  The hard part: mapping the site plan's real-world mm coordinates
  (relative to the slew center) onto the CAD model's own arbitrary
  local coordinate space. Chose NOT to rescale the model to fit
  FOOTPRINTS' width/front/rear figures - the CAD export is already
  dimensionally accurate (more so than those OEM-sheet-derived
  approximations), rescaling it would only introduce distortion.
  Instead: measure the model's own bounding box, then use FOOTPRINTS'
  front/(front+rear) as a FRACTION to locate where within that box the
  slew center sits along the model's length axis (Z, confirmed the
  longer axis for both cranes' exports) - this only requires knowing
  which END of the box is the front, not the model's true real-world
  size. That one fact - front-at-min-Z or front-at-max-Z - is the only
  thing that can't be derived from the geometry alone.
  Confirmed LTM 1110's orientation by rendering a straight-down
  (orthographic-style) view and visually identifying the driving cab
  (clear windshield/mirror geometry, unambiguous) - cab sits at min Z,
  so frontAtMinZ: true. Verified this is actually right, not just
  plausible, by rendering the calculated P1-P4 marker positions
  alongside the real model from the same straight-down angle: all four
  land exactly on the tips of the model's own four outrigger beams,
  both front pair and rear pair, with the left/right pairing also
  matching the 2D plan's own P2=front-right/P3=front-left convention.
  Shifted-position test (ghost pads) confirmed too: "must move" legs'
  solid pad relocates to the new spot (matches 2D - no ghost, since
  x already equals movedX there), "optional but clashing" legs show
  the current solid pad plus a distinct blue ghost box at the target
  spot, exactly mirroring the 2D canvas's own two-state behavior.
  LTM 1650 is a different story: its export is far sparser (28 parts
  vs 1110's 289, confirmed back in 10.76) and only has ONE of the two
  outrigger-beam pairs modeled at all - no driving cab geometry either,
  so there was nothing to visually confirm frontAtMinZ against. Used
  the same fraction-based math with a best-effort frontAtMinZ: true
  guess, then checked it the same way as 1110 - rendered the
  calculated markers against the model. The front pair (P2/P3) landed
  exactly on the one modeled beam, which is real confirmation of the
  length-axis and left/right calibration both being correct. The rear
  pair (P1/P4) landed right at the very edge of the model's own
  bounding box, close enough to the tip that the sparse export is more
  likely explanation than a calibration error (this model is plausibly
  just missing some rear-most chassis structure - the same document
  this crane's Composite Part/STEP-reimport saga came from, see
  10.75's methodology entry). Flagged uncertain: true on '1650' in
  CARRIER_CALIBRATION for this reason - the math is consistent and the
  confirmable half checks out, but nothing here should be taken as
  precisely confirmed the way 1110's is until a fuller carrier export
  exists for it.
  Marker/pad colours match the 2D legend exactly (#e5a900 current,
  #38bdf8 ghost) rather than inventing a separate 3D palette. Sync is
  replayed automatically once a still-loading model finishes (toggling
  3D on and calcCAD() firing happen at nearly the same time, so the
  very first sync call almost always arrives before the multi-MB model
  has finished fetching - would otherwise silently do nothing and wait
  for the next unrelated input change). CARRIER3D_VERSION bumped 1->2.
  Card subtitle and in-canvas label text updated to reflect that this
  is no longer look-and-see only, with an explicit "approximate, not
  survey-precision" caveat - the calibration is a real, checked
  alignment, but still built from OEM dimension-sheet figures and a
  bounding-box fraction, not a CAD-to-CAD coordinate match.

10.78 3D Carrier Preview: anchor calibration to the model's own outrigger
      geometry instead of only the FOOTPRINTS formula
  Person's own screenshot, on LTM 1650: the calculated pad marker and
  the model's own real outrigger foot (visible on the CAD geometry
  itself - a small red-marked cylinder at the end of the beam) were
  clearly two separate points, not lined up. "can we get these two
  points to line up thats is what all the p1/4 symbolize the outrigger
  pos." Fair - 10.77's calibration was entirely formula-derived
  (FOOTPRINTS' front/rear as a fraction along the model's measured
  length), and FOOTPRINTS is itself an OEM-sheet approximation, not
  measured off this specific CAD export - close, evidently not exact.
  Fix: when the model actually has its own outrigger beam/foot geometry
  (not guaranteed - LTM 1650's export is missing one whole beam pair,
  see 10.76/10.77), treat that geometry as ground truth and nudge the
  calibration to match it, rather than trusting the formula alone.
  refineCalibrationFromGeometry() finds each of the 4 corners' own
  farthest-reaching point (the foot sits at the very tip of the
  extended beam, so "the single most extreme point in that quadrant"
  finds it directly - no need to identify which named part is "the
  foot," which isn't possible anyway since Onshape's own part names
  are all just "Part N", nothing semantic to match against), matches
  each detected point to its real P-id using the existing frontAtMinZ/
  lateralSign flags, and shifts the WHOLE calibration's
  lateralCenter/slewZ by the average (detected - predicted) offset
  across whichever legs matched. A sanity check (detected distance
  from center must be within 0.5x-1.5x that leg's own known r) stops a
  missing leg's quadrant from grabbing some unrelated far-off part
  (cab, tail block) as a false match - exactly the situation 1650's
  rear pair is in.
  Result, checked the same way as 10.77 (zoomed screenshot of the
  actual rendered corner): LTM 1650's front-pair marker, previously
  visibly offset from the real foot, now sits essentially coincident
  with it. LTM 1110 - already good in 10.77 - stayed good; the
  refinement found a genuine but small correction there too (its
  formula estimate was close, not exact).
  Also flattened the leg marker (cylinder height 0.35->0.05, y-offset
  0.18->0.06): a raised marker reads as visually offset from the real
  foot in any angled (non-top-down) view purely from camera
  perspective, even once the underlying X/Z position is exactly
  right - most of what was left of the visible gap after the geometry
  fix was this, not a calibration error. Confirmed via a pure top-down
  render (matches 10.77's own verification method) that ground-plane
  X/Z alignment was already exact before this - the flattening is a
  legibility fix for the default angled camera, not a second
  correctness fix.
  Calibration is computed once per model and cached (calibrationCache
  keyed by modelKey) rather than redone on every calcCAD() call - the
  geometry scan traverses every mesh in the model, worth avoiding on
  every shift/pad keystroke. baseLegs (each leg's real CURRENT r/x/y,
  fresh from cadFleetData) added as a new argument to
  __carrier3dSyncOutriggers() specifically for this anchoring purpose -
  deliberately not reused from lastMapped's own x/y, which already
  reflect whatever the shift inputs currently show (calcCAD()'s own
  plotX/plotY logic), not necessarily the physical current position.
  CARRIER3D_VERSION bumped 2->3. Verified in-browser: all 5 cranes
  without a carrier model still correctly hide the card; LTM 1110 with
  pads + a shift still renders the ghost pad correctly (10.77's own
  behaviour untouched by this change); no console errors.

10.79 3D Carrier Preview: whole-chassis ghost footprint, gated behind
      Compare mode; Fit View / Top View camera controls
  Person's own request after seeing the per-leg ghost pads working:
  "can we get a ghosting box where the next cranes pos is going to be
  on the 3d model... doesnt need to be the carrier 3d model could be
  something like use used to repersent the bog mats ? just transperent
  dashed line version?" - i.e. don't re-render a whole second carrier
  model at the shifted spot, just reuse the same flat, dashed, ghost-
  pad visual language already built for the per-leg pads, sized to the
  whole chassis footprint instead of one pad.
  Sized/positioned using FOOTPRINTS (the same width/front/rear figures
  the 2D canvas's own drawFootprintBox() rectangle uses), offset by the
  current shift, through the same siteToWorld() calibration transform
  already used for everything else in this feature - no new
  coordinate math needed, just a bigger box.
  Person's own follow-up question caught something the first pass
  missed: "should we have that on a toggle aswell like the 2d version
  show old ver new pos?" - checked the 2D canvas's own logic and
  confirmed: its ghost FOOTPRINT rectangle only draws when
  compareMode is true (see its own `if (fp && compareMode && hasShift)
  drawFootprintBox(...)` call), while the per-leg ghost PADS are
  unconditional (drawn whenever a leg's position differs, regardless
  of compareMode). The 3D side had gotten this wrong - the new
  whole-chassis ghost was unconditional on any shift, not gated by
  compareMode like its 2D counterpart. Fixed: __carrier3dSyncOutriggers
  now also takes compareMode, and the whole-chassis ghost box checks it
  alongside shiftX/shiftY - per-leg ghost pads correctly left
  unconditional, matching 2D exactly in both cases now, not just one.
  compareMode threaded through from index.html's own `compareMode`
  variable (set by toggleCompareMode(), which already calls calcCAD()
  itself, so no extra wiring needed to keep it live).
  Extracted the shared ghost-box building (transparent fill + wireframe
  outline) into addGhostBox(), used by both the per-leg pad ghosts and
  the new whole-chassis one - and upgraded the wireframe from a plain
  solid LineBasicMaterial to an actual LineDashedMaterial (dashSize
  0.15, gapSize 0.1, wire.computeLineDistances() called after
  construction - required or a "dashed" material renders solid) since
  the person specifically asked for "dashed line" and the 2D plan's own
  ghost convention is dashed too - the per-leg ghosts got this upgrade
  for free along with the new footprint ghost, not just the new one.
  Also added, unprompted but a natural fit given how much this session
  has leaned on comparing the 3D view against the model's own geometry
  by eye: Fit View and Top View buttons (a small toolbar row, shown
  alongside the canvas). Fit View just re-runs the existing
  frameCamera(). Top View is new - deliberately NOT a literal straight-
  down camera (0.5° off vertical instead): true vertical is a
  degenerate case for OrbitControls, whose internal orientation math
  breaks when camera "up" and "forward" become parallel. An early
  version of this (used only for this session's own debugging
  screenshots, never shipped) changed camera.up itself to work around
  that, which left dragging behaved oddly afterwards - OrbitControls
  caches object.up at construction time and doesn't notice it changing
  later. The 0.5°-off-vertical approach avoids the degenerate case
  without ever touching camera.up, so drag-to-rotate afterwards behaves
  completely normally - confirmed by scripting a drag right after
  clicking Top View and checking the model actually rotated, not some
  broken/inverted state.
  CARRIER3D_VERSION bumped 3->5 (one bump for the ghost footprint work,
  one for Fit/Top View). Verified in-browser: shift without compare
  mode shows per-leg ghosts only; enabling compare mode adds the
  whole-chassis dashed ghost; Top View gives a clean straight-down
  render (useful for exactly the kind of alignment checking this
  session has been doing manually); drag-to-rotate and Fit View both
  work normally afterwards; no console errors.

10.80 3D Carrier Preview: current pads stay put, ALL legs ghost, linked
      Compare toggle on the 3D panel itself
  Person's own follow-up after 10.79: "can we have all future
  positions ghosted to there they will lay and leave the current bog
  mag pos the yellow ones were they are so i can visually see the
  confilicts." Real bug, not a preference: the solid "current" pad's
  position came from `leg.x/leg.y`, which is calcCAD()'s own plotX/
  plotY - and for a "must move" leg, plotX/plotY is ALREADY the moved
  target, not the true current spot (see calcCAD()'s own isRequired
  logic). That's the right call for the 2D canvas (a "must move" leg
  reads better shown at where it's going), but wrong for this: the
  person's whole point of the 3D ghosting is comparing a mat's TRUE
  current position against ALL the shifted targets at once, and a
  "must move" leg's solid pad silently jumping to its target - with no
  ghost at all, since x already equalled movedX there - hid exactly
  the comparison they were trying to make for those legs.
  Fixed by using baseLegs (already being computed and passed in for
  10.78's calibration anchoring, now used for BOTH purposes) as the
  source of truth for the solid marker/pad position instead of leg.x/
  leg.y - true current position, every leg, unconditionally. Ghost pads
  now key off leg.movedX/movedY instead of the old "only if it differs
  from leg.x" check - movedX/movedY is always the real fully-shifted
  target regardless of required/optional (calcCAD() sets it
  unconditionally, unlike plotX/plotY), so this correctly ghosts all
  four legs now, not just the ones the 2D canvas happens to ghost.
  Second part of the same message: "can we also have a small Compare
  old/new position check box linked to the top 2d one but also on the
  3d panel bar." Added a second checkbox to the 3D toolbar row,
  wired to the exact same `compareMode` flag as the original one atop
  the 2D plan - toggleCompareMode(sourceEl) now syncs BOTH checkboxes'
  checked state to whichever one fired the change event, so they can
  never show a different state from each other. Saves scrolling back
  up to the 2D card just to flip it while looking at the 3D view.
  CARRIER3D_VERSION bumped 5->6. Verified in-browser: solid pads for
  "must move" legs now stay at their real current spot instead of
  jumping (checked directly against the pre-fix screenshot); enabling
  compare mode via the NEW 3D checkbox ghosts all 4 legs and syncs the
  2D checkbox's own checked state too (and vice versa, same code path);
  cranes without a carrier model still correctly hide the card; no
  console errors.

10.81 3D Carrier Preview: fixed the pad/foot alignment, for real this time
  Person's own screenshot on LTM 1110: the calculated pad marker and the
  model's own real outrigger foot geometry were STILL clearly two
  separate points - not a small residual, a real visible gap on all four
  corners. This meant 10.78's original "geometry-anchored calibration"
  claim was wrong, or had regressed. Root-caused properly this time by
  reproducing it locally (confirmed: identical gap, even in a pure
  straight-down Top View where camera-angle parallax isn't a factor) and
  iterating through several wrong theories before landing on the real
  fix - worth recording all of them, since each one looked plausible and
  each one was checked, not assumed:
   1. Single global averaged offset (10.78's actual bug): applying one
      averaged (detected - predicted) offset uniformly to all four legs
      can only correct a constant frame-offset error, not the kind of
      per-leg/scale mismatch this turned out to have. Replaced with an
      independent per-axis least-squares LINE fit (site mm -> world
      metres) through however many legs' geometry could be confirmed -
      genuinely better, but the fitted line still doesn't pass through
      any single leg's own point exactly, so a real (smaller) gap
      remained even after this fix alone.
   2. Farthest-reaching single VERTEX per quadrant: wrong, because a long
      rectangular BEAM's own extreme corner is a real vertex too (unlike
      a round foot's bounding-box corner, which isn't a point on the
      shape at all) - this just found the beam's own tip, not the foot.
   3. Most compact single MESH: also wrong. Dumped every mesh within 1.5m
      of one corner and found 500+ of them - this Onshape export splits
      every real part into hundreds of tiny per-face sub-meshes
      (millimetre-scale bolt heads, decals, fillets), so "compact" just
      grabbed some near-zero-size decal.
   4. What actually works: aggregate sub-meshes back into their own PART
      first (each real Onshape part occupies exactly one parent node in
      this export - confirmed by dumping and aggregating: one 2.7m-long
      part is obviously the beam, one short 0.95m-tall part is obviously
      the jack cylinder, one 0.5x0.5m part sitting right at the model's
      own lowest Y is obviously the foot plate), THEN filter to parts
      within 15cm of the model's own lowest point (the foot plate TOUCHES
      THE GROUND - the cylinder above it, the beam, and nearby hardware
      don't), THEN take the LARGEST footprint among those ground-level
      survivors (the plate itself, not incidental ground-adjacent
      hardware). Also switched from the winning part's bounding-box
      centre to its own vertex centroid, less sensitive to a single
      asymmetric detail (an off-centre boss, a bracket on one side)
      skewing a bounding box away from the part's true visual centre.
   5. Even with (4)'s detection now correctly finding the foot plate,
      there was STILL a real residual gap - because a least-squares fit
      through 4 points doesn't pass through any one of them exactly, and
      that residual was exactly what was still showing on screen. Fixed
      by not routing a leg's own CURRENT position through the fitted line
      at all when its geometry was directly confirmed: legAnchors (leg id
      -> its own exact detected point) is now checked first in
      siteToWorld(), bypassing the fit entirely for that leg's current
      marker/pad. The fitted line is still used for extrapolating to a
      SHIFTED (ghost) position, where there's no real geometry to check
      against anyway - imprecision there is far less visible/important
      than getting a leg's own CURRENT position exactly right.
  Verified by re-testing the exact same corner after each of the 5
  changes above, in order, with cropped/upscaled screenshots each time -
  confirms this wasn't guesswork, each step was checked against the
  previous one before moving to the next theory. Final result: LTM
  1110's marker sits exactly on the real foot geometry at all four
  corners (checked in Top View, no visible gap at any corner); LTM
  1650's front pair (the only pair with real geometry to check against)
  shows the same result; the whole-chassis ghost footprint and per-leg
  ghost pads (10.79/10.80's own features) still work correctly with a
  shift applied and Compare mode on - unaffected by any of this, since
  ghosts deliberately never use legAnchors. CARRIER3D_VERSION bumped
  6->12 across the whole debugging session (each iteration needed its
  own cache-bust to actually test against a fresh browser). Full 7-crane
  regression re-run at the end: cranes without a carrier model still
  correctly hide the card, both 1110 and 1650 still load and render with
  no console errors.

10.82 3D Carrier Preview: added LTM 1130/1160/1250/1300, and a new 1650
      CWT model - person's own overnight batch
  Person sent the rest of their carrier exports (Nextcloud folder share,
  Myslewer/Carrier/*) plus a new low-poly LTM 1650 counterweight model
  (Myslewer/Counterweight/LTM1650CWT-low.glb) and said to take the time
  overnight to work through them. Folder share downloaded as a single
  zip via .../download (redirects to the WebDAV ?accept=zip endpoint,
  confirmed with curl -sIL before committing to the full download) -
  ~200MB total, but that's the person's whole working folder (source
  .dwg/.dxf/.pdf files, CWT models for cranes already wired up). Only
  extracted the specific files actually needed
  (Carrier/LTM{1130,1160,1250,1300}CARRIER.glb,
  Counterweight/LTM1650CWT-low.glb) rather than the whole archive -
  no reason to touch already-integrated CWT models that weren't asked
  for.
  Carrier models: added to CARRIER_MODELS/CARRIER_CALIBRATION following
  the exact same process as 1110/1650 - rendered each in Top View,
  confirmed frontAtMinZ by identifying the driving cab (all four show it
  at the same end/side as 1110's own confirmed orientation, so
  frontAtMinZ: true, lateralSign: 1 for all - not assumed, checked per
  crane). Then ran the SAME per-leg geometry confirmation now used for
  1110/1650 (10.81's fix) against each: 1160, 1250, and 1300 all
  achieved full 4/4 leg confirmation (legAnchors has an entry for every
  leg - checked directly, not inferred from the render alone). 1130 only
  got 2/4 (P1/P3 confirmed; P2/P4's own detected candidate for those two
  legs came out far too close to the chassis centreline to be a real
  outrigger foot - the sanity check correctly
  rejected it rather than anchoring to a wrong point, so those two legs
  fall back to the fitted line, same graceful degradation 1650's missing
  rear pair already relies on. Not a bug - the detection heuristic
  earning less confidence on a differently-structured export is exactly
  what the sanity check exists to catch safely rather than silently
  accept.
  New LTM 1650 CWT model: NOT wired up in this pass. Inspecting it
  (1771 nodes, 882 meshes, 44 top-level "Part N" bodies with no semantic
  naming beyond "winch 2" and two winch-related assembly nodes) made
  clear this needs a real partMap - every other crane's partMap so far
  has had either semantic part names or a small enough part count to
  map by direct visual/positional inspection with reasonable confidence.
  44 anonymous bodies for a crane with 12 real components (1 receptacle
  pair + linked center section, 1 five-tonne pair, 7 interchangeable
  ten-tonne pairs, 2 winches) - roughly 2-4 CAD bodies per real
  component - is a real mapping task, and getting it wrong would show a
  crew INCORRECT weight/composition data, not just a slightly-off pad
  render. That's a different risk category to the carrier work above,
  which is look-and-see plus an approximate pad position - worth being
  honest that this wasn't rushed through overnight alongside the lower-
  stakes carrier work. File is staged at
  counterweight/models/ltm1650-cwt-staging.glb (not referenced by
  anything yet) for the next session to pick up, ideally with the
  person's own input on which part numbers correspond to which
  positions rather than pure geometric inference.
  Verified in-browser: all four new carriers show correctly for their
  own crane and stay hidden for every other crane; full cycle (toggle
  3D, all 4 pads on, Compare mode on, shift applied, reset, pads off,
  3D off) run against all 6 cranes with a carrier model plus 1100 (no
  model) - no console errors anywhere. CARRIER3D_VERSION bumped to 15
  across this investigation.

================================================================================

10.83 3D Carrier Preview: fixed foot-plate selection (largest reach, not
      largest footprint) - LTM 1130 "quite off" report
  Person reported (with two mobile screenshots) that the LTM 1130 3D
  layout was "quite off" - a real, visible regression from 10.81's own
  working fix for 1110. Root cause: 10.81's foot-plate selection took
  the LARGEST FOOTPRINT among ground-level candidates in each quadrant.
  That happened to work for 1110 because the true foot plate was also
  the largest ground-level part nearby, but it isn't a safe rule in
  general - 1130's export has some other ground-level part (closer to
  the chassis centreline, not a real outrigger foot) with a bigger
  bounding box than the actual plate, so it won the footprint contest
  and got wrongly anchored to, exactly matching what the person saw on
  their screen.
  Confirmed by dumping every ground-level part's own reach/footprint/
  groundGap directly (temporary __carrier3dDumpAll debug hook, removed
  again once done) rather than guessing: the real foot plate (e.g.
  Part_8, reach ~4.05m, groundGap≈0, footprint ~0.373 m²) was sitting
  right there in the candidate list, correctly passing the ground-level
  filter, just losing the footprint contest to a closer/bigger part.
  Fix: selection criterion changed from largest-footprint to largest-
  REACH among ground-level candidates - reach is what actually,
  physically distinguishes an extended outrigger foot from anything
  else sitting near the ground (a step, a storage box, incidental
  hardware). Footprint is now only a minimum-size sanity filter
  (0.02 m² / 2cm x 2cm), rejecting decal-scale noise, not the primary
  selector.
  Verified visually against 1130 (screenshot crop: all 4 corners now
  align with real beam/foot geometry) and via a legAnchors-coverage
  regression across all 6 carrier-equipped cranes (1110/1130/1160/1250/
  1300/1650 all confirmed 4/4 legs). 1650 showing full 4/4 confirmation
  initially looked suspicious given 10.76/10.77's own note that its
  export was missing outrigger geometry - re-checked with a proper,
  unclipped Top View screenshot and found that note was itself wrong:
  1650's export DOES have real, symmetric foot-plate-plus-beam geometry
  for all four corners (front and rear pairs both ~0.49 m² footprint,
  ~5.1m reach, near-zero ground gap) - an earlier session's visual
  impression from a clipped/zoomed screenshot led to a mistaken belief
  the rear pair was entirely absent. Worth noting as a reminder to
  verify claims like "this model is missing X" against the actual
  geometry data rather than carrying them forward as established fact.
  CARRIER3D_VERSION bumped to 19; both temporary debug hooks
  (__carrier3dDebug, __carrier3dDumpAll) removed before shipping.

================================================================================

10.84 Header: app version number next to the logo
  Person wanted a version number shown next to the MYSLEWER wordmark,
  same idea as their norwest-forms PWA. Asked whether it should reuse
  the existing CACHE_VERSION (already bumped every deploy, so always
  accurate, but reads as an internal build string like "v51-beta38")
  or be a separate, clean, user-facing version - person chose the
  separate version. Added a plain "v1.0" span next to the h1, small
  and muted (0.6rem, --text-muted, vertical-align:super) so it reads
  as a version tag rather than competing with the wordmark itself -
  static markup, not wired to any constant, since this is meant to be
  bumped by hand on meaningful releases rather than tracked
  automatically like CACHE_VERSION/CWT3D_VERSION/CARRIER3D_VERSION
  are. Checked at both a normal and a narrow (360px) mobile viewport -
  no wrapping or overlap with the update-check button.

================================================================================

10.85 3D Carrier Preview: dropped Top View, made Fit View bird's-eye and
      fit everything drawn, not just the model
  Person asked to get rid of the separate Top View button and have Fit
  View do the bird's-eye framing itself, AND zoom out to fit everything
  currently on screen - not just the carrier model. The old Fit View
  (an angled 3/4 view) only ever measured the model's own bounding box
  (frameCamera(root)), so a ghost pad or the whole-chassis ghost
  footprint (both drawn well outside the model itself once a shift is
  applied - see 10.79/10.80) could sit off-screen entirely with no way
  to bring it into frame besides manually dragging/zooming.
  Removed the Top View button and its window.__carrier3dTopView
  entirely. Fit View now calls a new fitView() that: (1) unions the
  model's own bounding box with outriggerGroup's (the markers/pads/
  ghost boxes built by applySync - see sceneBox()), so anything
  currently drawn is included; (2) uses the same near-vertical (0.5°
  tilt) bird's-eye angle Top View used to, for the same OrbitControls-
  degenerate-case reason (10.79's own explanation still applies
  unchanged); (3) derives camera distance from the camera's actual
  vertical FOV and current aspect ratio, fit against whichever
  horizontal axis (lateral X or longitudinal Z) actually constrains the
  canvas's own shape, rather than the old fixed 1.3x-of-larger-
  dimension guess, which could crop the narrower axis on a canvas
  that isn't roughly square.
  Verified with a big compare-mode shift applied (so the ghost
  footprint sits well outside the model) at both a narrow mobile
  width and a wide desktop width - the whole chassis, current pads,
  AND the shifted ghost footprint all land fully inside frame at both
  aspect ratios, nothing clipped. CARRIER3D_VERSION bumped to 20.

================================================================================

10.86 3D Carrier Preview: removed the permanent "approximate" overlay text
  Person said the "Outrigger pad positions shown below are approximate -
  the CAD model is aligned to..." caption blocked the view - it sat as
  a translucent chip over the top-left corner of the canvas the ENTIRE
  time the preview was open (not just transiently), covering part of
  the model itself, unlike the "Loading 3D model…" / error text it
  shares an element with, which are genuinely transient. Removed it
  outright rather than relocating it - added a setLabel() helper that
  also toggles the element's own display:none when there's nothing to
  show, so an empty caption doesn't leave a stray padded chip visible
  (the div's CSS gives it padding/background/border-radius regardless
  of content, so clearing textContent alone wasn't enough). Loading and
  error states are unchanged. Verified across all 6 carrier-equipped
  cranes: label is empty and display:none once loaded, no console
  errors. CARRIER3D_VERSION bumped to 21.

================================================================================

10.87 Ground Bearing Pads: mirror the P1-P4 toggles onto the 3D toolbar
  Person wanted the per-leg pad toggles (Ground Bearing Pads - P1/P2/
  P3/P4, above the 2D plan) reachable from the 3D toolbar too, next to
  Compare old/new position - same reasoning as that toggle already
  being mirrored there (10.80). First asked to "move" them down;
  clarified mid-turn that the originals should stay too, for anyone
  using only the 2D view - a mirror, not a relocation. Pad size (Pad
  Length/Width) stays only in its original spot above - only the on/
  off state needed a second home.
  Added syncPadToggle(legId, sourceEl), the same mirrored-checkbox
  pattern as toggleCompareMode: whichever checkbox fires the change
  event updates its counterpart and calls calcCAD(). padEnabledFor
  still only ever reads cad-pad-N, so that stays the single source of
  truth; the 3D toolbar checkbox is purely a second control surface
  for it, not a second state. Also fixed a gap toggleCompareMode
  already had (but hadn't hit yet): opening the 3D view for the first
  time after already toggling a pad above would show the toolbar's
  checkbox unchecked until the next change event, since sync only ever
  ran ON a change event - toggleCarrier3D() now pulls the real current
  state into all 5 toolbar checkboxes (Compare + P1-P4) at open time.
  Verified: toggling P1/P3 above then opening 3D shows both checked on
  the toolbar; toggling P2 on the toolbar reflects back to the
  checkbox above; toolbar wraps cleanly at 420px and sits on one line
  at 900px, no overlap with Fit View/Compare; no console errors across
  all 6 carrier-equipped cranes.

================================================================================

10.88 LTM 1650 CWT: wired up the real model, real partMap
  Person supplied a NEW export (Myslewer/Counterweight/LTM1650CWT.glb -
  not the earlier "-low" one staged back in 10.82) plus their own
  Onshape instance tree screenshot describing each stack's top-to-
  bottom part order, and asked to confirm the read before uploading
  the model. Cross-checked their description against the file's real
  geometry rather than trusting either alone:
    - Every "Part N" node name in the GLB matches the Onshape tree
      exactly (occurrence-of-Part-N, no collisions anywhere in the
      file) - unlike the earlier "-low" export, this one names
      individual plates, not just "Part N" as a generic default.
    - World position confirms both the left/right split (X identical
      to several decimals within each side) and the exact top-to-
      bottom order the person described, independently, for both
      stacks: Right 6->12->1->2->3->10->7->15, Left 13->9->8->16->4->
      14->5->11.
    - Mesh bounding-box thickness confirms the 5t/10t split: Part 6/13
      (the two "5t" plates) are 0.389m thick vs 0.68m for every 10t
      plate, same footprint (2.192 x 2.192) throughout - consistent
      with a 5t plate being roughly half a 10t one.
    - Person separately confirmed Part 16's <2> instance count in the
      Onshape tree wasn't a hidden duplicate ("I just dragged it") -
      confirmed independently too: exactly one "occurrence of Part 16"
      node exists anywhere in the file, sitting exactly where its
      stack position predicts.
  Wired up model3d on '1650' for real: each 10t PAIR (one plate per
  side, matching the 2D diagram's own "×2" ids p10a..p10g) maps both
  that pair's GLB node names to the same app id, paired by physical
  stack rank; p5x2 the same way for the two 5t plates. Winch 2 is a
  single clean node; Winch 3 is a 6-body sub-assembly under cryptic
  Onshape-generated names (id11__$ etc) - confirmed unique, all 6
  mapped to w3. p1 (All Counterweight Frame Receptacles) deliberately
  left UNMAPPED - its own GLB representation is a large, complex
  nested sub-assembly (hundreds of anonymous sub-parts, a different
  problem than the 16 clearly-identified plates) - shown as background
  geometry, not 3D-clickable; the 2D diagram remains how it's toggled.
  First attempt used cwt3d.js's own auto-explode (no preExploded flag)
  since the raw GLB's plates were only 0.1m apart, which read as
  "real assembled position, needs auto-explode" by the same logic
  that's worked for every non-preExploded crane so far. Wrong: the
  person had already exploded the stack themselves in Onshape (0.1m
  WAS their intended gap, not the real touching thickness) - letting
  auto-explode run on top of that doubled the spacing AND left the
  large unmapped p1 base geometry static while the plates moved away
  from it, which read as visually broken (confirmed by screenshot,
  not just theorized). Fixed by setting preExploded: true once this
  was clarified - trusts the person's own layout untouched, consistent
  with everything staying spatially correct relative to the static
  base. Verified: 3D toggle appears for 1650, click-to-select works
  (confirmed via an actual hit, not just no-console-errors), no
  console errors, CWT3D_VERSION unaffected by this entry (see 10.89
  for the version bump that also landed alongside it).

================================================================================

10.89 CWT 3D viewer: front view + fit-to-model default, fixed a
      perspective depth bug found along the way
  Person asked for the CWT 3D viewer's default camera to be a front
  view with everything fit in frame ("zoom to fit or whatever it's
  called"), after LTM 1650's own default 3/4-angle view (10.88) left
  its left counterweight stack sitting behind the model's own central
  base/winch bulk, only visible after manually dragging to rotate -
  same class of problem carrier3d.js's Fit View already solved for the
  outrigger tab (10.85), applied here to the CWT tab's own viewer.
  Replaced cwt3d.js's frameCamera() 3/4-angle fixed-margin guess with
  a straight-on front view (camera on +Z, default up, looking at the
  model's own bounding-box center) sized from the camera's actual FOV/
  aspect ratio, same approach as carrier3d.js's fitView - fit against
  whichever of X (width) or Y (height) actually constrains the
  canvas's own shape.
  Regression-checked against every other CWT-3D crane (1250, 1130,
  1110, 1300, 1220) before trusting it as a safe default change, not
  just eyeballing the one crane that prompted it - caught a REAL bug
  doing this: LTR 1220's counterweight is nearly as deep (Z) as it is
  tall (Y), and the initial version measured required distance only
  from the bounding box's CENTER depth, which under a perspective
  camera under-fits anything with real thickness - the model's own
  front (near-camera) face sits closer than the "fit" distance and
  gets magnified past the frame edges. Confirmed by comparing computed
  fit distance against the model's own half-depth (halfZ=1.2 against a
  fit distance of only 3.2 before margin - the front face was ~1.3x
  closer than the framing assumed) rather than guessing from the
  screenshot alone. Fixed by adding each axis's own halfZ to its
  required distance, so the fit is valid at the model's NEAREST point,
  not just its average depth - a no-op for a genuinely flat model,
  the actual fix for a thick one.
  Verified all 6 cranes (1650, 1250, 1130, 1110, 1300, 1220): no
  console errors, everything fits in frame with visible margin on all
  sides, nothing clipped, click-to-select still functions (confirmed
  via an actual hit on LTM 1650's stack, not just visual inspection).
  CWT3D_VERSION bumped to 12.

================================================================================

10.90 HOTFIX: LTM 1650 CWT plates were completely unclickable in
      production - GLTFLoader sanitizes spaces out of node names
  Person reported, on the LIVE deployed app, that "the only clickable
  item is winch 3" - a real bug in code shipped in 10.88, not a
  hypothetical. Root-caused with a temporary debug hook dumping
  groupsByKey and namedParts (removed before shipping the fix, same
  as every other debug-hook episode this doc records - never ship
  with one left in):
    - THREE.js's GLTFLoader sanitizes node names on load, replacing
      spaces with underscores - "Part 6" becomes "Part_6", "winch 2"
      becomes "winch_2". Confirmed directly: dumping every Group/
      Object3D name actually present in the loaded scene showed
      "Part_6", "Part_12", "winch_2", etc, not the space-containing
      names the raw glTF JSON (and this app's own partMap, copied
      from that raw JSON) both used.
    - Every other crane's partMap already happened to use underscore-
      style names (Counterweight_Plate_2, Receptacle_Plate_1, etc) -
      not because anyone knew about this sanitization, but because
      those Onshape part names never happened to contain a literal
      space to begin with. LTM 1650's new export (10.88) was the
      first one where real part names DID contain spaces ("Part 6",
      "winch 2"), which is what finally made this pre-existing,
      previously-invisible GLTFLoader behavior matter.
    - Net effect: every "Part N" and "winch 2" partMap entry silently
      matched nothing at all. The ONLY thing that DID match was two of
      winch 3's six sub-parts (id2__$, id5__$ - no spaces in those
      names to sanitize) - and one of those two happens to reference a
      1264-primitive mesh, large enough to visually cover most of the
      model's surface. That combination is exactly what read as "only
      winch 3 is clickable" - not a coincidence, a direct consequence
      of the naming mismatch plus one oversized mesh filling the gap.
      Checked that oversized mesh's own world bounding box before
      assuming it was itself a problem: it sits centered in the
      model's own upper-middle area between the two stacks, not
      overlapping either one - consistent with being real, detailed
      winch mechanism geometry (plausible for a hydraulic winch's
      internals), not an export mistake. Left as-is.
  Fix: changed partMap keys to the underscore form GLTFLoader actually
  produces ('Part 6' -> 'Part_6', 'winch 2' -> 'winch_2').
  Found a SECOND, unrelated bug in the same pass, via the same debug
  dump: 4 of winch 3's 6 sub-parts (id11__$, id8__$, id17__$, id14__$)
  still didn't match even after the naming fix. Cause: a glTF mesh
  with exactly ONE primitive becomes a bare THREE.Mesh on load, not a
  Group wrapping one Mesh child the way every multi-primitive part in
  this app so far has landed - confirmed by checking each of the 6
  sub-parts' own primitive count directly (the 4 broken ones: exactly
  1 each; the 2 working ones: 1264 and 50). The traversal's own type
  filter only ever checked for Group/Object3D, silently skipping
  plain Mesh nodes - a real gap for any future single-primitive part
  on any crane, not an LTM-1650-only fix. Added `|| obj.isMesh` to the
  filter.
  Verified end-to-end after both fixes: groupsByKey now finds all 23
  expected keys (16 plates + winch_2 + all 6 winch 3 sub-parts,
  5104 total meshes correctly distributed across the right appIds,
  not one appId absorbing everything). Clicked an actual plate and
  confirmed both the visual selection AND the Total Counterweight
  readout updated correctly (10.0t for the 5t pair) - not just "no
  console errors," the actual number a crew would rely on. Regression-
  checked every other CWT-3D crane (1250, 1130, 1110, 1300, 1220) -
  unaffected, as expected, since their own partMaps never had a space
  to begin with. CWT3D_VERSION bumped to 13.
  Lesson for future CWT models: if a new Onshape export's own part
  names contain spaces (check the Onshape instance tree directly, the
  same way the person already screenshots it for stack-order
  confirmation), use underscores in the partMap from the start rather
  than copying the space-containing name and finding out the hard way.

================================================================================

10.91 LTM 1650 CWT: made p1 (base/receptacle) clickable in 3D too, as one
      rigid block - person's own suggestion
  10.88 deliberately left p1 (All Counterweight Frame Receptacles)
  unmapped, reasoning that its GLB representation - a large, complex
  nested sub-assembly with hundreds of anonymously-named sub-parts -
  was too risky to guess a per-sub-part mapping for. Person, inspecting
  the file directly in a separate CAD viewer, pointed out this was
  overthinking it: p1 is only ever toggled as ONE unit everywhere else
  in the app (no per-sub-part granularity in the 2D diagram either), so
  it doesn't need per-sub-part mapping at all - just ONE partMap entry
  pointing at the sub-assembly's own top-level container node, treated
  as a single rigid block the same way any other multi-body partMap
  entry already works in this app.
  Confirmed the container node's exact name empirically rather than
  assumed, having just been burned by an incorrect naming assumption in
  10.90: raw name "98037278_002 <1>" loads as "98037278_002_<1>" (space
  sanitized to underscore, "<1>" suffix preserved as-is - a data point
  worth keeping in mind for any future name containing similarly unusual
  characters, not just plain spaces). Note there's ALSO a bare
  "98037278_002" node in the same file - that's the model's own root,
  not the receptacle: mapping that one instead would make the entire
  model - stack, winches, everything - a single clickable p1, wrong.
  Added '98037278_002_<1>': 'p1' to the partMap. Verified both via a
  programmatic toggle and an actual canvas click: the whole base/
  receptacle frame highlights correctly, cleanly excluding the plates
  and winch mechanism above it (screenshotted and visually confirmed,
  not assumed from the click succeeding alone) - Total Counterweight
  shows 25.0t with the OEM-approved checkmark, matching the 2D data
  exactly. CWT3D_VERSION bumped to 14.
  Person separately asked about getting Onshape to export this
  sub-assembly as one true merged part instead, for a cleaner file -
  their first attempt (selecting the sub-assembly and exporting it
  directly) came back still showing every individual part in the tree,
  which is expected: exporting a selection of parts (to STEP or GLB)
  preserves whatever hierarchy already exists, it does not merge
  anything. Onshape's Composite Part tool is what actually performs a
  boolean union into one new solid body - it's a modeling operation
  done BEFORE export, not an export option. Explained the same
  procedure already proven for Winch 2* (10.75): create the Composite
  Part from the selected solids, export THAT specifically to STEP
  (composite parts themselves get silently dropped by Onshape's
  assembly-level GLB exporter, 10.69), reimport the STEP as a plain
  solid, swap it into the assembly in place of the old individual
  instances, rename if possible, re-export the whole assembly.
  UPDATE: person tried the Composite Part / STEP swap and hit friction
  getting it to work cleanly - decided the '98037278_002_<1>' block-
  mapping above is a fine PERMANENT answer for this crane, not a
  stopgap waiting on a cleaner export. Nothing to revisit here unless
  the person independently gets a genuinely merged export later; this
  isn't tracked as outstanding work.

================================================================================

11. HOW TO SOURCE A NEW CRANE'S COUNTERWEIGHT DATA + 3D MODEL  [CONVERSATION]
--------------------------------------------------------------------------------
Use this whenever a crane needs its CWT Combinations tab built or extended -
either from scratch, or adding a 3D model to a crane that currently only has
the 2D diagram. This is the person's own real, repeatable workflow, run the
same way across every crane so far - written down here as one procedure
instead of left scattered across the dozen-plus per-crane entries in section
10 that each describe living through some piece of it. Same relationship to
section 10 that section 9 has to section 5: section 10 is the record of what
actually happened and why (including the false starts); this section is the
current, distilled procedure.

11.1 Two source documents, from two different places
  a) The OEM operator manual PDF for that specific crane, "unlocked" (its
     own password/permission protection removed) before it's handed over.
     Just the Counterweight chapter needs extracting from it (10.1)
     - it's the source of the combination weights/ids/subset-sum data,
     not the 3D geometry.
     Extraction gotcha to check for, not assume away: this fleet's PDFs
     have shown up both plain and with a uniform per-character code-point
     shift applied by an embedded font subset (confirmed on the 1250 doc,
     10.1) - decode one word and confirm it reads correctly before
     trusting a bulk extraction. Tool availability isn't constant session
     to session either (poppler-utils present for some, absent for
     others, 10.1 vs section 9's own toolchain) - don't assume this
     session has what a previous one did.
  b) A GLB export of the vendor-supplied 3D CAD model, prepared in
     Onshape by the person (11.2) - this is the source of the 3D
     geometry/positions, not the weight data. The PDF and the GLB are
     independent inputs; nothing about one is inferred from the other.

11.2 Preparing the GLB in Onshape, before export
  Open the vendor-supplied 3D model in Onshape and:
  - Space the counterweight stack itself apart (~120mm between adjacent
    plates has been the standard gap used everywhere in this app -
    STACK_GAP in cwt3d.js matches it exactly, 10.59) so it reads as an
    exploded view rather than the plates' real touching-flush assembled
    position. This is now done BY HAND in Onshape for every new crane
    (10.71 onward), not left for the app's own auto-explode heuristics
    to figure out - see the UPDATE note on 10.59 for why the workflow
    moved this direction. The resulting model3d config uses
    preExploded: true, which tells cwt3d.js to trust these positions
    exactly and skip its own explode logic entirely.
  - Rename parts to real, meaningful names where Onshape allows it. Note
    a real Onshape UI limitation the person has hit directly: only the
    ASSEMBLY INSTANCE can always be renamed from the assembly tree; the
    underlying PART's own name (the one that actually ends up on the
    exported GLB node, see 11.4) sometimes can't be changed from there -
    it has to be renamed in the part's own Part Studio instead, which
    isn't always practical. When it isn't, the export ships with
    Onshape's own generic "Part N" names instead of anything semantic -
    not a blocker (see 11.4), just means the partMap has to be built by
    cross-referencing an Onshape instance-tree screenshot against real
    geometry instead of reading it straight off descriptive names.
  - Swap out any Composite Parts (an Onshape object type - a merged/
    boolean result, not a plain solid). These get SILENTLY DROPPED by
    Onshape's assembly-level GLB exporter even though they're a normal
    part of the tree (discovered the hard way on LTM 1110's Winch 2*,
    10.69 - confirmed by comparing the Onshape instance count against
    what the GLB actually contained, not assumed). The fix that's stuck:
    export the Composite Part on its own to STEP, reimport that STEP
    file as a plain solid, then swap the assembly's instance of the old
    composite-linked part for the reimported one. The reimported node
    lands with real geometry and survives the main assembly export
    normally after that (10.75) - the earlier approach (exporting the
    composite as its own small supplementary GLB and loading it
    alongside the main one via model3d.extraParts) still works and is
    still in cwt3d.js, but is no longer the first thing to reach for now
    that the STEP-swap fix is known to work directly in the main file.
  - Export the finished, exploded, composite-free assembly as one GLB.
    Two export-setting gotchas that have bitten before and are worth
    checking for on every new file, not just assumed fixed: Onshape
    defaults to Draco compression, which needs a matching decoder on the
    app's own side (already vendored, 10.70 - not a per-export toggle
    the person needs to remember, just worth knowing why an
    un-flagged-off compressed export still loads fine) - and if the
    export instructions above ever change to allow it, watch for literal
    SPACES in real part names ("Part 6", "winch 2") surviving into the
    export: THREE.js's GLTFLoader silently sanitizes spaces to
    underscores on load, so a partMap copied verbatim from the raw file/
    instance names will silently match nothing (10.90 - use the
    underscore form in the partMap from the start).

11.3 What actually gets handed over
  The PDF extract (11.1a) plus the finished GLB (11.2) - and, when part
  names came out generic ("Part N", not renamed), an Onshape instance-
  tree screenshot showing each stack's own physical order, described
  top-to-bottom with at minimum the top and bottom part numbers stated
  explicitly (10.87/10.88's LTM 1650 CWT session is the reference
  example - the person named the top and bottom of each stack and
  confirmed the tree's own listing order was the physical order,
  which turned out to let the middle positions be inferred too). This
  screenshot is cheap for the person to produce and removes almost all
  the guesswork on the receiving end - worth including as standard
  practice going forward, not just when something's ambiguous.

11.4 What Claude does with it
  - PDF: extract the combination chart, verify every printed combo
    reconciles exactly against the subset-sum of a fixed component list
    (10.2's method) - a combo that doesn't reconcile is a transcription
    error worth re-checking before shipping, not a data quirk to shrug
    off.
  - GLB: confirmed node names always carry the real "Part N" (or
    equivalent) identifiers, whether or not Onshape ever let them be
    renamed to something descriptive (10.88) - the ORIGINAL "-low"
    LTM 1650 export was avoided specifically for lacking this. When an
    instance-tree screenshot is available (11.3), cross-check the
    person's stated order against the model's own real geometry
    (world position per axis, mesh bounding-box size/thickness) before
    trusting either source alone - this is what caught that the tree's
    listing order really did match physical stack order, and separately
    confirmed a plate the person flagged as possibly-duplicated (a <2>
    instance count in the Onshape tree) was genuinely a single physical
    part (10.88).
  - Build the partMap from confirmed identifiers only, matching the
    app's own existing component ids (a stack's two matching plates -
    left and right - map to the SAME id, same "×2" pairing the 2D
    diagram already uses, not two separate ids).
  - Ship with preExploded: true (11.2) and verify with an ACTUAL CLICK
    test before calling it done - not just "no console errors." A wrong
    node-name assumption can leave every namedPart wired to a single
    wrong id while the model still loads and renders with zero console
    errors (10.90 - this exact failure mode shipped once and only
    surfaced when the person tried actually using it). Click a plate,
    confirm the Total Counterweight number itself changes correctly,
    not just that something highlights.
  - Regression-check every OTHER crane's 3D CWT view after any change to
    cwt3d.js itself (not just the new crane) - several of the real bugs
    caught this way (10.75's Group-vs-Object3D gap, 10.89's perspective-
    depth fit bug, 10.90's Mesh-type gap) were found specifically by
    sweeping the existing fleet after a change made for one crane, not
    by testing that one crane in isolation.

To ask for this workflow to run on a new crane, the person doesn't need to
re-explain any of the above - just say which crane, and provide (or point
to) the two source documents from 11.1. Nothing more is needed for Claude to
follow this section directly.

================================================================================

12. SOURCING A BRAND-NEW CRANE: WHAT'S PUBLIC VS WHAT NEEDS THE FULL MANUAL
    [CONVERSATION]
--------------------------------------------------------------------------------
Use this to triage what to ask the person for BEFORE starting on a crane
that isn't in the app at all yet, ahead of running section 9 (reeving) or
section 11 (CWT) - it's about which document covers which tab, not how to
process any one of them once in hand.

12.1 The public Liebherr product page (no login, e.g.
     liebherr.com/.../mobile-cranes/ltm-mobile-cranes/ltm-<model>) has a
     Downloads tab with a "Technical Data - Mobile crane <model> [m/t]" PDF
     - the same document TYPE as an OEM dimension sheet (the "Maße" page
     style - overall dimensions, axle loads, outrigger geometry). This
     alone is enough for:
       - Outrigger Support Positioning (P1-P4 figures, always read at
         100% extension - the person's own standing rule, confirmed
         independent of any specific crane).
       - Rope Retentioning (drum length/pull are typically in this sheet
         too).
     This PDF was NOT verified by directly fetching the live page in this
     conversation - WebFetch 404'd on it (Liebherr's site likely blocks or
     requires JS rendering this tool can't do), so what it contains was
     taken from the document TYPE's established pattern, not a live check
     of that specific page. Try fetching it directly when this actually
     comes up; if it 404s again, ask the person for the PDF itself rather
     than assuming the pattern holds.

12.2 What the public page does NOT reliably cover, confirmed by the
     person directly rather than assumed:
       - CWT Combinations: the product page's "ballast systems" section
         may show an exploded-view image (useful for the 2D diagram's own
         geometry/plate layout, same role as the artwork described in
         10.4), but not the actual combination-validity table - which
         specific plate combos are OEM-approved, at what weight. That
         table has come from the full Operator Manual's own
         "Counterweight" chapter for every crane in this app so far (see
         11.1a) - a public summary sheet isn't the same document.
       - Reeving Plan: the EINSCHERPLAN diagrams (section 9's own source)
         are an Operator Manual document too, not something the public
         product page carries.
       - 3D CAD models / GLB exports: never public - always the person's
         own Onshape work (section 11.2), regardless of crane.

12.3 Practical upshot for a brand-new crane
     Ask for the Operator Manual PDF (unlocked, per 11.1a) from the start,
     alongside/instead of just a product-page link - don't wait to
     discover the CWT/reeving gap after already starting on the parts the
     public page DOES cover. The public Technical Data sheet is still
     worth having even so: it can very likely be worked from independently
     for the Outrigger and Rope Retentioning tabs while the fuller manual
     extraction (CWT, reeving) happens in parallel, rather than gating
     everything on one document.

================================================================================

13. DRIVING W/ EQUIPMENT: FINDING THE OUTRIGGER EXTENSION % STAGES
    [CONVERSATION]
--------------------------------------------------------------------------------
For the Driving w/ Equipment tab's own request: showing each chart row's
required outrigger extension as a percentage (100/75/50/25/0) alongside the
raw length, matching the crane's actual PINNED support-base positions - the
ONLY positions Liebherr outriggers can physically be operated at (person's
own confirmed domain fact, not an assumption).

13.1 Best source: the manual has a table for this directly - check here
     FIRST
  Confirmed live on LTM 1160-5.2's operator manual, exact navigation path
  (from the PDF's own contents panel, not guessed): Operating Instructions
  -> 1 Description of Crane -> 1.03 Technical Data -> 3 Support ->
  3.1 Support Base. The table there ("Support base" / "Extension condition
  [c1 to c4]" / "Length [a]" / "Width [b]") gives the percentage DIRECTLY,
  already labeled - no derivation, no interpolation, no risk of confusing
  it with an unrelated dimension. LTM 1160-5.2's confirmed values:
    0%   ->  9.18m x 2.45m
    50%  ->  9.18m x 5.38m
    75%  ->  9.18m x 6.84m
    100% ->  9.18m x 8.30m
  Only 4 stages exist for this crane (no 25%) - confirmed two independent
  ways: this table, AND a separate place elsewhere in the same manual the
  person found listing only 100/75/50/0. Don't assume every crane has all
  five round-number stages; read what's actually there. Note Length [a]
  stays fixed at 9.18m across every stage here - only Width [b] changes
  with extension - worth checking whether that holds for every crane or
  is specific to this one's beam layout (e.g. a crane with front AND rear
  telescoping beams, not just left/right, could vary both dimensions).
  "1.03 Technical Data" (with this same "3 Support" numbering) is
  reasonably likely to be a STANDARD chapter/section structure across
  this manual series, not a one-off - worth checking this exact path
  first on any future crane's manual before falling back to 13.2.

13.2 Fallback: derive it from the general dimension sheet if 13.1's table
     isn't present
  The overall min-max range printed on the crane's dimension sheet (e.g.
  "2446-8306") gives the 0% and 100% endpoints directly. Intermediate
  values printed on the SAME dimension stack can be checked against
  simple linear interpolation - value = min + (max-min) x fraction -
  BEFORE trusting them as a stage: confirmed working (exact match) for
  LTM 1160-5.2's own 50%/75% figures cross-checked this way, matching
  13.1's table to within a few mm (rounding only).
  Real trap hit and caught during this exact exercise: a THIRD number in
  the same dimension stack (4270) did NOT fit the linear pattern as 25%
  (expected ~3911) - turned out to be the width of the CWT slabs, an
  entirely unrelated dimension that happened to sit in the same visual
  area of the drawing. A number that doesn't fit the linear check is a
  signal to ask what it actually is, not to force it into the pattern
  or discard the check. This is exactly why 13.1 (a table that states
  the percentage outright) is the preferred source whenever it exists -
  this derivation method is a fallback, not the first move.

13.3 Confirmed fleet-wide table (all 7 relevant cranes checked)
  Sourced from each crane's own operator manual via the 13.1 navigation
  path ("1.03 Technical Data -> 3 Support -> 3.1 Support Base"), confirmed
  standard across the whole LTM series - present and at that exact path
  in every LTM manual checked. Width [b] at each extension %, Length [a]
  fixed per crane (varies by crane, doesn't vary with extension %) unless
  noted otherwise:

    Crane          0%      25%     50%     75%     100%
    LTM 1110-5.1   2.4m    -       *5.0m   *7.0m   *7.0/8.3m
    LTM 1130-5.1   2.40m   -       5.00m   -       7.50m
    LTM 1160-5.2   2.45m   -       5.38m   6.84m   8.30m
    LTM 1250-5.1   2.70m   -       5.50m   6.90m   8.30m
    LTM 1300-6.2   2.68m   -       5.60m   7.06m   8.53m
    LTM 1650-8.1   2.62m   4.36m   6.10m   7.84m   9.58m

  LTM 1130-5.1 and 1160-5.2 only have 3-4 stages, not 5 - don't assume
  every crane has 25% and 75%; read what the table actually lists.

  * LTM 1110-5.1 is NOT symmetric front/rear like the rest of the fleet -
  its own manual table gives named positions instead of one clean %
  column, because front and rear support width differ at the same
  nominal stage:
    Retracted     -> rear 0%  / front 0%,   both 2.4m
    Reduced       -> rear 50% / front 55%,  rear 5.4m / front 5.0m
    Reduced wide  -> rear 75% / front 100%, rear 6.85m / front 7.00m
    Wide          -> rear 100%/ front 100%, rear 8.30m / front 7.0m
    Variable      -> continuously adjustable 0-100% both sides
  RESOLVED: the app's existing Driving w/ Equipment chart data for this
  crane (DRIVING_EQ_DATA['1110']) was sourced from a genuinely separate
  document - a per-crane "Driving with the equipment in place" book
  (chapter 15.05, own BAL number, own folder:
  Driving_with_the_equipment_in_place/, sibling to Operating_instructions_
  Crane/, NOT inside the main operator manual PDF at all). Downloaded and
  scanned it directly: every chart in that book's 122 pages uses a
  "Support width (min.B)" column with only two values across the entire
  crane, 2.45m and 5.00m - never 5.4m. Since 5.00m matches the Technical
  Data table's FRONT designation exactly (not the rear's 5.40m), the
  chart data already reflects the front-axle number, so it maps cleanly
  to 55%, not 50%. app's OUTRIGGER_STAGE_TABLE['1110'] = [[0,2.40],
  [55,5.00]] - no front/rear split needed after all, because the source
  chart itself never varies the number by boom direction, only the
  Technical Data summary table does.
  General lesson: when a crane's own "Support Base" table (13.1) looks
  asymmetric or ambiguous, check whether the SAME manual has a separate
  "Driving with the equipment in place" book/chapter before assuming the
  ambiguity carries through to the Driving w/ Equipment feature - the
  driving chart may already have picked a single number for you.

  LTR 1220 was checked and confirmed OUT OF SCOPE for this feature
  entirely, not a data gap: it's a crawler crane, riding on fixed-width
  crawler tracks rather than extendable outrigger beams. Its manual has
  no "Support Base" chapter at all - the closest analog is a separate
  "1.03.10 Outrigger pads" section, which covers ground-bearing mats for
  load distribution, an unrelated concept. Confirmed directly by the
  person too ("it uses tracks not outriggers") - don't go looking for a
  support-base % table on this crane again.

13.4 Renamed the app's outrigger corners from P1-P4 to C1-C4 to match
     Liebherr's own numbering
  The app's outrigger-pad code (cadFleetData, the Outrigger Pad Positions
  tab's toggles) had its own P1-P4 naming from before this session,
  already correctly assigned to physical corners: P1=Rear Right,
  P2=Front Right, P3=Front Left, P4=Rear Left. Checked this against
  Liebherr's own numbering, found directly in LTM 1130-5.1's manual,
  chapter 3.05 "Crane at the job site", section "1.2 Numbering of
  support" (confirmed as standard chapter numbering, same place other
  cranes' manuals define it): "1 Support right rear / 2 Support right
  front / 3 Support left front / 4 Support left rear" - this is the same
  numbering shown on the crane's own BTT/LICCON display. It matches the
  app's existing P1-P4 assignment exactly, corner for corner, which is
  also why the Technical Data "3.1 Support Base" table's "Extension
  condition [c1 to c4]" column groups c1/c4 as "Rear" and c2/c3 as
  "Front" (see 13.3) - same underlying numbering, just Liebherr's own
  "c" shorthand for it in that chapter. Because the assignment was
  already identical, this was a pure relabel (P1->C1, P2->C2, P3->C3,
  P4->C4: object keys P1_RR/P2_FR/P3_FL/P4_RL -> C1_RR/C2_FR/C3_FL/
  C4_RL across all 7 cranes in cadFleetData, the "name" field inside
  each, the two craneObj.P*_* usages in syncCarrier3DOutriggers/
  calcCAD, and the 8 checkbox labels on the Outrigger Pad Positions /
  3D Carrier Preview toolbar) - not a remap, no risk of assigning the
  wrong physical corner. If a future crane's numbering ever turns out
  to run the other rotation direction (some manufacturers do
  front-first), verify against that crane's own "Numbering of support"
  section before assuming this same C1=RR/C2=FR/C3=FL/C4=RL pattern
  holds - it's confirmed standard across the LTM series checked so far,
  not assumed universal.

13.5 LTM 1110-5.1's Driving w/ Equipment display now shows both sides
     of its asymmetric "Reduced" stage
  Per 13.3's front/rear resolution, the Driving w/ Equipment chart's
  5.00m figure for LTM 1110-5.1 is the C2/C3 (front) reading at 55% -
  C1/C4 (rear) sits at 50%/5.40m for that same physical pinned position,
  which the chart's own single "Support width (min.B)" column never
  states. A bare "(55% extension)" label would wrongly read as if all
  four corners were at 55%. Fixed by adding OUTRIGGER_ASYMMETRIC_PAIR
  (keyed by crane + the chart's own quoted width) so the render checks
  for a known asymmetric stage before falling back to the plain
  percentage - when present, shows both readings explicitly: "5.00 m -
  C2/C3 (front): 55% · C1/C4 (rear): 50%, 5.40 m". Only 1110 has an
  entry today, since it's the only crane whose own Support Base table
  splits front/rear at all (13.3) - every other crane in the fleet
  keeps the plain single-percentage line.

13.6 Follow-up: 13.4's P->C rename missed the Visual Site Plan canvas +
     its results table
  13.4 renamed the underlying data (cadFleetData keys, checkbox labels)
  but missed two places that build their OWN "P${...}" label text from
  the numeric item.id rather than reading the renamed object keys: the
  Visual Site Plan canvas (drawCADCanvas - the on-canvas leg labels,
  the tap-for-info box's id/type strings, and the dashed "if moved"
  ghost target label) and the "All Four Legs - Reference Frames" table
  underneath it (both its zero-shift and shifted-state row templates).
  Person caught this directly ("the remaining p1-p4 being used to
  label the out riggers"). Fixed all 8 sites (7 in drawCADCanvas/
  clickableTargets, 1 in the results table). Lesson: a rename pass
  needs to search for the LITERAL STRING PATTERN the display code
  builds from (here, the template `P${o.id}` / `P${item.id}`), not just
  the data structure's own field/key names - display code that
  reconstructs a label from a raw id rather than reading a stored name
  string won't show up in a search for the old key names.

13.7 Added LTM 1110-5.1's remaining T-only Driving w/ Equipment boom
     configs (T-22.6, T-33.8) under Advanced
  Person asked for "all T boom 445/525 tire Driving with equipment
  charts, non-fully-retracted ones under Advanced" - excluding TK,
  TVK, TNZK, TK-2.9 (folding-jib/lattice variants) entirely, plain
  telescopic "T" boom only.
  Source: the same crane's separate "Driving with the equipment in
  place" book (chapter 15.05, see 13.6's discovery of this being a
  genuinely separate document/folder from the main operator manual),
  re-downloaded and read directly rather than assumed from the table
  of contents alone.
  Mapped the book's 24-section table of contents against actual page
  content (chapter-page-N in the footer maps to PDF page N+27 in this
  specific file) and found only 3 sections are plain "T" (not
  TK/TVK/TNZK/TK-2.9):
    - Sections 1/14/15: T-11.5 (0/0/0/0/0/0) - FULLY RETRACTED. Section
      1 ("Description") is only 2 illustrative example rows, not a
      real chart; sections 14/15 are the actual full front/rear chart
      and were confirmed to be the exact source the app's existing
      baseline boomConfig was already built from (23/78 rows matched
      exactly) - nothing new to add here.
    - Sections 2/3: T-22.6 (46/46/46/0/0/0) - not fully retracted.
    - Sections 4/5: T-33.8 (46/46/46/46/46/46) - not fully retracted.
  Added T-22.6 and T-33.8 as new boomConfig entries with advanced:
  true (68/48 and 63/22 front/rear rows respectively), parsed
  programmatically from the extracted PDF text (grouped into 6-field
  row chunks: weight/hookBlock/boomAngle/supportWidth/axleA/axleB)
  rather than transcribed by hand, given the row counts involved -
  verified row-count parity (raw line count divisible by 6, no
  leftover fields) before writing anything into the app.
  Tire clarification: every page in all three T-only sections lists
  ALL three tire sizes (385/95, 445/95, 525/80 R25) against the SAME
  single data table - the tables are tire-agnostic in this specific
  book, they don't split into a separate 445/525-only table the way
  the person's framing implied. This differs from how the OTHER
  cranes' sourceNotes are worded ("chapter 15.05 Charts for 445/95
  R25 and 525/80 R25 tires") - 445/525 there likely reflects the
  fleet's actual fitted tires (525/80 R25 confirmed elsewhere as the
  fleet standard, see the CAD tab's footprint width sourcing) rather
  than a genuine per-tire chart split in every crane's own book. Not
  something to assume holds for every future crane either way -
  check each book directly.

13.8 Fleet-wide check: 1110 was the only gap, everyone else already
     complete
  After 13.7, checked every other crane's own "Driving with the
  equipment in place" book directly (not assumed) for the same thing -
  plain T-boom (excluding TK/TNZK/TVK/TVNZK/TS/TNZF-jib variants),
  not-fully-retracted configs missing from the app. Result: nothing
  else needed adding anywhere in the fleet.
    - LTM 1130-5.1: only ONE T-boom length exists in the entire book
      (T-12.7, already fully retracted) - already in the app.
    - LTM 1300-6.2: all three plain-T sections (T-14.7 front, T-14.7
      rear, T-19.6 front-only) already in the app, verified row-for-
      row exact against the 445/525-tire sub-chapter.
    - LTM 1650-8.1: person explicitly scoped this to "T3 only" (not
      the dozen-plus T3Y/T3N/T3NH/T3YVEN guying-variant sections this
      crane's huge 1213-page book also has for the SAME T-16.7 boom
      length) - both T3 sections (with/without winch 2) already in the
      app, verified exact.
    - LTM 1160-5.2 / LTM 1250-5.1: both use "Outer radius of
      counterweight" labeling instead of boom-length notation because
      their own "T operation" section is a SINGLE boom length
      (T-13.1) with counterweight-radius as the only variable - both
      radius variants already in the app, verified exact.
  LRT 1100-2.1 explicitly excluded from this whole feature, per the
  person: it's a rough terrain crane that generally always drives WITH
  its counterweight fitted - the "driving with the equipment in place"
  concept this feature is built around doesn't apply to it the way it
  does to the LTM (mobile crane) fleet. Not a data gap - don't go
  looking for a Driving w/ Equipment book for this crane.

================================================================================

14. LOAD CHART FINDER: T-BOOM CAPACITY LOOKUP (LTM 1110-5.1)  [CONVERSATION]
--------------------------------------------------------------------------------
New tab: person gives a working radius and a target load, the tool finds
which boom-length/telescoping configurations can lift it, ranked by
margin (top 5), with the exact extension % breakdown (e.g. "0/0/46/0/0/0")
needed to set it up. Scope, per the person's explicit request: T-boom
only (no lattice/folding jib), outriggers fully deployed, 360° slew, with
counterweight and wind-speed as selectable variables. This is a
fundamentally bigger undertaking than the Driving w/ Equipment charts
(section 13) - load charts are the crane's actual lifting-capacity
tables, not a single driving condition, and the source document turned
out to be enormous.

14.1 Source document reality check - 12,292 pages, not a normal manual
  The crane's "Load chart manual" folder has 4 PDFs, one per boom type:
  a "_T" suffix file (plain telescopic, what's needed), "_TK" and "_TVK"
  (jib variants, out of scope), plus an "_R" file (not investigated).
  The "_T" file is 46MB and, per PyMuPDF, 12,292 pages - not a
  hand-authored manual but a LICCON-software-generated exhaustive set,
  one page-group per valid crane configuration. Filtering to exactly
  what was asked for (fully-deployed support base + 360° + plain T, not
  T~ or T*) still leaves 1,532 pages.
  Two symbols the person flagged in advance as things to watch for,
  confirmed via the manual's own "40.62 Operating modes" chapter:
    T   = plain Telescopic boom - the real lifting mode, what's wanted.
    T~  = "Maintenance operation" - NOT a lifting chart at all, used
          only for servicing/lubricating the boom. Excluded.
    T*  = "Telescopic boom with auxiliary device" - requires a special
          rigging accessory assembled on the crane, not the standard
          configuration. Excluded.
  First impression was that this was scanned/image-only (page.get_text()
  on early pages returned near-nothing) - turned out that was just the
  cover/legend pages using embedded JPEGs; the actual data pages (from
  around page 50 onward) have real, extractable text. Confirmed by
  checking get_images()/get_text() on a spread of pages before assuming
  either way.
  Each page is ONE fixed combination of counterweight + wind-speed
  bracket (confirmed via the footer icons: a "0.0t"-style counterweight
  badge and an "11.2 m/s"-style wind badge appear on every page), and a
  full page is one page of MANY needed for that combo (~13 pages each,
  continuing to longer boom lengths across pages, not always in strictly
  ascending order - don't assume page order implies boom-length order,
  just union all columns found across a combo's page range). Full-
  document scan found 39 distinct counterweight levels (0.0t to 29.0t,
  following the crane's own plate-combination steps, not round numbers -
  matches the same weight steps already seen in the Driving w/ Equipment
  data) x 3 wind brackets (11.2 / 13.4 / 15.6 m/s) = 117 combos, all
  present (39 x 3 = 117 exactly, no gaps).

14.2 Extraction method - PyMuPDF find_tables(), not naive text order
  Raw page.get_text() comes out in scrambled reading order on these
  pages (confirmed by inspection - numbers appear in a sequence that
  doesn't match the visual grid at all). PyMuPDF's page.find_tables()
  reconstructs the actual grid structure directly and reliably once two
  real bugs in the first attempt were found and fixed:
    - t.cells (the table's flat per-cell bbox list) is NOT simple
      row-major (row*col_count + col) indexing - it has merges/gaps
      (404 entries for a 28x15=420 grid). The correct API is
      t.rows[r].cells[c], which returns properly ordered bboxes.
      Using the wrong indexing silently paired every capacity value
      with the WRONG cell's bbox, corrupting every radius assignment
      it touched - caught by comparing extracted values against a
      rendered image of the same page, not just checking the code ran.
    - The 6 "telescopic section extension %" rows were being detected
      by matching a column-0 label starting with "1" and containing
      "\n2" - which false-matched the "18.0\n20.0" RADIUS row (also
      starts with "1", also contains "\n2" via its second line "20.0").
      Fixed by matching on the "%" character instead, which only ever
      appears in the real extension-row label ("1\n2\n3\n4\n5\n% 6").
  find_tables() also merges pairs of adjacent radius rows into one
  detected table row (e.g. "3.0"/"3.5" show as one cell with a
  newline-joined pair of values) - can't just split-and-zip pairwise,
  because a column's data sometimes only covers ONE of the two radii
  (found a real case: the 26.4m boom column had a capacity at r=3.5 but
  nothing at r=3.0, and naive pairwise splitting would have assigned
  that lone value to the wrong radius). Fixed properly by using each
  detected cell's own bbox to re-query page.get_text("words", clip=bbox)
  and matching each value's actual y-coordinate against the cell's own
  vertical midpoint, rather than assuming order - verified this exact
  edge case by checking the value's real y0 against the two candidate
  radius label's y0 directly (matched the lower one, i.e. r=3.5, exactly
  as the rendered page showed).
  Verification method throughout: render the actual PDF page to a PNG
  (page.get_pixmap(dpi=200)) and compare every extracted number against
  it directly, not just spot-checking a few values against expectation.
  Did this twice, on two different (counterweight, wind) combos,
  including checking a mid-range page (page 6 of 13) to confirm
  multi-page continuation extracts cleanly too - both matched the
  rendered image exactly, including the sparse-cell edge case, before
  running the full extraction.

14.3 Final extraction numbers
  1,532 pages processed, 0 errors. 117 (cwt, wind) combos, 20,044
  distinct boom-length/extension-config columns, 364,647 individual
  radius->capacity data points. Compacted (numeric radius keys instead
  of string dict keys, [radius,capacity] pairs instead of nested
  objects, extension values split into [percent, isPinned] pairs) to
  5.58MB JSON, stored at loadchart/ltm1110-T-fulldeploy.json - each
  combo also carries its own chart code/reference (e.g. "0195" /
  "T197.001.12076", confirmed constant across all pages of one combo)
  so a result can be cross-checked against the physical/PDF chart.

14.4 Why this data is NOT inline in index.html like everything else
  Every other tab's data (COUNTERWEIGHT_DATA, DRIVING_EQ_DATA, etc.) is
  baked directly into index.html as JS. At 5.58MB for a single crane's
  single scope, doing that here would bloat every page load for a tab
  most visits never open. Fetched on demand instead - same lazy-load
  pattern already used for reeving diagrams (ensureReeveManifestLoaded):
  fetch('./loadchart/<file>.json'), cached in a module-level variable
  after first load, picked up by the service worker's normal
  CONTENT_CACHE fetch-and-cache-on-demand behavior with no sw.js changes
  needed (APP_SHELL precaching is the wrong tool here - CONTENT_CACHE's
  existing behavior already does exactly the "keep it once fetched"
  guarantee wanted).

14.5 Radius rounding - round UP, never interpolate, with a visible notice
  Chart radii are discrete, non-uniform steps (0.5m from 3-5m, 1m from
  5-16m, 2m from 16-40m) - not every possible radius is tabulated.
  Person's explicit call when asked: always round the requested radius
  UP to the next tabulated row (never interpolate), because that can
  only ever understate capacity relative to what was actually asked
  for, never overstate it - the safe direction for safety-critical data.
  Also explicitly asked for a visible notice whenever this rounding
  happens, so it's never silent - implemented as a dismissable-looking
  but always-shown banner stating both the requested and actual radius
  and why. loadChartRoundUpRadius() implements this as a simple ascending
  scan (radii list is stored pre-sorted) - first tabulated value >=
  requested wins.

14.6 Two counterweight modes
  "I have this fitted" (default/manual): person picks their actual
  counterweight from the 39 real levels this crane can build, results
  are scoped to exactly that level only.
  "Find minimum needed" (advanced, person's own request - "recommend
  the minimum cwt required to hit the highest capacity"): searches the
  39 levels ascending and stops at the FIRST (lowest) one where at
  least one config clears the target load at the rounded radius,
  reporting that level explicitly ("Minimum counterweight needed to hit
  this target: X t") alongside the same top-5-by-margin display. Not
  "the CWT that gives the single highest capacity overall" - scoped to
  the person's actual radius+target query, same as manual mode, just
  without them having to pick a starting counterweight and iterate by
  hand.
  Verified the browser-side query logic against the source JSON
  directly (not just by eyeballing the UI) - looked up the exact same
  (boom=22.6m, cwt=0.8t, wind=11.2m/s, r=10m) cell in the raw extracted
  data via a separate script and confirmed it matched the app's own
  "Option 1" result (16.9t) exactly before considering this done.

14.7 Site capacity rule (90%) and rigging weight deduction
  Two follow-up requests, both about NOT taking the printed chart figure
  at face value:
  1. Mine sites the person works on commonly cap usable capacity at 90%
     of the chart rating, not 100% (100% is where the LICCON overload
     alarms trigger). Person's own worked example, checked before
     building: chart says 2.4t at some radius, need to lift 2t, 90% of
     2.4t = 2.16t, which still clears 2t - math confirmed correct
     (2.4 x 0.9 = 2.16) before implementing. "Site Capacity Rule" toggle
     (100%/90%) multiplies the raw chart capacity before it's compared
     against the target - never interpolated or hidden, both the raw
     chart figure AND the derated "usable capacity" are shown in each
     result when the 90% rule is on, so the OEM number stays visible
     for cross-checking alongside the derated one actually being used.
  2. Rigging weight: a hook block (and a second block if winch 2 is
     rigged) hangs static off the boom tip before the actual load is
     even attached, and that weight counts against the chart's capacity
     too. This isn't just site practice - the OEM load chart manual's
     own "40.02 Basic information" section states it explicitly ("The
     weights of the load lifting equipment and the fastening equipment
     must be deducted"), confirmed by re-reading that exact section
     before telling the person this was correct. Implemented as:
     target load + block weight(s) = "effective target" the crane must
     actually clear, checked against the (possibly-derated) chart
     capacity. Mathematically identical to "deduct rigging weight from
     capacity instead" - this framing was chosen so the number the
     person types into "Target Load" always means exactly what they
     typed, with the rigging math shown as an explicit, separate line
     above the results rather than silently folded into either number.
  Block weights are NOT a new hardcoded list - reused FLEET_BLOCK_SPEC
  (same fleet block catalog the Minimum Hook Block Weights tab already
  draws its recommendations from), so there's one source of truth for
  this crane's actual block fleet rather than a second, potentially
  drifting copy. Base block weight only (no auxiliary ballast sub-
  selection, unlike that tab's own aux weight lists) - this is about
  static rigging weight, not hook-block sizing.
  Winch 2's block is a second, independently-selected block from the
  same fleet list, shown only when the "Winch 2 fitted" checkbox is on,
  and its weight is simply added to the primary block's - both blocks
  are static weight on the boom tip if both are rigged, regardless of
  which one is actively hoisting.
  Verified every combination by hand before shipping: baseline (no
  derate, no rigging) 16.90t capacity / 11.24t effective target (10t +
  1.24t block) = 5.66t margin; with 90% derate, usable capacity
  16.90 x 0.9 = 15.21t, margin 15.21 - 11.24 = 3.97t; swapping to a
  lighter 0.90t block dropped the effective target to 10.90t, margin
  6.00t; adding winch 2 + a 0.45t second block brought rigging to
  1.35t, effective target 11.35t, margin 5.55t - all four recomputed by
  hand and matched the rendered UI exactly, zero console errors.

14.8 HOTFIX: whole-number counterweights (0.0/11.0/17.0/29.0t) silently
     returned "no configuration" even when the data had a good match
  Person hit this directly - r=26m, 2t target, 29.0t counterweight (the
  crane's actual max) returned no results at all, despite the raw data
  having a config good for 8.5t at that exact cell (verified directly
  against the JSON before assuming it was a UI bug rather than a
  genuine "physically can't be done" result).
  Root cause: the combo lookup built its key with a plain template
  literal, `${cwt}|${wind}`. The JSON's own combo keys were written by
  Python as str(float) - e.g. "29.0", "11.0", "0.0" - always with the
  decimal point, because that's how compact_data.py originally split
  and rejoined the classify-pass keys. JS's default Number->string
  coercion drops the trailing ".0" for whole numbers (String(29) ===
  "29", not "29.0"), so the browser-built key "29|11.2 m/s" silently
  missed the real "29.0|11.2 m/s" entry - `if (!combo) continue` just
  skipped it with no error, indistinguishable from a genuine "physically
  impossible" result. Every fractional counterweight (0.8, 3.3, 6.9,
  etc.) coincidentally worked fine, because JS's coercion of THOSE
  numbers happens to match Python's - the bug only ever hit whole-number
  levels: 0.0, 11.0, 17.0, 29.0 out of the 39 total.
  Fixed by using cwt.toFixed(1) to build the key instead of bare string
  interpolation - matches the data's one-decimal-place format for every
  value in the set, not just the ones that happened to coincide.
  Verified against the person's exact reported inputs (r=26m, 2t,
  29.0t CWT, 90% rule, 63t block + winch 2 + default second block)
  before and after the fix - "no configuration" before, 5 real results
  after, top one at 3.91t margin, matching the raw-data cross-check
  (8.5t chart capacity x 0.9 - 3.74t effective target = 3.91t) exactly.
  Also re-verified auto (find-minimum-CWT) mode separately, since it
  iterates the same whole-number levels internally - correctly skips
  0.0t and lands on 6.9t as the minimum for a similar query, 0 console
  errors either way.
  Lesson: never build a data lookup key from a bare template literal
  across a language boundary (Python-written JSON, JS-read at runtime)
  without checking that both sides format numbers identically - a
  silent miss (falls into the same code path as "genuinely no match")
  is far more dangerous here than a thrown error would have been, since
  it's indistinguishable from a correct negative result without
  independently checking the source data.

14.9 Excluded live-telescoping configs from the recommendation list
  Person explained a "-" on a telescopic section (e.g. an extension
  breakdown like "0+/0+/0+/-100/0+") means that section may telescope
  UNDER LOAD up to that %, not that it must be pinned there. These are
  niche charts for obstacle-avoidance work - telescope in past an
  obstruction, place the load, telescope back out while still under
  load - a specialised rigging technique, not a normal boom
  configuration a crew should be steered toward by default. This
  confirms and extends 13.6/13.7's own "+" = must be pinned, "-" =
  can telescope under load" reading of the OEM notation - already
  extracted and stored per-section (isPinned true/false in each
  column's ext array) since section 14's original build, just not yet
  filtered out of the results shown.
  Per the person's explicit request, any column with at least one "-"
  section is now excluded from ALL load chart finder results (manual
  and auto/find-minimum-CWT mode alike) - not hidden behind a toggle,
  removed outright, matching how T~/T* operating modes are already
  fully out of scope for this feature (see 14.1) rather than offered
  as an opt-in. Swept both modes across several (radius,
  weight, counterweight) combinations, including the whole-number
  counterweight values 14.8 just fixed, confirmed zero "-" markers
  survive into any displayed result and each query still returns a
  full set of options (the filter isn't so aggressive it starves
  results) - 0 console errors.

================================================================================

15. LOAD CHART FINDER: MULTI-CRANE EXPANSION  [CONVERSATION]

Person asked for the same Load Chart Finder feature (section 14) extended
to the rest of the fleet - LRT 1100-2.1, LTM 1130-5.1, LTM 1160-5.2,
LTM 1250-5.1, LTM 1300-6.2, then LTM 1650-8.1 last, in that order. 1160/
1250/1650 additionally need a ballast (counterweight tray) radius
selector, and 1650's auto/find-minimum-CWT mode needs that selector
exposed too, not just manual mode. This section covers LRT 1100; later
cranes get their own subsections as they're built.

15.1 Source manuals for this expansion do NOT come from re-uploads
  The person's answer to "where are the source PDFs" was "a link/location
  you already have access to" - this is the person's own Nextcloud share
  (local.idull.au, public share token in the URL, WebDAV endpoint
  /public.php/dav/files/<token>/), the SAME place section 14's 1110
  "_T" file was originally pulled from mid-conversation (a curl -u
  "<token>:" against a `local.idull.au/.../Load_chart_manual/*_T.pdf`
  path, found by grepping this session's own transcript for the earlier
  download command - the file itself doesn't persist in the container
  between turns, only the fact that it was fetched from there does).
  Every crane folder follows the same layout:
    Liebherr cranes/<MODEL>/[OPERATIONS MANUAL - Serial # NNNNNN/[<serial>
    _<model>_v100_en/]]Load_chart_manual/*.pdf
  (the extra "OPERATIONS MANUAL - Serial #.../<serial>_..._v100_en/"
  nesting level is present for some cranes and not others - LTM 1160/1650
  skip straight to Load_chart_manual, LRT 1100/1130/1250/1300 have it -
  don't assume a fixed depth, PROPFIND (Depth: 1) each level and follow
  what's actually there). Confirmed via WebDAV PROPFIND that all six
  requested cranes have a Load_chart_manual folder before starting any
  extraction work.
  Each Load_chart_manual folder holds one PDF per BOOM MODE (a "_T" file
  = plain telescopic, wanted; "_TK"/"_TVK"/etc = jib variants, out of
  scope; a "tlt-R_..." file with no mode suffix = a different chapter,
  not investigated, same as 14.1's "_R file"). Sizes vary hugely by
  crane - LRT 1100 has one 3.2MB file with no mode suffix at all (this
  crane's boom system only has one main-boom type, "T", so there's
  nothing to disambiguate - confirmed via its own 40.60/40.62 chapters,
  15.2 below); LTM 1650's "_T3" file is 8.9MB but sits next to sibling
  files up to 204MB for other T3-variant modes (T3N, T3F, etc) - the
  person confirmed T3 (not T3Y/T3N/T3F/...) is the one wanted before any
  of those larger files were downloaded.

15.2 LRT 1100-2.1 - much smaller scope than 1110, and two behaviors flip
  184 pages total, ONE file (no boom-mode suffix needed - chapter 40.60
  "Short description of component groups" confirms this crane's only
  main-boom type is "T", auxiliary boom type "K" for an optional folding
  jib; chapter 40.62 confirms the only main-boom operating mode is
  literally "T", no T~/T* equivalents to exclude). Chapter 40.90 "Load
  charts" (data pages) runs pages ~111-183.
  Support base ALSO varies on this crane (7.30m / 5.50m / 2.69m widths,
  all at a fixed 7.48m other dimension) - the same "fully deployed
  outriggers" scope from 14.1 applies here as "widest support base"
  (7.30m); the narrower two are out of scope, same reasoning as excluding
  T~/T*. Combined with 3 counterweight levels (0.0/8.0/14.0t) and fixed
  360 degree slew, in-scope T-mode data is only 3 combos (chart refs
  T273.00011/00012/00013), 2 pages each - about 40x smaller than 1110's
  117-combo, 1532-page job. Confirmed with a full-page classification
  pass before extracting anything, not assumed from the folder listing.
  Two structural differences from 1110, both confirmed against this
  crane's OWN "40.65 Description of the load chart" icon-legend chapter
  before writing any extraction logic - do not assume 14's conventions
  transfer to a new crane's manual without re-reading its own legend:
    a) Wind speed is per-COLUMN here, not a page-level bracket. Every
       boom-length/extension config has its own "maximum permissible
       wind speed" printed in the same table (a "m/s" row under the four
       extension-% rows), varying column to column on the same page
       (e.g. 14.3/14.3/12.8/12.8/11.1/... across one page's 12 columns).
       There is no fixed set of wind brackets to toggle between for this
       crane, so this data's meta.windMode = 'column' (vs 1110's
       implicit 'page' - the field defaults to 'page' when absent so
       1110's existing JSON needed no changes). index.html branches on
       this: 'column' mode hides the wind toggle entirely, shows an
       explanatory note instead, and displays each result's own wind
       rating as an informational line rather than filtering on it.
    b) The pin/live-telescope notation is INVERTED from 1110's. This
       crane's own legend states it explicitly (40.65, "Extension
       condition of telescopic sections"): "+" suffix = section MUST be
       pinned (normal case); NO suffix at all (a bare number, e.g. plain
       "50") = section must NOT be pinned, i.e. live-telescope-under-
       load - the exact concept 14.9 excludes, just flagged with the
       opposite character. Person's call after seeing this: "treat all
       lrt1100 as pinned i dont think there is any true unpinned
       sections" - so extraction records isPinned=true unconditionally
       for every LRT 1100 section regardless of the raw +/bare symbol,
       rather than reading and filtering on it. This means 14.9's
       exclusion filter (ext.some(e => e && e[1] === false)) naturally
       never fires for this crane's data without needing a code branch -
       it only ever produces true.
  Also found two footnote codes (107 = loads above 69.9t need an
  auxiliary block, 108 = loads above 80.4t need an auxiliary device) that
  appear on every T-mode page's top-left corner as a standing warning
  icon. Read literally, this is a rigging/hook-capacity constraint
  ("above X tonnes you need a bigger block"), not a boom-configuration
  restriction like T*'s "requires a special rigging accessory" (14.1) -
  chose NOT to exclude on this basis, unlike T~/T*/live-telescope. Both
  thresholds are well above what this crane's realistic lift range
  covers, so this wasn't pursued further (no per-cell threshold check
  wired in) - flagged to the person as a judgement call rather than
  silently decided either way.

15.3 Extraction: find_tables() worked directly, one bug found and fixed
  Unlike 1110, page.get_text(sort=True) alone produces a fully readable,
  correctly-ordered table for this crane's PDFs (no scrambling) - but
  find_tables() was still used for the actual extraction since it
  handles the sparse-cell column alignment correctly without needing
  1110's bbox/y-position fallback machinery (that fallback code was kept
  in the script as a defensive path, just never triggered on this
  crane's data - confirmed by not seeing it fire during extraction).
  One real bug, caught before it reached the shipped JSON: the four
  extension-% rows plus the wind-speed row (labelled "1"/"2"/"3"/"4"/
  "m/s" in the chart's own left-hand column) are FIVE separate rows in
  find_tables()'s output, each with its own per-column values - but only
  their shared label cell (column 0) gets merged into one newline-joined
  "1\n2\n3\n4\nm/s" block by find_tables(), unlike the radius rows above
  them where every column's cell is what's newline-joined. First attempt
  wrongly assumed the same merge pattern applied to all 13 columns of
  this block (not just column 0), which silently produced only 1 of the
  4 real extension values per column and left wind rating as None for
  every row. Caught immediately by cross-checking parsed output against
  the same page rendered as an image (page 120, T273.00013) before
  building the full script - not just checking the script ran cleanly.
  Fixed by reading raw[ext_start + r] as five independent rows directly
  rather than trying to split column 0's merged text and reapply it
  elsewhere.
  Verified the corrected extraction against three separate pages spanning
  all three combos (visual render of one page's full grid, plus two more
  combos' raw JSON cross-checked cell-by-cell against the manually-read
  page text) before shipping - every boom length, extension %, wind
  rating, and radius/capacity pair matched exactly, including the
  two-page "page 2 of 2" continuation columns (a single 50.0m boom-length
  column each, merged into the same combo as its 12-column first page).
  Final size: 3 combos, 39 columns, 546 datapoints, ~9.5KB JSON - reuses
  the same lazy-fetch loadchart/<file>.json pattern as 1110 (14.4),
  wired into the existing LOAD_CHART_FILES/LOAD_CHART_CRANE_LABELS maps
  with no other index.html cranes touched. FLEET_BLOCK_SPEC already had
  an '1100' entry from an earlier feature, so the hook-block/Winch 2
  selectors needed no new data, just picked it up automatically.
  Regression-checked LTM 1110 in the same tab after these changes (wind
  toggle still page-bracket mode, reeving column still shows, CWT lookup
  still correct) - 0 console errors, matches pre-change output exactly.

15.4 LTM 1130-5.1 - per-column wind confirmed as the norm going forward,
     and a genuinely different extension-row layout in the same PDF family
  2770 pages, one file (no boom-mode suffix - like LRT 1100's file, this
  one's own 40.60/40.62 chapters confirm main-boom types are just "T"
  (telescopic) with an optional "V" telescopic-extension add-on, "K"/
  "NZK" for auxiliary jib variants; scope stays plain "T", nothing else).
  Person confirmed after seeing LRT 1100's per-column wind rating that
  most of the remaining fleet (1130/1160/1250/1300) will read the same
  way, with 1650 the one exception to check for when that crane comes
  up - matches what this crane's own charts show (a "m/s" row varying
  per column, not a fixed page-level bracket) and its own 40.65 icon
  chapter confirms the standard "+" = pinned / "-" = can telescope under
  load reading (the SAME way round as 1110, not LRT 1100's inverted
  version - re-read each crane's own legend chapter regardless, per
  14.1/15.2's rule, rather than assuming either convention carries over).
  Support base also varies here (2.40m / 5.00m / 7.50m widths) - unlike
  LRT 1100 though, the narrower widths aren't just narrower, they also
  cap slewing to a restricted ±10° arc instead of full 360°; only the
  widest (7.50m) width offers 360°, so "fully deployed outriggers" and
  "360° slew" are the same filter on this crane (confirmed - not
  assumed - by checking the slew badge on both the 5.00m and 7.50m width
  pages directly). A first classification pass that only kept (cwt,
  outrigger-width) as its dedupe key silently merged the 360° and ±10°
  page sets together under one key (a broken regex that always matched
  '?' for slew, not caught until an actual side-by-side page comparison
  showed near-identical-but-not-quite capacity numbers between two
  "same key" page groups) - fixed by adding slew to the key before
  trusting any combo count from the scan. Final in-scope set: 28
  counterweight levels x 10 pages each = 280 pages (chart refs
  T205.00301 through T205.00328) - a full-document scan confirmed this
  before extracting anything, not assumed from a partial sample.
  This crane's PDF pages ALSO print hoist rope reeving per column (a
  "* n *" row, strand count per column) - LRT 1100's chart doesn't have
  this row at all; the extraction script reads it when present and
  leaves the JSON's reeving field null when it isn't, same field either
  way.
  One real structural difference from both prior cranes' extension-row
  layout, caught by comparing find_tables() output against a rendered
  page before writing the full script (not assumed to match either
  prior crane): this crane has 5 telescopic sections, and while their
  labels ('1'/'2'/'3'/'4'/'5') are merged into one stacked label cell
  same as before, the actual DATA rows for the block came out grouped
  at 2-then-2-then-1 sections per raw table row (not "1 row per
  section" like LRT 1100, and not "1 merged pair per row" like the
  radius rows above it) - confirmed against a page render (T205.00325,
  column 12: section 3 correctly a "0-" live-telescope marker, matching
  both the JSON output and the rendered chart pixel-for-pixel). Rather
  than hardcode this grouping (which could easily be a THIRD shape on
  the next crane), the extraction script was rewritten to a general
  algorithm: read the real section count from the label cell's own
  digit tokens, then walk forward summing each raw row's own stacked-
  value depth (however many values are actually stacked in that row's
  cells) until every section is accounted for, whatever the grouping
  turns out to be - verified this generalized form still extracts LRT
  1100's simpler 1-row-per-section layout correctly too (same formula,
  depth=1 falls out naturally), so no per-crane branch was needed.
  Verified extraction against two separate pages (T205.00325 columns 1
  and 12, one all-pinned and one with a real "0-" live-telescope marker)
  cross-checked pixel-for-pixel against rendered images before shipping
  - radius/capacity values, wind ratings, reeving counts, and every
  section's pin state all matched exactly, including the "-" case this
  crane actually needed (unlike LRT 1100, where the person said to
  treat everything as pinned - here the exclusion filter from 14.9 has
  real work to do: 2,492 individual unpinned-section instances found
  across the full dataset before any filtering).
  Final size: 28 combos, 3,752 columns, 60,177 datapoints, ~980KB JSON.
  Wired into LOAD_CHART_FILES/LOAD_CHART_CRANE_LABELS with no changes
  needed elsewhere (FLEET_BLOCK_SPEC already had an '1130' entry).
  Regression-checked LRT 1100 and LTM 1110 in the same tab afterward -
  0 console errors, both still matched their pre-change output.

15.5 LTM 1160-5.2 - first crane needing the ballast (counterweight tray)
     radius selector, and its own new T*-equivalent exclusion
  1495 pages, one file (main boom types just "T"/"V" again, same as
  1130 - no per-mode file split needed). This crane's own 40.65 icon
  chapter confirms the standard "+"=pinned/"-"=live-telescope reading
  again (not LRT 1100's inverted one) - checked directly, not assumed
  from 1130 just because both are non-LRT Liebherrs.
  Found this crane's OWN T*-equivalent: a handful of pages (3, out of
  ~1386 T-mode pages) print operating mode as "T *)" instead of plain
  "T" - a footnoted variant tied to a single-position, 0°-slew,
  extreme-capacity chart (160t at 3m radius on one page checked) that
  reads exactly like 1110's T* ("requires an auxiliary device/special
  rigging accessory", 14.1) even though the footnote symbol itself
  ("*)" vs 1110's literal "*" suffix) differs - excluded on the same
  basis, checked by tallying mode-label counts across the whole
  document before writing any extraction logic, not assumed from a
  single sample page.
  Outrigger support base varies (2.45 / 5.38 / 6.84 / 8.30m) with the
  same "only the widest offers 360°" pattern as 1130 (confirmed via
  each width's own slew badge, not assumed to carry over) - "fully
  deployed" = 8.30m, and that's the same filter as 360° again here.
  The actual new thing this crane needed: at the 8.30m/360° scope,
  counterweight levels of 10.0t and above each print TWO separate
  charts for the exact same tonnage - one with the counterweight tray
  badge reading "x4.8m", one reading "x5.3m" (a distance from the slew
  axis, confirmed by cross-referencing the same cwt+outrigger+slew
  combination showing genuinely different capacity numbers between the
  two - not a duplicate/misprint). The two lowest levels (0.0t/4.0t)
  print no radius badge at all - only one physical mounting exists at
  that little counterweight, nothing to choose between. This is what
  the person had asked for as the "ballast radius selector" back before
  any of this crane's charts were opened, and it lines up exactly with
  what's actually in the PDF once found (a full-document badge scan,
  not assumed from theory-chapter wording - the icon-explanation
  chapter never separately describes a counterweight-radius icon, it
  only shows up as an extra token appended to the counterweight badge
  on the pages that have it).
  Implementation: JSON combo keys gained a third segment
  ('cwt|wind|ballastRadius'), with the two radius-less counterweight
  levels' columns duplicated verbatim under BOTH radius keys at
  extraction time - simpler than teaching the lookup code a fallback
  path, and means selecting either radius always finds every
  counterweight level the crane actually offers, never showing "no
  results" just because the crew's chosen radius happens not to apply
  at the low end. New UI: a ballast-radius toggle (mode-btn row, same
  pattern as CWT/wind), shown only when meta.hasBallastRadius is set,
  and left visible in BOTH manual and auto/find-minimum-CWT counterweight
  modes per the person's explicit instruction - it's the crew's choice
  either way, not something the auto-finder should quietly pick for them.
  Verified two pages (one radius-less low-cwt combo, one 4.8m-radius
  combo) against rendered images before shipping - reeving, wind
  ratings, extension states and radius/capacity values all matched
  exactly, including cross-checking that selecting 4.8m vs 5.3m in the
  UI actually pulled different chart references (0054 vs 1467) and
  different capacity numbers for literally the same boom/cwt request,
  not the same data shown twice under two labels.
  Final size: 24 stored combo entries (22 real + 2 duplicated), 2,608
  columns, 43,182 datapoints, ~700KB JSON. Regression-checked LRT 1100,
  LTM 1110, and LTM 1130 afterward (ballast-radius UI correctly stays
  hidden for all three) - 0 console errors.

15.6 LTM 1250-5.1 - both axes at once (page-level wind bracket AND
     ballast radius), 13,223-page source, real T* back in play
  Biggest source document since 1110's own 12,292-page file. This
  crane's own 40.62 chapter has a genuine "T*" main-boom mode again
  ("Telescopic boom with auxiliary device") - the SAME literal symbol
  1110 used (not 1160's differently-punctuated "T *)"), confirming this
  really is a per-crane naming detail, not a fleet-wide constant to
  hardcode - re-checked directly rather than assumed either way, same
  as every crane so far.
  Badge layout is a full step back to 1110's shape: counterweight,
  counterweight radius, support base, slew, AND a single page-level
  wind rating are all fixed per page, with NO per-column "m/s" row in
  the table at all (confirmed against a page render before writing the
  extraction script - this crane needed its own reeving-row handling
  too, since without a wind row after the 5 section rows, the very next
  row is instead the reeving row, labelled "EST1", not a "* n *" row
  positioned before the extension block like 1130/1160). This crane
  also has its OWN "counterweight radius" icon explicitly documented in
  its 40.65 chapter ("large counterweight radius" / "small counterweight
  radius") - the first crane so far to actually name the concept
  directly rather than it only being inferable from comparing two
  same-tonnage charts (1160's case).
  Net effect: this is the first crane with THREE independently
  selectable axes at once - counterweight, wind bracket (page-level,
  reusing 1110's UI unchanged), and ballast radius (reusing 1160's UI
  unchanged) - and both existing UI code paths turned out to already
  compose correctly together with zero additional JS changes beyond
  registering the crane, since the wind-group and ballast-group
  visibility toggles were written as independent conditionals from the
  start (14/15.5), not an if/else assuming only one could apply.
  Full-document classification scan (a genuine ~150s job at 13,223
  pages, run in the background rather than blocking the session) found
  21 counterweight levels x 2 ballast radii (4.8m/5.6m) x 3 wind
  brackets (11.2/13.4/15.6 m/s) = 126 in-scope combos at the fully-
  deployed 8.30m support base + 360° slew, each a clean contiguous
  14-page run - verified programmatically (no non-contiguous ranges)
  before extracting. One near-miss caught during verification, not a
  real bug: a page sampled during reconnaissance (badge: 78.0t/5.6m
  radius/11.2m/s) looked like it should match combo key
  "78.0|5.6|11.2" but that key's actual stored page range was over a
  thousand pages away - turned out the reconnaissance page had a
  restricted ±10° slew, correctly excluded by the 360°-only scope
  filter, and a SEPARATE 360°-slew chart elsewhere in the document
  happens to share the exact same counterweight/radius/wind badge
  values. Re-verified against the actual stored page (1316, chart ref
  T259.101.12002) before trusting the extraction - a reminder that
  matching badge values alone don't guarantee it's the same chart
  series once a crane has enough combos in play.
  Final size: 126 combos, 20,034 columns, 328,416 datapoints, ~5.3MB
  JSON - comparable in scale to 1110's own dataset. Regression-checked
  LRT 1100, LTM 1110, and LTM 1160 afterward - 0 console errors.

15.7 LTM 1300-6.2 - back to a single axis (no ballast radius on this
     crane), per-column wind, 6 telescopic sections
  1344 pages. Real "T*" mode again (auxiliary device exclusion, same as
  1250). This crane's own 40.65 icon chapter documents Counterweight
  and Crane-supported (support base) icons but never a counterweight-
  radius icon - checked directly rather than assumed from 1250's
  precedent, and confirmed empirically too (no second radius value ever
  appears in any footer badge across the classification scan). Matches
  what the person's original crane list implied (only 160/250/650 were
  named as needing the selector) - good agreement between the manual's
  own documentation and the request, not just a coincidence worth
  flagging either way.
  Support base varies (2.68 / 5.60 / 7.06 / 8.53m) with the same
  "widest = only 360°" pattern as 1130/1160/1250 - confirmed via each
  width's own slew badge rather than assumed to repeat automatically.
  11 counterweight levels at the 8.53m/360° scope (0 through 96t),
  16-17 pages each, all contiguous - found via a ~16s full-document
  scan (much faster than 1250's 13k-page job, this file is only 1,344
  pages). One real regex bug caught immediately by a 0-combos result
  on the first attempt (not silently trusted): the footer badge's
  counterweight and outrigger-length values sit on the SAME line here
  ("96.0      8.90 x     360°"), unlike the newline-separated version
  the regex was copied from - fixed by comparing the failing pattern
  against the actual page text before touching real extraction logic.
  Extension-row layout is yet a fourth distinct shape (all now handled
  by the same general "read section count from label digits, walk rows
  by their own depth" algorithm with zero per-crane branching in the
  extraction code itself, only in how each script locates the
  page-specific badge fields): 6 sections, reeving ("ESTn"), AND wind
  ("m/s") all stacked into ONE merged label cell, each as its own
  single-value-per-column row - reeving directly following the 6
  section rows, wind directly after that. Verified two pages (cwt=96.0
  and cwt=0.0, both start-of-combo pages) against rendered/dumped text
  before shipping - boom lengths, radius/capacity values, reeving, and
  wind ratings all matched exactly.
  Final size: 11 combos, 2,139 columns, 36,516 datapoints, ~620KB JSON.
  Regression-checked LRT 1100, LTM 1110, LTM 1130, and LTM 1250
  afterward (both wind-group and ballast-group UI correctly hidden for
  this crane) - 0 console errors.

15.8 LTM 1650-8.1 (T3 boom) - last of the six, and ballast radius turned
     out to mean something genuinely different on this crane
  Sixth and last of the person's requested crane list. 1,803-page source
  ("_T3" file specifically - the person confirmed mid-session this is
  the one wanted, not the sibling _T3Y/_T3N/_T3F files sitting alongside
  it at up to 204MB each). This crane's own 40.60 chapter names its main
  boom sign directly as "T3" (= "Telescopic boom 54 m", as opposed to a
  sibling "T5" = 80m boom in a completely different source file) -
  T3 is the mode symbol itself here, not a suffix on a generic "T", so
  the T*-equivalent exclusion is "T3*" and the Maintenance-operation
  exclusion is "T3~" (confirmed present via a mode tally across the
  whole T3 file before assuming either symbol carries over unchanged
  from 1110/1160/1250's own conventions).
  Ballast radius exists on this crane too, per the person's original
  request - but it does NOT take the shape either prior implementation
  (15.5's per-combo pair, 15.6's same idea) assumed. First investigation
  pass only checked the page's own TOP badge for a radius/angle value
  and found it present on just 2 of 17 counterweight levels, each with
  only ONE value - not a parallel pair to choose between like 1160's
  charts - and reported this dead end to the person rather than guessing
  a shape onto data that didn't support it. Person's reply named the
  real numbers directly (6.4/7.4/8.4) and the actual use case: filtering
  by ballast-swing clearance ("i may or may not be limited on
  clearance... to have my receptacle out all the way to 8.4"), which
  led to checking one of the "no radius badge" pages' full table
  structure directly rather than trusting the earlier top-badge-only
  scan - the real values were sitting in the extension-condition block
  the whole time, as two extra per-page rows ('m' = radius, '°' = angle,
  same uniform value repeated across every column on a page) that a
  badge-only regex never looked at. Person independently confirmed the
  three fixed pairs before extraction was finished (6.4m=135°,
  7.4m=90°, 8.4m=45°) - matched exactly once found.
  This is a structurally different kind of "ballast radius" than 15.5/
  15.6: not a chart-selection axis picked before searching (their
  meta.ballastRadiusMode = 'combo', baked into the JSON combo key) but a
  per-column property of the chart itself - which specific boom/cwt/
  wind combination happens to need which radius isn't the crew's choice,
  it's fixed by the chart. What the person wants selectable is a MAXIMUM
  they're prepared to accept (site clearance), filtering OUT any column
  whose required radius exceeds it - meta.ballastRadiusMode = 'column'.
  Implementation: extraction stores radius (and angle, display-only,
  never filtered) as two more per-column fields alongside boom length/
  reeving/wind. The existing ballast-radius toggle UI (built for 15.5)
  is reused unchanged for the widget itself, branching only on
  ballastRadiusMode: 'column' mode changes the label text ("Maximum
  Ballast Radius (Site Clearance)"), shows an explanatory disclaimer,
  defaults to the LARGEST radius (least restrictive - "no constraint" is
  the common case, unlike 'combo' mode which defaults to the first/
  smallest), and filters columns (radius > selected -> excluded) instead
  of selecting a combo-key segment. Column tuple position 4/5 means
  different things per crane now (wind rating for windMode:'column'
  cranes vs radius/angle for ballastRadiusMode:'column' cranes) - never
  ambiguous in practice since no crane has needed both at once, but the
  render code reads meta flags to decide which each time rather than
  assuming position 4 means one fixed thing.
  Verified the clearance filter actually reaches into different
  underlying chart series, not just relabeling the same numbers:
  restricting from the default 8.4m to 6.4m on the same query dropped
  the top result's capacity from 179.90t to 167.20t and its reeving from
  16x to 22x, and auto/find-minimum-CWT mode honored the same filter
  (also kept visible in auto mode, per the person's explicit instruction
  for the ballast-radius selector generally, 15.5).
  Wired in as crane key '1650-t3' (not bare '1650') to match the
  existing FLEET_BLOCK_SPEC entry built for this exact boom variant, so
  the hook-block/Winch 2 selectors needed no new data either.
  Final size: 51 combos, 4,935 columns, 86,807 datapoints, ~1.35MB JSON.
  Regression-checked all five previously shipped cranes afterward - 0
  console errors, all five still matched pre-change output exactly.
  This closes out the six-crane expansion the person asked for (LRT
  1100, LTM 1130/1160/1250/1300/1650), each shipped and verified on both
  channels individually per section 15.1-15.8 above.

15.9 Wired Load Chart Finder into the existing "Cache All Offline
     Content" mechanism
  All seven crane JSON files were already covered by the app's general
  rule (the service worker's fetch handler caches anything it
  successfully fetches into the persistent CONTENT_CACHE, forever) - so
  a crane already opened once while online was already available
  offline from then on. What was missing: the Load Chart Finder tab
  had no "Offline Availability" card and wasn't part of the
  allOfflineContentUrls() list the "Cache All Offline Content" button
  and status indicator (Reeving Plans / CWT Combinations / Driving w/
  Equipment tabs) already draw from - a crane never manually opened had
  no way to be pre-cached, and there was no way to check "is everything
  actually cached" for load charts specifically.
  Fix: added Object.values(LOAD_CHART_FILES) into
  allOfflineContentUrls() (function bodies aren't evaluated until
  called, so it not mattering that LOAD_CHART_FILES is declared later
  in the same script than allOfflineContentUrls() itself - by the time
  either is actually invoked, at page-load or on a button click, every
  top-level const in the script has already run), and added the same
  "Offline Availability" card (status span + Cache All button +
  progress bar) already used on the other three tabs to the bottom of
  the Load Chart Finder tab, plus added 'loadchart-tab' to the tab-
  switch list that refreshes the status indicator. No new mechanism
  built - purely plugging the existing one in for a tab that had been
  left out of it.
  Verified via Playwright: allOfflineContentUrls() now lists all 7
  loadchart/*.json paths, clicking "Cache All Offline Content" on the
  Load Chart Finder tab actually caches them (confirmed via
  caches.match() on every required URL, not just trusting the button's
  own completion message), the status flips to "Downloaded" afterward,
  and the same completion is reflected on the OTHER tabs' status spans
  too (shared cache pool, as expected) - 0 console errors.

15.10 Compare Cranes sub-tab - "find the best of several cranes for this
      lift" as its own mode, not a Find Boom Configuration option
  Person floated this as "a concept thought" first (multi-select 3+
  cranes, one radius/weight, get each crane's minimum counterweight and
  best-margin chart back) - answered with feasibility and the one real
  design snag (rigging weight/hook block choice is per-crane, doesn't
  generalize across a multi-select) rather than building anything yet,
  per the general rule for a floated idea rather than a request. Person's
  follow-up settled both open questions directly: drop rigging weight
  entirely for this mode, and present it as two sub-tabs inside the
  existing Load Chart Finder tab rather than a new top-level tab -
  "Find Boom Configuration" (unchanged) and "Compare Cranes" (new).
  Implementation:
  - HTML: a mode-toggle pair (lc-subtab-find/lc-subtab-compare, same
    .mode-toggle/.mode-btn pattern used everywhere else in this feature)
    switches which of two wrapper divs (lc-panel-find / lc-panel-compare)
    is visible - the existing three Find Boom Configuration cards moved
    into the first wrapper unchanged, nothing about their own behavior
    touched. Offline Availability stays outside both wrappers, visible
    regardless of sub-tab (it already covers every crane's JSON
    regardless of which sub-tab last touched them).
  - Compare Cranes gets its own crane multi-select (checkboxes built
    from the same LOAD_CHART_FILES/LOAD_CHART_CRANE_LABELS maps every
    other part of this feature already uses - one source of truth, no
    separate crane list to keep in sync), shared radius/weight inputs,
    and its own 90%-site-rule toggle (kept, unlike rigging weight -
    it's a single global multiplier, not something that varies per
    crane the way a hook block catalog does).
  - New computeLoadChartBestForCrane(data, radiusInput, weightInput,
    derateOn) factors out the core "ascend counterweight levels, stop at
    the first one with any qualifying column, keep that level's best-
    margin column" search - the same logic renderLoadChartResults()'s
    auto/find-minimum-CWT mode already does, but self-contained (no
    reliance on the Find Boom Configuration sub-tab's own module-level
    state - loadChartWind/loadChartBallastRadius/loadChartCwtMode belong
    to that sub-tab only) since Compare needs to run this once per
    selected crane independently, each with that crane's OWN default
    wind bracket (lowest) and ballast radius (widest/most permissive,
    matching the existing "column mode defaults to largest" convention
    from 15.8) rather than sharing one global setting across cranes with
    different data shapes.
  - Compare has its own crane-data cache (loadChartCompareCache, keyed
    by crane) separate from the Find sub-tab's single-crane
    loadChartData/loadChartCraneKey pair - Compare needs several cranes'
    data resident at once for a side-by-side table, Find only ever needs
    one. Both fetch the identical URL per crane though, so they share
    the same service-worker CONTENT_CACHE entry - switching sub-tabs
    never re-downloads a crane already opened in the other one.
  - Results sorted by margin descending (best option first, matching
    Find Boom Configuration's own top5 ordering) with out-of-range and
    no-qualifying-configuration cranes sorted to the bottom rather than
    hidden, so a crane that genuinely can't do the lift is still visible
    as a result, not silently dropped from the list.
  One real bug caught before shipping (not by inspection, by an actual
  edge-case query): the "no qualifying configuration" branch returned
  {none: true} without also carrying the rounded-up actualRadius the
  message template needed, rendering "...clears 5000.00t at undefinedm"
  - fixed by returning actualRadius on every branch, not just the
  success path. Verified an impossible target weight (5000t) and an
  out-of-range radius (999m) both render clean messages afterward, then
  a normal 3-crane query (1110/1130/1300, 10m/20t) sorted correctly by
  margin and recalculated correctly when the 90% toggle was flipped
  (1110's minimum counterweight rose from 6.9t to 8.5t, as expected -
  more counterweight needed once only 90% of chart capacity counts) -
  and confirmed switching back to Find Boom Configuration afterward
  was completely unaffected (separate state, separate cache) with a
  full regression sweep across all seven cranes - 0 console errors.

15.11 Driving w/ Equipment: relabeled the duplicate-row "Option A"/
      "Option B" per the OEM explanation found in 10.41's update
  Person confirmed wanting the relabel after the OEM-note discovery
  logged in 10.41's update (LTM 1160-5.2's own manual: Example 1 =
  permissible boom angle without axle-load optimization, Example 2 =
  optimal angle with optimized/lower axle load, same physical setup).
  Before relabeling unconditionally by position (index 0 / index 1),
  checked this actually holds across DRIVING_EQ_DATA's real rows rather
  than assuming the OEM's stated example order survived extraction
  unchanged - spot-checked every duplicate-weight pair in LTM 1110's
  own front-direction table (8.8/7.7/7.2/6.9/3.6/1.1/0.8/0.0t, 8 pairs)
  and found the SECOND row's axle-B figure lower than (or equal to) the
  first in every single case, several also showing a lower axle-A too -
  consistent with "index 1 = optimized, always" holding as extracted,
  not just usually.
  Changed the label generator (previously `Option
  ${String.fromCharCode(65+i)}`, i.e. literal "Option A"/"Option B") to
  "Permissible Angle" for index 0 and "Optimized Angle (Reduced Axle
  Load)" for index 1, falling back to the old lettered label for a
  hypothetical 3rd+ row (never seen in practice - the OEM's own chart
  structure only ever pairs two examples per weight - but kept as a
  defensive fallback rather than assuming it can never happen).
  Verified via Playwright (LTM 1110, front, 8.8t): renders "PERMISSIBLE
  ANGLE" first (boom angle 6°...29°, axle B 17t) then "OPTIMIZED ANGLE
  (REDUCED AXLE LOAD)" second (6°...27°, axle B 16t) - matches the
  OEM's own example order and the spot-check above exactly - 0 console
  errors.

16. CRANE SETUP TAB: 360-DEGREE SLEW CLEARANCE
  New feature, new tab ("Crane Setup", between Crane Positioning and Rope
  Retentioning). Concept: when parked alongside a structure (wall,
  stockpile, fence), how far does the superstructure's widest rear-swing
  point (counterweight, or Winch 2 if it sticks out further) oversail the
  side of the carrier during a full 360° slew - i.e. how much stand-off
  clearance is actually needed, or whether an already-parked crane has
  room to slew at all.

  Geometry (confirmed with the person before building anything): the slew
  axis sits on the carrier's centerline. Park the crane with that
  centerline parallel to the structure being cleared and the worst case
  is when the superstructure has rotated so the boom/CWT points straight
  sideways - at that angle the full swing radius becomes pure sideways
  distance from centerline, so subtracting half the carrier width (B/2)
  gives exactly how far the swing point oversails the side of the
  carrier. Standard tail-swing-beyond-track-width calc. Angle the crane
  off-parallel to the structure and the real clearance only gets bigger
  than this number, never smaller - so this is a conservative/worst-case
  figure by construction, not an average.

  Source data: each crane's own Operating Instructions manual (NOT the
  Load_chart_manual folder used for section 15 - a sibling
  Operating_instructions_Crane PDF, found via the same Nextcloud WebDAV
  share, chapter 1.03 Technical Data > 1 Dimensions). carrierWidthB comes
  straight off that chapter's tyre-size table (525/80 R25 column, this
  fleet's standard tyre per company policy/WA axle regulation - confirmed
  by the person, no need to track the other two tyre options at all,
  unlike Driving w/ Equipment's tyre-agnostic table which the fleet
  policy doesn't help simplify). The radius figures are NOT plain text -
  they're hand-drawn CAD leader-line labels on the chapter's plan-view
  figure, rotated 180° (same "scrambled" presentation as every dimension
  figure encountered this session, e.g. 1110's Fig.154887 back in the
  outrigger work) - read by rendering the PDF page to a high-DPI PNG and
  eyeballing the rotated digits, same method as always for this kind of
  figure.

  This went through a full report-and-confirm cycle before any number was
  trusted, because the CAD figures turned out to vary far more than
  expected - worth recording in full since it's a cautionary example of
  why "looks like the same pattern as the last crane" cannot be assumed:
  - LTM 1110/1130/1300: a clean two-radius pattern (inner = one
    component, outer = the other) - but 1110/1130 have CWT as the
    SMALLER/inner radius and Winch 2 as the larger/outer, while 1300 is
    the reverse (Winch 2 smaller, CWT larger) - confirmed directly by the
    person, not an assumption carried over from 1110/1130's pattern. Do
    NOT assume inner-vs-outer position predicts which component it is on
    a crane not yet checked.
  - LRT 1100: three radius labels near the superstructure (R4180, R7135,
    R7920) with no way to tell from the line art alone which was the
    CWT's rear-swing radius versus something unrelated (turned out R7135/
    R7920 were NOT superstructure-swing radii at all - sent crops to the
    person rather than guess, who confirmed the actual CWT figure was the
    smaller one I'd initially half-suspected was a red herring, R4190,
    off by 10mm from my own rotated-digit misread of R4180). This crane
    also has a real second winch (FLEET_BLOCK_SPEC already carries
    separate w1/p1 vs w2/p2 rope specs for it) but the person confirmed
    it sits entirely within the CWT's radius, so it's tracked as a single
    fixed point, not a pair.
  - LTM 1160/1250: THREE radius labels near the CWT, not two - initially
    unexplained (three points along the tray's non-circular outline?
    three unrelated features?). Sent crops rather than guess. Turned out
    both cranes have a genuine "vario ballast" (extended counterweight
    tray) option, same concept already modeled for the Load Chart
    Finder's ballastRadiusMode:'combo' cranes (section 15.5) but this is
    a DIFFERENT chapter/figure with its own independent radius numbers,
    not the same 4.8m/5.3m figures already in the load chart JSON - do
    not assume the two are interchangeable. Resolved to: 1160 standard
    tray radius (4730mm) coincides with Winch 2's own radius so only one
    figure is needed for the "standard" case, plus a second figure
    (5230mm) if the vario tray is fitted; 1250 keeps Winch 2 as its own
    separate fixed point (4975mm) alongside standard (4780mm) and vario
    (5580mm) CWT tray radii - three genuinely independent numbers, not
    two.
  - LTM 1650: the radius labels found (R7950, R7300) traced a long curve
    running most of the vehicle's length rather than a tight corner near
    the slew center, and didn't fit the pattern at all - the person
    redirected to reuse the SAME 6.4/7.4/8.4m site-clearance radii
    already established for this crane's Load Chart Finder ballast
    selector (section 15.8) rather than extracting anything new, since
    it's the same physical vario-ballast positions being described.
  - LTR 1220 (crawler): explicitly excluded by the person - no 525/80
    tyres (it runs on tracks), out of scope for this feature entirely.
    Its counterweight-tab task in the tracker is unrelated/separate work.
  - LRT 1100 also has no 525/80 tyre option (single fixed tyre, 29.5
    R25), but unlike 1220 it's still an on-road-relevant crane for this
    feature, so its carrierWidthB (3300mm) is read directly off its own
    dimension figure's front-view width dimension instead of a tyre-size
    table lookup - flagged in the UI (widthNote) so the fleet-wide
    "525/80 R25" disclaimer text doesn't silently misrepresent this one
    crane's actual figure as a tyre-size substitute.

  Data model: SLEW_CLEARANCE_DATA, keyed the same as CARRIER_MODELS/
  CARRIER_CALIBRATION (bare crane key, e.g. '1650' not '1650-t3' - this
  feature isn't boom-mode-specific the way the Load Chart Finder is).
  Each entry has `fixed` (points whose radius never changes) and
  `variants` (null, or a {label, options[]} pair for cranes with a
  selectable tray/ballast radius - only one is ever physically fitted at
  a time, rendered as a mode-toggle, defaulting to index 0/first option
  on every crane switch rather than remembering the last selection across
  different cranes).

  UI: new "Crane & Configuration" card (crane select + variant toggle
  when applicable) and a "360° Slew Clearance" results card (radius +
  computed clearance per point, in meters, plus a "limiting point" line
  picking whichever point has the largest radius - that's the one that
  actually governs how much stand-off is needed). A third card links
  over to the Crane Positioning tab for the eventual 3D visualization
  (see below) rather than embedding a second 3D viewer inline.

  3D radius-circle toggle (the person's original "would be good to see"
  ask, alongside the numeric table) is DEFERRED, not shipped in this
  pass - vendor/carrier3d.js's viewer is a single hardcoded-canvas module-
  level instance (one scene/camera/renderer, tied to
  #carrier-3d-canvas-wrap in the Crane Positioning tab specifically), so
  a second concurrent 3D view on a different tab isn't a drop-in reuse -
  it needs either a small refactor to parameterize which DOM element the
  viewer renders into, or a scene-graph clone() of the already-cached GLB
  for a second concurrent instance. Rather than half-build this under
  time pressure, shipped the fully-correct numeric feature now (it's the
  actual clearance figure that matters) and left the 3D piece as a
  visible fast-follow via the "3D Visualization" card's link + explanatory
  text, instead of silently dropping it or shipping a rushed version.

  Tested via Playwright across all 7 in-scope cranes (1100/1110/1130/
  1160/1250/1300/1650): every clearance figure hand-verified against
  radius minus half the carrier width, variant toggle switches correctly
  (1160/1250/1650), the "no variant" cranes correctly hide that card
  section, and the Crane Positioning cross-link correctly activates that
  tab/button pair without leaving two tabs simultaneously marked active -
  0 console errors throughout.

  UPDATE (same day): restructured into a Crane Positioning sub-tab, and
  the deferred 3D radius circle shipped after all.
  Person clarified the original ask right after the first ship: "Crane
  Setup" was meant to be a SUB-tab of Crane Positioning, not a sibling
  top-level nav tab - and the pre-existing outrigger/site-plan/3D-carrier/
  bog-mat content needed its own sub-tab name too ("Support Pad
  Placement") rather than staying as Crane Positioning's only content.
  Removed the standalone "Crane Setup" nav button and slewclear-tab div;
  reused the exact mode-toggle/panel-show-hide pattern already
  established by the Load Chart Finder's Find/Compare sub-tabs
  (setLoadChartSubTab) for a new setCadSubTab('pads'|'setup'), with
  cad-panel-pads holding everything that used to be cad-tab's entire
  content and cad-panel-setup holding the slew-clearance UI. The "3D
  Visualization" card's cross-link now just calls setCadSubTab('pads')
  (same-tab sub-tab switch) instead of switchTab() to a different
  top-level tab.
  This also removed the original reason for deferring the 3D radius-
  circle overlay: once Crane Setup and the 3D carrier viewer are sub-tabs
  of the SAME parent tab, there's no second concurrent viewer instance to
  build at all - the existing single carrier3d.js viewer just needs a new
  capability, not a second instance. Added window.__carrier3dSetSlewCircles
  (vendor/carrier3d.js) alongside the existing __carrier3dSyncOutriggers:
  draws a flat ring on the ground plane per requested radius, centred on
  siteToWorld(cal, 0, 0) (the same slew-centre point the outrigger pads
  already anchor to) using whichever calibration computeCalibration()
  already cached for that model - no new calibration math needed, since
  the slew centre doesn't depend on which feature is asking for it. Ring
  radius is drawn directly in world metres (radius_mm/1000), not run
  through cal.xSlope/zSlope, since those carry the site-plan's own
  approximate-footprint mm->model mapping while the CAD model itself is
  already dimensionally accurate - a real physical radius just needs the
  plain unit conversion, same as every other physical size already drawn
  (pad boxes, etc). Bumped CARRIER3D_VERSION (21->22) since the vendor
  file changed.
  UI: a "360° Slew Radius" checkbox row was added to the existing 3D
  Carrier Preview card's toolbar (index.html's
  renderCarrier3DSlewToggles(), called from updateCarrier3DAvailability()
  on every crane switch) - one checkbox per SLEW_CLEARANCE_DATA point,
  including every variant option shown side by side (not behind a
  selector) so both "Standard" and "Vario Ballast" can be ticked at once
  to compare directly on the model. Distinct colours per point
  (SLEW_CIRCLE_COLORS, red/purple/green/orange) deliberately different
  from the gold/cyan/white already used for pad markers/ghosts, so a
  slew ring is never mistaken for one of those. Checkboxes rebuild from
  scratch (and reset to unticked) on every crane switch; the rebuild
  fires onSlewCircleToggle() once at the end specifically so the 3D
  view's own pendingSlewCircles[modelKey] state (in carrier3d.js) can't
  keep showing a stale circle from a previous crane if the person
  switches back to it later without re-ticking anything.
  Verified via Playwright + screenshots (LTM 1110): both Counterweight
  and Winch 2 circles render as concentric rings centred correctly on the
  slew axis (Winch 2's larger ring visibly containing Counterweight's
  smaller one, matching their real radii), unticking one removes only
  that ring, and switching to LTM 1160 correctly rebuilds the checkbox
  list to its own Standard/Vario Ballast pair with both unticked. Full
  regression sweep across all 7 top-level tabs plus the existing
  outrigger pad-toggle/compare-mode controls afterward - 0 console
  errors throughout.

  UPDATE (same day): removed the Crane Setup sub-tab's "3D Visualization"
  card. It was only ever a one-line pointer ("see this on the Support Pad
  Placement sub-tab") written back when Crane Setup was still its own
  top-level tab with no easy way over to the 3D view - once it became a
  sub-tab of the same parent as Support Pad Placement, that card was just
  telling the person to use the mode-toggle sitting directly above it.
  Person asked not to have it duplicated across sub-tabs; removed
  entirely rather than kept as dead weight, since the toggle buttons
  already make switching obvious with no card needed.

  UPDATE (same day): person clarified further - wanted BOTH sub-tabs to
  have their OWN 3D Carrier Preview card (so neither one requires
  switching to the other just to see the model), with the slew-radius
  toggle checkboxes living EXCLUSIVELY on Crane Layout's copy, and the
  sub-tab renamed Crane Setup -> Crane Layout throughout (ids too:
  cad-subtab-layout/cad-panel-layout, mode string 'layout' not 'setup').

  This reopens the exact tradeoff the original 3D-toggle deferral was
  about, in a new shape: TWO cards now need a 3D view, but
  vendor/carrier3d.js is still a single hardcoded-canvas module (one
  scene/camera/renderer/controls, previously tied to one fixed
  #carrier-3d-canvas-wrap). Rather than spin up a second concurrent
  WebGL context (real cost - a duplicate GLB parse/upload for a model
  already sitting in modelCache, and every mobile browser caps how many
  live WebGL contexts it'll allow at once), generalized the ONE renderer
  to be re-parentable: ensureRenderer(wrapId, labelId) now moves the
  existing canvas into whichever wrap asked for it (a no-op if already
  there), returning whether it actually moved. Since the two sub-tabs are
  mutually exclusive (only one ever visible), this is invisible to the
  person - it just looks like "each sub-tab has its own persistent 3D
  view."
  __carrier3dActivate(modelKey, url, wrapId, labelId) gained the wrapId/
  labelId params and a new case its old two-branch shape (same model ->
  no-op; different model -> full clearScene()+swap) didn't cover: SAME
  model, DIFFERENT wrap - e.g. Support Pad Placement showing LTM 1110
  while Crane Layout's own crane select ALSO happens to be on 1110. The
  old "if (currentModelKey === modelKey) return;" early-return would have
  silently left the canvas parented wherever it was, so the newly-
  activated card would show nothing at all. Now: a wrap change (with or
  without a model change) always proceeds; a same-model/different-wrap
  transition additionally drops the PREVIOUS context's own overlays
  (clearOutriggers()+clearSlewCircles()) without touching the model
  itself, since Pad Placement's outrigger markers have no business
  showing up in Crane Layout's context or vice versa - whichever card
  just took over immediately pushes its own fresh overlay state right
  after activate() returns (syncCarrier3DOutriggers() for Pad Placement,
  onSlewCircleToggle() for Crane Layout), same as they always did.
  setCadSubTab() now actively reclaims the canvas on every switch, not
  just show/hide a div: switching to 'pads' re-runs activateCarrier3D()
  if carrier3DActive is true, switching to 'layout' re-runs
  activateCarrier3DLayout() if carrier3DLayoutActive is true - each is a
  small extracted helper (previously inlined in toggleCarrier3D()/
  toggleCarrier3DLayout()) so the exact same activation logic runs
  whether triggered by the toggle button or by a sub-tab switch finding
  its own "3D on" flag already set from before the person navigated away.
  Also fixed sceneBox() (used by Fit View) to include slewCircleGroup's
  own bounds, not just the model's and outriggerGroup's - a circle can be
  (and often is) far wider than the carrier itself, e.g. LTM 1650 at its
  8.4m ballast radius, so Fit View would otherwise frame just the model
  and clip the ring off-screen.

  Real bug caught during testing, not by inspection: circles simply
  didn't draw at all when Crane Layout was used WITHOUT first opening
  Support Pad Placement's own 3D preview for the same crane. Root cause:
  applySlewCircles() read calibrationCache[modelKey], which is ONLY ever
  populated by computeCalibration() inside applySync() - i.e. only by
  Support Pad Placement's own outrigger sync. The original comment
  reasoned "outrigger sync always runs first... before a person could
  realistically reach the checkbox" - true when Crane Setup only had
  Support Pad Placement's crane to work with, false now that Crane Layout
  has its own independent crane select (sc-crane) that may never have
  been opened on the Pad Placement side at all. Fixed with a SEPARATE
  fallback cache (formulaCalibrationCache) computed from just
  computeFormulaCalibration(root, footprint, calibration) - the same
  formula the full refinement itself starts from, good enough to anchor a
  slew-centre ring without needing any outrigger leg geometry to refine
  against. Deliberately kept in its own cache rather than writing into
  calibrationCache, so if Support Pad Placement's own sync runs LATER for
  the same model, it still gets the full leg-anchored refinement, not
  silently stuck on the cheaper stand-in a lookup-order shortcut would
  have left behind. index.html's onSlewCircleToggle() now passes
  FOOTPRINTS[modelKey]/CARRIER_CALIBRATION[modelKey] through
  __carrier3dSetSlewCircles() so this fallback has what it needs; a new
  pendingSlewCircleContext map carries the same two values through to the
  async-model-still-loading replay path in __carrier3dActivate.

  Testing note: Playwright's native .click() (and even a JS-dispatched
  .click() via evaluate) hung indefinitely against the 3D toggle buttons
  in this sandboxed environment, with "GPU stall due to ReadPixels"
  console warnings alongside - consistent with Playwright's own element-
  stability screenshotting fighting a live, actively-rendering WebGL
  canvas under software rendering (SwiftShader, no real GPU in this
  sandbox), not an app defect. Confirmed by calling the exact same
  handler functions directly via page.evaluate() (toggleCarrier3D(),
  toggleCarrier3DLayout(), onSlewCircleToggle()) - all returned in
  well under a second with correct results and zero console errors, just
  through a code path that skips Playwright's click-specific stability
  polling. All functional verification for this update was done this
  way, cross-checked with screenshots.
  Verified: both cards' own 3D toggle correctly loads/shows their
  respective crane; checking Standard + Vario Ballast circles on LTM
  1160 (opened directly via Crane Layout, no prior Pad Placement 3D use)
  renders two correctly-centred, correctly-scaled concentric rings;
  switching Support Pad Placement -> Crane Layout -> Support Pad
  Placement reclaims the canvas each time (confirmed via each canvas's
  actual DOM parent id), shows the right crane with outrigger markers
  restored and zero leftover slew circles from the other context, and a
  full regression sweep across all 7 top-level tabs plus the existing
  outrigger pad-toggle/compare-mode controls came back clean - 0 console
  errors throughout every check.

  UPDATE (same day): reordered Crane Layout's cards (3D Carrier Preview
  now above 360° Slew Clearance's numeric table, not below it - person's
  preference, no functional change) and added the dimension-line
  annotation from the earlier "hypothetically is this possible" mockup -
  confirmed as a real ask, not just a concept, with a toggle ("Show
  clearance measurement") sitting under the existing per-point radius
  checkboxes.
  When on, draws a real 3D dimension line for every currently-checked
  circle: a line from the carrier's own side (half the carrier width from
  the slew centre) straight out to that circle along world +X - the same
  "parked parallel to the structure" direction the numeric clearance
  figure itself already assumes - with a short perpendicular tick at each
  end (standard dimension-line convention) and a text label at the
  midpoint showing the same clearance figure as the numeric table, so the
  two can be cross-checked against each other directly.
  The label is a THREE.Sprite billboard (a canvas-texture quad that
  always faces the camera), not an HTML overlay tracking a projected
  screen position every frame the way the transient "Loading 3D model…"
  label does - simpler, and correct at any camera angle/zoom without
  needing to run projection math on every OrbitControls frame, since it's
  a real object living in the 3D scene rather than a DOM element
  shadowing one.
  Implementation: applySlewCircles() (vendor/carrier3d.js) gained a
  carrierWidthMm param - when present, draws the line/ticks/label
  alongside each circle instead of just the ring on its own.
  __carrier3dSetSlewCircles()'s pendingSlewCircleContext also carries
  carrierWidthMm through to the async-model-still-loading replay path,
  same as footprint/calibration already did. index.html's
  onSlewCircleToggle() reads the new checkbox and passes
  SLEW_CLEARANCE_DATA[modelKey].carrierWidthB through only when it's
  checked (null otherwise, which the vendor code treats as "circles only,
  no dimension line" - unchecking removes the line/label but leaves any
  ticked circles alone, confirmed by inspection: unticking only removes
  the dimension-line geometry, the LineLoop circles are separate objects
  in the same group and untouched).
  Verified via direct function invocation + screenshots (same testing
  approach as the previous update, for the same Playwright/WebGL-click
  reason): dimension line for LTM 1650's 6.4m circle renders with a
  "4.85m" label exactly matching the numeric table's own clearance figure
  for that point, ticks visible at both ends; unchecking the box removes
  the line/label while the circle itself stays drawn; full regression
  sweep across all 7 top-level tabs afterward - 0 console errors
  throughout.

  UPDATE (same day): the 3D dimension line's label now reads in mm
  ("4850mm") instead of metres ("4.85m") - person's preference, the
  numeric table above it stays in metres. One-line change
  (clearanceMm = Math.round(c.radius - halfWidthM * 1000), computed
  straight from the mm radius rather than round-tripping through metres)
  since c.radius already arrives in mm from index.html's own circle data.

  UPDATE (same day): added a third Crane Layout toggle - "Show outrigger
  distances (C1-C4, baseline position)" - OEM-drawing-style dimension
  lines from the slew centre out to each of the 4 outrigger legs' own
  baseline position, labelled with the real distance (e.g. "C1: 5049mm").
  Person's own framing of the two Crane Positioning sub-tabs, which
  explains why this belongs on Crane Layout and not Support Pad
  Placement: Crane Layout is "will I be able to feasibly set up the crane
  in this position at all" (their company also has access to a Franna -
  a smaller pick-and-carry crane - on SOME but not all jobs, to help move
  bog mats/pads into place; this tab's job is deciding whether the crane
  fits before anyone's committed to a spot), Support Pad Placement is the
  physical "if I have to place these mats myself, here's exactly where"
  execution tool. Confirmed against a real OEM drawing crop the person
  sent (rotated dimension text, arrow+label style) - matches what this
  session already had to read constantly for LTM 1650/1160/1250's own
  swing-radius figures (section 16 above), same convention.
  Reused the SAME line+ticks+label drawing code the clearance measurement
  already built, generalized: addDimensionLine(group, p1, p2, color,
  labelText) now takes two arbitrary ground-plane points instead of
  assuming the line always runs along world +X - the clearance line still
  does (it's a real physical constraint of that measurement), but each
  outrigger leg sits at its own distinct angle from the slew centre, so
  the tick marks needed to be genuinely perpendicular to whatever
  direction that specific line runs, not hardcoded to the Z axis. Uses
  each leg's BASELINE (unshifted) r/angle straight from cadFleetData - the
  exact same figures already shown in the "Baseline" column of Support Pad
  Placement's own "All Four Legs" table - not Support Pad Placement's
  current shift/pad state, since Crane Layout has no shift inputs of its
  own at all (matches "deciding if you'll be able to feasibly set up" -
  a planning question, not "where did I put the mat this time").
  New third pending/context map pair (pendingLegDimensions/
  pendingLegDimensionContext) mirroring pendingSlewCircles' own replay-on-
  model-still-loading pattern, its own legDimensionGroup (cleared
  alongside outriggerGroup/slewCircleGroup on every wrap-change - a person
  with BOTH Support Pad Placement's outrigger markers AND Crane Layout's
  leg-dimension lines toggled on at different times must never see one
  bleed into the other's context), and its own line in sceneBox() so Fit
  View accounts for it too.
  Also fixed a real (if minor) memory-leak gap noticed while touching this
  code: material.dispose() alone does NOT dispose a texture the material
  holds (e.g. the dimension labels' canvas-texture Sprites) - Three.js
  doesn't cascade that automatically. Added a shared disposeGroup() helper
  (geometry + material + material.map) used by all three clearXxx()
  functions now, not just the new one.
  Verified via Playwright + screenshots (LTM 1110): all 4 legs render
  correctly-labelled dimension lines matching the "All Four Legs" table's
  own Baseline figures exactly (C1 5049mm, C2 6098mm, C3 5857mm, C4
  5267mm); toggling a slew-radius circle on at the same time coexists
  cleanly with no visual or state interference; unticking the outrigger-
  distances checkbox removes only those lines, leaving any active circle
  untouched; a full regression sweep across all 7 top-level tabs came back
  clean - 0 console errors throughout.

  UPDATE (same day): added a fourth Crane Layout toggle - "Show ground
  layout marks (paint-out dimensions)" - genuinely different from the
  "outrigger distances" toggle directly above, despite both being C1-C4
  dimension lines on the same tab. The outrigger-distances toggle draws
  each leg's diagonal ("as the crow flies") distance from the slew centre
  - exactly what's needed once the crane is already parked, to place
  support pads (Support Pad Placement's own job). This new toggle is for
  BEFORE the crane arrives: the figures a crew uses to mark the crane's
  whole footprint out on bare soil with a tape measure and line-marking
  paint, so they know exactly where to park it. Person walked through
  their own real-world procedure step by step with worked numbers (find
  the outrigger's own edge-of-carrier line via (span-B)/2, walk that line
  the leg's longitudinal offset, mark the leg's centre; separately confirm
  the slew centre by measuring in from that same line) and provided an
  annotated OEM drawing screenshot showing the convention: a line
  projected from the slew centre out to meet the straight line between two
  outriggers. A diagonal alone can't be marked out on soil with a tape
  measure and no other reference - there's no way to know what bearing to
  swing the tape at, you'd be guessing (person's own words) - hence
  needing the offsets decomposed into two independently-measurable,
  right-angle components instead.
  Data model (GROUND_LAYOUT_DATA, index.html): per crane, rearSpan/
  frontSpan (the OEM's own printed total support width between that
  pair's two outriggers, fully extended) plus each leg's own longitudinal
  offset (C1-C4, mm, along the carrier's centreline from the slew centre
  to that leg's own "station", read directly off the OEM Dimensions
  sheet's vertical witness-line dimension chains). Lateral position is
  DERIVED, not stored separately: full distance from centreline = span/2;
  distance from the carrier's own edge (what a crew actually measures in
  the field, since it's a physical reference locatable without knowing
  where the slew centre is) = (span - carrierWidthB) / 2, reusing
  FOOTPRINTS[modelKey].width for B rather than duplicating it (confirmed
  identical to SLEW_CLEARANCE_DATA's own carrierWidthB for all 7 cranes).
  Genuinely asymmetric left/right per leg (not a mirrored pair) - same as
  the existing "as the crow flies" r/angle data already was, confirmed
  against the real OEM drawings rather than assumed symmetric.
  Extraction (LTM 1110-5.1, 1130-5.1, 1160-5.2, 1250-5.1, 1300-6.2,
  1650-8.1, LRT 1100-2.1 - all 7 slew-clearance cranes except LTR 1220,
  a crawler crane with no outriggers in this sense, per explicit "not the
  LTR" instruction): read from each crane's own cached OEM Operating
  Instructions PDF (Nextcloud WebDAV source, Load_chart_manual's sibling
  Operating_instructions_Crane folder), "1.03 Technical Data > 1
  Dimensions" figure, same rotated-text/high-zoom-crop reading technique
  established earlier in this session for the swing-radius figures.
  Cross-validated every single leg on every crane via Pythagoras against
  cadFleetData's own trusted r values (longitudinal^2 + (span/2)^2 should
  equal r^2) BEFORE trusting any reading - this caught two real reading
  mistakes during extraction (LTM 1160: misread a decimal-adjacent digit
  producing a ~40-60mm-off prediction that was still directionally
  consistent enough to trust once re-verified via a tighter crop; LRT
  1100: initially grabbed the SIDE-ELEVATION view's own vertical dimension
  chain instead of the PLAN view's - the two numerically overlap for one
  side by coincidence, since the side view happens to align with one
  specific track, which made a wrong reading look plausible until the
  actual plan-view crop was found and gave a much better cross-check
  match). Match quality varied by crane but every leg on every crane
  landed within 66mm of the trusted r value (most within 1-30mm, several
  cranes - LTM 1250/1300/1650 - within 2mm on every leg), confirming both
  the readings and the leg-pairing convention (page-left bracket =
  vehicle-right legs, page-right = vehicle-left; upper chain value = rear
  leg, lower = front leg - consistent across all 7 cranes checked).
  Drawing (vendor/carrier3d.js): two dimension lines per leg, not one -
  deliberately mirrors the two numbers a crew actually uses, not a single
  combined figure they'd have to do their own subtraction on.
    1. Slew centre -> station point on the centreline (0, stationYMm) -
       the longitudinal figure, labelled "C1 fwd/back: 3059mm" etc.
    2. Carrier edge (edgeXMm, stationYMm) -> leg (legXMm, stationYMm) -
       the lateral figure, labelled "C1 out from edge: 2733mm" etc. Starts
       at the carrier's own edge, not the centreline - the short gap
       between centreline and edge is deliberately left undrawn, since
       it's just half the carrier's own known/fixed width, not something
       to remeasure on site.
  Own fourth pending/context map pair (pendingGroundLayoutMarks/
  pendingGroundLayoutContext) and groundLayoutGroup, mirroring the
  outrigger-distances toggle's own pattern exactly - cleared alongside the
  other three overlay groups on every wrap-change, included in sceneBox()
  for Fit View, replayed from the pending map if the model was still
  mid-fetch when the toggle was flipped. index.html's onGroundLayoutToggle()
  computes each leg's stationYMm/edgeXMm/legXMm/lonMm/latMm from
  GROUND_LAYOUT_DATA + FOOTPRINTS and hands them to
  window.__carrier3dSetGroundLayoutMarks(); reused GROUND_LAYOUT_COLORS
  (same 4-colour cycle as the slew circles) purely so each leg reads as a
  visually distinct line when several toggles are on at once.
  Also noticed while touching activateCarrier3DLayout() that the existing
  "outrigger distances" toggle's own checked state wasn't being replayed
  on Crane Layout <-> Support Pad Placement sub-tab switches (only
  onSlewCircleToggle() was called there) - harmless in practice, since
  switching sub-tabs always runs through renderCarrier3DSlewToggles()
  first anyway (via initSlewClearanceTab -> onSlewClearanceCraneChange ->
  renderSlewClearanceResults -> updateCarrier3DLayoutAvailability), which
  rebuilds the whole toggle panel from scratch and unchecks everything
  regardless - but added onLegDimensionToggle()/onGroundLayoutToggle()
  calls alongside onSlewCircleToggle() there anyway for consistency, so
  all three toggles are replayed the same way if that rebuild-on-every-
  switch behaviour is ever changed later.
  Verified via Playwright + screenshots (same direct-function-invocation
  workaround as every other 3D toggle test this session, since native
  .click() hangs once a WebGL canvas is actively rendering in this sandbox
  - see earlier entries in this section): LTM 1110 renders all 8 lines
  (2 per leg x 4 legs) with labels matching the extracted data exactly
  (C3 fwd/back 4680mm, C4 fwd/back 3235mm, C1/C4 out from edge 2733mm,
  C2/C3 out from edge 2082mm - matches the (7013/2-1425)=2081.5mm figure
  confirmed earlier against the person's own field-measured number,
  rounding to 2082 here); LTM 1650 (the Y-outrigger design, structurally
  different beam geometry from the other 6 cranes) also renders correctly
  with all four "out from edge" labels reading the expected 3239mm
  (9578/2-1550); unchecking clears the group cleanly with 0 console errors
  throughout both runs.

  UPDATE (same day): two people flagged the same real problem within a
  minute of the ship above landing, both about the longitudinal ("fwd/
  back") line specifically - it ran straight down the carrier's own
  centerline (X=0) from the slew centre to each leg's station, same as an
  OEM drawing's own vertical witness line does on paper, but on a 3D model
  viewed from above that line sits directly UNDER the carrier body, so its
  label was unreadable; and because all four legs' longitudinal lines
  shared that one centerline, a right-side leg's own figure was visually
  indistinguishable from a left-side one stacked on the same line.
  Fixed by moving the longitudinal dimension line off the centerline
  entirely - drawn at X = that leg's own full lateral offset (legXMm, the
  same X the leg itself sits at) instead of X = 0, running from a point
  level with the slew centre out at that offset, straight down/up to the
  leg's own position (which is exactly where legXMm/stationYMm already
  point - no separate "station on the centerline" concept needed any
  more). This puts each leg's own longitudinal line beside the carrier on
  its own respective side - matches how a real OEM drawing actually lays
  the vertical chain out (offset beside the plan view, not through it) -
  clear of the carrier body the whole way, and visually separates left
  from right since the two sides no longer share a line.
  Added a new addWitnessLine() helper (thin, dashed, no end ticks, no
  label - LineDashedMaterial, deliberately styled differently from
  addDimensionLine's solid+tick+label look) so the short reference
  connector from the true slew centre out to each leg's own offset line
  reads as "this is where it's measured from", not as a third measurement
  a crew would need to go verify - same reasoning that already justified
  leaving the centerline-to-edge half-carrier-width gap undrawn in the
  lateral line.
  Verified via Playwright + screenshots (LTM 1110): all four legs now
  render on their own respective side (C1/C2 right, C3/C4 left), every
  label fully clear of the carrier body and readable, dashed witness lines
  visible connecting back to the slew centre; 0 console errors.

  UPDATE (same day): two smaller, unrelated requests landed together.
  First, swapped the Crane Positioning sub-tab button order - Crane Layout
  now sits on the left, Support Pad Placement on the right (was the other
  way round) - a pure DOM reorder of the two <button> elements, default
  landing tab (Support Pad Placement) unchanged.
  Second, added a "Bog Mat Marking" card to Support Pad Placement - the
  same inside-edge/outside-edge-from-the-crane concept the ground layout
  marks toggle already uses on Crane Layout, but applied here to a pad's
  own CURRENT (possibly shifted) position instead of the crane's fixed
  baseline, since this is the execution tab. Person's own worked example:
  a leg whose own edge-to-pad-centre distance is 4m, with a 2m-wide pad,
  marks as inside edge (closer to the crane) at 4 - 2/2 = 3m out, outside
  edge at 4 + 2/2 = 5m out - lets a crew square a mat up on bare ground
  with just a tape and paint, without needing to first find the pad's own
  centre point.
  New renderMatMarking() function, called at the end of calcCAD() (so it
  stays live across every shift/pad-toggle/pad-size change, same as every
  other calcCAD()-driven result on this tab): for each leg with a pad
  currently toggled on, edgeToCenterMm = abs(leg.x) - carrierWidthB/2
  (leg.x is whatever position the rest of the tab is already showing for
  that leg - baseX, or the shifted newX once a "must move" shift makes
  that the real target - same value the solid "current" pad rectangle is
  already drawn from), then inside/outside = that +/- half the pad's own
  WIDTH (not length - Pad Width is the lateral dimension per its own
  label, "runs inline with carrier length" is Pad Length instead). New
  card (cad-mat-marking-card), hidden whenever no leg currently has a pad
  toggled on, a 4-column table (Outrigger / To Pad Center / Inside Edge /
  Outside Edge) matching the existing "All Four Legs" table's own style -
  deliberately its own separate compact card rather than two more columns
  bolted onto that existing table, which is already dense enough on a
  narrow mobile viewport.
  Verified via Playwright (LTM 1110): single-leg (C2) and all-four-legs
  cases both render correct figures (C2: 2.89m to centre, 1.69m inside,
  4.09m outside, matching abs(4312mm x) - 1425mm halfB = 2887mm exactly);
  unchecking every pad toggle hides the card entirely; 0 console errors.

  UPDATE (same day): Crane Layout is now the sub-tab that opens by default
  when landing on Crane Positioning (was Support Pad Placement). Swapped
  which button carries the "active" class and which panel's inline style
  starts as display:none (cad-panel-layout now visible by default,
  cad-panel-pads now display:none by default) - switchTab()'s own cad-tab
  handling already checked cad-subtab-layout's own "active" class to
  decide whether to lazy-init the Crane Layout sub-tab's crane selector,
  so no logic changes were needed there, just the two static defaults.
  Verified via Playwright: landing on Crane Positioning now shows Crane
  Layout's panel immediately (pads panel display:none, layout panel
  unset/visible), sc-crane already populated with all 7 cranes; 0 console
  errors.

  UPDATE (same day): person caught a real accuracy bug in the Bog Mat
  Marking table above, within minutes of it shipping - LTM 1110's C2 and
  C3 read meaningfully different "to pad center" distances (2.89m vs
  2.10m) despite being a mirrored pair with an identical physical
  outrigger span, so they should always read the same when unshifted.
  Root cause: the table was built from leg.x, which comes from
  cadFleetData's r/angle (r*sin(angle)) - fine for its EXISTING jobs
  (clash detection, canvas/3D drawing, the "All Four Legs" table), all of
  which only care about the overall reach, but cadFleetData's angle is
  only stored to one decimal place, which isn't precise enough to isolate
  the LATERAL component alone - exactly the same class of imprecision this
  session already worked around once before, when GROUND_LAYOUT_DATA had
  to be extracted fresh from each crane's own OEM dimension chain instead
  of being derived from r/angle (see the "ground layout marks" entries
  above).
  Fix: added a second, accurate lateral-only position (accurateX) to each
  mappedOutriggers entry, computed via a new accurateLateralX() helper -
  side * (span/2), where span is GROUND_LAYOUT_DATA[modelKey].frontSpan or
  .rearSpan (exact, OEM-sourced, identical for both legs of a mirrored
  pair) rather than r*sin(angle). Deliberately does NOT touch leg.x itself
  or any of its existing consumers - kept as a parallel field so the pad
  drawn on the canvas/3D view is unchanged, only the marking table's own
  calculation moved onto the more accurate figure. Mirrors the exact same
  required-to-move/shift logic plotX already uses (accuratePlotX =
  isRequired ? accurateBaseX + shiftX : accurateBaseX) so it still
  responds correctly to a lateral shift - verified a +3m shift moves only
  the two legs actually required to move (by the full 3m, exactly), while
  the two legs that aren't required stay at their own unchanged figure,
  same as the "All Four Legs" table's own optional-vs-required behavior.
  renderMatMarking() updated to read leg.accurateX instead of leg.x; card's
  own on-page disclaimer text corrected to describe this instead of
  claiming it matches the drawn pad's own position.
  Verified via Playwright (LTM 1110, all four pads on): unshifted, C2 and
  C3 now both read 2.08m/0.88m/3.28m exactly (previously 2.89m vs 2.10m to
  centre), C1 and C4 both read 2.73m/1.53m/3.93m exactly; a +3m lateral
  shift correctly moves only the two required legs by the full 3m each,
  leaving the other two unchanged; 0 console errors.

  UPDATE (same day): two more requests landed together - person asked
  whether an earlier message about the Visual Site Plan card had come
  through (it hadn't been actioned yet), plus a new request for a 3D
  toggle showing the bog mat inside/outside edge marks.
  First: Visual Site Plan (the 2D canvas card on Support Pad Placement) is
  now hidden by default behind a "Show Visual Site Plan" button, same
  pattern as the 3D Carrier Preview card right below it - keeps the panel
  shorter to scroll past when the canvas isn't wanted. New toggleSitePlan()
  wraps the toolbar/canvas/legend in a single #cad-siteplan-wrap, toggling
  its display and re-running calcCAD() on reveal - needed because
  drawCADCanvas() sizes itself off canvas.parentElement.clientWidth/Height,
  which reads 0 the whole time the wrap is display:none, so a plain
  re-show without a fresh draw would leave the canvas blank until the next
  unrelated input change.
  Second: added a "Show mat edge marks" checkbox to Support Pad
  Placement's own 3D toolbar - the 3D counterpart of the Bog Mat Marking
  table, drawing both the inside-edge and outside-edge dimension lines
  directly on the model for every leg that currently has a pad toggled
  on. New computeMatMarkingData() helper factored out of renderMatMarking()
  so the table and the new 3D toggle read from exactly one shared
  calculation - can't quietly drift apart from each other. Wired through
  the same fifth pending/context map pair pattern as every other toggle on
  this shared canvas (pendingMatEdgeMarks/pendingMatEdgeContext,
  matEdgeGroup, clearMatEdgeMarks() - included in clearScene(), sceneBox(),
  both branches of __carrier3dActivate()), plus a self-heal call in
  activateCarrier3D() alongside syncCarrier3DOutriggers() so switching
  away to Crane Layout and back doesn't leave the marks blank even though
  the checkbox still shows checked.
  First attempt drew both lines starting at the SAME point (the carrier's
  own edge) running out along the exact same ray - inside line fully
  contained within outside line's own span - with each label placed at its
  own line's midpoint, same as every other dimension line in this app.
  Looked right in isolation but wasn't: screenshotted and found the two
  labels overlapping into unreadable stacked text at any reasonable zoom,
  since the label sprites are wide relative to how close together their
  two midpoints land when collinear. Fixed by offsetting the INSIDE line
  onto its own separate parallel line (400mm toward the vehicle's own
  centre, sign-flipped so it's genuinely "inward" for both front and rear
  legs) rather than collinear with the outside line - matches how the real
  OEM dimension chains already read throughout this app: nested
  measurements drawn as separate parallel lines, not chained end to end
  along one. A thin dashed addWitnessLine() connects the true edge point
  to where the offset inside line begins, same visual language as the
  ground layout marks' own witness connector.
  Verified via Playwright (LTM 1110, all four pads on): Visual Site Plan
  starts hidden, canvas is correctly sized (non-zero) once shown; 3D mat
  edge marks render for all four legs with both labels per leg fully
  legible and non-overlapping, figures matching the 2D table exactly
  (C3: 3282mm outside / 882mm inside); 0 console errors throughout.

  UPDATE (same day): person reported ground layout marks "leaking" from
  Crane Layout into Support Pad Placement's own 3D view - reproduced
  exactly: open Crane Layout's 3D, turn on ground layout marks, switch to
  Support Pad Placement, open ITS 3D (mat edges unchecked, no pads
  toggled) - the ground layout lines showed up anyway.
  Root cause, found in __carrier3dActivate() (vendor/carrier3d.js): the
  function's own comment already said the right thing - "nothing needs
  replaying from here for the synchronous case; only the pendingX replay
  ... for a model still mid-fetch is this function's own responsibility" -
  but the CODE didn't match the comment. The `if (cached)` branch (i.e.
  the model's already loaded, no async fetch needed) unconditionally
  replayed every pendingX map right after the "different card took
  ownership" branch had JUST cleared those exact groups a few lines
  earlier - directly undoing its own clearing. Whichever overlay the
  PREVIOUS card had toggled on would silently reappear on whichever card
  just took over, even though that card owns no matching checkbox and its
  own toggle read unchecked. A genuinely old bug (same shape existed for
  slew circles/leg dimensions since the very first dual-3D-card build,
  see the "single-3D-card misunderstanding" fix earlier in this file) -
  just not surfaced/reported until ground layout marks made it visible.
  Fix: removed the pendingX replay entirely from the `if (cached)` branch
  (all five: sync, slewCircles, legDimensions, groundLayoutMarks,
  matEdgeMarks), keeping it ONLY in the async loadModel() callback branch,
  where it's genuinely needed - a model still being fetched over the
  network when a toggle fires can't apply immediately (each toggle
  setter's own `if (!root) return` early-exit confirms this), so must be
  replayed once loading finishes. Confirmed safe to remove from the cached
  branch because every one of the five toggle setter functions
  (__carrier3dSyncOutriggers, __carrier3dSetSlewCircles,
  __carrier3dSetLegDimensions, __carrier3dSetGroundLayoutMarks,
  __carrier3dSetMatEdgeMarks) already has its own "apply immediately if
  root is cached and currentModelKey matches" fallback built in - which is
  exactly what each card's own activate() wrapper in index.html calls
  synchronously right after __carrier3dActivate returns. The replay in the
  cached branch was therefore not just wrong but entirely redundant.
  Verified via Playwright, both directions: Layout's ground layout marks
  no longer appear on Pad Placement's 3D view after switching (unchecked
  mat-edges box, no pads toggled, screenshot clean); Pad Placement's own
  outrigger pad markers + mat edge marks likewise don't leak onto Layout's
  view when switching the other way; switching back to Layout afterward
  still renders its own toggles correctly when re-checked - 0 console
  errors throughout.

  UPDATE (same day): three smaller fixes landed together.
  1. "All Four Legs — Reference Frames" table is now hidden by default
  behind a "Show All Four Legs" button, same collapsed-by-default pattern
  as Visual Site Plan and the 3D Carrier Preview cards. New
  toggleLegsTable() - simpler than toggleSitePlan() since the table
  content itself (#cad-results) is plain HTML already kept correctly
  up to date by calcCAD() regardless of visibility, so no recalc-on-show
  step is needed, just a display toggle.
  2. Fixed a real staleness bug: changing Pad Width or Pad Length only
  updated the 2D Bog Mat Marking table - the 3D mat edge marks kept
  showing the OLD figures until the 3D view was closed and reopened
  (which happened to mask the bug, since activateCarrier3D()'s own
  self-heal call re-triggers onMatEdgeToggle()). calcCAD() already called
  syncCarrier3DOutriggers() on every recalc to keep the pad MESHES
  themselves live, but never called onMatEdgeToggle() to keep the
  DIMENSION LINES live too - added that call alongside it.
  3. Bog Mat Marking table now reads in mm instead of m - person's own
  reasoning: a tape measure in Australia reads in mm, and this table
  exists specifically to be read directly off a tape on site. Matches the
  unit already used by every other 3D dimension label in the app (ground
  layout marks, mat edge marks, clearance measurement) - only the 2D
  table itself was still in metres.
  Verified via Playwright: legs table starts hidden, expands correctly;
  changing Pad Width from 2.4m to 3m live-updates BOTH the 2D table (582mm
  inside / 3582mm outside) AND the 3D labels (screenshotted, "C2 outside:
  3582mm" / "C2 inside: 582mm") without touching the 3D toggle; table
  cells read whole mm throughout; 0 console errors.

17. HEADER SHARE BUTTON + QR CODE
  New icon button in the header, beside the existing update-check refresh
  icon - opens a small panel with a QR code, the app's own URL as plain
  text, a "Copy Link" button, and a native Share… button (only shown when
  navigator.share exists) - lets a crew member hand the app straight to
  someone else on site (open camera, scan, tap the link) without them
  having to type a URL in.
  Person asked for "a share button like you did for my norwest forms
  pwa" - that other project isn't in this session's repo scope, so rather
  than guess at its exact implementation, asked two quick clarifying
  questions instead: how to source the QR image (their own pasted image,
  which this session has no file-path access to and no tool to extract
  raw bytes from a chat-inline image; vs generating a fresh one), and what
  the button should actually do. Answered: generate the QR code fresh
  (encoding the app's own URL, not a static image), header icon opening a
  small panel with QR + Copy Link + native share.
  QR generation: vendored vendor/qrcode.js - Kazuhiko Arase's
  "qrcode-generator" (MIT-licensed, dependency-free, ~56KB, no build
  step) - same "no CDN, no external service" approach already used for
  three.js/GLTFLoader/DRACOLoader elsewhere in this app, for the same
  reason: the app needs to keep working offline once cached, and a QR
  code generated locally from window.location is always correct - it
  can't silently point at a stale image someone forgot to regenerate
  after the URL changed, the way a static uploaded QR image could.
  Loaded lazily via a manually-created <script> tag (not dynamic
  import() - qrcode.js is a plain classic script, not an ES module) the
  first time the share panel opens, rather than paying its ~56KB on every
  page load for a feature most visits never touch - same lazy-load
  reasoning as carrier3d.js/cwt3d.js, just via a different mechanism since
  the underlying file isn't a module.
  qrcode(0, 'M') - type 0 lets the library auto-pick the smallest QR
  version that fits the URL (confirmed by reading the library's own
  make() - it only auto-detects when typeNumber < 1, tries versions 1-39
  until the data fits), error-correction level M (15% recovery, the
  standard middle-ground default). createSvgTag({ scalable: true }) emits
  a viewBox-only SVG (no fixed pixel size) so the panel's own CSS
  (#share-qr-wrap svg { width: 180px; height: 180px }) controls the
  on-screen size instead.
  shareUrl() strips any query string/hash from window.location - the
  app's own canonical URL is always the meaningful thing to share, not
  whatever incidental state (a scroll anchor, a future deep link) happens
  to be in the address bar at the moment the panel opens.
  Copy Link reuses navigator.clipboard.writeText() where available,
  falling back to the classic hidden-textarea + document.execCommand('copy')
  trick for older/unusual browsers that lack the Clipboard API entirely -
  and reuses the app's own existing showToast() (originally built for the
  update-check button) for the "Link copied." confirmation, rather than
  inventing a second toast mechanism.
  Panel is a simple fixed-position card with a click-to-dismiss backdrop
  (#share-backdrop), not a full modal library - consistent with how
  small, occasional-use UI elsewhere in this app (the update-check toast,
  the update-available banner) is built directly rather than reached for
  a dependency.
  Verified via Playwright: panel opens with a valid QR SVG rendered,
  correct URL shown as text, native-share button correctly hidden in a
  browser context without navigator.share, backdrop click closes the
  panel; Copy Link (tested with clipboard permissions granted) copies the
  exact URL and shows the "Link copied." toast; header layout checked at
  a 360px-wide viewport - QR button, refresh button and the wordmark all
  sit clear of each other with no overlap; 0 console errors throughout.

18. NAV REORDER + NEW CHAIN SHORTENING TAB
  Two requests: move Crane Positioning to sit between Load Chart Finder
  and Driving with Equipment in Place (was between Minimum Hook Block
  Weights and Rope Retentioning); add a brand new tab, Chain Shortening,
  between Minimum Hook Block Weights and Rope Retentioning.
  Nav order is purely a DOM order of the .nav-tabs <button> elements -
  switchTab() shows/hides tab-content divs by id regardless of button
  order, and confirmed the tab-content divs themselves were ALREADY in a
  different physical order than the nav buttons even before this change
  (rope-tab's own <div> sits earlier in the file than hook-tab's, despite
  hook-tab appearing first in the nav) - so reordering the buttons alone
  was sufficient, no risk of breaking anything by leaving the content
  divs' own file order untouched.
  Chain Shortening: person provided their own reference spreadsheet
  (Chain_Length_Calculator.xlsx) to match exactly. Read it directly via
  openpyxl (both the formulas themselves and their computed example
  values, to cross-check): a lookup table of diameter (mm) -> pitch per
  link (mm) - 8->24, 10->30, 13->39, 16->48, 19->57, 22->66, 26->78 (every
  row happens to be exactly 3x the diameter for this chain range, but kept
  as an explicit table matching the source sheet rather than assumed as a
  general multiplier, in case that stops holding for a diameter added
  later) - plus the shortening formula itself: Amount to Shorten =
  Current - Desired; Links to Remove = ROUND(Amount to Shorten / Pitch per
  Link, 0); Actual Finished Length = Current - (Links to Remove x Pitch);
  Difference from Target = Actual Finished Length - Desired. The rounding
  step means a chain often can't land exactly on the requested desired
  length (can't remove a fractional link) - Difference from Target is
  surfaced directly in the result table rather than hidden, so a person
  isn't surprised the finished chain is a few mm off what they typed in.
  Person's own two example phrasings ("I need to remove X amount, how
  many links" and "I have an 8m chain, I need it to be 5.6m, how much to
  remove") both reduce to the exact same current+desired -> links flow the
  reference sheet already implements, so built the one mode that matches
  the sheet exactly rather than two separate input paths.
  New CHAIN_PITCH_TABLE constant + calcChainShortening()/initChainTab()
  functions, in the "TAB 2B" spot in the JS (mirroring the "TAB 3B: CRANE
  SETUP" naming precedent already used for the earlier Crane Layout
  sub-tab, since this is also a small satellite feature bolted on beside
  an existing numbered tab rather than a completely new "TAB 8"), wired
  into the page's own init block alongside the other tabs' startup calc
  calls.
  Verified via Playwright: nav renders in the exact requested order
  (Reeving Plans, CWT Combinations, Load Chart Finder, Crane Positioning,
  Driving with Equipment in Place, Minimum Hook Block Weights, Chain
  Shortening, Rope Retentioning); diameter dropdown lists all 7 sizes;
  default inputs (8mm, 6000mm, 5500mm) reproduce the reference sheet's own
  worked example exactly (21 links, 5496mm actual, -4mm difference); the
  person's own 8m->5.6m example at 8mm gives a clean 100 links / 5600mm
  exact / 0mm difference; a full click-through of all 8 tabs afterward
  came back with 0 console errors throughout.

18. 3D CARRIER PREVIEW: FIT VIEW SCROLL + BOG MAT GHOSTING/DIMENSIONS
  Batch of small 3D-view refinements, iterated with the person over several
  rounds before landing, held as local commits (not pushed) until the
  whole batch was settled - see each commit's own message for the
  blow-by-blow.
  Fit View (both 3D cards - Support Pad Placement and Crane Layout) now
  smooth-scrolls the card itself to the vertical centre of the viewport in
  addition to reframing the camera, via document.getElementById(
  currentWrapId).scrollIntoView({block:'center'}) - carrier3d.js already
  tracks currentWrapId internally (which of the two cards currently owns
  the shared canvas), so no new plumbing needed. Confirmed via Playwright:
  wrap's own bounding-box midpoint lands exactly on the viewport's
  midpoint after clicking Fit View.
  Bog mat pads (the "current" pad mesh drawn per leg in applySync) went
  from a near-solid 0.9 fill opacity down to 0.12, with the outline
  switched from solid to dashed (matching the "if moved" ghost pad's own
  established dashed convention) - the mat edge dimension lines drawn
  through the pad's own footprint were getting buried under the old
  near-opaque fill. The dashed outline itself then needed its own opacity
  set explicitly (0.45) in a follow-up round - it had no transparent/
  opacity property at all, so it defaulted fully opaque and was
  out-competing the dimension lines for attention even after the fill
  itself was ghosted down.
  Mat edge dimension lines (inside/outside per leg) went through three
  attempts before landing: (1) offset onto parallel lines by a flat
  400mm to stop their labels overlapping near the mat's inner corner: (2)
  misread a follow-up sketch as asking for the whole line to run vertical
  along the mat's outer edge - corrected by the person: "I want the txt
  moved not the line showing what the txt represents", reverted to the
  original line geometry with only the label's own position along it
  biased via a new labelT param on addDimensionLine (0=start, 1=end,
  default 0.5); (3) person's actual reference, an OEM outrigger dimension
  sheet photo showing two lines offset onto the object's own two opposite
  ends for separation, not chained through the same station - final
  version offsets inside/outside onto their own parallel lines at roughly
  the mat's own near/far ends (0.4x pad length each direction, leaving a
  small margin off the true corner) rather than a flat 400mm or a label
  trick - real geometric separation, matching the reference photo's own
  style.
  Verified via Playwright/screenshot at each round; final state: 0 console
  errors across a full click-through of all 8 tabs, dimension lines
  visually confirmed sitting at the mat's own two ends with a clean gap,
  ghost pad fill and outline both visibly faded below the dimension
  lines' own full opacity.

19. BUG FIX: "MUST MOVE" STATUS FLIPPING BASED ON PAD CHECKBOX STATE
  Person's own report: with a shift entered and only C4's pad toggled on,
  then Compare old/new turned on, then C1/C2/C3 toggled on - the mat edge
  marks for C1/C2/C3 showed the FUTURE (shifted target) position instead
  of the current one.
  Root cause: calcCAD()'s own isRequired/clash determination
  (checkClash(relX, relY, fp, legPad) and checkClash(newX, newY, fp,
  legPad)) was passed legPad - which is null whenever that SAME leg's own
  pad checkbox happens to be unticked, falling back to a bare-point clash
  test (clashesWithFootprint) instead of the real pad-sized rectangle test
  (padClashesWithFootprint). Toggling a leg's own pad checkbox ON could
  therefore flip that same leg from "optional" to "MUST MOVE" with every
  other input unchanged - confirmed directly: shiftX=1.867m, shiftY=0 on
  the LTM 1130 put C1 exactly in the boundary zone where its bare contact
  point sits clear of the chassis footprint but a 2.4x3.5m pad centred on
  that same point would reach into it - C1 read "optional" with its pad
  off and "MUST MOVE" with it on, no other change. Since accuratePlotX
  (what Bog Mat Marking reads) jumps to the shifted target position
  whenever isRequired is true, this pad-toggle flip was exactly what
  surfaced as the person's bug.
  Fix: the clash tests now always use sharedPadDims() (the shared pad
  size entered above, same for all four legs) rather than legPad -
  whether a mat's own footprint would clash with the chassis is a
  physical fact about the shift and the shared pad size, not something
  that should depend on which leg's checkbox happens to be ticked at the
  moment. legPad itself is untouched for its other job (what's actually
  drawn/shown per leg).
  Verified: reproduced the exact flip on the pre-fix code (C1 optional ->
  MUST MOVE from toggling only its own checkbox, shiftX=1.867m), then
  confirmed identical, order-independent results after the fix - same
  Bog Mat Marking figures for C1-C4 whether pads are toggled on before or
  after Compare old/new, and the same MUST MOVE status regardless of that
  leg's own pad checkbox state. Full click-through of all 8 tabs
  afterward: 0 console errors.

20. TARGET MAT MARKS (SECOND TOGGLE, "IF MOVED" MAT DIMENSIONS)
  Follow-on from the previous entry's bug fix. Person's own reaction to
  seeing shifted-position figures leak into the single existing toggle:
  "I actually wouldn't mind these but it would need to be toggled under
  another button like current mats marks and future mats marks". Renamed
  the existing toggle "Show mat edge marks" -> "Mat Marks" -> (this round)
  "Current Mat Marks", and added a genuinely new second toggle, "Target
  Mat Marks", showing the same inside/outside dimension lines computed
  against each optional leg's TARGET (if-moved) position instead of its
  current one.
  calcCAD() gained accurateTargetX (accurateBaseX + shiftX, unconditional -
  unlike accuratePlotX/accurateX, which only jumps to the shifted spot
  once a leg is actually required). computeMatMarkingData() took a
  useTarget parameter: false reads leg.accurateX/leg.y (unchanged,
  existing behaviour); true reads leg.accurateTargetX/leg.movedY and
  filters out any leg where target equals current (i.e. required legs -
  their current position already IS their target, so showing it twice
  under both toggles would be pure duplication) AND any leg flagged
  leg.clash (the "All Four Legs" table's own NOT POSSIBLE) - moving a pad
  there would clip the chassis, and its own edgeToCenterMm can come out
  smaller than half the pad's width, making insideMm go negative. That's
  technically correct (the pad really would overlap the crane) but not a
  real marking figure a crew could ever act on, so it's excluded outright
  rather than shown with a confusing negative number. Confirmed via
  Playwright against LTM 1130 at two different shifts: one where a
  target's own inside figure came out -742mm before this filter (leg
  correctly excluded after); one with a genuinely valid target (C2,
  optional, not clashing) that renders correctly.
  carrier3d.js: matEdgeGroup (current) and a new targetMatEdgeGroup
  (target) are two fully independent groups/clear functions/pending-
  replay maps, mirroring every other toggle already on this shared canvas
  (slew circles, leg dimensions, ground layout marks) - each checkbox
  clears and redraws only its own group. addDimensionLine gained an
  optional `dashed` parameter (LineDashedMaterial instead of
  LineBasicMaterial for the line itself; the tiny end ticks stay solid
  regardless - dashing something that small reads as broken, not
  "future") - Target's lines render dashed, Current's stay solid, same
  dashed = "if moved" convention already established by the ghost pad
  boxes elsewhere in this app, so both toggles read as visually distinct
  even when on at once for neighbouring legs.
  Verified via Playwright: a scenario with one required leg (target
  correctly absent, current already shows its shifted figure) and one
  valid-target optional leg (both Current and Target render, solid vs
  dashed clearly distinguishable, labels land in genuinely different
  screen positions since the leg's own current vs target Y differ);
  live-updates confirmed by changing Pad Width with both toggles open -
  target marks correctly disappeared once the wider pad made that same
  leg go from optional to required; switching to Crane Layout and back to
  Support Pad Placement with both toggles left on self-heals correctly (no
  duplicate/leaked groups); 0 console errors across the full scenario set
  and a full click-through of all 8 tabs.

21. TARGET MAT MARKS: EMPTY-STATE HINT
  Person's report: with a large shift ("+4 right"), Current Mat Marks
  showed the shifted figures, then unchecking it and checking Target Mat
  Marks "does nothing". Reproduced on LTM 1130 with shiftX=4m, shiftY=0:
  C1/C2 both come out required (their current position already IS their
  target, correctly excluded from Target per the previous entry's design),
  and C3/C4 - optional - both come out leg.clash=true (their own "if
  moved anyway" spot, accurateTargetX around 250mm against a chassis
  half-width of 1425mm, would land the mat well under the chassis), also
  correctly excluded. So at this particular shift, every single leg
  legitimately has nothing valid to show under Target - not a broken
  toggle, just a real "nothing possible right now" outcome that happened
  to be silent.
  Added a small hint div (#carrier-3d-target-empty-hint, same
  cwt-zoom-hint styling already used for the "drag to rotate" hint under
  the 3D canvas) that appears specifically when Target Mat Marks is
  checked AND buildMatEdgeMarks(true) comes back empty - explaining why,
  rather than leaving an unexplained blank toggle a person can't
  distinguish from a bug. Hidden the rest of the time (Target unchecked,
  or Target checked with real marks to show).
  Verified via Playwright: hint hidden with only Current checked; hint
  visible (confirmed via getComputedStyle) with only Target checked at
  the +4m/0m shift that produces zero valid targets; hint disappears
  again once the shift changes to one with a genuinely valid target
  (1.5m/1.5m, C2's own case from the previous entry); full click-through
  of all 8 tabs afterward: 0 console errors.

22. MAT MARKS REDESIGN: CURRENT NEVER JUMPS, TARGET COVERS EVERY LEG
  Person's clarified spec, after seeing Current Mat Marks jump to the
  shifted figures for a required leg: "current mat marks... show were
  they current or should sit from its current position and never change
  just being able to toggle it on and off" - and for Target: "if im
  moving +4 to the right I want this to show me from the current edge of
  crane where the inside and outside will be of the C1/C2 mats" (C1/C2
  being required legs in their own example, not just optional ones) -
  "if the mats will land in the future pos underneath the current pos of
  the carrier there is no need to give dimension as they wont be
  useful". Purpose, confirmed with the person: helps with using a tape
  measure and line marking paint on the ground.
  This superseded both the original Bog Mat Marking design (deliberately
  showed a required leg's shifted position under "current", reasoning
  being that's where the crew should actually place it) and the previous
  entry's Target filter (excluded every required leg, since under the OLD
  design current==target for them).
  calcCAD(): accurateX (the field Current Mat Marks reads) no longer ever
  jumps to the shifted spot for a required leg - it's now unconditionally
  accurateBaseX, the leg's true physical position, full stop. Added
  currentY (unconditionally baseY, same "never jumps" treatment for the Y
  component - leg.y/plotY themselves are untouched, since those still
  correctly drive where the leg/pad marker itself gets DRAWN on the
  canvas/3D view, a separate concern from what the marking figures
  should read). accurateTargetX was already unconditional from the
  previous entry and needed no change.
  computeMatMarkingData()'s target filter dropped the "required legs
  never appear" exclusion entirely (that was only ever a side effect of
  the old jumping accurateX) - now only excludes a leg with literally no
  shift of its own (target trivially equals current) or one flagged
  leg.clash (NOT POSSIBLE - would land under the current chassis).
  Confirmed via Playwright on LTM 1130, shiftX=4m/shiftY=0 (C1/C2
  required, C3/C4 optional-but-clashing): Current Mat Marks' own numbers
  for all 4 legs came back byte-identical between shift=0 and shift=4m/0
  (proving it truly never changes); Target now correctly includes C1 and
  C2 (previously excluded), still correctly excludes C3/C4.
  New clutter this surfaced: a leg's target Y equals its current Y
  whenever the shift is pure sideways (very common - the person's own
  "+4 right" example), which put Current's and Target's inside/outside
  lines for the SAME leg on the exact same row, garbling their labels
  together (screenshotted: "C2 outside: C2 outside: 7525mm", genuinely
  unreadable overlapping text). Fixed in carrier3d.js's drawMatEdgeMarks:
  Target's whole inside/outside pair now gets pushed an extra 0.9x pad
  length further out along Y before applying its own inside/outside
  split, unconditionally (simpler than only applying it when the two
  actually coincide, and harmless when they already differ) - Current
  and Target now always land in clearly separate bands. Re-screenshotted
  to confirm: fully legible now, no character overlap.
  Full click-through of all 8 tabs, plus a spot-check at an extreme
  10m shift (nothing required or clashing - target correctly non-empty
  for all 4 legs, hint correctly hidden): 0 console errors throughout.

23. LTM 1160 UK VARIANT: REAL TAP-DIAGRAM, BUILT FROM THE PERSON'S OWN DXF
  Person wanted the UK-market counterweight variant of the 160t crane
  added. No OEM combination chart exists for it (the document that would
  say which plate subsets are actually approved) - only a photo of the
  exploded-view diagram with weights labelled per piece (5t/6.5t/5t top
  plates, 10t/14t/10t base blocks, 3.5t base spacer - 54t total across 7
  pieces), and a DXF the person traced themselves in AutoCAD LT (a closed
  polyline around each piece's own silhouette, over that same photo).
  Went through several false starts before landing on the right shape:
  (1) first read "map to total weight calculator" as wiring into Load
  Chart Finder - wrong, person clarified "nothing to do with load
  charts, im only trying to build a CWT combo"; (2) built a plain tick-
  list (no diagram) reasoning that no approved-combination chart meant no
  diagram was possible - also wrong, person asked "why isn't there an
  image I can click", clarifying the DXF boxes were always meant to
  BECOME the clickable diagram, not just back a total-weight tally;
  (3) the DXF's own linked reference image (an IMAGE entity pointing to
  a local Windows path) never transferred, and pasting the photo inline
  in chat doesn't give file access either - only an @-attached file does,
  or a Nextcloud share fetched via WebDAV (see the next entry) - that's
  how the actual photo file finally arrived.
  The payoff: the DXF's own IMAGE entity records the EXACT insert point
  and per-pixel u/v vectors used when the photo was placed in AutoCAD
  (confirmed image_size (532,458) matches the photo's real pixel
  dimensions exactly), so every traced vertex converts to an exact photo-
  pixel coordinate with zero guessing: px = (wx-insert.x)/u_pixel.x,
  py = image_size.y - (wy-insert.y)/v_pixel.y. Parsed via ezdxf: all 158
  LINE entities on the 'boxes' layer form exactly 7 closed loops (all
  158 vertices degree-2 in the adjacency graph - simple disjoint
  polygons, no branching), one per piece. Rendered all 7 outlines back
  over the actual photo at their computed coordinates as a verification
  step before writing any hotspot data - every outline landed exactly on
  its own piece's silhouette, and reading the weight label already
  printed inside each outline gave an unambiguous, inspection-verified
  id->weight mapping (no manual matching or guessing which shape is
  which).
  Reused the diagram/hotspot-tap/zoom machinery every other crane already
  has here, rather than a bespoke UI: COUNTERWEIGHT_DATA['1160'] gained a
  ukVariant block with the same shape as a top-level crane entry
  (diagramImage/diagramWidth/diagramHeight/components[with hotspots]),
  but combos: [] (deliberately empty - no OEM chart exists, so nothing
  is ever flagged as an approved combination, matching the pre-existing
  "not an OEM-approved combination" warning path every crane already has
  for an unmatched selection). New cwtData(modelKey) helper is the single
  point every CWT function now reads crane data through - resolves to
  ukVariant's own components/diagram when cwtVariant==='uk', the regular
  top-level entry otherwise - so cwtComponentMap/cwtFindMatchingCombo/
  populateCwtComboSelect/cycleCwtSlotGroup/renderCwtTab etc. all work
  unmodified for the UK variant. populateCwtComboSelect() hides the
  "Jump to Known Combination" card whenever combos is empty (UK), rather
  than showing a pointless empty dropdown. Global | UK toggle only
  appears when the selected crane's data carries a ukVariant key
  (currently just 1160) - person's own request to keep it inside the
  existing crane entry, not a new crane in the dropdown.
  Diagram image saved as counterweight/img/ltm1160-cwt-uk-exploded.png
  (the person's own reference photo, not OEM artwork - flagged as such
  in figRef, same convention as the 1300 global variant's own re-drawn
  diagram).
  Verified via Playwright: toggle only appears for 1160; all 7 hotspot
  aria-labels read the correct name/weight; tapping all 7 (via direct
  toggleCwtPlate() calls, since a synthetic bounding-box-center click
  landed in the spacer piece's own concave notch between its two pins -
  a test-script artifact, not a real interaction, since real SVG polygon
  hit-testing follows the actual fill shape, not its bounding box) totals
  exactly 54.0t matching the photo; "Jump to Known Combination" correctly
  hidden for UK, correctly shown for Global; switching Global<->UK and
  crane<->crane always resets to Global with no stale selection; existing
  Global/EU diagram-tap flow for 1160 (and a spot-check across every
  other crane) completely unaffected; 0 console errors across a full
  click-through of all 8 tabs.

24. PERSON'S NEXTCLOUD - HOW TO PULL A FILE FROM "MY NEXTCLOUD"
  Person's own reference point whenever they say "my Nextcloud" or "same
  link I've pasted in the past": local.idull.au, the same host already
  referenced in sections 14/15/18/20 for the original carrier/counterweight
  exports and OEM manual PDFs. A share link looks like
  https://local.idull.au/index.php/s/<TOKEN> - the token is a public
  share token, used as the WebDAV basic-auth username with an empty
  password: `curl -u "<TOKEN>:" https://local.idull.au/public.php/webdav/`
  lists (PROPFIND) or downloads (GET) whatever that particular share
  exposes - a single file, or a whole folder tree (browse with `curl -u
  "<TOKEN>:" ... -X PROPFIND -H "Depth: 1"`, fetch a specific path by
  appending it, URL-encoding spaces as %20). This session's own token
  (Twn8N4Z7xb3QiEk) exposed the 1160 UK reference photo directly at the
  share root plus Carrier/ and Counterweight/ sibling folders - each new
  share the person sends will have its own fresh token, but the pattern
  (extract token from the /s/ URL, WebDAV GET/PROPFIND with that token as
  the basic-auth username) is the constant part worth remembering. This
  particular share's token was scoped narrowly to just what that
  conversation needed - a later, broader share (section 25) turned out to
  expose the person's whole Nextcloud root instead, so "my Nextcloud"
  resolves to whatever token they most recently sent, not a fixed token
  value - always re-derive from the latest /s/<TOKEN> link they give.

25. LTM 1300-6.2 UK VARIANT - REAL OEM DATA, NOT AN ESTIMATE
  Same starting point as 1160 UK (methodology.txt 23): person supplied a
  DXF (10 piece outlines traced in AutoCAD LT over their own reference
  photo, boxes layer) plus the photo itself (824x442, GB flag "96 t"
  badge, on Nextcloud). Person's explicit scope instruction: move the
  existing (at the time PROVISIONAL) COUNTERWEIGHT_DATA['1300'] entry to
  become the "Global" variant as-is, and add this DXF+photo data as a new
  "UK" ukVariant sibling - same cwtData()/Global|UK toggle architecture
  1160 UK already built, no JS changes needed, just data.
  Loop extraction hit a new failure mode 1160 didn't have: a naive
  vertex-based or edge-based walk of the 254 LINE segments only found
  9 loops instead of 10, because the receptacle (13t) and counterweight-7
  (3t) outlines share a real boundary vertex where they touch - any
  single-path walk that greedily picks "the first unvisited edge" at that
  vertex can wander from one piece's outline into the other's instead of
  closing its own loop. Fixed by using shapely's polygonize() (planar
  subdivision from the full set of line segments, via
  shapely.ops.unary_union + polygonize) instead of a hand-rolled graph
  walk - correctly resolves all 10 closed faces regardless of shared
  vertices, no special-casing needed. World->pixel transform (DXF's own
  IMAGE entity: insert (0,0,0), u_pixel/v_pixel 0.2646202699126753,
  image_size 824x442, exact match to the real photo's pixel dimensions)
  reused unchanged from the 1160 method. Rendered all 10 polygons over
  the actual photo as a verification step (same as 1160) - every outline
  landed exactly on its own piece, confirming the shapely fix.
  Then the person asked to look up "the ltm1300 operator manual, chapter
  4.07 counterweight and combinations" on their Nextcloud - a different,
  broader share this time (a whole personal Nextcloud root, not a scoped
  folder - see the note appended to section 24), containing a full
  2167-page Liebherr operating-instructions PDF for this exact crane
  under CRANES/Liebherr cranes/LTM1300-6.2/071845_LTM_1300-6.2_v100_en/
  Operating_instructions_Crane/. Chapter 4.07 "Counterweight" (pages
  931-940) turned out to be the SAME chapter the already-shipped Global
  entry's manualUrl/manualPage:931 was already pointing at - but nobody
  had actually read it against the Global entry's own (admittedly
  PROVISIONAL) combo data before now. Reading it: "1 Overview of
  counterweight" lists the exact 10 physical pieces (1x receptacle plate
  1, 1x counterweight 7, 1x counterweight plate 2, 2x counterweight plate
  3, 1x counterweight plate 4, 2x auxiliary ballast 5, 2x auxiliary
  ballast 6) matching the person's 10 DXF loops and photo one-for-one,
  and "3 Counterweight combinations" / "3.1 Special ballast 66 t and
  special ballast 86 t" gives the full confirmed tier table: 0/13/16/26/
  36/46/56/66/76/86/96t, where 66t and 86t are "special ballast" tiers
  that use three spacer pins in place of the second counterweight-plate-3
  copy rather than an actual plate. This is a genuinely different base
  pair (receptacle 13t + counterweight-7 3t = 16t combined) than the
  existing Global entry's own base pair (receptacle 6t + Plate 5 10t =
  16t combined, per that entry's own comments "Unique to this
  (global/export) variant") - not a contradiction, just different
  physical hardware between the export/Global crane and this UK one, so
  the Global entry was left completely untouched as instructed.
  ukVariant.combos is therefore NOT empty like 1160 UK's was (no chart
  existed there) - it's the manual's own tier table, transcribed exactly,
  with manualUrl/manualPage:931 carried over onto the ukVariant block
  too since it's confirmably the same document/page. sourceNote reflects
  this is confirmed OEM data, not an estimate.
  Inner-vs-outer wing pairing (aux5 = inner pair closer to the main
  stack, aux6 = outer pair) follows the same left+right-symmetric
  convention as the Global entry's own aux5/aux6, matched by stack
  position since the manual's chart doesn't distinguish inner/outer by
  name, only by weight (all 4 wing pieces are 10t).
  Image saved as counterweight/img/ltm1300-cwt-uk-exploded.png, added to
  sw.js APP_SHELL. model3d intentionally omitted from ukVariant - no 3D
  model exists for this variant's actual hardware - which correctly
  keeps the 2D/3D view toggle hidden via renderCwtTab()'s existing
  `data.model3d ? '' : 'none'` gate, no code change needed.
  Verified via Playwright: Global|UK toggle appears for 1300 only;
  ukVariant has no model3d (view toggle hidden); all 8 components/10
  hotspots present with correct aria-labels; direct toggleCwtPlate()
  calls across all 8 components total exactly 96.0t; all 11 combo tiers
  compute to 0/13/16/26/36/46/56/66/76/86/96t exactly matching the
  manual; a real (non-force) click on the visible receptacle-plate area
  correctly resolves to "Receptacle Plate 1" with the "OEM-approved
  combination" checkmark, confirming the nested 3t Counterweight-7
  hotspot (listed after the 13t receptacle in the components array, so
  it paints on top in the SVG) doesn't swallow clicks meant for the
  surrounding 13t piece; switching Global<->UK leaves the Global entry's
  own data/3D model completely unchanged; full regression sweep across
  every crane's CWT tab plus the other 8 app tabs, 0 console errors.

26. 360 SLEW CLEARANCE - "BEYOND REAR OF CARRIER" (mirrors the existing
    "beyond side" feature, plus a tool box add-on toggle)
  Person's request: the existing 360° Slew Clearance card only ever
  measured how far a swing-radius point oversails the carrier's SIDE
  (radius - carrierWidthB/2) - wanted the same idea for the REAR (radius
  - some rear reference), since a crew backed up close to something
  behind the crane needs that figure just as much as the side one.
  Complication: most of the LTM fleet has a rear-mounted "combi box"
  accessory that sticks out further back than the bare chassis - wanted
  a toggle to include or exclude it from the rear figure.
  Turned out the base rear reference didn't need sourcing at all - it
  already existed. FOOTPRINTS[modelKey].rear (methodology.txt 4.5,
  "distance from slew center to the rear tip of the chassis body") is
  exactly the same measurement already used for the chassis-footprint
  collision check, sourced and cross-validated per crane back when that
  feature was built. Every crane in SLEW_CLEARANCE_DATA already has one.
  The tool box depth needed real sourcing though. Confirmed 810mm from
  TWO independent OEM manuals, not one: LTM 1300-6.2's own operator
  manual (chapter 1.03 Technical data > 9.16 Combi box, fetched from the
  person's Nextcloud - 0.81m depth x 2.50m width x 1.03m height) and LTM
  1110-5.1's own manual (same chapter, "16 Combi box", 0.81m x 2.50m x
  1.02m) - same physical part, independently dimensioned in two separate
  manuals, exact match on the depth that matters here. Checked all 7
  fleet manuals for it: LTM 1130-5.1/1160-5.2/1250-5.1 each confirm the
  box is actually fitted (same "Overloading of the combi box is
  prohibited" warning page in all three) but don't repeat its own
  dimensioned drawing in these particular manual editions - treated as
  the same fleet-standard 810mm part given the exact two-source
  agreement, not a guess. LRT 1100-2.1's manual has zero "combi box"
  mentions anywhere in 1197 pages (different chassis architecture, not
  road-driven the same way as the LTM range) - correctly gets no tool
  box option. LTM 1650-8.1 was confirmed directly by the person - a
  combi box never gets fitted to it - also correctly gets no tool box
  option. COMBI_BOX_DEPTH_MM{} holds the 5 LTM keys only (1110/1130/
  1160/1250/1300); its absence for a modelKey is what drives the "Include
  tool box" checkbox's own visibility in both places it appears.
  Two places this needed wiring, both mirroring the existing side-
  clearance pattern exactly rather than inventing a new one:
    1. The numeric "360° Slew Clearance" table (Crane Layout tab) gained
       a 4th column, same per-point rows as the existing side column,
       computed as radius - (FOOTPRINTS.rear + (toolbox checked ?
       COMBI_BOX_DEPTH_MM : 0)). New "Include tool box" checkbox (only
       rendered when the selected crane has a COMBI_BOX_DEPTH_MM entry)
       drives scIncludeToolbox, reset to false on every crane switch -
       same reasoning as every other per-crane toggle in this app, a
       choice made for one crane should never silently carry over and
       apply to the next crane's own figure.
    2. The 3D Carrier Preview's toggle list gained a second checkbox
       "Show clearance measurement (beyond rear of carrier)" alongside
       the existing side one, plus a conditionally-rendered "Include tool
       box" sub-checkbox nested under it. Both feed the same
       onSlewCircleToggle() the side checkbox already used, which now
       also computes rearMm and passes it through to
       window.__carrier3dSetSlewCircles as a new 6th argument.
  carrier3d.js's applySlewCircles() gained the matching rearMm parameter
  and draws a second dimension line per circle when it's set - same
  addDimensionLine() helper the side line already used, same per-circle
  loop, just a different pair of endpoints. The side line hardcodes
  world +X since every crane's calibration is guaranteed lateral-
  symmetric; the rear line deliberately does NOT hardcode +Z the same
  way - it computes BOTH endpoints via siteToWorld(cal, 0, mm), which is
  the one place in this file that already carries the correct sign per
  crane (computeFormulaCalibration's own dirSign, chosen so site-space
  +Y/rear always maps to increasing world Z regardless of which end of a
  given crane's own CAD export sits at min vs max Z - see that
  function's comment). Hardcoding +Z here would have silently drawn the
  rear dimension line pointing at the FRONT on any crane calibrated with
  frontAtMinZ false, without erroring - confirmed correct instead of
  assumed, by cross-checking the 3D line's own on-screen label against
  the numeric table's figure for the same crane/point (642mm in both
  places for LTM 1300's Winch 2, no toolbox - see verification below).
  pendingSlewCircleContext (the replay-once-loaded state for a model
  still mid-fetch when the toggle fires) now carries rearMm alongside
  carrierWidthMm, same replay pattern as everything else in this file.
  CARRIER3D_VERSION bumped 41->42 (carrier3d.js itself changed).
  Verified via Playwright: numeric table's new rear column present and
  correct for all 7 cranes with FOOTPRINTS-only rear figures (including
  a legitimate NEGATIVE clearance for LRT 1100 - Counterweight's own
  4.19m radius doesn't even reach its 4.39m rear overhang, a real result,
  not a bug); "Include tool box" checkbox correctly hidden for 1100/1650,
  correctly shown and correctly shifts the rear column for the other 5;
  checkbox state resets on crane switch; 3D preview's two new checkboxes
  render with correct conditional nesting; a real 3D activation +
  Fit View screenshot for LTM 1300 shows both dimension lines rendered
  correctly - side line labelled 3470mm (exact match to the numeric
  table's 3.47m for Winch 2) and rear line labelled 642mm (exact match to
  the numeric table's 0.64m for the same point, before ticking the tool
  box checkbox); full regression sweep across every crane on both Crane
  Layout and Support Pad Placement (which shares carrier3d.js) plus the
  other 4 top-level tabs, 0 console errors throughout.

  26.1 "Include tool box" now defaults ON, not off
    Person independently hand-verified the LTM 1300 figure against the
    OEM dimension chain (2407+1650+320 rear-to-slew-centre + 810mm box =
    5187mm; 5600mm Counterweight radius - 5187mm = 413mm) - matched the
    app's own 412mm to within 1mm (the app's FOOTPRINTS.rear of 4378mm
    vs. their 4377mm hand-sum, an already-known rounding difference from
    how that figure was originally cross-validated, not a new error).
    Then flagged that the checkbox defaulting unticked was actively
    wrong for how the fleet is really run: every LTM crane that carries
    a combi box has it physically fitted in real life, so an unticked
    default was showing an 810mm-too-generous rear clearance unless
    someone remembered to tick the box every single time - a real
    number, but not the number a crew should see without asking.
    Fixed by defaulting scIncludeToolbox to true (was false) and adding
    `checked` to both the numeric table's static checkbox and the 3D
    preview's per-crane-rendered one, keeping the toggle itself (still
    resets to true, not the previous choice, on every crane switch -
    someone unticking it for the rare box-less crane never carries that
    over) rather than removing it and hardcoding the box into
    FOOTPRINTS.rear directly, since the checkbox is what surfaces the
    correction as a visible, explained figure instead of a silent one.
    CACHE_VERSION v139->v140, app version v3.9->v3.10.

  26.2 Two more LTM 1650 radius points: TY's and T3N
    Person's first message read as two more REAR-reference figures
    (given the immediately preceding rear-clearance work), but the
    numbers (11646mm, 11485.4mm) didn't fit that pattern at all - over
    3x the 1650's own base rear overhang (3717mm). Asked before touching
    anything; person corrected: these are two more SWING RADIUS points
    (same category as the existing Counterweight/Winch 2/Ballast Radius
    entries), not rear-overhang references - "clearance required if the
    TY's are fitted and open to 40 degrees" and "if the N assembly is
    fitted to the T3 boom".
    Added both to SLEW_CLEARANCE_DATA['1650'].fixed (was empty) rather
    than folding them into the existing "Ballast Radius (Site Clearance)"
    variants group - fixed points are simultaneously-checkable
    independent checkboxes, variants are a mutually-exclusive picker
    (only one ballast tray radius is ever physically fitted at once).
    TY's and T3N describe separate rigging configurations, not
    alternatives to the ballast tray or to each other, so fixed is the
    correct shape - person can check either, both, or neither. No other
    code changes needed anywhere: both the numeric 360° Slew Clearance
    table and the 3D preview's own toggle list already render every
    `fixed` entry generically (same mechanism the Counterweight/Winch 2
    checkboxes already used for other cranes), so the two new points
    just appeared correctly in both places, side AND rear clearance
    columns, the moment the data was added.
    Verified via Playwright: both new checkboxes present with correct
    labels/radii in the numeric table and the 3D toggle list; side/rear
    clearance columns compute correctly for both (TY's: 11.65m radius ->
    10.10m beyond side, 7.93m beyond rear with tool box N/A for 1650;
    T3N: 11.49m -> 9.94m / 7.77m); TY's correctly becomes the new
    limiting point (largest radius, beating the pre-existing Ballast
    Radius options); full regression sweep, 0 console errors.
    CACHE_VERSION v140->v141, app version v3.10->v3.11.

27. REMOVED: Crane Layout's "Show outrigger distances (C1-C4, baseline
    position)" toggle
  Person's request. Removed cleanly end to end rather than just hiding
  the checkbox, since nothing else in the app referenced any of it:
    - index.html: the checkbox itself (renderCarrier3DSlewToggles), its
      onLegDimensionToggle() handler, and both call sites
      (renderCarrier3DSlewToggles' own render pass and
      activateCarrier3DLayout's re-sync on switching back to the Crane
      Layout sub-tab).
    - carrier3d.js: legDimensionGroup (the THREE.Group holding the
      lines), clearLegDimensions()/its 3 call sites (clearScene,
      sceneBox's Fit View bounding-box union, and the "switching cards"
      clear block that also clears slew circles/ground layout/mat edge
      marks), applyLegDimensions(), pendingLegDimensions/
      pendingLegDimensionContext (the replay-once-loaded state) and
      their own replay-branch call, and the window.__carrier3dSetLegDimensions
      export itself. Left a comment cleanup on
      pendingGroundLayoutMarks's own comment too, since it referenced
      "pendingLegDimensions above" and "the diagonal C1-C4 lines" by
      name for context that no longer exists.
    Nothing else in the app called any of this - it was Crane Layout's
    own toggle only, entirely separate from Support Pad Placement's own
    (still-present) C1-C4 diagonal "as the crow flies" distances and its
    ground layout marks, which use a completely different code path.
    CARRIER3D_VERSION bumped 42->43 (carrier3d.js changed).
    Verified via Playwright: checkbox and its label gone from the 3D
    toggle list; every remaining toggle (radius points, side/rear
    clearance + tool box, ground layout marks) still renders and
    functions correctly with 0 console errors; full regression sweep
    across every crane on both Crane Layout and Support Pad Placement
    (which shares carrier3d.js) plus the other 4 top-level tabs, 0
    console errors throughout.
    CACHE_VERSION v141->v142, app version v3.11->v3.12.

28. BETA-ONLY: Welcome tab "What Each Tab Does" brought up to date
  Person asked to check whether the Welcome tab (beta-trial only, never
  shipped to main - see CLAUDE.md) still covered every real tool tab.
  It didn't: checked the nav bar's 8 tool tabs against the "What Each
  Tab Does" list and found only 6 described - Load Chart Finder and
  Chain Shortening were missing entirely (both added to the app after
  this card was last written), and Crane Positioning's own entry
  ("Outrigger Support Positioning") only covered the Support Pad
  Placement sub-tab, saying nothing about the Crane Layout sub-tab's own
  now-substantial feature set (360 slew clearance, 3D carrier preview,
  ground layout marks).
  Rewrote the list to match the nav bar's own order exactly (previously
  it didn't - Driving with Equipment in Place and Minimum Hook Block
  Weights were ahead of where Load Chart Finder/Crane Positioning now
  sit), added the two missing entries, and expanded Crane Positioning's
  entry to describe both its sub-tabs by name. Beta-only file
  (welcome-tab only exists on beta-trial/myslewer-beta, per CLAUDE.md) -
  no main-branch change, no cherry-pick to main.
  Verified via Playwright screenshot: all 8 entries render in the
  correct order with 0 console errors.
  CACHE_VERSION v51-beta93->v51-beta94 (beta-trial's own scheme, no
  main-side CACHE_VERSION or app-version bump since main is untouched).

29. BETA-ONLY: Trial extended another week
  Person's request. window.TRIAL_EXPIRES (index.html, right after
  <body>) was 2026-08-19T00:00:00 - bumped to 2026-08-26T00:00:00 (+7
  days from the existing expiry, not from today). Single source of
  truth for both the hard lockout check and the Welcome tab's own
  trial-end-display text, so both updated from the one edit with no risk
  of drift. Beta-only file, no main-branch change.
  Verified via Playwright: window.TRIAL_EXPIRES reads the new date,
  Welcome tab displays "Wednesday 26 August 2026", trial-expired class
  correctly absent (not locked out).
  CACHE_VERSION v51-beta94->v51-beta95.

30. Header "Offline Content" icon
  Person's request: the "Cache All Offline Content" button only existed
  buried inside an "Offline Availability" card on 4 specific tabs
  (Reeving Plans, CWT Combinations, Driving with Equipment in Place,
  Load Chart Finder) - nothing showed it on the other 4, so there was no
  single obvious place to go check "is everything downloaded" or trigger
  it. Wanted a permanent icon in the header, left side (opposite the
  existing share/update-check pair on the right), using a download-into-
  a-box style icon.
  Icon: Feather's own "download" glyph (open-top box + arrow into it) -
  matches the download/archive concept cleanly and keeps the same icon
  style (stroke-only, 24x24 viewBox) already used for the two right-side
  buttons, rather than introducing a different icon set.
  New #cache-btn opens a #cache-panel styled identically to the existing
  #share-panel (same fixed top-center overlay + backdrop pattern,
  toggleCachePanel()/closeCachePanel() mirroring toggleSharePanel()/
  closeSharePanel()). The panel's own contents are a straight copy of
  the existing "Offline Availability" card markup, using the exact same
  classes (.cache-all-status, .cache-all-btn, .cache-all-progress-wrap/
  -bar/-text) the 4 existing per-tab instances already use - both
  cacheAllOfflineContent() and checkOfflineContentStatus() already
  operate via document.querySelectorAll() across every matching element
  on the page rather than a specific id, so this new instance needed ZERO
  changes to either function to participate correctly; it just works.
  toggleCachePanel() also calls checkOfflineContentStatus() on open,
  since content can get cached passively elsewhere in the app between
  page load and whenever the panel is opened - re-checking on open keeps
  the header's own status from ever showing stale.
  Verified via Playwright: icon renders top-left, panel opens on click
  showing live status ("Not fully downloaded (192 missing)" pre-cache,
  matching the per-tab cards exactly), closes on backdrop click; full
  regression sweep across every crane/tab, 0 console errors.
  CACHE_VERSION v142->v143, app version v3.12->v3.13.

29. Ground layout marks: half carrier width dimension
  Person's request, with a CAD screenshot as reference: a dimension
  bracket labelled "1550mm" drawn beside (not through) a carrier, in
  open space, for a value that's exactly half a 3100mm-wide carrier -
  wanted this same half-width figure added to "Show ground layout marks"
  as part of Crane Layout's 3D preview.
  This exact gap was already known and deliberately left undrawn -
  applyGroundLayoutMarks' own comment in carrier3d.js already called it
  out: "The short unmarked gap between the centerline and the edge point
  is deliberately left undrawn - it's just half the carrier's own
  (known, fixed) width, not something to remeasure." Person's request
  reverses that call for this specific figure - wanted it drawn after
  all, matching the reference image's own convention.
  Needed zero index.html changes: onGroundLayoutToggle() already passes
  FOOTPRINTS[modelKey] straight through to
  window.__carrier3dSetGroundLayoutMarks() as its own footprint
  argument (previously only used there to compute a fallback slew
  calibration), so footprint.width was already available inside
  applyGroundLayoutMarks() without adding a new parameter anywhere.
  Added two new dimension lines (carrier3d.js only) - one per side, from
  the slew centre (already computed as p0) out to (±halfB, 0) - drawn at
  Y=0 (through the slew centre's own station) since that's a single,
  unambiguous reference point that's clear of every per-leg line above
  (those sit at each leg's own stationYMm, essentially never exactly 0),
  and in a neutral off-white (#f8fafc, matching LEG_MARKER_COLOR) rather
  than any one leg's own colour, since this figure isn't tied to a
  specific leg - reuses the existing addDimensionLine() helper directly,
  no new drawing code needed.
  CARRIER3D_VERSION bumped 43->44 (carrier3d.js changed).
  Verified via Playwright + screenshot for LTM 1300: both "½ carrier
  width: 1550mm" labels render on either side of the carrier body at the
  slew centre's own station, correctly matching carrierWidthB/2 (3100mm
  -> 1550mm), positioned clear of and not overlapping any of the 4 per-
  leg dimension lines; full regression sweep across every crane/tab, 0
  console errors.
  CACHE_VERSION v143->v144, app version v3.13->v3.14.

30. Header cache icon redrawn as an actual treasure chest
  Person's follow-up on 27 (the header "Offline Content" icon): the
  original was a generic download-into-a-tray glyph, close enough to
  ballpark the "treasure chest with a download arrow" ask but not the
  real thing. Person supplied an actual chest icon (ChatGPT-generated,
  solid black/white with a keyhole latch and corner rivets) and asked
  for it in the app's own gold, or something similar in that style.
  Redrawn from scratch as a stroke-only line icon (not the solid-fill
  style of the supplied reference) to match the two existing header
  icons' own visual language exactly (Feather-style: 24x24 viewBox,
  stroke-width 2, round caps/joins, no fill) rather than mixing icon
  styles in the same header. Built from an open lid (two trapezoid
  paths meeting at a gap in the middle), a rounded-rect body, a
  keyhole (circle + short stem) on the front, and a downward arrow
  (line + chevron polyline) passing through the lid's gap into the
  keyhole - same "treasure chest + download arrow" concept as the
  reference, redrawn in the app's own icon language. Colour is free:
  #cache-btn already sets color:var(--accent-gold) and every path uses
  stroke="currentColor", the same mechanism the share/update-check
  icons already use - no icon-specific colour code needed anywhere.
  Verified via a standalone render at both the real 18px button size and
  a large preview before committing it, then via Playwright screenshot
  in the actual header (gold, reads clearly as a chest+arrow at real
  size); full regression sweep, 0 console errors.
  CACHE_VERSION v144->v145, app version v3.14->v3.15.

31. BETA-ONLY: Bug-report email added to Welcome tab
  Person's request: myslewer@gmail.com (confirmed against the on-file
  address after the person's first message typed it one character short
  - checked before publishing rather than assuming the typo was
  intentional). Added as a mailto: link in the Welcome tab's own intro
  card, right after the existing "that's exactly the kind of feedback
  this trial is for" line. Beta-only file, no main-branch change.
  Verified via Playwright: mailto href reads exactly
  mailto:myslewer@gmail.com; full regression sweep, 0 console errors.
  CACHE_VERSION v51-beta98->v51-beta99.

32. Header cache icon redrawn again - solid two-tone, not stroke outline
  Person's feedback on 30's stroke-line chest: "the top of the chest lid
  looks cut off". Rendered 5 more stroke-outline variants (open dome
  lid, closed dome, corner studs, flat-top case, plain archive box) to
  isolate the actual problem before guessing at a fix - person picked
  none of them, said the whole family "look like a mailbox not a chest"
  and asked for something closer to their own reference image's actual
  style.
  Root issue wasn't the specific geometry, it was the render technique:
  the reference image is a SOLID two-tone flat icon (black silhouette +
  white cutout details for the arrow/keyhole/trim lines), not a thin
  stroke-only line icon like the app's existing share/update-check
  icons. A thin-stroke chest outline was never going to read as
  convincingly "chest-shaped" at 18px regardless of which curve
  variant - not enough contrast/mass to the shape.
  Switched technique entirely: built 3 solid-fill variants (fill=
  currentColor for the chest body, fill="#272a30" - the app's own
  --input-bg value - for cutout details layered on top, exactly
  mirroring the reference's black+white two-color construction just
  recoloured). Person picked "H": a loaf-top chest silhouette, two
  --input-bg-coloured horizontal bands near the top (lid trim lines,
  matching the reference's own detail), a downward arrow cut into the
  lid, a keyhole cutout at the base centre, and 4 small corner-rivet
  cutout dots - closest match to the reference image of the 8 total
  variants explored across both rounds.
  Implementation note: the button's SVG previously carried
  fill="none" stroke="currentColor" on the outer <svg> tag (inherited by
  every child element, the mechanism the share/update-check icons still
  use) - removed entirely for this icon and replaced with an explicit
  fill="currentColor"/fill="#272a30" on every individual path/shape,
  since a solid icon has no use for those inherited stroke attributes
  and leaving them in place would have inherited an unwanted gold
  outline onto the cutout shapes too.
  Verified via Playwright screenshot (both full header context and a
  cropped zoom of just the button) - chest silhouette reads clearly at
  real 18px size with no cut-off top; full regression sweep, 0 console
  errors.
  CACHE_VERSION v145->v146, app version v3.15->v3.16.

32. Half carrier width dimension corrected: rear station, C1 side only
  Person's correction on 29: their own reference drawing had this
  dimension positioned at the VERY REAR of the carrier, not through the
  slew centre's own station (Y=0) - and only on one side (C1's side),
  not both, since it's a fixed symmetric figure with no point drawing it
  twice.
  Repositioned the single dimension line from (0,0)->(halfB,0) [drawn
  twice, ±halfB] to a single line at footprint.rear's own station -
  (0,footprint.rear)->(halfB,footprint.rear) - reusing the same
  footprint.rear figure the rear-clearance feature (methodology.txt 26)
  already established as "distance from slew centre to the rear tip of
  the chassis body", so this now sits exactly at the carrier's own rear
  edge, matching the person's drawing. +X only (C1/C2's own side in
  legSpecs above), not ±X.
  CARRIER3D_VERSION bumped 44->45 (carrier3d.js changed).
  Verified via Playwright + screenshot for LTM 1300: single "½ carrier
  width: 1550mm" label now renders at the very bottom (rear) of the
  carrier body on the C1 side only, clear of the 4 per-leg lines; full
  regression sweep, 0 console errors.
  CACHE_VERSION v146->v147, app version v3.16->v3.17.

33. Crane Layout: collapsible cards + new "Layout Dimensions" text card
  Person's request, two parts (explicitly held off the half-width-
  under-carrier fix from 32 for a later session):
    1. A new "Layout Dimensions" card below the 3D Carrier Preview,
       listing the same ground-layout figures the 3D preview's "Show
       ground layout marks" toggle already draws (per-leg fwd/back +
       out-from-edge, plus the half carrier width), as plain numbers -
       for anyone who'd rather just read a table than orbit a 3D model.
    2. Both the new card and the existing "360° Slew Clearance" card
       made collapsible, closed by default, to cut down the information
       overload Crane Layout had accumulated across this session's
       string of additions (rear clearance, tool box toggle, TY's/T3N,
       ground layout marks, half carrier width...).
  Refactored onGroundLayoutToggle()'s own marks/halfB computation out
  into a new shared computeGroundLayoutMarks(modelKey) - used by both
  the existing 3D toggle path and the new renderLayoutDimensionsList(),
  so the two can never drift apart from each other (same numbers,
  computed once). renderLayoutDimensionsList() is independent of the 3D
  toggle's own on/off state and of whether a crane even has a 3D model
  at all (GROUND_LAYOUT_DATA/FOOTPRINTS cover the same 7 cranes either
  way) - always populated the moment a crane is selected, called from
  renderSlewClearanceResults() (already fires on every crane switch).
  Collapsible mechanism: native <details>/<summary> - the app already
  had exactly one precedent for this (Driving with Equipment's
  "Advanced" boom config toggle), reused here rather than inventing a
  second pattern. <summary class="card-title"> reuses the existing
  card-title look exactly (same gold/uppercase/border styling already
  used everywhere), with the native disclosure marker hidden (both
  ::-webkit-details-marker and browsers where display:flex already
  suppresses it) in favour of an explicit ▾/▴ chevron that rotates on
  the [open] attribute, so the same card-title bar now doubles as an
  always-legible expand/collapse control without changing how any other
  (non-collapsible) card-title looks anywhere else in the app - the CSS
  only targets details.card > summary.card-title specifically.
  Verified via Playwright: both cards render closed by default with
  correct chevron direction; layout-dims-results/halfwidth populate
  correctly even while the card is collapsed (data isn't gated on the
  <details> being open); expanding both shows all figures matching the
  3D preview's own numbers exactly; full regression sweep, 0 console
  errors.
  CACHE_VERSION v147->v148, app version v3.17->v3.18.

34. Chain Shortening: "Shorten By Amount" input mode
  Person's request: didn't want to have to know/enter a chain's current
  length just to find out how many links to remove - wanted to type the
  amount to shorten by directly (e.g. 500mm) and get links to remove for
  the selected diameter.
  Added an "Input Method" mode-toggle (Current & Desired Length / Shorten
  By Amount) above the existing inputs - same mode-toggle/mode-btn
  pattern already used elsewhere in the app (Load Chart Finder's own
  sub-tabs, Driving with Equipment's direction toggle), not a new UI
  pattern. 'amount' mode swaps the Current/Desired Length fields for a
  single "Shorten By" field and reuses the exact same formula
  (calcChainShortening() now branches on chainMode, both branches share
  CHAIN_PITCH_TABLE and the same round-to-nearest-link math) - "Actual
  Finished Length" (which needs a starting length to mean anything) is
  replaced with "Actual Amount Shortened" (Links to Remove x Pitch per
  Link) in this mode, "Difference from Target" carries over unchanged
  (still Actual - Target, just against the shorten amount instead of the
  desired length).
  Note for the person: their own example (500mm on 8mm chain = 20 links)
  doesn't match what the existing, already-verified CHAIN_PITCH_TABLE
  (8mm -> 24mm/link, matching their own reference spreadsheet) computes -
  500/24 = 20.83, which rounds to 21 links (504mm actual, +4mm over), not
  20. Didn't change the pitch table over an offhand example - flagged
  this instead, same formula/table both modes already share.
  Verified via Playwright: default length-mode result unchanged from
  before this change; amount-mode for 8mm/500mm gives 21 links/504mm/
  +4mm as described above; field visibility toggles correctly between
  modes both directions; full regression sweep, 0 console errors.
  CACHE_VERSION v148->v149, app version v3.18->v3.19.

35. AR real-world-scale placement, Phase 1 (LTM 1110, Android only)
  Person asked whether the 3D models could be viewed at real-world scale
  in a physical space via AR, to check clearances. Scoped it first (own
  discussion, not logged as a section since no code changed), person
  approved Phase 1: Android WebXR only, LTM 1110's carrier model only, as
  a single real-device pilot before wiring the other 5 carriers or
  building the separate iOS path (AR Quick Look needs a per-model USDZ
  conversion this app has no pipeline for yet - a later phase, not this
  one).
  No new library - vendor/three/ already ships GLTFLoader/DRACOLoader/
  OrbitControls fully offline (see this file's earlier entries), so
  hand-rolled WebXR hit-test code fits that existing pattern far better
  than pulling in Google's separate <model-viewer> web component, which
  would duplicate a three.js runtime already vendored here.
  carrier3d.js: renderer.xr.enabled = true at creation (harmless with no
  session active). The render loop itself had to change from a hand-
  rolled self-chaining requestAnimationFrame chain to renderer.
  setAnimationLoop(onFrame) - that's the only mechanism three.js has for
  transparently swapping between plain rAF and an active XRSession's own
  frame timing, which hit-testing needs access to (frame.
  getHitTestResults() only exists on the WebXR frame callback's own
  argument, never on a plain rAF tick). onFrame branches on renderer.xr.
  isPresenting: controls.update() only while NOT presenting (OrbitControls
  has no meaning once the device's own camera pose drives the view),
  hit-test only while presenting.
  Placement anchor: "tap to place" places the model's own SLEW CENTRE at
  the tapped point, not the CAD export's raw local origin (wherever
  Onshape happened to put it) - reuses computeFormulaCalibration (already
  written for the slew-circle/ground-layout features) to find that point,
  wraps the loaded model in a THREE.Group offset so the group's own
  origin lands exactly on the slew centre/ground point, and both
  positions (tap-to-place) and rotations (the overlay's own ⟲/⟳ buttons)
  are applied to that GROUP, not the model directly - rotating a crane
  around its own slew axis is what a crew actually needs to check
  clearance against a wall at different orientations, not a spin around
  some arbitrary CAD-export corner.
  Re-tappable, not one-shot: every tap while the reticle's visible moves
  the crane to that new spot, rotation carries over across a re-tap
  (only the overlay's own buttons change it) - a first guess at
  placement is rarely exactly right and re-tapping is the obvious way to
  nudge it without a separate unlock control.
  index.html: new AR_SUPPORTED_MODELS = ['1110'] gates which crane shows
  the button at all; window.__carrier3dARSupported() (navigator.xr.
  isSessionSupported('immersive-ar')) is also checked before showing it,
  so the button never appears on iOS Safari or desktop rather than
  appearing and failing on tap. Button added to both 3D-preview toolbars
  (Support Pad Placement's own card and Crane Layout's) since both
  already share the one carrier3d.js viewer instance.
  Known Phase 1 limitations, flagged for the person rather than silently
  shipped as if complete: no auto-orientation (the crane always loads
  facing whatever direction it was placed at last, no compass/north
  alignment); only LTM 1110 has the button; no iOS support yet; I have no
  camera/motion-sensor hardware to test real AR placement/scale accuracy
  myself, so this needs verification on an actual Android device before
  being trusted for a real clearance check.
  Verified via Playwright (as far as it's possible without real AR
  hardware): both toolbar buttons stay hidden in headless Chromium
  (navigator.xr exists but isSessionSupported resolves false, same as a
  desktop/non-AR browser would see) - confirms the feature-detect gate
  itself works and the button doesn't appear where it shouldn't. Full
  regression sweep on the ordinary orbit-preview render path (now running
  through setAnimationLoop instead of the old rAF chain) - both 3D cards
  still load/render/rotate correctly, 0 console errors. The actual AR
  session code (hit-test, placement, rotation) could NOT be exercised by
  this testing - no WebXR-capable device available in this environment.
  CARRIER3D_VERSION 45->46, CACHE_VERSION v149->v150, app version
  v3.19->v3.20.

36. AR real-world-scale placement: widen to all six carriers
  Person tried Phase 1 (LTM 1110 only), came back happy with it and
  asked for the next step. The placement/rotation code in carrier3d.js
  was never LTM-1110-specific - it always just reused whichever crane's
  own FOOTPRINTS/CARRIER_CALIBRATION entry the caller passed in (same
  figures the orbit preview, slew circles and ground layout marks
  already trust for that crane), so widening AR_SUPPORTED_MODELS to
  Object.keys(CARRIER_MODELS) - all six carriers with a 3D model at all
  - was the whole change, no carrier3d.js edit needed.
  One flagged caveat, not a blocker: LTM 1650's own CARRIER_CALIBRATION
  entry carries an `uncertain` flag (frontAtMinZ never visually
  confirmed against a straight-down render with a real cab, unlike the
  other five - see that constant's own comment, methodology.txt 10.77/
  10.82). AR inherits exactly whatever the orbit preview already shows
  for 1650 - no new uncertainty introduced by AR itself, just carried
  forward unchanged.
  Verified via Playwright: toggled the 3D Carrier Preview for all six
  cranes (1110/1130/1160/1250/1300/1650) in Crane Layout, confirmed the
  AR button's feature-detect gate behaves identically for every one of
  them (stays hidden in headless Chromium, same as a real non-WebXR
  browser would see), 0 console errors including for 1300/1650's larger
  GLBs. Real on-device placement/rotation for the five newly-added
  carriers still needs verification on an actual Android device, same
  standing limitation as Phase 1.
  carrier3d.js unchanged - CACHE_VERSION v150->v151, app version
  v3.20->v3.21.

37. LTM 1650 CARRIER_CALIBRATION: resolving the `uncertain` flag
  Person asked how the `uncertain` flag on LTM 1650's frontAtMinZ could
  actually be fixed, rather than just left as a standing caveat. The
  other five cranes were confirmed by rendering a straight-down view and
  visually spotting the driving cab - 1650's export is too sparse for
  that (28 parts, no cab, only one of the two outrigger-leg pairs
  modeled), so that check was never possible for it the same way.
  Cross-checked it a different way instead, using data already in the
  app: a Playwright script loaded ltm1650-carrier.glb directly (same
  GLTFLoader/DRACOLoader path carrier3d.js uses) and found the one
  modeled leg pair as two ~0.7m x 0.7m foot plates at two Z stations,
  ~10.66m apart. That gave two INDEPENDENT ways to estimate where the
  slew centre sits, neither one referencing the other:
    1. computeFormulaCalibration's own estimate, from the chassis's
       front/rear overhang fractions alone (FOOTPRINTS['1650']: front
       14585mm, rear 3717mm) - never looks at leg geometry at all.
    2. Solving for the slew-centre Z that makes the two DETECTED feet
       match GROUND_LAYOUT_DATA's own real OEM leg-station figures
       (rear legs C1/C4 ~4000-4529mm, front legs C2/C3 ~6042-6530mm) -
       never looks at overhang fractions at all.
  Under the existing frontAtMinZ: true, these two independent estimates
  land within ~1m of each other (plausible - it's the same kind of
  formula-vs-refined-geometry gap the other five cranes had before their
  own full 4-corner refinement). Under the flipped assumption, they land
  ~8.6m apart - two independently-derived estimates of the same physical
  point don't do that unless the underlying orientation assumption feeding
  both of them is wrong. That's real evidence the current value was
  already correct, not backwards - one notch below the other five cranes'
  own visual cab confirmation, but a genuine cross-check rather than a
  guess. Person confirmed removing the flag rather than leaving it
  pending an in-person check that may never happen.
  Removed `uncertain: true` from CARRIER_CALIBRATION['1650'] (the
  frontAtMinZ/lateralSign values themselves are unchanged - this was
  always just a confidence marker, never read anywhere in the code).
  Reasoning documented inline in CARRIER_CALIBRATION's own comment.
  CACHE_VERSION v151->v152, app version v3.21->v3.22.

38. AR: carry bog mats + slew circles (and other active overlays) along
  Person asked whether the bog mat marks and slew radius circles could
  show up in AR too, not just the bare carrier. They were right to ask -
  as shipped, entering AR pulled the model out of the orbit-preview scene
  into its own arGroup, but left slewCircleGroup/matEdgeGroup/etc exactly
  where they were, still drawn at the OLD orbit-preview position - once
  the crane moved into AR space those groups would've been left behind as
  stray floating geometry with nothing to do with the placed model. Real
  bug, not just a missing feature.
  Fix: reparent whichever overlay groups are currently populated
  (outriggerGroup, slewCircleGroup, groundLayoutGroup, matEdgeGroup,
  targetMatEdgeGroup - all five, not just the two asked about, since
  there's no extra cost to including all of them and each tab's own
  toggles only ever populate the groups relevant to it anyway - Support
  Pad Placement never has slew circles active, Crane Layout never has mat
  edges) into arGroup alongside the model root, each carrying the SAME
  slew-centre offset as root. Every one of these groups was drawn via
  siteToWorld(cal, x, y), which resolves through cal.lateralCenter/
  groundY/slewZ - the exact same point root gets offset by - so giving
  the group that identical offset before reparenting puts its geometry in
  the same slew-centre-relative local space as the model, and it now
  moves/rotates with arGroup exactly like the model does.
  One correctness fix bundled in along the way: __carrier3dEnterAR used
  to call computeFormulaCalibration() fresh for root's own offset, an
  UNREFINED estimate - while the overlay groups themselves were drawn
  using whatever computeCalibration()/ensureSlewCalibration() had
  actually cached (the REFINED, leg-geometry-corrected calibration, once
  Support Pad Placement has synced that model). Using two different
  calibrations for root vs its own overlays would have silently
  reintroduced the exact "real visible offset" bug refineCalibration
  FromGeometry was built to fix (methodology.txt 10.78/10.81) - the mats/
  circles would render at a small but real offset from the placed model
  in AR. Switched to ensureSlewCalibration(modelKey, root, footprint,
  calibration) instead, which returns the same cached value (refined if
  available, else the formula fallback) that every overlay-drawing
  function already prefers - guarantees root and its overlays always
  share one calibration.
  Verified: the offset/reparent math itself proven exactly (not just
  visually) via a standalone Node script using vendor/three's own Vector3/
  Group/Matrix4 directly - placed a simulated overlay point through the
  same offset-then-reparent-then-translate-and-rotate sequence
  __carrier3dEnterAR/onARSelect/rotateARModel actually perform, compared
  the resulting world position against the analytically expected one
  (rotate the slew-centre-relative offset, then translate to the tap
  point) - matched to float precision (8.8e-16m). Playwright regression
  on the unmodified surrounding code: toggled all 8 of LTM 1650's slew-
  circle/ground-layout checkboxes and both of LTM 1110's mat-edge/pad
  checkboxes via direct event dispatch (Playwright's own actionability
  checks were timing out on this environment's software-WebGL GPU stalls
  once enough scene geometry piled up - an environment rendering-
  performance issue unrelated to this change, confirmed by the fact that
  dispatching the same change event directly worked instantly with zero
  errors) - 0 console errors either way, AR button still correctly hidden
  in headless Chromium. The actual reparented overlays' on-device
  appearance in a live AR session still needs real-hardware verification,
  same standing limitation as the rest of this feature.
  CARRIER3D_VERSION 46->47, CACHE_VERSION v152->v153, app version
  v3.22->v3.23.

39. Hide LRT 1100-2.1 from Support Pad Placement's crane select
  Person's request: LRT 1100-2.1 has no CARRIER_MODELS entry (it never
  has, unlike every LTM in that list) - Support Pad Placement's own 3D
  Carrier Preview has nothing to show for it, so hide it from that one
  tab's crane select until the person provides its 3D model.
  Removed the <option value="1100"> from #cad-crane only - left it in
  every other crane select that doesn't depend on a 3D model (sc-crane/
  Crane Layout, r-model/Rope Retentioning, and the CWT/Driving with
  Equipment tabs' own selects), since those all work fine for LRT 1100
  today. Left a comment in place of the removed option explaining exactly
  what re-adding it needs (an outrigger/models/ltm1100-carrier.glb file
  plus CARRIER_MODELS/CARRIER_CALIBRATION/FOOTPRINTS entries), so it's a
  one-line restore once that model exists rather than something to
  rediscover later.
  cadFleetData['1100'] and every other '1100' data table are untouched -
  this only affects which option this one <select> offers; the crane's
  underlying data still exists everywhere it's used.
  Verified via Playwright: cad-crane's option list no longer includes
  1100 (defaults to LTM 1110-5.1 instead), sc-crane and r-model both
  still list it, 0 console errors.
  CACHE_VERSION v153->v154, app version v3.23->v3.24.

40. Fullscreen toggle on all three 3D model viewers
  Person's request, after a "just a question" about whether the crane
  layout 3D view could go fullscreen: yes, and add it to every 3D viewer
  in the app, not just that one - carrier-3d-wrap (Support Pad
  Placement), carrier-3d-wrap-layout (Crane Layout), cwt-3d-wrap (CWT
  Combinations).
  Deliberately fullscreens the OUTER wrap div, not the canvas directly -
  both carrier3d.js and cwt3d.js already listen for window's own 'resize'
  event and read their inner canvas-wrap's clientWidth/clientHeight on
  every fire (see each module's own resizeRenderer()), which the browser
  fires automatically on entering/exiting fullscreen - so the renderer/
  camera correctly resize to fill the screen with ZERO changes needed to
  either module. This is only a DOM/Fullscreen API + CSS + a small shared
  toggle function in index.html's own classic script - no CARRIER3D_
  VERSION/CWT3D_VERSION bump needed, matching the convention that those
  only bump when the vendor files themselves change.
  One button per wrap, all sharing the same .carrier3d-fullscreen-btn
  class/markup (same generic multi-instance pattern already used for the
  offline-cache buttons) - positioned top-right inside the wrap (label
  overlay already owns top-left), so it's reachable both in and out of
  fullscreen without a separate fullscreen-only control. Swaps between a
  Feather-style expand/compress SVG pair on 'fullscreenchange'/
  'webkitfullscreenchange', keyed by comparing document.fullscreenElement
  against each button's own data-wrap attribute (so only the button whose
  own wrap is actually fullscreened swaps its icon, not all three at
  once).
  Hidden by default and only revealed once the Fullscreen API is
  feature-detected as available (document.documentElement.
  requestFullscreen) - iOS Safari on iPhone has never supported
  fullscreening an arbitrary element (video-only), so the button simply
  doesn't appear there rather than doing nothing when tapped, same
  feature-detect-then-reveal pattern already used for the AR button.
  Verified via Playwright: all three buttons correctly reveal
  (feature-detected as supported in headless Chromium); calling the
  toggle function actually engaged real fullscreen (document.
  fullscreenElement became the target wrap, confirmed via a screenshot
  showing the model filling the full viewport with the button correctly
  positioned top-right); 0 console errors. One thing NOT fully verified
  in this environment: the icon swap fires correctly when called
  directly, but the 'fullscreenchange' event itself didn't fire when
  fullscreen was triggered via Playwright's programmatic page.evaluate()
  call rather than a genuine trusted user click - a known automation-
  environment quirk (requestFullscreen succeeded regardless, so the
  underlying API call itself is fine), not a code defect, since real
  browsers reliably fire fullscreenchange for a real user tap. Worth a
  quick on-device glance to confirm the icon actually flips when tapped
  for real.
  CACHE_VERSION v154->v155, app version v3.24->v3.25.

41. Reeving Plans: name the models in the "shared" folding jib note
  Person noticed the Folding Jib (shared) config's note reads "Same
  accessory reeving plan used across these four models." without saying
  which four - the crane/config DROPDOWN label does name them ("Folding
  Jib (1110/1130/1160/1200-1)"), but the separate note shown below it
  didn't repeat them, so a person looking at just the note has to go
  back and cross-reference the dropdown.
  Text lives in reeving/manifest.json (fetched at runtime, not in
  index.html - grep for the exact phrase came up empty until checking
  there), specifically the folding-jib-shared entry's own "note" field.
  Checked every other entry in the manifest for the same vague-note
  pattern ("these X models"/"these models") - only this one had it.
  Reworded to "Same accessory reeving plan used across LTM 1110, LTM
  1130, LTM 1160, and LTM 1200-1." - matches the models already named in
  that entry's own "label" field (LTM 1200-1 isn't one of this app's own
  supported cranes, kept as-is since it's the pre-existing sourced model
  list, not something to second-guess without the OEM reference in
  hand). Confirmed via a JSON-level diff against the pre-edit file that
  this was the ONLY field that changed anywhere in the manifest - no
  incidental reformatting/reordering from the edit script.
  Bundled in a related fix while touching this file: reeving/manifest.
  json wasn't in sw.js's own APP_SHELL, so it was falling into the
  weaker CONTENT_CACHE (cache-forever, never refreshed on a CACHE_
  VERSION bump) - the exact same bug class methodology.txt 10.28 already
  documented for counterweight diagrams, and for the same underlying
  reason: this file's SVG diagrams genuinely don't change once shipped
  (real OEM diagrams), but the manifest ALSO carries editable prose
  (labels, notes) around them, which this very fix just proved does
  occasionally need correcting. Added './reeving/manifest.json' to
  APP_SHELL (not the SVGs themselves, which stay in CONTENT_CACHE,
  matching the stable-content reasoning) - without this, a person who'd
  already opened Reeving Plans once would keep seeing the old vague
  wording forever, even after this exact update shipped.
  Verified via Playwright: reeve-crane's "Shared Accessory" group ->
  folding-jib-shared config renders the corrected note text, 0 console
  errors.
  CACHE_VERSION v155->v156, app version v3.25->v3.26.

42. LTM 1300 global + LTM 1160 UK: real OEM counterweight charts
  Person got hold of both variants' own OEM operator manuals (via a
  Nextcloud share - the first link given was file-drop/upload-only,
  server rejected reads outright ("Only PUT, MKCOL and MOVE are allowed
  on files drop") - a second, properly read-enabled share link worked).
  Extracted chapter 4.07 Counterweight from each via pdftotext/pdftoppm
  (poppler-utils, installed fresh - pypdf was present but broken, a
  cffi/cryptography native-module crash unrelated to this task):
    - LTM 1300-6-2-003 (24006-06-02), PDF pages 930-958.
    - LTM 1160-5-2-003 (25310-09-02), PDF pages 841-874.
  Cross-checked every extracted table against a rendered page image
  before trusting any of it (safety-relevant numeric data) - caught a
  genuine anomaly this way: LTM 1160 UK's 13.5t combo is printed TWICE
  in the actual source PDF with identical composition, confirmed via
  the rendered page image, not a text-extraction artifact - only
  entered once in the app's own data (no reason to duplicate an
  identical combo).

  LTM 1300 global (COUNTERWEIGHT_DATA['1300'], top-level/non-UK entry):
  Real chart REPLACES the previous provisional data, which had mirrored
  a different (EU) variant's chart and invented a "Counterweight Plate
  5" component with no basis in this crane's own real chart. That
  invented component had also been baked into the 3D model's own
  partMap (a GLB node literally named "5" mapped to it) - so fixing the
  data alone wasn't enough; the 3D model needed correcting too.
  Investigated the actual GLB geometry directly (Playwright + the
  vendored GLTFLoader, same technique as the LTM 1650 orientation check
  in section 37) rather than guessing: node "5" turned out to be REAL
  geometry (same 4.06x~0.18x2.4m footprint as every other main-stack
  plate), sitting physically ABOVE node "4" - but the model only had
  TWO Plate 3 copies (nodes "3"/"3_1"), one short of the chart's
  required three, and the OEM chart explicitly warns Plate 4 must sit
  on TOP of the stack (a warning tied to a specific 46t-tier accident
  risk) - contradicting "5" being a genuine 5th plate on top of "4".
  Rendered the model with node "5" highlighted magenta and the two
  confirmed Plate 3 copies green (side/iso/top views) and sent them to
  the person to check against the manual directly rather than guessing
  further - person confirmed: node "4" is actually a mislabelled THIRD
  Plate 3, and node "5" is actually the real Plate 4 (matches the
  geometry findings exactly: 10 slabs total when counting every node,
  matching the real max-combo part count of 1+1+3+1+2+2=10).
  Fixed model3d.partMap: '4' -> 'p3c' (was 'p4'), '5' -> 'p4' (was
  'p5'). Component list: added p3c (3rd interchangeable Plate 3,
  reusing the OLD p4's hotspot polygon unchanged - the diagram already
  drew 6 main-stack slabs, just 2 of them mislabelled), removed the
  invented p5, renamed the real Plate 4 to reuse the OLD p5's hotspot
  polygon. Combos rebuilt from the real chart: 0/6/16/26/36/46/56t
  (sequential stacking) plus 66/86t ("special ballast" tiers - spacer
  pins replace the 3rd Plate 3, not modelled as a component, same
  convention already used for the un-modelled spacer pins) and 76/96t
  (all three Plate 3 + one or two aux ballast pairs). manualPage
  corrected 931->930 (actual chapter start).

  LTM 1160 UK (COUNTERWEIGHT_DATA['1160'].ukVariant): clean upgrade, no
  ambiguity - the existing ukVariant block was built from a person-
  supplied WhatsApp reference photo with generic descriptive names
  ("Base Block Centre", "Top Plate", etc) and an empty combo list, since
  no OEM chart existed for this variant at the time. This chart's own
  weights (3.5/5/5/6.5/10/10/14t) matched that photo-traced version's 7
  pieces exactly, confirming it's the same hardware - AND the reference
  photo itself already had the real OEM part numbers ("5","4","3","2",
  "6","6") printed on the pieces, visually confirming the id assignment
  after the fact (matched independently via weight + the OEM's own
  Fig.126856 showing part 5 as the topmost plate). Kept every existing
  hotspot polygon unchanged, only renamed ids/labels and added the real
  combo list (16 tiers - genuinely branching, not a simple ladder:
  Plate 3 and Plate 5 are both 5t but tracked as distinct named parts,
  not an interchangeableGroup, since the chart uses them independently
  in different combos, sometimes together). 44t is a "special ballast"
  tier needing adapters 7/8 in place of Plate 3/Plate 5 - adapters not
  modelled as components, same convention as 1300's spacer pins. Added
  manualUrl/manualPage (previously absent - no source doc existed to
  link).
  Verified via Playwright: computed every combo's total directly against
  the components' own weights for both cranes - all 27 combined tiers
  (11 for 1300, 16 for 1160 UK) match the OEM chart exactly, 0 mismatches.
  3D view: selected 1300's max 96t combo, confirmed all 8 parts highlight
  correctly, "OEM-approved" flag shows, composition table lists the
  correct 3x Plate 3. 1160 UK: selected max 54t combo, confirmed OEM-
  approved flag and correct composition against the reference photo's own
  printed numbers. 0 console errors throughout.
  Not yet done: neither variant has a permanent hosted manualUrl for
  this specific document yet (1300 still points at its old placeholder
  link) - update once the person has somewhere to host the new PDFs.
  CACHE_VERSION v156->v157, app version v3.26->v3.27.

43. Load Chart Finder: highlight "Margin over target"
  Person asked to brainstorm ways to make this figure stand out, then
  picked "style just that row" (bold, colour, background tint) over the
  other options discussed (a standalone badge, a callout box, tiered
  colour by how thin the margin is).
  Confirmed first that margin is always >= 0 wherever it's shown - the
  candidate-filtering loop already discards anything below the target
  before margin is even computed (`if (usableCapacity < weightInput)
  continue;`), so this is a "make the good number pop" job, not a
  pass/fail colour-coding job. Styled with var(--success) (green) +
  bold + a subtle rgba(16,185,129,0.12) row background - reuses the
  exact same green already used for .recommendation-box and other
  positive/confirmed results elsewhere in the app, not a new colour
  introduced just for this. Applied to both places this row appears
  (Load Chart Finder's own results and Compare Cranes' per-crane
  breakdown) so the two stay visually consistent.
  Verified via Playwright: ran a real Load Chart Finder query, confirmed
  the row renders with the green tint/bold text across multiple result
  options, 0 console errors.
  CACHE_VERSION v157->v158, app version v3.27->v3.28.

44. Load Chart Finder: default hook block selections
  Person's request: "Hook Block Fitted" should default to that crane's
  own 3-sheave block, "Winch 2 Block Fitted" should default to its load
  block - both selects previously just took whatever FLEET_BLOCK_SPEC
  happened to list first for that crane, no real default logic at all.
  New defaultLoadChartBlockId(blocks, wantLoadHook) helper: matches by
  NAME against the same FLEET_BLOCK_SPEC catalog already used elsewhere
  (Minimum Hook Block Weights tab) rather than hardcoding per-crane
  answers - every one of the 7 cranes currently in this catalog has
  exactly one block whose name contains "3-Sheave" (confirmed by
  reading every entry), so that's the main block's default with no
  per-crane exception needed. Winch 2 prefers a block whose name
  contains "Load Hook" - only 3 of the 7 cranes actually stock one in
  this catalog (1650-t3, 1300, 1100); the other 4 (1250/1220[unused by
  this tab]/1160/1130/1110) fall back to the LIGHTEST block available
  instead of being left on an arbitrary first entry - the closest real
  equivalent to "just a hook" when the catalog has nothing plainer.
  Verified every crane's resulting default against the real catalog by
  hand before trusting it (both defaults, all 7 cranes) - full table
  matched exactly.
  Found and fixed a real latent bug while testing this: onLoadChartCrane
  Change()'s fetch had no guard against a stale response landing after
  a newer one - switching cranes fast enough (well beyond normal click
  speed, only surfaced by automated rapid-fire testing) could let an
  in-flight fetch for the PREVIOUS crane resolve after the new one and
  silently overwrite its already-correct render, including these new
  block defaults, with stale data. Added a one-line guard (bail if
  document.getElementById('lc-crane').value no longer matches the
  crane this fetch was actually for) - pre-existing bug, not introduced
  by this change, but directly threatened the new feature's reliability
  so fixed alongside it rather than filed away.
  Verified via Playwright: single fresh-page-load check per crane
  (isolates the defaulting logic itself) - all 7 correct. Rapid
  sequential crane-switching on one page (the scenario that exposed the
  race) - re-ran after the guard, all 7 still correct, 0 stale
  carryover. Visual check: LTM 1300 with Winch 2 toggled on shows
  "12.5t Single Load Hook (0.50 t)" as the Winch 2 Block Fitted default.
  0 console errors throughout.
  CACHE_VERSION v158->v159, app version v3.28->v3.29.

45. LRT 1100-2.1 carrier model
  Person supplied their own Onshape GLB for the LRT 1100 (via Nextcloud -
  the first share link given was file-drop/upload-only again, same as
  methodology.txt 42's first attempt; a second, read-enabled link to the
  actual "myslewer" project folder worked, with a Carrier/ subfolder
  holding this file alongside re-confirmed copies of the other five
  carriers already in the repo, matching their existing file sizes).
  Saved as outrigger/models/lrt1100-carrier.glb (not ltm1100-, matching
  this crane's own real designation - same convention already used for
  LTR 1220's counterweight model, ltr1220-cwt.glb).
  This is the crane the "hide until you provide the 3D model" fix
  (methodology.txt 39) was explicitly waiting on - re-added the
  <option value="1100">LRT 1100-2.1</option> to #cad-crane (Support Pad
  Placement) that was pulled then, following the comment left in its
  place verbatim (CARRIER_MODELS/CARRIER_CALIBRATION both gained a '1100'
  entry, FOOTPRINTS already had one). AR_SUPPORTED_MODELS needed no
  separate change - it's already Object.keys(CARRIER_MODELS), so this
  crane picked up "View in AR" for free.
  frontAtMinZ determination: this crane's own FOOTPRINTS/GROUND_LAYOUT_
  DATA front/rear figures are both nearly symmetric (4300mm/4388mm
  overhang, ~7300mm span either way) - unlike LTM 1650's case
  (methodology.txt 37), a geometry-only cross-check against real leg-
  station figures wouldn't have reliably distinguished a wrong guess
  from a right one here, since both hypotheses would solve to similarly
  self-consistent slew-centre estimates. Needed an actual visual read
  instead: rendered both ends close-up (not just a single straight-down
  pass, which was ambiguous for this crane on first look) and found the
  MAX-Z end has a raised cab structure with ladders on both sides
  (classic driver's-cab access), while the MIN-Z end is just the flat
  deck with the slew bearing bore and a small mirror/vent - unambiguous
  once actually rendered close-up. frontAtMinZ: false - the OPPOSITE of
  every other crane in CARRIER_CALIBRATION, a real difference in this
  particular export, not an inconsistency to "fix".
  Cross-checked the result against an independent, already-trusted
  number rather than taking the visual read alone: toggled the "beyond
  rear of carrier" clearance measurement (built in an earlier session,
  computed straight from FOOTPRINTS - never depended on the 3D
  calibration at all) against the Counterweight radius circle - got
  "-198mm" drawn at the flat-deck end, exactly matching radius(4190) -
  rear overhang(4388) computed independently. The dimension line landing
  at the flat-deck end (not the cab end) confirms frontAtMinZ: false is
  wired correctly, not just visually plausible.
  Verified via Playwright: model loads and renders in both Support Pad
  Placement and Crane Layout's 3D previews, cad-crane's option list has
  1100 back, AR button correctly present-but-hidden (feature-detected,
  same as every other crane in headless Chromium), 0 console errors.
  CACHE_VERSION v159->v160, app version v3.29->v3.30 (next bump after
  this one rolls over to v4.0 per CLAUDE.md's own cap).

================================================================================
46. LRT 1100: "move forward" looked like it moved backward

  Person's report: on Support Pad Placement, shifting the LRT 1100
  "forward" visibly moved it backward - works correctly for every other
  crane. Given methodology.txt 45's frontAtMinZ:false determination was
  the one genuinely uncertain part of last session's LRT 1100 work, this
  read at first like that guess might simply be wrong and needed
  re-checking from scratch.

  It wasn't. Re-verified frontAtMinZ:false from three more independent
  angles before touching any code: (1) the actual OEM LICCON button
  labels, straight from the operator manual text ("Select support 3 front
  left" / "support 2 front right" / "support 4 rear left" / "support 1
  rear right") - matches cadFleetData's existing C1_RR/C2_FR/C3_FL/C4_RL
  assignment exactly, so the leg data (separate from the 3D calibration)
  was never in question; (2) the manual's own Fig.145199 elevation
  drawing, which shows a real windshield-and-door cab (not just an engine
  box) sitting at the same end as the "4190" counterweight-radius
  dimension chain and the boom base - i.e. the true front; (3) rebuilt the
  calcCAD() math for a 10m forward shift by hand and confirmed C1 (a real
  rear-right leg, angle 43 deg) lands at 149.5 deg after the shift -
  correctly swung into the front-angle range. Playwright-tested the 2D
  Visual Site Plan side-by-side against LTM 1110 for the identical 10m
  shift - pixel-for-pixel the same behavior, target cluster and "FRONT"
  label both land on the correct side for both cranes. The underlying
  math, leg data, and 2D plan were never the bug.

  The actual bug: the 3D preview's DEFAULT camera angle (frameCamera(),
  shown the instant a carrier model opens, before anyone clicks Fit View)
  was a fixed local-space offset - always +Z in the model's own raw
  export coordinates, never adjusted for calibration. For the other six
  carriers (all frontAtMinZ:true, all exported with front at local min-Z
  by whatever consistent convention their CAD source used), that fixed
  +Z offset happens to sit on each model's REAR side, so every one of
  them opens rear-first: cab small and distant, rear axles filling the
  foreground - confirmed by rendering LTM 1110's own default view before
  touching anything, which showed exactly that. LRT 1100 is the one
  carrier whose raw export has front at local MAX-Z instead (a real
  difference in this particular Onshape export, not a mistake - see
  methodology.txt 45) - so that same fixed +Z offset lands on ITS front
  side, and the crane opened nose-on, cab filling the foreground, unlike
  every other crane in the fleet. A forward shift was always moving the
  ghost/target position toward the true front - but with the camera
  planted in front of the crane instead of behind it like normal, that
  motion reads on screen as growing toward the viewer instead of
  receding, which feels exactly like "backward" even though the
  underlying position was always correct. Confirmed by rendering LRT
  1100's own default view too: ladders/cab structure filled the
  foreground, opposite of LTM 1110.

  Fix: frameCamera(root, calibration) now flips the Z offset by the same
  dirSign convention computeFormulaCalibration already uses (frontAtMinZ
  true -> +1, false -> -1), so the initial camera always lands on the
  site-space rear side regardless of which end of the raw GLB happens to
  sit at local +Z. __carrier3dActivate gained a 5th (optional) calibration
  parameter to carry this through from both call sites in index.html
  (activateCarrier3D, activateCarrier3DLayout), each already holding
  CARRIER_CALIBRATION[modelKey] in scope. Fit View (fitView(), the
  near-top-down button) was never affected - its tilt is 0.5 degrees off
  vertical, functionally camera-angle-agnostic regardless of which side
  it's nominally biased toward.

  Re-verified via Playwright after the fix: LRT 1100's default view now
  opens rear-first, ladders/cab small and distant - matching LTM 1110's
  own default view convention exactly. No other crane's default view
  changed (dirSign is +1 for all six of them, so the fix is a no-op for
  every carrier except this one).

  CARRIER3D_VERSION 47->48 (vendor/carrier3d.js changed). index.html also
  changed (the two call-site edits passing calibration through), so
  CACHE_VERSION and the app version both bump too, same as any other
  index.html change.

================================================================================
47. LRT 1100: frontAtMinZ was actually wrong, not just the camera

  methodology.txt 46's camera-angle fix made the default 3D view
  consistent again, but the person came back with the real root cause
  directly: CARRIER_CALIBRATION's frontAtMinZ: false for the LRT 1100 was
  itself backward - what methodology.txt 45 identified as the front (the
  MAX-Z end's raised, ladder-flanked structure) is actually the rear.
  Concrete symptom that proved it: the "Show ground layout marks" 3D
  overlay's own half-carrier-width dimension line - explicitly labelled
  "(rear, C1 side)" in the plain-text Layout Dimensions card - was
  rendering at the FRONT end of the model instead, on live testing.

  Root cause of the original wrong read: this crane's operator manual
  lists "Crane cab" as a component of "2 Crane superstructure", not of
  "1 Crane chassis" (chapter 1.01 Terminology, Fig.146075 vs Fig.146074) -
  i.e. on an LRT (rough-terrain) crane the only cab is mounted on the
  ROTATING SUPERSTRUCTURE, used for both driving and operating, not fixed
  to the carrier. The carrier-only GLB this app renders has no cab on it
  at all. The raised, ladder-flanked structure methodology.txt 45 found at
  the MAX-Z end and read as "classic driver's-cab access" was something
  else entirely - almost certainly the engine bay (a real carrier
  component per Fig.146074's own parts list, item 5) - which happens to
  sit at the carrier's REAR, not front, on this particular machine.
  Everything methodology.txt 45 cross-checked against (cadFleetData's
  C1-C4 leg assignment, the literal OEM LICCON button labels, the
  counterweight-radius clearance figure) was and remains correct in its
  own right - none of that data was wrong. The mistake was narrower: which
  physical end of the 3D model those correct front/rear labels belonged
  to. The "independent" cross-check in methodology.txt 45 wasn't actually
  independent of the error - it rendered its own confirmation using the
  same (wrong) calibration it was trying to verify, so it validated
  internal self-consistency, not truth against the real machine. Lesson:
  a cross-check that itself depends on the value being checked can't
  catch that value being wrong.

  Fix: CARRIER_CALIBRATION['1100'].frontAtMinZ flipped false -> true -
  same convention every other carrier in the fleet already uses. Nothing
  else needed changing: FOOTPRINTS, cadFleetData, GROUND_LAYOUT_DATA and
  SLEW_CLEARANCE_DATA were all already correct (they were never derived
  from the 3D model's Z-axis guess in the first place), and
  computeFormulaCalibration/siteToWorld's dirSign compensation (see
  vendor/carrier3d.js) was already correctly designed to handle either
  polarity - it just needed the right input. methodology.txt 46's camera
  fix stays in place (still correct, still a no-op for every crane except
  one calibrated with frontAtMinZ: false - currently none, after this
  fix, but worth keeping for whatever crane needs it next).

  Re-verified via Playwright: the ground-layout half-width dimension line
  now renders at the C1/rear end as labelled; Support Pad Placement's 3D
  "if moved" ghost for a forward shift now lands near the correct
  (min-Z) end; the 3D default view still opens rear-first, unchanged from
  methodology.txt 46's fix (dirSign is now +1 for this crane too, so that
  fix is currently a no-op here, by design). 0 console errors.

================================================================================
48. LRT 1100: methodology.txt 47 was itself wrong - reverted

  Person sent their own reference photos of the real machine, one end at
  a time, plainly labelled "front" and "rear". The "front" photo shows
  the MAX-Z end - bore, raised ladder-flanked structure, stepped platform
  - the exact same end methodology.txt 45 originally (and correctly)
  called the front, before methodology.txt 47 flipped it. The "rear"
  photo shows the plain MIN-Z end. methodology.txt 47's theory (cab lives
  on the superstructure, so the carrier model can't have one, so the
  structure found must be something else at the other end) was a
  plausible-sounding chain of reasoning built without ever actually
  confirming which end the person's original complaint was about -
  wrong conclusion from a real fact (this crane's cab genuinely is
  superstructure-mounted) stretched into an unsupported one (therefore
  the carrier's own front must be the other end).

  Reverted CARRIER_CALIBRATION['1100'].frontAtMinZ back to false, the
  original methodology.txt 45 value. methodology.txt 46's camera-
  consistency fix is untouched and still correct - that was always a
  separate, genuinely real bug (the default view opening nose-on instead
  of rear-first) and is very likely what the person's original "half
  width dimensions on the front" report was actually seeing: a confusing
  default camera angle, not a wrong calibration value. Best current
  understanding is the ground-layout half-width dimension line was
  probably already rendering at the geometrically correct (rear, C1 side)
  end all along under frontAtMinZ: false - methodology.txt 47 never
  actually screenshotted the "before" state to confirm the reported
  symptom before changing the value, only reasoned from the report
  itself; that gap is exactly how a plausible wrong fix got shipped.
  Re-verified via Playwright after reverting: default 3D view still
  opens rear-first (methodology.txt 46 intact), 0 console errors.

  Lesson for next time a front/rear report comes in on any crane: get a
  screenshot of the actual current (allegedly wrong) state FIRST, before
  reasoning toward a fix - a mid-air diagnosis, however internally
  consistent it seems, isn't a substitute for looking at what's actually
  on screen when it's this cheap to just look.

================================================================================
49. LRT 1100: methodology.txt 48 was ALSO wrong - the original 45 render
    had the two ends mislabelled at the source

  Person came back a third time with concrete, specific evidence: "C1 is
  in C2 pos and vice versa, C4 and C3 are in each other's spot, half
  width dimensions still on the front, needs to move to the rear" - then
  sent an annotated screenshot of the app's own ground-layout-marks view
  with C3/C2 hand-labelled "front" (top) and C4/C1 hand-labelled "rear"
  (bottom).

  This time, instead of re-reasoning from the existing (evidently
  unreliable) close-up renders, rebuilt the identification from scratch
  with a throwaway page (not the live app - a bare Three.js scene loading
  ONLY outrigger/models/lrt1100-carrier.glb directly, full manual camera
  control, no OrbitControls polar-angle limits, no possibility of the
  live app's own calibration influencing what's on screen) and rendered
  genuine close-ups of both ends. Result: MIN-Z has a real ladder (clear
  rungs), an oval mirror/sensor disc, and a stepped platform. MAX-Z has a
  plain louvred radiator/engine grille - no ladder, nothing cab-adjacent
  about it. This is the OPPOSITE of what methodology.txt 45 originally
  reported ("MAX-Z has the raised, ladder-flanked structure"). Comparing
  both fresh renders directly against the person's own two reference
  photos from earlier in this saga: their "front" photo (bore, disc,
  ladder, stepped platform) matches the MIN-Z render exactly, feature for
  feature - not the MAX-Z one. methodology.txt 45's very first close-up
  pass had the two ends' renders mixed up at the source - a mistake in
  that one ad-hoc script, not a genuine ambiguity in the model - and
  every subsequent round this session (46 kept it, 47 correctly sensed
  something was wrong but landed on the wrong theory to explain it, 48
  reverted back to the original wrong value on the strength of photos
  that, on this re-examination, actually confirm the OPPOSITE) inherited
  that one original mislabelling.

  Fix: CARRIER_CALIBRATION['1100'].frontAtMinZ back to true - the third
  flip this session (45: false, 47: true, 48: false, 49: true). Re-
  verified by rendering the live app's own actual ground-layout-marks
  view (not just the throwaway comparison) and confirming pixel-for-pixel
  agreement with the person's annotated screenshot: C3/C2 at the top
  (front), C4/C1 at the bottom (rear), half-width dimension now at the
  rear. 0 console errors.

  Why trust this one over the previous three: this is the first round
  built on a direct side-by-side of a labelled reference photo against a
  freshly, independently rendered close-up of each end - not on
  describing what a structure "reads like" from a screenshot that could
  itself have been mislabelled, and not on reasoning about which theory
  sounds more plausible. Every earlier round in this saga skipped
  straight to reasoning about calibration flags without first re-
  deriving the one fact everything else depends on: which physical end
  is which. That's the actual lesson here, sharper than
  methodology.txt 48's "look before reasoning" - it's specifically LOOK
  AT THE RAW MODEL AGAIN FROM SCRATCH before trusting any earlier
  session's own claimed visual identification, because that identifi-
  cation is exactly the kind of thing that can be silently wrong at the
  source and then get treated as settled fact by everything downstream.

================================================================================
50. CWT Combinations: auto-scroll to centre on switching to 3D

  Person's request: clicking "3D Model" on the Counterweight Stack card
  should bring that card front-and-centre in the viewport, not just flip
  the toggle and leave the page wherever it happened to be scrolled -
  same UX carrier3d.js's own Fit View button already provides (see its
  own comment: on a phone the card can sit mostly below the fold, so a
  view change alone can leave a person still having to scroll manually to
  see it). CWT's 3D view doesn't have a Fit View button of its own to
  piggyback on, so this fires directly from setCwtView('3d') instead:
  `document.getElementById('cwt-stack-card').scrollIntoView({ behavior:
  'smooth', block: 'center' })`. Scrolls the whole card (toggle buttons,
  viewer, hint text, total-weight readout) rather than just the 3D wrap
  div, so the person lands on the complete context, not just the model
  floating with no surrounding UI visible. Only fires switching TO 3D,
  not away from it - matches "when I click the 3d model tab" specifically.
  Verified via Playwright at both a laptop-ish viewport and a phone-sized
  one (390x844): card's own vertical centre lands within a pixel of the
  viewport's centre in both, 0 console errors.

================================================================================
51. Chain Shortening: "Mark Cut Point in AR"

  Person's own idea, following an earlier exploratory chat about AR
  measurement in general: given a computed result like "remove 54 links /
  2340mm", could AR show exactly where the 54th link physically falls
  from a chosen starting link, rather than the person having to count
  links by eye or trust a tape measure end staying put on a hanging
  chain?

  Ruled out up front: true "point the camera at the chain, it recognises
  and highlights link 54 automatically" needs reliable per-link computer
  vision (identify, segment and count individual chain links across
  lighting/angle/chain type) - not realistic with what's available here,
  and would be flaky even attempted. What IS genuinely buildable, and
  what got built: tap the reference link in AR (real-world hit-test
  placement, not CV), tap a second point further along the same chain to
  set direction, and get a marker placed at the exact real-world distance
  computed by the calculator (links-to-remove x pitch) along that
  direction - the person then counts/aligns the physical chain against a
  known-correct point in space, rather than trusting an unsupported tape
  measure to stay put or hold a straight line down a hanging/coiled
  chain.

  Built as a new standalone module, vendor/armeasure.js, deliberately NOT
  folded into carrier3d.js despite reusing its WebXR AR patterns
  (immersive-ar session request with hit-test + optional dom-overlay,
  ring reticle, canvas-texture text sprite, session lifecycle/cleanup) -
  this feature has no GLB model to load and no orbit-preview mode, so
  pulling in carrier3d.js's GLTFLoader/DRACOLoader/OrbitControls would
  only add dead weight neither this file nor its first-load cost needs.
  Same dynamic-import-on-first-use convention as cwt3d.js/carrier3d.js -
  never fetched until "Mark Cut Point in AR" is actually pressed.

  Wired into the Chain Shortening tab: calcChainShortening() now also
  calls setChainARResult(distanceMm, links) on every recalculation -
  distanceMm is always "links to remove x pitch" (the physical amount of
  chain being removed), the same real quantity regardless of which input
  mode (Current & Desired Length vs Shorten By Amount) produced it, so
  one code path covers both. The button is feature-detected exactly like
  every other AR entry point in this app (updateChainARButtonVisibility,
  same navigator.xr.isSessionSupported('immersive-ar') check as
  updateARButtonVisibility) and additionally hidden whenever there's no
  valid result yet or links-to-remove rounds to 0 (nothing meaningful to
  mark). Added to allOfflineContentUrls() too (armeasure.js + the shared
  three.js core files, deduped against the two existing viewer bundles'
  own three.js entries) so "Cache All Offline Content" covers it the same
  as every other fetched-on-demand asset.

  Verified via Playwright (real WebXR hardware isn't available in this
  environment, same limitation the original carrier3d.js AR feature was
  shipped under - see its own "present-but-hidden, feature-detected"
  testing note): button correctly hidden with no navigator.xr; correctly
  shown once navigator.xr.isSessionSupported is mocked to resolve true;
  clicking it triggers the dynamic import, computes the right label from
  the live calculator result (confirmed against the exact numbers shown
  in the result table), and calls requestSession with the correct options
  (requiredFeatures: ['hit-test'], optionalFeatures: ['dom-overlay'], a
  real dom-overlay root) - checked directly by capturing the call rather
  than assuming it; the on-screen AR overlay renders with the correct
  first-tap prompt. A session-request failure (mocked, since a fully
  WebXR-compliant fake session object is impractical to construct without
  real hardware) is caught and logged without any uncaught exception -
  confirmed pageerror stayed empty separately from the expected
  console.error. allOfflineContentUrls() confirmed to include the new
  file without throwing. 0 uncaught page errors across every test.
  ARMEASURE_VERSION starts at 1.

================================================================================
52. Crane AR: choose what the tap point anchors (slew centre or a leg),
    and rotate around that same point

  Person's own two-part request: (1) can the AR tap point represent
  something other than the slew centre - specifically a chosen outrigger
  leg, for a rigger who knows exactly where C1 needs to sit (a known hard
  point, an existing mat, a survey mark) and wants to lock onto that
  instead; (2) can the model be rotated around a point for alignment.
  These turned out to be the same piece of work, not two - see below.

  Added a small "what does the tap point represent" <select> (Slew Centre
  default, or C1-C4) next to both "View in AR" buttons (Support Pad
  Placement's and Crane Layout's), shown/hidden in lockstep with the
  button itself via updateARButtonVisibility()'s new optional third
  parameter. enterCarrier3DAR() reads it and, for a leg choice, computes
  that leg's own site-space (x,y) via a new legSiteXY() helper - same
  r/angle-to-x/y formula calcCAD() already uses inline for its own
  baseX/baseY, just extracted so AR placement can use it too. Passed
  through to carrier3d.js's __carrier3dEnterAR() as a new optional 4th
  parameter, anchor: { xMm, yMm, legId }.

  Inside __carrier3dEnterAR, this replaced the old hardcoded "always
  offset by the slew centre" logic with siteToWorld(cal, anchor.xMm,
  anchor.yMm, anchor.legId) when an anchor is given (still siteToWorld(
  cal, 0, 0) - the slew centre - when it isn't, i.e. unchanged default
  behaviour). That one substitution answers BOTH halves of the request at
  once: arGroup's own local origin becomes whichever point was resolved,
  and rotateARModel() has always pivoted arGroup around its own origin -
  it never needed to change at all. Once a leg is the anchor, "tap to
  place" puts that leg at the tapped point, and the existing rotate
  buttons swing the model around that SAME leg, keeping it locked to the
  point while heading gets dialled in - exactly the alignment behaviour
  the person was asking for in their second question, without a single
  extra line of rotation-specific code. This is why the two requests are
  documented together instead of as separate features - a Group's origin
  drives translation and rotation simultaneously, so anchoring one
  anchors both for free.
  The AR overlay's own instruction text now names the anchor explicitly
  ("Tap the ground to place C1 there" vs "...place the crane's slew
  centre there") via buildAROverlay()'s new optional label parameter, so
  there's no ambiguity on-screen about which point is being placed.

  Verified via Playwright (mocked navigator.xr, same real-hardware
  limitation noted in methodology.txt 51's own testing section):
  legSiteXY('1110','C1_RR') returns {xMm:4185.8, yMm:2823.4, legId:1} -
  confirmed by hand against cadFleetData's own r=5049/angle=56 for that
  leg (r*sin(56)=4185.6, r*cos(56)=2823.4, matches to rounding). Captured
  the actual anchor object enterCarrier3DAR() passes into
  __carrier3dEnterAR() by wrapping the function - matches legSiteXY's own
  output exactly, confirming the wiring from the <select> through to
  carrier3d.js is correct, not just the math in isolation. Confirmed the
  overlay text updates correctly for all five anchor choices (slew, C1,
  C2, C3, C4) - the first pass at this test read "slew centre" for every
  choice, which looked like a real bug at first; turned out to be leftover
  overlay elements from earlier failed mock session attempts (a
  necessarily-incomplete fake XRSession can't fully satisfy Three.js's
  renderer.xr.setSession(), so cleanup never ran) stacking up in the DOM
  and being matched by a query that should have targeted only the latest
  one - cleaning up between attempts and querying precisely resolved it,
  confirming the underlying feature was correct all along and the earlier
  reading was a test artifact, not app code. Anchor-select visibility
  toggling confirmed alongside the AR button. Both toolbars (Support Pad
  Placement, Crane Layout) share the same enterCarrier3DAR()/
  updateARButtonVisibility() code paths, so verifying one path thoroughly
  covers both.

================================================================================
53. Load Chart Finder Compare: road-legal counterweight as the minimum
    basis [CONVERSATION]

  Person's own reasoning, in full: some cranes can carry a meaningful
  amount of counterweight on their own superstructure while driving on
  public roads and still stay under a 12t/axle limit - meaning that
  counterweight doesn't need a separate truck to bring it to site, it's
  already on the deck. Compare Cranes' existing "minimum counterweight
  needed" figure doesn't distinguish this at all - it just reports
  whatever the chart's true lowest-clearing CWT level is, silently
  implying a truck might be needed without ever saying so. The person's
  point: a smaller crane whose real minimum for a given lift sits AT or
  UNDER its own road-legal limit can be deployed with zero extra
  logistics, which can make it the more efficient real-world choice even
  against a nominally bigger/more capable crane whose own minimum
  requires trucking extra ballast in - efficiency, not raw capacity, is
  what the toggle is meant to surface.

  Data: ROAD_LEGAL_CWT = { '1110': 13.4, '1130': 6.6 }. The 1110's
  figure came from Liebherr's own LTM 1110-5.2 product page, "Driving
  conditions" section - a diagram (image-only, not extractable by a
  plain URL fetch; the person sent a screenshot instead) showing three
  scenarios: ≤10t/axle -> 1.1t CWT, ≤12t/axle with a lattice fly-jib
  fitted -> 11.3t CWT, ≤12t/axle base boom (no jib) -> 13.4t CWT. 13.4t
  is the one that applies here (Compare assumes base boom). That page is
  technically for the 5.2 revision, not the 5.1 already in this app's
  fleet data - confirmed by the person as materially the same crane
  (differs only by year of manufacture), so applied directly rather than
  waiting on 5.1-specific documentation. The 1130's figure has no live
  source page at all (discontinued, delisted from Liebherr's current
  site) - it's the person's own figure from direct operational
  knowledge, tagged as such rather than implying a citable source that
  doesn't exist. Deliberately sparse beyond these two: per the person's
  own reasoning, bigger classes (1300+) generally need more CWT than is
  road-legal, or reaching a useful capacity means swapping to a smaller
  hook block - which is its own truck, defeating the entire point - so
  there was no ask to chase down figures for the rest of the fleet.

  Mechanism: computeLoadChartBestForCrane gained an optional maxCwt
  parameter - the same ascending "find the lowest CWT level that
  clears" loop it already had, just pre-filtered to exclude any level
  above the cap when one's supplied. A crane with no ROAD_LEGAL_CWT
  entry gets no cap at all (maxCwt stays undefined), so the toggle has
  literally no effect on it - it falls back to today's normal chart-wide
  search rather than being excluded or guessed at, exactly as discussed
  with the person beforehand. New "Minimum Counterweight Basis" toggle
  (Full chart / Road-legal only) sits next to the existing Site Capacity
  Rule toggle, same mode-toggle pattern. Every result row states plainly
  which basis actually applied to that specific crane - "capped at
  13.4t" vs "no road-legal CWT figure for this crane, showing the normal
  chart-wide minimum" - so a mixed comparison (some cranes capped, some
  not) can't be misread as uniform. When nothing within the cap clears
  the target, a second uncapped call (only made in that specific case,
  not on every render) reports what WOULD have worked, framed as needing
  a truck - genuinely useful context rather than a bare "no" if the true
  answer is "yes, but not truck-free."

  Verified directly against real load chart data (not synthetic/made-up
  numbers) via Playwright: a 12m/10t scenario came back 0.0t minimum for
  all three test cranes (1110, 1130, 1300) - too light to exercise the
  cap at all, both modes identical, which is correct behaviour but not a
  useful proof by itself. Probed computeLoadChartBestForCrane directly
  across a range of target weights for the 1130 at a fixed radius to
  find where its true minimum actually crosses the 6.6t cap - at 11t
  target weight the true minimum (6.6t) sat exactly on the cap boundary
  and was correctly still included (boundary is inclusive, <=, not <);
  at 12t the true minimum jumped to 9.8t, above the cap, and the capped
  search correctly returned "none" instead of silently reporting 9.8t.
  Confirmed the actual rendered HTML for that exact 12t case shows all
  three intended pieces together: the "capped at 6.6t" note, "no
  configuration within this crane's road-legal counterweight clears
  it", and "would clear it at 9.8t - above this crane's road-legal
  limit, so that'd need a truck." Also confirmed the "no road-legal CWT
  figure" note appears correctly for the LTM 1300 (no ROAD_LEGAL_CWT
  entry) with its own result completely unaffected by the toggle either
  way, in the same comparison run as the two capped cranes.

================================================================================

59. LTM 1650 Crane Layout: rear-clearance toggle for the removable
    sliding beam box

  Same idea as the existing "Include tool box in rear figure" toggle on
  the 360 Slew Clearance card, extended to a second, unrelated accessory
  that only exists on the 1650: the removable rear "sliding beam box"
  (Schiebeholmkasten in the OEM docs; some editions translate it
  "outrigger box" - same 9.4t part). The person flagged this from their
  own site practice: the crane gets positioned first, then the box goes
  on while building the crane out - so they wanted a way to show ground
  clearance behind the carrier with or without it counted, same as the
  existing toolbox toggle does for the other models.

  The open question going in was which direction to apply it: does
  FOOTPRINTS['1650'].rear (3717mm) already include the box, needing it
  subtracted when shown "without", or does it exclude the box, needing
  it added when shown "with"? Settled by primary-source cross-checking,
  not by guessing:

  - Rendered and read Fig.151675 ("Crane dimensions") from the person's
    own downloaded LTM 1650-8.1 Operating Instructions PDF - captioned
    only "1 On-road driving / 2 All-wheel steering", no footnote
    anywhere about the sliding beam box being fitted or removed.
  - The person then supplied a Liebherr sales brochure PDF
    (assets-cdn.liebherr.com) for the same crane. Its own "Maße"
    dimension page (S3273.04, same "On-road driving" caption) carries
    three overall-length figures (18302/19572/20717mm) - checked the
    actual vector line endpoints in the PDF (not just eyeballing) and
    confirmed all three differ only by FRONT-end reference points
    (1145mm apart, matching a separately-labelled front dimension), not
    by anything at the rear. No footnote on that page names the box
    either. FOOTPRINTS['1650'].front + rear (14585+3717) matches the
    18302 figure exactly, confirming the app's existing rear baseline is
    the box-off "on-road driving" figure already in this drawing.
  - The person pushed back hard on this, pointing to a "Baustellenfahrt"
    (jobsite driving) page in that same brochure where the rear of the
    carrier is drawn with a green-highlighted box absent from the
    "Straßenfahrt" (road driving) page's equivalent row. First pass at
    comparing the two got it wrong - mixed up the superstructure
    counterweight housing (identical, present in both) with the
    carrier's own rear structure (which genuinely differs) - corrected
    once re-examined properly: the jobsite-driving page really does draw
    the box fitted; the road-driving page really does draw it absent.
  - This isn't a contradiction of the box-off dimension drawing, it
    completes the picture: Straßenfahrt = public road travel (box on
    its own trailer), Baustellenfahrt = on-site maneuvering after the
    box has already been unloaded and fitted - exactly the sequence the
    person described from the start.
  - Clinching confirmation from the same brochure's "Transportplan"
    page: a literal parts list of everything that comes off the crane
    and rides its own trailer for road transport - TY guying (19.2t), N
    assembly unit (13t), Winch 3 (7.9t), Hoist gear 2 (4.7t), and the
    Schiebeholmkasten/Outrigger box itself, named explicitly, 9.4t,
    2980x1679mm front face / 1610x1720mm side profile - matching the
    Operating Instructions manual's own Fig.158040 (Sliding beam box)
    digit-for-digit. Confirms it travels separately by road, consistent
    with the box-off baseline in the dimensioned drawing.

  Net conclusion: FOOTPRINTS['1650'].rear (3717mm) is box-OFF, so the
  toggle ADDS length when checked, not subtracts - opposite of the
  person's initial guess, but the person's underlying instinct (this
  needs to be checked against the actual drawing, don't just assume)
  was right, and their pushback caught a real mistake in an earlier
  comparison pass along the way.

  The add amount is the box's own depth, 1610mm - its front-to-back
  projection when mounted at the rear (spanning the carrier's ~3.1m
  width with its 2.98m front face) - not the 2980mm figure, which is
  the box's width across the vehicle, not how far it sticks out behind
  the carrier.

  New SLIDING_BEAM_BOX_DEPTH_MM = { '1650': 1610 } constant, kept
  separate from COMBI_BOX_DEPTH_MM (different part, different fleet -
  only the 1650 has this one - and critically the opposite default).
  renderCarrier3DSlewToggles() gained a second nested checkbox
  (#carrier-3d-include-slidingbeambox) alongside the existing toolbox
  one, shown only when the current model has an entry in the new
  constant. Defaults UNCHECKED (the combi box toggle defaults checked)
  because for the 1650 the box-off state is the normal starting point
  per the person's own site practice, not the exception. onSlewCircle
  Toggle() folds both toggles' contributions into rearMm additively
  (footprint.rear + toolbox-if-checked + sliding-beam-box-if-checked),
  so a model could in principle have both someday without the code
  needing to change, though no current crane does.

  Verified via Playwright: toggle renders only for the 1650 (absent for
  every other model), starts unchecked, label reads "Include sliding
  beam box in rear figure (+1.61m)", and the rear figure correctly goes
  3717 -> 5327mm when checked. No console errors. CACHE_VERSION
  v215->v216, app version v5.20->v5.21.

60. Correction to 59: sliding beam box add-on was 1610mm, should have
    been 1270mm

  Caught by the person re-measuring the OEM dimension drawing themselves
  and coming back with centre-of-slew-to-rear figures of 3717mm (box
  off - matches FOOTPRINTS['1650'].rear exactly) and 4987mm (box on).
  4987-3717 = 1270mm, not the 1610mm shipped in 59.

  The mistake: 59 had the right raw numbers already in hand (the sales
  brochure's "Maße" page carries 18302mm and 19572mm as two overall
  vehicle lengths sharing the exact same front reference point) but
  used the box's own ISOLATED depth figure from a completely different
  drawing (1610mm, the Transportplan page's standalone technical
  drawing of the box by itself) instead of the NET delta between the
  two totals that were sitting on the same dimensioned drawing, right
  next to each other. 19572-18302 = 1270mm exactly, and 19572-14585
  (FOOTPRINTS['1650'].front) = 4987mm exactly - matching the person's
  own independently-derived figure to the millimetre. The box evidently
  nests/overlaps with the existing frame by around 340mm when actually
  mounted, rather than adding its full 1610mm depth flush - so the
  isolated-component drawing was the wrong source to pull the add-on
  number from, even though it was a genuine OEM figure for a genuine
  OEM part. Both totals needed to come from the SAME dimensioned
  drawing to get the real net effect.

  SLIDING_BEAM_BOX_DEPTH_MM['1650'] corrected from 1610 to 1270.
  CACHE_VERSION v216->v217, app version v5.21->v5.22.

61. 3D calibration bug found via 59/60: slew centre estimate skewed by
    ANY baked-in accessory, not just the 1650's box

  Direct follow-on from 60. The person asked a sharp question after
  seeing the rear-clearance line in the 3D preview NOT sit flush against
  the visibly-modeled sliding beam box even with the toggle checked:
  "seems weird that if it's already baked in should the dimension line
  be under the carrier and it's not... shouldn't that line be right up
  against the rear box?"

  Root cause, confirmed by actually loading the GLB and measuring it
  (not just reasoning about it): carrier3d.js's computeFormulaCalibration
  estimates where the slew centre sits within a crane's 3D mesh by
  taking the model's own measured front-to-back length and splitting it
  front/rear using the FOOTPRINTS ratio (front / (front+rear)). The
  1650's exported mesh measures 19.561m - matching the box-ON total
  (19.572m = 14.585 front + 4.987 rear-with-box, see 60) almost exactly,
  not FOOTPRINTS' box-OFF total (18.302m) the split assumed. Splitting a
  19.561m mesh using an 18.302m-based ratio pushed the estimated slew
  centre about 1.0m too far toward the rear - confirmed by hand
  (frontTipZ + frontOverhang gives -6.372, the old formula gave -5.368,
  a 1004mm difference) and confirmed by checking the resulting rear-edge
  point against the mesh's own real geometry (old formula placed it
  380mm PAST the model's actual rear tip - floating in empty space
  behind the visible box, exactly what the person was looking at).

  Checked whether this was 1650-specific before touching shared code.
  It wasn't: loaded all 7 cranes' GLBs and compared each one's measured
  length against its own FOOTPRINTS total -

    1100 (no combi box, no sliding beam box): -6mm      - clean
    1110 (combi box):                        +626mm
    1130 (combi box):                        +415mm
    1160 (combi box):                        +425mm
    1250 (combi box):                        +420mm
    1300 (combi box):                        +421mm
    1650 (sliding beam box):                +1259mm

  Every crane WITH a COMBI_BOX_DEPTH_MM entry showed a few-hundred-mm
  mismatch; the one crane with neither box showed essentially none. Same
  underlying issue on 6 of 7 cranes, just smaller everywhere but the
  1650 (whose box is over twice the size of the standard combi box).

  Fix: computeFormulaCalibration in carrier3d.js now anchors the slew
  centre off the FRONT overhang alone (front tip + FOOTPRINTS' own front
  distance in mm), not off a proportional split of the model's whole
  measured length. Nothing in this calculation depends on the model's
  total length or rear overhang any more, so a baked-in rear accessory
  on any crane's export - this one, one of the other 5, or a future
  crane added later - structurally cannot skew it. Mathematically an
  exact no-op for a crane whose mesh happens to measure precisely
  FOOTPRINTS' own total (confirmed: 1100 barely moved, 15mm vs 17mm).

  Verified this was a genuine improvement, not just a different guess,
  by checking BOTH the old and new formula against each crane's own
  geometry-refined calibration (refineCalibrationFromGeometry, which
  anchors off the model's real outrigger foot-plate positions - the
  closest thing to ground truth available, used by Support Pad
  Placement once outriggers are synced). Old formula's error vs that
  ground truth, by crane: 1100 17mm, 1110 507mm, 1130 455mm, 1160 486mm,
  1250 498mm, 1300 430mm. New formula's error against the same ground
  truth: 1100 15mm, 1110 69mm, 1130 171mm, 1160 192mm, 1250 207mm, 1300
  129mm. Closer (often dramatically so) on every single crane, no
  regressions anywhere. The 1650 has no full outrigger geometry to
  refine against (missing one beam pair, see 10.76/10.77), so checked
  directly against the model's own measured rear tip instead - the new
  formula's rear-edge point (with the sliding beam box toggle on) landed
  within 11mm of the box's real modeled tip, vs 1014mm short of it
  (past it, into empty space) under the old formula - which is exactly
  the visible symptom the person spotted and asked about.

  This also means the earlier "resolved... agrees within ~1m" note on
  the 1650's CARRIER_CALIBRATION uncertain flag (see 37) was very likely
  quietly absorbing this exact same error as generic cross-check noise,
  rather than a genuinely unexplained ~1m of slack - it now has a real,
  fixed, named cause.

  What did NOT change: any plain mm figure anywhere in the app
  (FOOTPRINTS, GROUND_LAYOUT_DATA, COMBI_BOX_DEPTH_MM,
  SLIDING_BEAM_BOX_DEPTH_MM, the rear-clearance toggle's own maths, the
  "Layout Dimensions" text table). None of that goes through this 3D
  calibration path - only the 3D-rendered POSITION of things (dimension
  lines, ground-layout markers, AR anchor placement) was ever affected.

  CACHE_VERSION v217->v218, app version v5.22->v5.23.

62. Follow-on from 61: vendor/carrier3d.js was never in the service
    worker's precache list, so the calibration fix (and any future
    carrier3d.js edit) could silently never reach a browser that had
    already opened the 3D view once

  Direct result of the person actually checking 61's fix on their own
  device with Fit View and seeing "way off" - a real, large gap between
  the rear-clearance line and the visible box, contradicting a fresh-
  browser-profile test that showed the line touching the box to within
  a pixel. Investigated why the SAME shipped code could produce two
  different results, rather than assuming either result was wrong.

  Root cause: sw.js's APP_SHELL array (the list of files precached and
  refreshed on every CACHE_VERSION bump) never included
  './vendor/carrier3d.js'. index.html imports it dynamically
  (`import('./vendor/carrier3d.js')`-style, not a normal <script src>
  page asset), so it was never automatically swept up the way core page
  assets are - it fell into the generic fetch-once/cache-forever
  CONTENT_CACHE path, same as everything else not explicitly listed.
  CONTENT_CACHE is - by design, see this file's own comments - NEVER
  cleared by a CACHE_VERSION bump. So a person who opened the 3D
  Carrier Preview even once, at any point before 61 shipped, had the
  OLD, buggy carrier3d.js cached forever - every later CACHE_VERSION
  bump updated index.html (which they'd see as a new version number in
  the header) but never touched that already-cached copy, so 61's fix
  silently never reached them despite the app visibly reporting v5.23.

  This is the exact same failure mode already documented in this file's
  own comments for the counterweight diagrams (10.28) and reeving
  manifest (41) - a person can keep seeing stale content/behaviour
  across multiple version bumps because the fix landed in CONTENT_CACHE
  the first time and nothing ever cleared it. Just never applied to
  carrier3d.js specifically until now, because nobody had a reason to
  distrust its caching before.

  Fix: added './vendor/carrier3d.js' to APP_SHELL. The existing
  activate() handler already purges any stale CONTENT_CACHE duplicate of
  a URL that's newly added to APP_SHELL (same mechanism that fixed the
  counterweight diagrams originally) - no new logic needed, just adding
  the one missing entry. Deliberately did NOT add the vendor/three/*
  library files (GLTFLoader.js, DRACOLoader.js, OrbitControls.js,
  three.module.min.js) or the .glb carrier models - those are genuinely
  stable, unedited vendored/OEM content, same reasoning as the reeving
  SVGs staying in CONTENT_CACHE.

  A person on an affected device needs one fresh load past this fix to
  clear it - the in-app refresh button (CHECK_FOR_UPDATE) re-fetches
  every APP_SHELL entry with cache:'reload', so once this specific fix
  is live it should be enough on its own without needing a manual
  browser cache clear.

  CACHE_VERSION v218->v219, app version v5.23->v5.24.

63. Follow-on from 61/62: the rear-clearance line's own rendering was
    getting occluded by the carrier model, producing angle-dependent,
    sometimes-backwards results even with the calibration fix live

  After 62's fix landed and the person confirmed they were actually on
  the new code (v5.24), they compared the sliding beam box toggle ON vs
  OFF from a single fixed Fit View angle and found the OPPOSITE of what
  the math says: OFF (rearMm=3717, near point tucked further under the
  model) read as flush, ON (rearMm=4987, near point right at the
  model's real edge) read as visibly gapped - backwards, and internally
  contradictory with an earlier straight-down test that had shown near-
  identical placement for both states. Chased this down properly rather
  than re-asserting the (already re-verified, still correct) numbers.

  Root cause: addDimensionLine's THREE.LineBasicMaterial and the slew
  circle's own LineLoop material used normal depth-testing (Three.js's
  default), same as every other object in the scene. Both sit at ground
  level (center.y + ~0.03-0.04m) - well below a carrier's own body
  height - so whichever stretch of the line or circle happens to pass
  "under" the solid carrier geometry from the CURRENT camera's angle
  gets silently hidden by the z-buffer, and which stretch that is shifts
  with camera position, not with the actual mm figure being shown. From
  directly overhead this mostly self-cancels (confirmed via projected
  screen-space math: the two states really do differ by the correct
  ~19px/1.27m once measured properly - see below), which is why an
  earlier top-down check looked deceptively close-enough; from an
  angled/elevated view it doesn't cancel out at all, and can invert
  which state looks more "correct" depending on exactly how each
  state's near point sits relative to the model's silhouette from that
  specific angle.

  Fix: addDimensionLine's line + end-tick materials and the slew
  circle's own LineLoop material all now render with depthTest: false,
  depthWrite: false, and a renderOrder of 10, so they're always fully
  visible on top of the model regardless of camera angle or whether the
  reference point they're drawn from happens to sit "inside" the
  model's own footprint.

  Verified two ways, not just re-asserted: (1) projected the true
  world-space rearEdge point through the actual live Three.js camera for
  both toggle states from one fixed, unmoved Fit View - screen positions
  differ by ~19 CSS px (≈38 physical px at 2x device scale), matching
  the expected 1.27m shift almost exactly (predicted ~665.5px/627.0px
  physical, measured rendered pixels 665-676 / 627-676). (2) confirmed
  the rendered pixels themselves now show it: box ON, the line's tick
  sits right at the model's real edge (12px tall mark, nothing before
  it); box OFF, red pixels run continuously from the tick's true (now
  visibly-inside-the-model) position all the way out - the line now
  visibly crosses the TOP of the modeled box on its way out, which
  looks unusual at a glance (a line drawn across solid-looking geometry)
  but is the honest, angle-independent picture of "here's where the
  rear would be without a box the model can't actually remove" -
  exactly what the person had predicted correctly back in 60 ("if I
  toggle it off, the dim line should project under the crane").

  CACHE_VERSION v219->v220, app version v5.24->v5.25.

64. Chain Shortening: "Extend By Shackles" mode - a third alternative to
    shortening, using shackles married in series instead of the grab
    hooks, avoiding the need to lower the hook all the way down to reach
    the shortener claws

  Person's own framing for why: sometimes the extra length needed is
  small enough that grabbing shackles and building it up is quicker/
  safer than fully lowering the hook to reach the chain's own shortener
  hardware.

  Data: Green Pin bow shackle WLL/pin diameter (b)/inside length (f),
  pulled directly from Green Pin's own official "Chapter 1 - Shackles"
  catalog (greenpin.com), pages 28-29 - G-4161 (Bow Shackle SC, screw
  collar pin) and G-4163 (Bow Shackle BN, safety bolt+nut). b and f are
  identical between the two pin-type variants (only h/i/j/k/weight
  differ), so this is deliberately ONE combined table (19 sizes, 0.33t
  to 85t) - the person doesn't need to pick a pin type for this
  calculation at all, it's irrelevant to it.

  This replaced an earlier text-only shackle chart the person had pasted
  from elsewhere, which turned out to have real errors once checked
  against the primary source directly: a, b, c, e, f, g matched exactly,
  but weight was wrong on every row from 8.5t up (matching neither
  official variant), h (overall length) was wrong specifically at 35t
  and 55t, and the 42.5t and 85t rows were missing entirely. None of
  those wrong figures (h, weight) are used by this feature, but the
  discrepancy was worth finding before building on top of unverified
  data - see the person's own diagram (11 lettered dimensions, a-k)
  which is what surfaced the mismatch when cross-checked.

  f is the length ONE shackle adds to a leg when married bow-to-pin in
  series with the next (person's own confirmed rigging practice - two
  25t shackles = +178mm x 2 = +356mm).

  Design, worked out with the person across several rounds rather than
  assumed:
  - The attachment-point shackle is mandatory, exactly one, chosen
    directly by the person (whichever size physically fits the lifting
    lug's pin hole) - not something the optimiser gets to pick.
  - Shackles further down the chain, once they're only marrying onto the
    PREVIOUS shackle's bow rather than threading the lug itself, CAN be
    a bigger size than the attachment one for fewer total shackles
    (agreed explicitly - a bow can safely take a range of smaller pins,
    the other direction can't happen physically).
  - Rather than the tool auto-picking "best" sizes from the whole table,
    the person ticks which additional sizes are actually in THEIR kit -
    the tool only searches combos across ticked sizes, never suggesting
    gear that wasn't confirmed as on hand.
  - Every shackle in the chain, not just the attachment one, has to
    clear the load weight - the dropdown/checklist are both pre-filtered
    to WLL >= load weight before a person can even see them.
  - Combining shackle-adds with the SAME per-link rounding math the rest
    of this tab already has (CHAIN_PITCH_TABLE, nearest-whole-link) -
    shackles get the length close in coarse increments, a small number
    of removed links trims the remainder, same "Difference from Target"
    concept as every other mode here.

  Real bug caught by testing it against the person's own worked example
  before shipping, not just trusting that "it runs": the very first
  scoring version picked whichever combo landed closest to the target
  mm-for-mm, with total shackle count only as a tie-breaker. For load
  20t, lug 25t, target 348mm, only 25t ticked, it returned 6 additional
  25t shackles PLUS removing 23 chain links (almost a full metre of
  chain) to land within 1mm - technically "more accurate" than the
  obvious 2-shackle/0-link/8mm-over answer, but defeats the entire point
  of the feature (avoiding heavy chain manipulation). Fixed by scoring
  on total operation count (shackles + links removed) first, with
  precision only breaking further ties once a candidate is already
  within tolerance (half a link's pitch - about as precise as this tab's
  own rounding claims to be anywhere else); precision only starts
  dominating again once NO candidate gets that close, so a genuine
  shortfall still gets flagged rather than masked by a busy rig. Also
  caught and fixed two related bugs while testing this: the "short of
  target" warning was firing on completely routine few-mm undershoots
  (should only fire once the miss exceeds that same tolerance), and its
  "one more Xt shackle would close this" suggestion could recommend a
  size that was already ticked and already correctly rejected by the
  search - now filtered to genuinely un-ticked sizes only.

  Verified via Playwright, both direct function calls and real UI clicks
  (select/check/fill, not just calling the JS functions): the person's
  own worked example (load 20t, lug+only-ticked 25t, target 348mm)
  returns exactly 2x25t/0 links/+8mm, matching their own manual math.
  Also verified: undershoot reporting when ticked sizes genuinely can't
  reach target (with the max search bound respected, no infinite/runaway
  search), a mixed-availability case, the chain diameter select
  correctly re-running the shackle calc (not the shortening calc) when
  changed while this mode is active - a real gap caught in testing,
  since the diameter dropdown's onchange had always called
  calcChainShortening() directly - and no regression to the two existing
  modes.

  CACHE_VERSION v220->v221, app version v5.25->v5.26.

65. Extend By Shackles: show ranked assorted combos, not just one "best"

  Follow-up request straight after 64 shipped: the person wanted to see
  several combos, not just the single winner, and asked directly how to
  even rank them ("least weight add or less shackle required?"). Weight
  wasn't actually available to rank by - SHACKLE_SIZES only carries WLL/
  pin diameter (b)/inside length (f), not physical mass in kg, and adding
  that would mean a fresh pass through the Green Pin catalog for a column
  this feature doesn't otherwise need. Asked directly rather than
  guessing; the person confirmed the real intent was simplest rigging /
  fewest connection points, i.e. exactly the shackle-count-first
  principle 64 already scores by - so no new data pull needed.

  bestShackleExtendCombo (singular, returned one winner) became
  bestShackleExtendCombos (plural) - same search, same rankScore per
  candidate (totalOps first, |diff| only breaking ties within tolerance,
  precision-only fallback once nothing clears tolerance), but every
  candidate is now collected instead of only keeping the running best,
  sorted by that same rankScore, and the top 5 returned. calcShackleExtend
  renders them as a second table (Combo / Additional Shackles / Links
  Removed / Final Length Added / Diff from Target), #1 marked "(best)",
  rather than the old single-row result block. The shortfall fail-box
  still keys off combos[0] (the actual best) exactly as before - showing
  more options underneath doesn't change when the warning fires.

  Verified via Playwright against the same worked example as 64 (load
  20t, lug 25t, 25t ticked, target 348mm): 5 distinct rows, #1 still the
  expected 2x25t total/0 links/+8mm, and further rows genuinely different
  combos (2x/3x/4x/5x additional 25t) not near-duplicates - including one
  row (5 additional, +30 links) that lands exactly on target (0.0mm diff)
  but correctly ranks last since it needs far more total operations than
  #1. Also checked a mixed-size-ticked case (25t + 35t both available) -
  produces genuinely varied combos (single of each, doubles, one-of-each
  mix), not just repeats of one size. And the shortfall case (target set
  far beyond what 8 additional shackles could ever reach): all 5 rows
  still populate (ranked by which gets relatively closest once nothing
  clears tolerance, same "precision matters again" fallback from 64), the
  fail-box still fires correctly, and the max-additional-shackles bound
  is still respected (never a runaway search).

  CACHE_VERSION v221->v222, app version v5.26->v5.27.

66. Extend By Shackles: added a Combo Weight column

  Follow-up request right after 65 shipped: show the actual weight of
  each combo so the person can factor rigging mass into the crane's own
  load chart (rigging weight counts against a crane's rated capacity same
  as the load itself does).

  Needed real per-size mass data that SHACKLE_SIZES didn't carry yet (it
  only had WLL/pin diameter/inside length). Went back to the same primary
  source as 64 (Green Pin's own "Chapter 1 - Shackles" catalog) and read
  the weight column off both G-4161 SC (page 28, printed pg.28) and
  G-4163 BN (page 29, printed pg.29) directly - not reusing the person's
  earlier pasted text chart, since 64 already found that one had wrong
  weights above 8.5t.

  Which variant's weight to use, given the table is still deliberately
  ONE combined list (not split by pin type, per 64's reasoning): used
  G-4163 BN's own value at every size BN offers (0.5t-85t) - BN is the
  heavier of the two variants (extra bolt+nut hardware vs SC's screw
  collar pin), so it's the conservative choice for a figure whose entire
  purpose is feeding a capacity calculation - it will never understate
  what the rigging weighs. BN's table doesn't have a 0.33t row at all
  (starts at 0.5t), so that one size falls back to G-4161 SC's own weight
  (0.02kg), the only source that has it.

  Combo Weight = attachment shackle + every additional shackle in that
  specific combo, by their own catalog mass - deliberately NOT reduced
  for links removed (chain per-link mass isn't tracked anywhere in this
  calculator, and reducing it would need that data to stay honest).
  Verified via Playwright against the same worked example as 64/65 (load
  20t, lug 25t): #1 (1 additional 25t) computes to 28.44kg = lug 14.22 +
  additional 14.22, exactly matching G-4163's own printed 25t weight
  doubled - and every other row in a mixed 25t/35t ticked test reconciled
  the same way by hand (e.g. 2x35t = 14.22 + 19.53x2 = 53.28kg).

  CACHE_VERSION v222->v223, app version v5.27->v5.28.

67. Fix LTM 1160 outrigger 3D model - beams now fully extended

  Shadowed from main (see main's own methodology.txt for the full
  investigation) - the LTM 1160 carrier GLB's outrigger beams weren't
  rendering fully extended at max reach. Corrected model file swapped
  in unchanged from main; no beta-specific auth/admin code involved.

  CACHE_VERSION v51-beta149->v51-beta150.

68. Fix unsorted Counterweight Currently Fitted dropdown (Driving w/ Equipment)

  Shadowed from main - the weight dropdown wasn't sorted, just whatever
  order the source rows happened to be in. Added .sort((a,b) => b - a)
  so it lists heaviest first, matching every other weight dropdown in
  the app. No beta-specific code involved.

  CACHE_VERSION v51-beta150->v51-beta151.

69. Extend By Shackles: add Gunnebo Oblong Master Link M-2622-10

  Shadowed from main - added the Gunnebo Grade 100 Oblong Master Link
  (WLL 41.0t, Bar Diameter 45mm, Inside Length 340mm, weight 12.9kg) as
  an ADDITIONAL item option in Extend By Shackles, alongside shackles.
  Kept as its own MASTER_LINK_SIZES table rather than folded into
  SHACKLE_SIZES, since that table's own sourcing comment is specifically
  Green Pin's shackle catalog - mixing a different manufacturer/component
  type in would misrepresent where every other row came from. No
  beta-specific code involved.

  CACHE_VERSION v51-beta151->v51-beta152.

70. Extend By Shackles: clarify attachment shackle in the combo table

  Shadowed from main - combo rows only listed the ADDITIONAL items,
  leaving the mandatory attachment/lug shackle unstated and easy for a
  first-time user to miss. Added a muted "(plus the Nt attachment
  shackle above)" note to each combo row. No beta-specific code
  involved.

  CACHE_VERSION v51-beta152->v51-beta153.

71. LTM 1650 rear outrigger box: real show/hide toggle

  Hand-ported from main (couldn't cherry-pick cleanly - main's version
  is anchored to the admin-only "Identify Parts" click-to-inspect tool,
  which depends on window.__isAdmin from the Firebase Auth system beta
  intentionally doesn't have; Identify Parts itself wasn't worth
  porting since it'd be permanently inert here with no admin flag ever
  set). What DID come over: carrier3d.js's PART_GROUPS table (currently
  just 1650 -> rearOutriggerBox -> 9 confirmed "Part N" node names) and
  window.__carrier3dSetPartGroupVisible(modelKey, groupKey, visible),
  plus a real (non-admin) "Hide Rear Outrigger Box" checkbox on the 3D
  carrier preview, shown only when window.__carrier3dGetPartGroups
  reports a group for the selected crane. No auth/admin dependency
  anywhere in this feature itself - safe and complete on its own.

  Also brought the routine curated-shadow set current with main as
  part of this pass: LTM 1160 outrigger 3D model beam-extension fix,
  the Driving w/ Equipment weight-dropdown sort fix, and both Extend By
  Shackles additions (Gunnebo Oblong Master Link, attachment-shackle
  note) - all self-contained, no auth/admin dependency, cherry-picked
  cleanly bar the routine sw.js CACHE_VERSION conflict and this
  branch's own methodology.txt/request-log.md divergence (beta doesn't
  track request-log.md at all - dropped on every cherry-pick, same as
  it's never existed here). Explicitly did NOT bring over anything
  built on the Firebase Auth system added to main a long time back
  (Admin Dashboard, Firestore-backed permissions, invite tokens, shared
  Lift Logger pools, Cab Entry) - beta-trial has never had that system
  and doesn't have a Firebase project/hosting configured for it; a
  naive full resync would have grafted the whole login system onto
  beta for the first time and likely locked out every beta user (no
  googleAllowlist entries exist for a beta audience). Flagged this
  architecture split to the person before doing any of this rather
  than guessing silently.

  CACHE_VERSION v51-beta153->v51-beta154.

72. Move the rear outrigger box toggle into Crane Layout

  Shadowed from main - person's feedback right after shipping section
  71: they went looking for "Hide Rear Outrigger Box" and expected to
  find it in Crane
  Layout, next to the existing "Include sliding beam box in rear figure
  (+1.27m)" toggle - not where it actually landed (Support Pad
  Placement's own toolbar). Makes sense in hindsight: both toggles are
  about the same rear-of-carrier geometry, and Crane Layout's slew-
  toggles panel (renderCarrier3DSlewToggles) is where every other
  rear-figure checkbox already lives (Include tool box, Include sliding
  beam box) - Support Pad Placement never had a reason-based home for
  it, it just happened to be where Identify Parts was built first.

  Moved rather than duplicated: pulled the checkbox out of the
  carrier-3d-toolbar block (Support Pad Placement, cad-crane), added it
  into renderCarrier3DSlewToggles's own template string (Crane Layout,
  sc-crane), styled to match the existing rear-figure checkboxes it
  sits below, gated on a new plain REAR_OUTRIGGER_BOX_MODELS constant
  rather than window.__carrier3dGetPartGroups - that panel can render
  before carrier3d.js has ever been dynamically imported (it's shown as
  soon as a crane's picked, independent of whether "Show 3D Carrier
  Model" has been clicked), so gating on a function that might not
  exist yet would have hidden the checkbox on first paint.

  toggleCarrier3DPartGroup() picked up a third parameter (craneSelectId)
  since Crane Layout's crane select is sc-crane, not cad-crane - the
  old hardcoded cad-crane read would have silently read the wrong
  dropdown from the new location. Also added an explicit re-show call
  at the end of renderCarrier3DSlewToggles (mirroring the existing
  onSlewCircleToggle()/onGroundLayoutToggle() calls there) - the
  checkbox always starts unticked on every crane-switch rebuild
  (innerHTML replace), so without this a box hidden on a previous visit
  to 1650 could stay hidden in the 3D model itself after switching away
  and back, even though the fresh checkbox reads unticked.

  Verified directly against the real scene graph (temporary debug hook,
  removed before shipping, same as section 71's own verification): all
  9 mapped nodes visible by default in the new location, correctly flip
  invisible/visible on check/uncheck, checkbox confirmed absent for an
  unmapped crane (1110) and gone from its old Support Pad Placement
  location (only one element with that id anywhere in the page now). No
  beta-specific code involved.

  CACHE_VERSION v51-beta154->v51-beta155.

73. Fix "Hide Rear Outrigger Box" hiding the entire chassis

  Shadowed from main - person's report right after the toggle-move
  shipped there, screenshot attached: checking the box hid nearly the
  whole carrier body, leaving just axles/wheels/outrigger beams. Of the
  9 mesh names read off Onshape (section 71), "Part 16" was wrong -
  its own world-space bounding box spans 19 of the whole carrier's
  20-unit front-to-rear length, i.e. it's the main chassis frame, not
  a rear-only component. The other 8 all sit tightly within 3 units of
  the rear extreme - genuinely small, genuinely the rear outrigger box.
  Dropped Part 16 from PART_GROUPS.1650.rearOutriggerBox (now 8
  entries). Verified directly against the scene graph (temporary debug
  hook, removed before shipping) plus a real before/after screenshot
  pair: chassis, cab, wheels and the other three outrigger legs stay
  fully visible, only the intended rear leg's own box geometry hides.
  Unlike main, the buggy 9-entry version DID reach this branch's live
  Pages site first (section 71's port, pushed to both beta-trial and
  myslewer-beta's own main before this bug was found) - shipping this
  fix promptly rather than treating it as lower-priority just because
  it was caught on main first. No beta-specific code involved.

  CACHE_VERSION v51-beta155->v51-beta156.

74. Revert the rear outrigger box hide feature entirely

  Shadowed from main - person asked to remove the whole feature
  outright (checkbox mapping still read backwards after the merge
  attempt, and Part_16's own geometry sticking out visually detached
  once the legs beside it hide looked bad regardless of direction).
  Removed PART_GROUPS, normalizePartName,
  window.__carrier3dGetPartGroups and window.__carrier3dSetPartGroupVisible
  from carrier3d.js, and every call site in index.html
  (REAR_OUTRIGGER_BOX_MODELS, the standalone checkbox,
  toggleCarrier3DPartGroup). Confirmed via a direct diff against this
  branch's own pre-feature commit (dad9faa~1) that carrier3d.js is now
  byte-identical to before this feature ever landed here, and
  index.html's only remaining difference is the CARRIER3D_VERSION
  bump. No beta-specific code involved.

  CACHE_VERSION v51-beta156->v51-beta157.
75. Beta program wind-down: updated the trial-expired overlay's message,
    scheduled the actual GitHub Pages shutdown five days out

  The beta program itself has concluded. window.TRIAL_EXPIRES (set to
  2026-08-26T00:00:00, see the inline script right after <body>) had
  already reached its date as of this change, so the existing trial-
  lock mechanism (section on the Beta Trial lock, added earlier - flips
  html.trial-expired, hides header/nav/.container, shows
  #trial-expired-overlay) was ALREADY live and active for anyone
  loading a fresh copy - nothing new needed there. What changed is
  purely the overlay's own content: it previously said the generic
  "This trial build of Myslewer has expired... contact the person who
  sent you this link", written for an ordinary mid-trial lockout, not
  an actual program conclusion.

  Replaced with a proper closing message - thanks the person for
  taking part, and states plainly that the beta SITE itself (not just
  the app inside it - the actual GitHub Pages hosting) will stop being
  served on a specific date. That date is deliberately NOT a second
  hardcoded literal: window.BETA_SHUTDOWN_DATE is computed as
  TRIAL_EXPIRES + 5 days, so the trial-end date and the site-shutdown
  date can never drift out of sync with each other the way two
  separately-typed dates eventually would. The five-day gap is
  intentional headroom - someone who missed the exact moment the trial
  expired still gets several days to see this closing message (via a
  normal app reopen, once their device's cached copy updates - see
  below) before the link goes dead entirely, rather than the site
  vanishing the instant the trial number ticked over.

  "Clearing existing users' caches" in this app's own architecture
  just means a normal CACHE_VERSION bump (myslewer-v51-beta149 ->
  myslewer-v51-beta150 here) - sw.js already does skipWaiting()/
  clients.claim() on activation (see its own top-of-file comment), so
  this propagates the same way every other change in this app ever
  has: no special one-off cache-busting mechanism needed, and none
  invented for this. Confirmed the trial-expired class + new overlay
  text render correctly via a local Playwright check before shipping -
  screenshot showed "BETA TRIAL COMPLETE" with the computed shutdown
  date reading "Monday 31 August 2026", matching TRIAL_EXPIRES+5 days
  exactly.

  Actually taking the GitHub Pages site OFFLINE on that date (disabling
  Pages in the myslewer-beta repo's own Settings) is a separate, manual
  step - no tool available in this session can change a repository's
  Pages configuration directly, so it isn't something this change can
  do on its own. Told the person plainly rather than silently leaving
  it undone or pretending otherwise.

  CACHE_VERSION beta149->beta150, app version v5.1->v5.2.
